//     This code was generated by a Reinforced.Typings tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.

// Stand-in for what the codegen step in "Getting your contract into your
// client" emits for the guide's example Uplink, so the guide's own blocks
// compile against a real file rather than a promise. Shaped exactly like a
// real one: camelCased, every property optional (AutoOptionalProperties), and
// each [SitrepUnit] property retyped from a bare number to a Value.
import type { Value } from "@ksp-gonogo/sitrep-sdk";

export interface ReactorStatus {
  online?: boolean;
  outputPower?: Value<"kW">;
  coreTemp?: Value<"K">;
  throughput?: Value<"thermalUnits">;
}

/** Bare numbers: ApplyUnitValueTypes skips inbound-only command args, [SitrepUnit] or not. */
export interface SetOutputArgs {
  targetPower?: number;
}
