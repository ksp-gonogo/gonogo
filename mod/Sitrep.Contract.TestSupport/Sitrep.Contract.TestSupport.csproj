<Project Sdk="Microsoft.NET.Sdk">
  <!--
    NOT a test project (no IsTestProject, no test SDK): it holds assertions, it
    does not hold [Fact]s. Every [Fact] stays in the Uplink's own Tests project,
    naming that Uplink's own types, so a red still names the Uplink that broke.

    net10.0 to match the Tests projects that reference it. Sitrep.Contract's
    netstandard2.0 leg is what resolves for that reference, the same way each
    Uplink's Tests project already consumes its own <Uplink>.Contract slice.
  -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <!-- Same reasoning as Sitrep.Contract: the assertions' doc comments are
         what an author sees when they hover one, and this assembly ships in
         KspGonogo.Sitrep.Contract's net10.0 group. -->
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);1570;1571;1572;1573;1574;1591;1734;0419</NoWarn>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <DebugType>portable</DebugType>
    <!-- IsPackable=false because this project is not packed BY ITSELF. Its
         output ships inside KspGonogo.Sitrep.Contract's net10.0 group, placed
         there by Sitrep.Contract.Package: one package, not
         two, so a second package id must never appear. -->
    <IsPackable>false</IsPackable>
    <AssemblyName>Sitrep.Contract.TestSupport</AssemblyName>
    <RootNamespace>Sitrep.Contract.TestSupport</RootNamespace>
  </PropertyGroup>

  <!-- xunit.assert alone, not the xunit metapackage: this project needs
       Assert, not a test runner or the [Fact]-shaped analysers. Version pinned
       to the same 2.9.x line every Tests project references, so there is one
       xunit.assert in the graph. -->
  <ItemGroup>
    <PackageReference Include="xunit.assert" Version="2.9.2" />
  </ItemGroup>

  <!-- SitrepContractAttribute/SitrepUnitAttribute/Vec3: core's cross-package
       surface, the same three things every Uplink contract slice reaches for. -->
  <ItemGroup>
    <ProjectReference Include="..\Sitrep.Contract\Sitrep.Contract.csproj" />
    <!-- Sitrep.Core is referenced but not compiled against: nothing in this
         assembly names a Core type. The reference is the carrier. It puts
         Sitrep.Core.dll beside this assembly everywhere this assembly goes,
         so a Tests project that wires a real Courier/Archive delay engine gets
         the engine itself alongside the fakes rather than a double of it.

         Which means: the net10.0 group of KspGonogo.Sitrep.Contract and
         vendor/devkit both carry Sitrep.Core.dll, and a Tests project resolves
         it the same way it resolves this assembly. A PLUGIN still may not:
         Sitrep.Core stays out of the net472 and netstandard2.0 groups, which
         are what KSP resolves. -->
    <ProjectReference Include="..\Sitrep.Core\Sitrep.Core.csproj" />
  </ItemGroup>
</Project>
