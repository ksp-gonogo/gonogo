// Graph series colours come from the shared categorical data palette so
// they stay consistent with telemetry tags, body markers, etc.
export {
  DATA_PALETTE as GRAPH_PALETTE,
  dataColor as paletteColor,
} from "../shared/dataPalette";
