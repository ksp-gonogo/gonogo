{
  "_meta": {
    "scenario": "no-target",
    "notes": "No target set in KSP. tar.name is absent so the widget's target name read stays undefined — it renders the empty-state hint 'No target set in KSP'."
  },
  "tar.type": ""
}
