asyncapi: 3.0.0
info:
  title: Gonogo Sitrep contract
  version: 16.2.0
  description: |-
    The wire contract of the Gonogo mod's telemetry socket: 76 telemetry
    channels a client subscribes to, and 53 commands it dispatches, with the payload
    schema and the contract's own prose for each.

    **Generated.** Emitted by `scripts/asyncapi-doc.mjs` from the committed
    TypeScript contract under `mod/sitrep-sdk/src/__generated__/`, which
    `mod/codegen.sh` in turn generates from the C# in `mod/Sitrep.Contract/`. The
    C# is the source of truth. Do not hand-edit this file; change the C# doc comment
    or the declaration and regenerate.

    **Point of view.** Operations are written from the CLIENT's side: a telemetry
    channel is `receive` and a command is `send`. The mod is the server.

    ## One socket, many channels

    Every channel below is multiplexed over a single WebSocket. A channel's
    `address` is the value of the `topic` field on a `stream-data` frame, or of
    the `command` field on a `command-request`; it is not a URL path, and there is
    no second connection to open. The `session` channel carries the frames that
    belong to the connection rather than to any one channel.

    Everything the mod sends is one of four frame types, unioned as
    `components.schemas.InboundFrame`: `stream-data`, `command-response`,
    `event` and `error`. Write that router first: every per-channel message below
    narrows one of its arms, and none of them adds a fifth.

    Route on the `type` field. Each arm pins it with a `const`, so a plain JSON
    Schema validator discriminates the union without help, and the `discriminator`
    keyword on `InboundFrame` is a label on that rather than the thing doing the
    work. `InboundFrame` is a named union nothing `$ref`s: it exists to be read,
    and the two arms that are frames in their own right carry their vocabularies on
    their schemas, `EventMsg` for the two `name` values and `ErrorMsg` for the
    three `code` values. The same prose reaches you through
    `components.messages.event` and `components.messages.error`, which is where
    the channels point.

    ## Nothing arrives before you ask for it

    Delivery is subscription-gated. A client receives a topic only after sending
    `subscribe` for it, and a frame the mod emits with no subscriber is DROPPED
    rather than queued: connecting and waiting yields silence forever. This is a
    property of the protocol and appears nowhere in a message shape, so a
    schema-first reader will not find it.

    ## A quantity crosses as a bare number

    The generated TypeScript types every quantity as `Value<"m">`, an object with a
    magnitude and a unit. That is the shape AFTER the SDK hydrates it. The wire
    carries a bare JSON number, and the schemas here say so, with the unit token on
    `x-sitrep-unit`. Send `{ magnitude, unit }` and the mod will not understand
    it.

    ## An enum crosses as its ordinal

    Never as its name. Where the member set is a real constraint it is a `oneOf` of
    `const` values carrying each member's name and prose. A `[Flags]` bitmask has
    no member set to validate against, because the wire carries an OR of members, so
    those carry the member table on `x-sitrep-enum` and constrain only
    `integer`. `x-sitrep-enum-kind` says which of the two an enum is.

    ## The `x-sitrep-*` annotations

    Facts the contract states that JSON Schema and AsyncAPI have no slot for. Each
    is read off the declaration site rather than written here, so an annotation that
    is absent means the declaration did not state it.

    - `x-sitrep-unit`, on a schema: the token the value crosses in, from the
      contract's `[SitrepUnit]`. `text`, `flag`, `id`, `enum` and `n/a`
      are tokens in this vocabulary too and mean what they say, so absent, `n/a`
      and `1` are three different things
    - `x-sitrep-enum` and `x-sitrep-enum-kind`, on an enum's schema: see the
      section above
    - `x-sitrep-delivery`, on a telemetry channel: which outbox lane its samples
      ride. `lossy-latest` coalesces to the freshest sample per topic, so a client
      that falls behind sees the latest state and never the ones it skipped.
      `reliable-ordered` delivers every sample, in order, never coalesced. This is
      not a tuning knob: the `reliable-ordered` channels are the DISCRETE
      occurrences, and a client that treats `flight.started` as lossy misses a
      launch whose frame a later one replaced
    - `x-sitrep-delay-role`, on a telemetry channel: `delayed` rides the
      light-time clock, so the value is what the vantage KNEW rather than what is
      true now. `true-now` bypasses it, and is for a ground-side fact with no
      analogue in flight, such as whether the SCANsat assembly is installed at all
    - `x-sitrep-keyframe-interval-ut`, on a telemetry channel: the UT seconds
      after which it re-emits unconditionally, with nothing changed. It is what
      makes a late subscriber, a quickload and a rejoin recoverable without waiting
      for the next real change. The seconds are GAME seconds and the two clocks are
      not proportional: UT runs at up to 100000x wall-clock under timewarp and does
      not advance at all while the game is paused. So this is not a wall-clock
      duration, and a wall-clock timer sized off it fires long before the keyframe
      under warp and never fires at all while paused. Measure staleness in the same
      clock the number is in, by watching `Meta.validAt` advance; if you need a
      wall-clock bound, convert with the live `warpRate` on `time.warp` and treat
      its `paused` as "no keyframe is due"
    - `x-sitrep-absence-is-data`, on a telemetry channel: `true` means an empty
      value is a real answer and arrives as a tombstone from the first tick (no
      target selected, no crew aboard). Without it a channel is held back until it
      has had a real value, so silence there means "not yet" rather than "none"
    - `x-sitrep-delayed`, on a command channel: whether the write rides the
      uplink instead of taking effect when it is pressed
    - `x-sitrep-empty`, on a schema: the shape IS the absence of one, as
      `NoCommandArgs` is, rather than a shape whose fields were dropped

    ## This is not a list of every channel on the wire

    It cannot become one. A DYNAMIC namespace is registered at RUNTIME and
    materialises its channels per subject, so no statically declared type exists for
    the generator to find and nothing about it appears here: `silence.<guid>.*`,
    `currency.<guid>.<currency>`, per-vessel part actions, and each Uplink's own.
    Asking this document "is there a per-vessel X" gets a confident no about a
    channel that has been published all along. An Uplink's own channels and commands
    are likewise absent: they live in the Uplink's contract slice, not in the core
    one this document is generated from.

    The prefix alone does not tell you which half a channel is in, and `fleet.` is
    the case that proves it: `fleet.silence` is STATICALLY declared and is in this
    document, while `fleet.<guid>.<field>` is dynamic and is not. Reading the one
    as evidence about the other goes wrong in both directions.

    ## What a schema cannot say about this contract

    - **Absence and staleness are a CLIENT-side projection.** A reader hook answers
      with a state machine over never-seen, not-ours, known-absent, fresh, stale, and
      stale-with-a-model. None of that is in a message shape: it is what the client's
      timeline store makes of the frames it has. A schema-first author will assume a
      payload is either present or missing, and it is neither
    - **The delay model.** A value is relative to a VANTAGE, a command centre, and
      arrives light-time late. That is why `validAt` and `deliveredAt` are
      different numbers, why a command is delayed and a fact about the local station
      is not, and why a stale value can still be worth acting on. The tokens fit in a
      schema; what they cost an operator is a paragraph
    - **Why a field is shaped as it is.** The reasoning behind the shapes is in the
      contract's own doc comments, which is why this document carries every one of
      them into a `description` rather than emitting a list of names
  license:
    name: MIT
    url: https://github.com/ksp-gonogo/gonogo/blob/main/LICENSE
  tags:
    - name: session
      description: Frames belonging to the connection rather than a channel
    - name: alarm
      description: 2 telemetry channels, 2 commands
    - name: career
      description: 3 telemetry channels, 9 commands
    - name: commandCentre
      description: 2 telemetry channels, 0 commands
    - name: comms
      description: 10 telemetry channels, 1 command
    - name: crash
      description: 1 telemetry channel, 0 commands
    - name: deployed
      description: 1 telemetry channel, 0 commands
    - name: dv
      description: 2 telemetry channels, 0 commands
    - name: fleet
      description: 1 telemetry channel, 0 commands
    - name: flight
      description: 5 telemetry channels, 0 commands
    - name: game
      description: 1 telemetry channel, 0 commands
    - name: isru
      description: 2 telemetry channels, 0 commands
    - name: ksp
      description: 1 telemetry channel, 6 commands
    - name: parts
      description: 1 telemetry channel, 0 commands
    - name: recovery
      description: 1 telemetry channel, 0 commands
    - name: reliability
      description: 2 telemetry channels, 0 commands
    - name: robotics
      description: 2 telemetry channels, 9 commands
    - name: science
      description: 6 telemetry channels, 2 commands
    - name: spaceCenter
      description: 7 telemetry channels, 0 commands
    - name: system
      description: 3 telemetry channels, 1 command
    - name: target
      description: 1 telemetry channel, 0 commands
    - name: time
      description: 2 telemetry channels, 2 commands
    - name: vessel
      description: 20 telemetry channels, 21 commands
  externalDocs:
    description: "Writing an Uplink: the client half, the mod half, and the codegen between them"
    url: https://github.com/ksp-gonogo/gonogo/blob/main/docs/creating-an-uplink.md
defaultContentType: application/json
servers:
  mod:
    host: localhost:8090
    protocol: ws
    title: The Gonogo mod's telemetry socket
    description: "Served by the mod inside the running game. The host and port are configurable per install; `localhost:8090` is the default the client falls back to when nothing else is configured. There is no path: the URL is `ws://<host>:<port>`."
channels:
  alarm.scet:
    address: alarm.scet
    description: |-
      Carries an array of `ScetAlarm`.

      One armed SCET alarm as the simulation host holds it: the `alarm.scet`
      channel is a bare array of these.

      The roster exists so an operator can see what is still armed after a
      reconnect, and so a client can disarm an entry it no longer remembers. It is
      ground-side bookkeeping, a list of things somebody asked for rather than a
      reading of any craft, which is why the channel does not ride the reveal
      clock.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: reliable-ordered
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 3600
    messages:
      streamData:
        $ref: "#/components/messages/streamData.alarm.scet"
      event:
        $ref: "#/components/messages/event"
  alarm.scet.arm:
    address: alarm.scet.arm
    description: |-
      Takes `ScetAlarmArmArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `alarm.scet.arm`'s args: register an alarm with the simulation host, or
      replace one already registered under the same `ScetAlarmArmArgs.id`.

      Never delayed. Arming changes nothing aboard the craft, so there is no
      light-time fiction to honour, and the same reasoning `time.setWarpIndex` has
      always carried applies: this is a control on the simulation, not a signal to
      a spacecraft. Delayed it would also be unusable, because an alarm for an
      event less than one light-time away could never be armed in time, and a
      delayed command is dropped outright during a blackout, which is exactly when
      a SCET alarm earns its keep.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.alarm.scet.arm"
      commandResponse:
        $ref: "#/components/messages/commandResponse.alarm.scet.arm"
  alarm.scet.disarm:
    address: alarm.scet.disarm
    description: |-
      Takes `ScetAlarmDisarmArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `alarm.scet.disarm`'s args: forget the alarm with this id. Silently succeeds
      for an id the host does not hold, so a client reconciling its list against
      the roster never has to ask first.

      Never delayed, for the reason its opposite is not: the inverse of an instant
      act must not be slower than the act.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.alarm.scet.disarm"
      commandResponse:
        $ref: "#/components/messages/commandResponse.alarm.scet.disarm"
  alarm.scet.fired:
    address: alarm.scet.fired
    description: |-
      Carries a `ScetAlarmFired`.

      The notice that a SCET alarm has fired and the warp has been stopped.

      **It says THAT one fired and WHEN, and deliberately nothing about the
      craft.** The stop is universal, because warp is; the telemetry is not, and
      stays a light-time behind. So an operator can be told their alarm went off
      while every reading beside it still shows the craft as it was minutes ago,
      and this payload carries nothing that would close that gap early. Whatever
      the craft was doing at `ScetAlarmFired.firedAtUt` arrives when it arrives.

      The two fields are the honest minimum. The id is the operator's own handle,
      which tells them nothing they did not already write down. The instant is
      inseparable from the stop: without it a warp that halted at one universal
      time is indistinguishable from one that halted at another, and the operator
      cannot tell which of two armed alarms stopped them.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: reliable-ordered
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 3600
    messages:
      streamData:
        $ref: "#/components/messages/streamData.alarm.scet.fired"
      event:
        $ref: "#/components/messages/event"
  career.contract.accept:
    address: career.contract.accept
    description: |-
      Takes `ContractActionArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      Args shared by `career.contract.accept`/`decline`/`cancel`: the contract's
      stable `ContractID` (stringified; the same id the READ side emits for each
      contract, `career.status`'s `contracts[].id`). Which of the three verbs is
      valid depends on the contract's current state (accept/decline require an
      offered contract, cancel an active one); an out-of-state request comes back
      `CommandErrorCode.ModeUnavailable`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.contract.accept"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.contract.accept"
  career.contract.cancel:
    address: career.contract.cancel
    description: |-
      Takes `ContractActionArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      Args shared by `career.contract.accept`/`decline`/`cancel`: the contract's
      stable `ContractID` (stringified; the same id the READ side emits for each
      contract, `career.status`'s `contracts[].id`). Which of the three verbs is
      valid depends on the contract's current state (accept/decline require an
      offered contract, cancel an active one); an out-of-state request comes back
      `CommandErrorCode.ModeUnavailable`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.contract.cancel"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.contract.cancel"
  career.contract.decline:
    address: career.contract.decline
    description: |-
      Takes `ContractActionArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      Args shared by `career.contract.accept`/`decline`/`cancel`: the contract's
      stable `ContractID` (stringified; the same id the READ side emits for each
      contract, `career.status`'s `contracts[].id`). Which of the three verbs is
      valid depends on the contract's current state (accept/decline require an
      offered contract, cancel an active one); an out-of-state request comes back
      `CommandErrorCode.ModeUnavailable`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.contract.decline"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.contract.decline"
  career.crew.fire:
    address: career.crew.fire
    description: |-
      Takes `FireCrewArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `career.crew.fire`'s args: a hired kerbal's `ProtoCrewMember.name`, the same
      id the READ side emits for each roster row (`spaceCenter.crewRoster`'s
      entries). Firing (`KerbalRoster.SackAvailable`) costs nothing and simply
      returns the kerbal to the applicant pool, so it is reversible (a re-hire
      brings them back with the same stats). Valid only on a kerbal whose current
      roster standing is Available; a name that doesn't resolve on the hired-crew
      roster comes back `CommandErrorCode.NotFound`, one that resolves but isn't
      Available (Assigned/Dead/Missing) comes back
      `CommandErrorCode.ModeUnavailable`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.crew.fire"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.crew.fire"
  career.crew.hire:
    address: career.crew.hire
    description: |-
      Takes `HireApplicantArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `career.crew.hire`'s args: the applicant's `ProtoCrewMember.name`, the same
      id the READ side emits for each applicant (`spaceCenter.astronautComplex`'s
      `applicants[].name`), so a client hires the applicant it read. Hiring debits
      the current recruit cost from funds and moves the applicant into the crew
      roster. An applicant that has left the pool since (a stale pool, someone
      else hired, KSP refreshed it) comes back `CommandErrorCode.NotFound`; an
      unaffordable hire `CommandErrorCode.Range`; a full roster (Astronaut Complex
      cap) or a non-career save `CommandErrorCode.ModeUnavailable`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.crew.hire"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.crew.hire"
  career.facilities:
    address: career.facilities
    description: |-
      Carries a `CareerFacilities`.

      The `career.facilities` channel payload: the space centre's buildings, each
      with the tier it stands at and the ladder it stands on.

      **It arrives only while the game can answer, and stops otherwise.** A
      facility's tier count is readable from the live building objects, which KSP
      instantiates at the space centre, in the editor and in flight near the KSC,
      and nowhere else. From the tracking station or from orbit there is no
      reading to take, so this channel goes SILENT rather than reporting a row of
      nulls. The last reading stands, dated, through the client's ordinary
      staleness machinery: a whole channel can be held and said to be held, where
      a nullable field on a channel that keeps ticking cannot.

      A tier count does not change during a save, so a ladder held from an earlier
      scene is still true. The tier standing on it can move, and does when the
      player buys an upgrade, so both travel together and are dated together
      rather than one being trusted further than the other.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.career.facilities"
      event:
        $ref: "#/components/messages/event"
  career.facility.upgrade:
    address: career.facility.upgrade
    description: |-
      Takes `UpgradeFacilityArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `career.facility.upgrade`'s args: the facility's id as the READ side keys
      it: the `SpaceCenterFacility` enum name (e.g. `"VehicleAssemblyBuilding"`,
      `"LaunchPad"`), the same id `career.status`'s `facilities` map uses.
      Upgrading raises the facility one tier and deducts its upgrade cost from
      funds.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.facility.upgrade"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.facility.upgrade"
  career.mode:
    address: career.mode
    description: |-
      Carries a `CareerMode`.

      The `career.mode` channel payload: a single `GameMode`, the active save's
      mode. Produced by `Sitrep.Host.CareerViewProvider.BuildCareerMode`, which
      reads the raw `Game.Modes.ToString()` string `Gonogo.KSP.KspHost` captures
      each tick. The whole payload is `null` only when no game is loaded at all
      (main menu / no save): a "no data yet" absence, never a fabricated mode;
      once a save is loaded the mode is always one of the four `GameMode` members.

      **Typing-only mirror.** This type reproduces the EXACT serialized shape
      `CareerViewProvider.BuildCareerMode` emits (`{ "mode": <int> }`, the enum's
      integer ordinal, matching every other enum in this codec; see
      `Sitrep.Core.Serialization.JsonWriter`). It is a codegen marker, not
      serialized itself.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.career.mode"
      event:
        $ref: "#/components/messages/event"
  career.status:
    address: career.status
    description: |-
      Carries a `CareerStatus`.

      The `career.status` channel payload: the KSC and career-mode snapshot, in
      four groups (economy, contracts, strategies, tech). The space centre's
      buildings are NOT here: they ride `CareerFacilities`, for the staleness
      reason that type's own doc gives.

      **Three states, and they mean different things.** The whole payload is
      `null` in SANDBOX, where there is no career at all. A non-null payload with
      a sub-group `null` means career mode is running and that group is genuinely
      unavailable this tick. All four top-level keys are ALWAYS present, each
      nullable, never omitted, so a missing key is a protocol error rather than an
      absent group.

      Every number is nullable and `null` is never a sentinel: a field is `null`
      whenever the raw value is absent or non-finite. The two counts
      (`CareerStrategies.activeCount`, `CareerTech.unlockedCount`) are the only
      non-nullable numbers, because an empty list counts as zero rather than as
      unknown.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.career.status"
      event:
        $ref: "#/components/messages/event"
  career.strategy.activate:
    address: career.strategy.activate
    description: |-
      Takes `ActivateStrategyArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `career.strategy.activate`'s args: the strategy's stable id plus the slider
      fraction to activate it at. `ActivateStrategyArgs.strategyId` is
      `StrategyConfig.Name` (e.g. `"OutsourceRnDStrategy"`): the exact same id the
      READ side emits for each strategy (`career.status`'s `strategies[].id`), so
      a client activates using the id it already read.
      `ActivateStrategyArgs.factor` is the 0..1 slider fraction the strategy is
      committed at (its up-front funds/science/reputation cost scales with it); it
      is best-effort: a strategy with no factor slider ignores it and activates at
      its fixed factor.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.strategy.activate"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.strategy.activate"
  career.strategy.deactivate:
    address: career.strategy.deactivate
    description: |-
      Takes `DeactivateStrategyArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `career.strategy.deactivate`'s args: the strategy's stable
      `StrategyConfig.Name` id (see `ActivateStrategyArgs.strategyId`).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.strategy.deactivate"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.strategy.deactivate"
  career.tech.unlock:
    address: career.tech.unlock
    description: |-
      Takes `UnlockTechArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `career.tech.unlock`'s args: the tech node's `techID` (the same id the READ
      side emits for each tech node, `career.status`'s `tech.nodes[].id`).
      Unlocking deducts the node's science cost.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.career.tech.unlock"
      commandResponse:
        $ref: "#/components/messages/commandResponse.career.tech.unlock"
  commandCentre.roster:
    address: commandCentre.roster
    description: |-
      Carries an array of `CommandCentreEntry`.

      One command centre in the `commandCentre.roster` channel: a vantage/
      authority the operator can command from and observe at (Plan 3). The union
      of the stock CommNet home nodes (KSC, Extra Ground Stations, Kerbal
      Konstructs sites) and crewed control-source vessels. Produced by the mod's
      command-centre enumeration pass.

      The channel is a BARE ARRAY of these entries (tagged `isArray: true`, like
      `SpaceCenterPoiEntry`), one per active centre keyed by
      `CommandCentreEntry.id`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 1000
    messages:
      streamData:
        $ref: "#/components/messages/streamData.commandCentre.roster"
      event:
        $ref: "#/components/messages/event"
  commandCentre.separation:
    address: commandCentre.separation
    description: |-
      Carries a `CommandCentreSeparation`.

      How far every active command centre is from every other, one-way, along the
      routed CommNet path. The number a human at one vantage needs to know how
      long their words take to reach a human at another.

      **Sparse, and that is the contract.** A pair with no route has NO entry
      rather than a zero or a sentinel: commands and messages ride the relay
      network, so an unroutable pair has no separation to quote, and inventing one
      would make an unreachable correspondent look merely distant. A reader that
      finds no entry for a pair knows the separation is unavailable, which is a
      different fact from it being large.

      Each centre against ITSELF is always present as an explicit zero: a node is
      exactly no distance from itself, and without the row a reader would fall
      through to "unavailable" for the one pair it is most certain about.

      DELAYED, on the same reasoning as `comms.delay`: a separation is measured
      between two places a signal has to cross, so the figure a reader holds is
      the one their own light has brought them. It is the separation as OBSERVED,
      never as it is at this instant, and a pair whose geometry has changed since
      says so one light-time later.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 1000
    messages:
      streamData:
        $ref: "#/components/messages/streamData.commandCentre.separation"
      event:
        $ref: "#/components/messages/event"
  comms.commandCentre:
    address: comms.commandCentre
    description: |-
      Carries a `CommsCommandCentre`.

      The `comms.commandCentre` payload: identifies WHICH command centre the
      active vessel's control path currently terminates at, vanilla KSC or a
      crewed control-source vessel (the stock "6-kerbal command center" mechanic),
      so a client can show its own stats against the right name instead of
      assuming KSC. Shares its id/kind scheme with `CommandCentreEntry` (the
      `commandCentre.roster` union): it names ONE entry from that same set,
      whichever one the vessel's own `ControlPath` resolved to this tick. Every
      field is null when there is no live remote centre right now (no connection,
      or the terminal node matches neither a ground station nor a crewed control
      source), the existing comms.link/comms.connectivity "No signal" case already
      covers that for a reader.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.commandCentre"
      event:
        $ref: "#/components/messages/event"
  comms.connectivity:
    address: comms.connectivity
    description: |-
      Carries a `CommsConnectivity`.

      The `comms.connectivity` payload: always-present, sourced from the elected
      backend. Ground-side truth about whether the active vessel has a control
      link home right now.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.connectivity"
      event:
        $ref: "#/components/messages/event"
  comms.controlState:
    address: comms.controlState
    description: |-
      Carries a `CommsControlState`.

      The `comms.controlState` payload: always-present, elected backend.
      `CommsControlState.reason` is a nullable annotation (absent = no
      annotation), never an empty-string sentinel.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.controlState"
      event:
        $ref: "#/components/messages/event"
  comms.degrade:
    address: comms.degrade
    description: |-
      Carries a `CommsDegrade`.

      The `comms.degrade` payload: how degraded the active vessel's link home is
      right now, on one fixed scale, as the comms backend in force grades it.

      `CommsDegrade.level` runs from 0, nothing wrong, to 1, nothing usable
      getting through. It is ABSENT when nothing graded the link, and absent is a
      third answer rather than a low one: "nobody rated this" and "this link is
      perfect" are opposite instructions to anything choosing a quality, so a
      consumer must branch on the absence rather than default it to a number.

      **Read this rather than deriving a quality from `comms.signalStrength`.**
      That field is 0..1 too, and it is a different quantity on a stock install
      than on a RealAntennas one: a range fraction against an antenna curve versus
      spare room on a data-rate ladder. Nothing on the wire distinguishes them, so
      `1 - signalStrength` is two different quality curves on two saves. This
      channel names its rule, so a consumer acting on the number can see which
      grading produced it.

      DELAYED, like the link observations it grades and unlike its always-live
      `comms.delay` sibling. A rating is an observation of the craft's link, so an
      operator should learn of a degradation one light-time after it happened, at
      the same instant the telemetry that suffered it arrives. It is delay-gated
      on the ordinary terms, so through a blackout it holds at last-known; the
      disconnect edge itself reaches a client on `comms.link`, which is the
      connectivity authority and is exempt from that freeze precisely so it can
      report it.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.degrade"
      event:
        $ref: "#/components/messages/event"
  comms.delay:
    address: comms.delay
    description: |-
      Carries a `CommsDelay`.

      The `comms.delay` payload: the CORE SignalDelay capability's output, gated
      by the `comms.signalDelay.enabled` config flag. `CommsDelay.oneWaySeconds`
      distinguishes two DIFFERENT "no delay" cases by value (R7: typed absence,
      never a single overloaded sentinel):

      - **null**: no measurable `CommsPath` (no path home, or incomplete hop
        geometry). There is nothing to measure, so nothing is reported.
        `CommsDelay.source` is `CommsDelaySource.None`.
      - **0**: the delay feature is disabled (`comms.signalDelay.enabled = false`)
        but the vessel IS connected. A genuine "zero delay applied", not an
        absence. `CommsDelay.source` is also `CommsDelaySource.None` here: the two
        cases share the same `Source` and are told apart only by whether the value
        is null.
      - a real number: `CommsDelay.source` is `CommsDelaySource.SignalDelay`;
        gonogo's own light-time math over the elected backend's hop geometry.

      Two of the zeroes name their own reason instead of sharing
      `CommsDelaySource.None`: `CommsDelaySource.Simulation` and
      `CommsDelaySource.NoCommsModel`. The second is what tells a client that a
      board showing no path and no relay graph is a save with the CommNet
      difficulty option off, not a craft in a permanent blackout: the blackout
      reports `null` here and `connected:false` on `CommsLink`, and this reports
      `0` and `connected:true`.

      DELAYED, like the telemetry it describes. A light-time is measured over the
      route a signal actually took, so the figure that reaches an operator is the
      delay as it WAS when the light left, and a craft whose delay has grown says
      so one light-time after it grew. Read it as an observation rather than as
      the current state of the link.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.delay"
      event:
        $ref: "#/components/messages/event"
  comms.link:
    address: comms.link
    description: |-
      Carries a `CommsLink`.

      The `comms.link` connectivity MetaTopic: the ONE client-facing answer to "is
      there a control link home right now?", carried as a **Delayed,
      freeze-EXEMPT** channel (see `ChannelEngine.ConnectivityMetaTopic`). It is
      the delayed successor to the de-publicised TrueNow `CommsConnectivity`
      observation channel: clients (the app's SignalLossIndicator/CameraFeed, the
      kOS terminal's line-mode gate) read `comms.link.connected` instead of any
      raw `comms.*` observation.

      **Why its own topic, freeze-exempt:** the link state is what REPORTS the
      freeze, so: exactly parallel to `comms.delay` being exempt from its own
      delay: it must be exempt from the freeze it drives. It reveals the
      disconnect edge at `T+delay` (you learn of the outage one light-time after
      it happens) and keeps reporting `connected:false` through the blackout, so
      the client's "NO SIGNAL" flips at the correct delayed instant. The
      `VesselComms` observation struct (signalStrength/controlState) stays Delayed
      AND freeze-gated: it freezes at last-known through the outage.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.link"
      event:
        $ref: "#/components/messages/event"
  comms.network:
    address: comms.network
    description: |-
      Carries a `CommsNetwork`.

      The `comms.network` payload: always-emitted, but its richness tracks the
      elected backend (a "backend-dependent detail"). Under bare CommNet this may
      be a single home-edge; under RealAntennas it enumerates the relay graph.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.network"
      event:
        $ref: "#/components/messages/event"
  comms.occlusion:
    address: comms.occlusion
    description: |-
      Carries a `CommsOcclusion`.

      The `comms.occlusion` payload: always-present, sourced from the elected
      backend (the PROVIDER axis `Comms.cs`'s header describes). The declared
      occlusion model, named, with its rule already applied to every celestial
      body the game knows about.

      TRUE-NOW like the rest of the comms family, and for a stronger reason than
      most: this is not an observation of the vessel at all, it is a statement
      about the universe's geometry and the rule the elected backend applies to
      it. Delaying it would mean a predictor computing tomorrow's blackout from
      yesterday's model.

      Effectively static within a session: the body set does not change and the
      multipliers change only if the player edits the difficulty settings. The
      producer republishes an unchanged instance, which the emitter's change-gate
      suppresses, so the channel costs a keyframe and nothing else.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.occlusion"
      event:
        $ref: "#/components/messages/event"
  comms.path:
    address: comms.path
    description: |-
      Carries a `CommsPath`.

      The `comms.path` payload: always-present, elected backend. Ordered hops from
      the active vessel to KSC. Empty `CommsPath.hops` = no path home (a real,
      control-loss state, not absence-of-data).

      DELAYED, and NEVER RECKONABLE. The route is the one the arriving signal
      took, so it reveals with the telemetry that came down it. It also carries no
      forward model and cannot be given one: a route changes DISCRETELY, a relay
      drops below the horizon and the whole chain re-solves to different hops, and
      every basis a reckoner could declare (Kepler propagation, dead reckoning,
      rate integration) moves a continuous quantity. What you are shown is the
      topology AS OBSERVED; nothing may extrapolate it forward.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.path"
      event:
        $ref: "#/components/messages/event"
  comms.setSimulationDelayPolicy:
    address: comms.setSimulationDelayPolicy
    description: |-
      Takes `SetSimulationDelayPolicyArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      Arguments to `comms.setSimulationDelayPolicy`: apply signal delay during a
      simulation, or cut it.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.comms.setSimulationDelayPolicy"
      commandResponse:
        $ref: "#/components/messages/commandResponse.comms.setSimulationDelayPolicy"
  comms.signalStrength:
    address: comms.signalStrength
    description: |-
      Carries a `CommsSignalStrength`.

      The `comms.signalStrength` payload: always-present, elected backend. 0..1.
      CommNet gives a coarse range-fraction; RealAntennas gives a
      link-budget-derived value.

      A save with the stock CommNet difficulty option off models no link budget at
      all and reports 1 here: nothing attenuates a link that is not modelled. A
      reader that needs to know this is not a grading has
      `CommsDelaySource.NoCommsModel` on `comms.delay`, which is the one
      discriminator for the whole family rather than a second one per field.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.comms.signalStrength"
      event:
        $ref: "#/components/messages/event"
  crash.lastCrash:
    address: crash.lastCrash
    description: |-
      Carries a `CrashReport`.

      The payload for the `crash.lastCrash` channel: a single "last notable crash"
      record for the current save, delivered on the Delivery.ReliableOrdered event
      lane. Mirrors the wire shape the consumers already parse
      (`FlightOutcomeBanner.parseCrash`, `LaunchDirector`) field-for-field; the
      frozen captures in `packages/app/src/__tests__/fixtures/crash-payloads.ts`
      are the wire ground truth this type names.

      TYPING/codegen marker only. The producer (`Gonogo.KSP.CrashUplink`)
      hand-flattens the live-KSP crash into a `Dictionary<string, object?>` via
      `Sitrep.Host.Crash.CrashPayload.Build` before publishing, so JsonWriter only
      ever sees the dictionary: this POCO exists solely so the TS SDK has a
      concrete payload type to name (it is on `WirePayloadCoverageTests`'s
      producer-flatten allowlist for exactly that reason).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: reliable-ordered
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 3600
    messages:
      streamData:
        $ref: "#/components/messages/streamData.crash.lastCrash"
      event:
        $ref: "#/components/messages/event"
  deployed.bases:
    address: deployed.bases
    description: |-
      Carries an array of `DeployedEntry`.

      One entry in the `deployed.bases` channel payload, a Breaking Ground
      deployed-science experiment. The channel payload is a BARE ARRAY
      (`DeployedEntry[]`) or `null`. Unlike the other two channels,
      `deployed.bases` is captured GLOBALLY across every loaded vessel: a deployed
      cluster is its own ground vessel, so an entry normally describes a vessel
      OTHER than the active one, distinguished by `DeployedEntry.vesselName`.
      Typing-only mirror of
      `Sitrep.Host.BreakingGroundViewProvider.BuildDeployedEntry`; see
      `ExperimentEntry` for the "no wire change, all fields nullable" rationale.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.deployed.bases"
      event:
        $ref: "#/components/messages/event"
  dv.stages:
    address: dv.stages
    description: |-
      Carries an array of `StageDeltaVEntry`.

      One ΔV-producing stage of the active vessel, straight from KSP's STOCK
      `VesselDeltaV` stage simulation: the same numbers the in-game ΔV app shows,
      with atmosphere, ISP, crossfeed and staging all handled by the game. Only
      the stages that actually produce ΔV appear, the same set the in-game app
      lists rather than the raw stage list, so stage numbers can be sparse.

      The `dv.stages` payload is a BARE ARRAY of these or `null`: never a wrapper
      object. The whole array is `null` when the stock simulation is not ready or
      there is no active vessel.

      **Every field is nullable, and `null` is never a sentinel.** A field is
      `null` whenever the raw value is absent OR non-finite, so a stage the
      simulation reports as `NaN` or `Infinity` reaches you as `null` rather than
      as a number you would have to test.

      Carries no `meta` of its own: provenance rides the envelope
      (`StreamData.Meta`), never the payload body.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.dv.stages"
      event:
        $ref: "#/components/messages/event"
  dv.summary:
    address: dv.summary
    description: |-
      Carries a `StageDeltaVSummary`.

      The `dv.summary` channel payload: the whole-vessel ΔV rollup KSP's stock
      `VesselDeltaV` exposes alongside the per-stage `StageDeltaVEntry` list: the
      ΔV-producing stage count plus the vacuum / sea-level / current totals and
      total burn time. A SINGLE WRAPPER OBJECT (or `null` when the stock sim isn't
      ready / there is no active vessel), so the Topic tag sits on this type
      directly with the default `IsArray = false`.

      **Typing-only mirror** of `StageDeltaVViewProvider.BuildSummary`, same
      convention as `StageDeltaVEntry`: hand-built by the provider, never
      serialized itself, no per-payload `Meta` (it rides the envelope).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.dv.summary"
      event:
        $ref: "#/components/messages/event"
  fleet.silence:
    address: fleet.silence
    description: |-
      Carries a `FleetSilence`.

      The fleet-wide silence roster on `fleet.silence`: every vessel the tracker
      holds a reckoning for, in one payload.

      **Why this exists when `silence.<guid>.state` already does.** A per-vessel
      topic can only be read by something that already knows which vessel to ask
      for, which makes it unusable as the input to anything that has to work the
      fleet out for itself. Concretely: a contribution declares its dependencies
      STATICALLY at module load, so no contribution can name a per-guid topic, and
      the client-side bridge that reaches those topics only holds vessels some
      component is ALREADY subscribed to. A fan-out over that bridge sees exactly
      the vessels a widget had already rendered, which is circular. One static
      topic carrying every entry breaks the circle: a Processor declares it once,
      derives once per frame, and a contribution fans out over entries that
      genuinely exist.

      **Delayed on the MAIN node, and that is a real difference.**
      `FleetVesselSilence` rides the per-vessel node, so each vessel's reckoning
      arrives on that vessel's own light-time and is freeze-exempt. A single
      aggregate cannot do that: one payload has one node and one delay. So this
      rides the main node's delay, exactly as `SystemVessels` does while carrying
      per-vessel `CommsConnected` alongside the per-subject-delayed
      `FleetVesselContact`. The per-vessel topic stays authoritative for one
      vessel on that vessel's own clock; this is the fleet-wide index. A consumer
      that needs the former must not substitute the latter.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.fleet.silence"
      event:
        $ref: "#/components/messages/event"
  flight.current:
    address: flight.current
    description: |-
      Carries a `FlightCurrent`.

      The `flight.current` channel payload: a UT-indexed **Value** (LossyLatest +
      DelayRole.Delayed, mirroring every `vessel.*` channel): the authoritative
      "what flight is this, and what phase is it in" reading for whichever vessel
      gonogo is presently reporting as active. That is the vessel the game is
      flying, with one exception: a kerbal on EVA does not become the subject of
      this reading, the craft they stepped out of stays it for as long as that
      craft is in the world. `FlightCurrent.phase` reuses `Situation` rather than
      inventing a parallel enum; see FlightLifecycleSampler doc reference in
      `Sitrep.Host.Flight` for the exact phase source.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.flight.current"
      event:
        $ref: "#/components/messages/event"
  flight.ended:
    address: flight.ended
    description: |-
      Carries a `FlightEnded`.

      The `flight.ended` channel payload: a Delivery.ReliableOrdered +
      DelayRole.Delayed event, fired once per flight when it stops being trackable
      (recovered, crashed/destroyed, or reverted). Rides the SAME delay class as
      `crash.lastCrash`/`recovery.lastSummary`, so it inherits the already-proven
      revert-before-reveal erasure invariant
      (`RevertBeforeRevealErasesAReliableOrderedDelayedEventForever`, commit
      `82132a08`) for free: no new reveal-gate work needed.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: reliable-ordered
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 3600
    messages:
      streamData:
        $ref: "#/components/messages/streamData.flight.ended"
      event:
        $ref: "#/components/messages/event"
  flight.simulation:
    address: flight.simulation
    description: |-
      Carries a `FlightSimulation`.

      The `flight.simulation` channel payload: is this a rehearsal, and is signal
      delay being applied to it.

      **TrueNow, and it has to be.** This is meta about the stream rather than a
      reading from a craft, the same disposition `comms.delay` takes: a channel
      that told an operator "this is a simulation" only after the light-time had
      elapsed would be describing the board they were looking at four minutes ago.

      **Absence is data.** A stock install publishes nothing here, because it has
      nothing to say; see `FlightSimulation.simulated`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    x-sitrep-absence-is-data: true
    messages:
      streamData:
        $ref: "#/components/messages/streamData.flight.simulation"
      event:
        $ref: "#/components/messages/event"
  flight.started:
    address: flight.started
    description: |-
      Carries a `FlightStarted`.

      The `flight.started` channel payload: a Delivery.ReliableOrdered +
      DelayRole.Delayed event, fired the moment a genuinely NEW flight begins
      (first-ever observation of a vessel id, or a switch onto a vessel this
      session has never tracked before; see `FlightLifecycleSampler`'s doc comment
      for the exact started-vs- vesselChanged distinction).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: reliable-ordered
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 3600
    messages:
      streamData:
        $ref: "#/components/messages/streamData.flight.started"
      event:
        $ref: "#/components/messages/event"
  flight.vesselChanged:
    address: flight.vesselChanged
    description: |-
      Carries a `FlightVesselChanged`.

      The `flight.vesselChanged` channel payload: a Delivery.ReliableOrdered +
      DelayRole.Delayed event, fired whenever the operator's active-vessel focus
      moves to a DIFFERENT, already-known vessel
      (docking/undocking/EVA/tracking-station reselect): decoupled from
      `FlightStarted`/`FlightEnded`: switching focus away from a still-flying
      vessel does not end its flight.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: reliable-ordered
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 3600
    messages:
      streamData:
        $ref: "#/components/messages/streamData.flight.vesselChanged"
      event:
        $ref: "#/components/messages/event"
  game.dlc:
    address: game.dlc
    description: |-
      Carries a `GameDlc`.

      The `game.dlc` channel payload, which KSP expansions ("DLC") are installed,
      produced by `Sitrep.Host.SystemViewProvider.BuildGameDlc`. This is the
      `Meta.Dlc` path: a ground-side, scene-independent game fact (the install has
      the expansion or it doesn't), NOT a per-tick capture flag. It lets a widget
      distinguish "the player has no DLC" from "the DLC is present but nothing is
      deployed yet", chiefly `DeployedScience`, which reads Breaking Ground.

      Mirrors the exact serialized shape `SystemViewProvider.BuildGameDlc` emits
      (a wrapper object `{ "breakingGround": bool, "makingHistory": bool }`); it
      is a typing/codegen marker so a widget resolves a real payload type instead
      of `unknown`, and does NOT participate in serialization (the provider emits
      the live value tree that `JsonWriter` walks; see SitrepTopicAttribute). The
      whole payload is `null` (not an all-false object) when no sample has landed
      yet, the provider's "no data yet" vs. "DLC genuinely absent" distinction.

      Same `game`/`system`-domain convention as `SystemBodies`: no per-payload
      `Meta` field, its `Meta` rides the envelope (`StreamData.Meta`), never the
      payload body. This is a ground-side fact, so its Topic is
      `DelayRole.TrueNow`: DLC presence is known independent of any vessel's comms
      link.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.game.dlc"
      event:
        $ref: "#/components/messages/event"
  isru.converters:
    address: isru.converters
    description: |-
      Carries an array of `IsruConverterEntry`.

      One chemical converter on the active vessel. Field set matches stock's
      surface: whether it is running, and the recipe it is running, at live rates.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.isru.converters"
      event:
        $ref: "#/components/messages/event"
  isru.drills:
    address: isru.drills
    description: |-
      Carries an array of `IsruDrillEntry`.

      One drill (resource harvester) on the active vessel. The field set is
      deliberately exactly what stock ISRU has: resource, abundance, rate, deploy,
      running, plus the two identification fields every list-shaped payload in
      this contract carries. It is not "stock's fields with nulls for what a
      richer mod does", it is the literal intersection, and the intersection
      happens to be everything stock has. Anything one provider knows and another
      does not goes in `IsruDrillEntry.extensions`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.isru.drills"
      event:
        $ref: "#/components/messages/event"
  ksp.launch:
    address: ksp.launch
    description: |-
      Takes `LaunchArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `ksp.launch`'s args: load a saved craft onto a launch site. The craft is
      identified by `LaunchArgs.shipName` plus the `LaunchArgs.facility` it was
      saved from (`"VAB"`/`"SPH"`, case-insensitive, the host bridges it to KSP's
      `EditorFacility` and rebuilds the on-disk `.craft` path server-side, so the
      wire never carries a native KSP type or an absolute path). An empty ship
      name or an unrecognised facility fails admission
      (`CommandErrorCode.NotFound`/`CommandErrorCode.Range`) before the game is
      ever touched.

      `LaunchArgs.crew` is a real array of kerbal names (empty = launch unmanned),
      NOT the legacy semicolon-joined blob the old action string used: the command
      surface is JSON, so the client unwinds its `;`-encoded crew list back into
      an array before dispatching and the host assigns each name into a free craft
      seat.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.ksp.launch"
      commandResponse:
        $ref: "#/components/messages/commandResponse.ksp.launch"
  ksp.recover:
    address: ksp.recover
    description: |-
      Takes `NoCommandArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      The empty args shape, for the core commands that operate on the current
      flight or the active vessel and so take nothing: `vessel.control.stage`,
      `vessel.target.clear`, `ksp.recover`, `ksp.revertToLaunch` and
      `ksp.toTrackingStation`.

      **Send no `args` at all for these five.** The SDK's `send()` takes no second
      argument for a command typed this way, and anything you do put on the wire
      is read and discarded. Every other command requires its args.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.ksp.recover"
      commandResponse:
        $ref: "#/components/messages/commandResponse.ksp.recover"
  ksp.revertAvailability:
    address: ksp.revertAvailability
    description: |-
      Carries a `RevertAvailability`.

      The `ksp.revertAvailability` Topic payload: whether the two stock in-flight
      "revert" actions are currently available, so a widget (LaunchDirector) can
      gate its Revert-to-Launch / Revert-to-Editor controls exactly like KSP's own
      pause menu does. Produced by
      `Sitrep.Host.SystemViewProvider.BuildRevertAvailability` from the two static
      `FlightDriver` bools KSP reads when it draws those very buttons.

      The whole payload is `null` (no key emitted) outside the flight scene: the
      two flags are only meaningful in flight, and the backing `FlightDriver`
      statics carry stale values from the previous flight otherwise. When present,
      both bools are concrete (never null): a `false` means "this revert is
      genuinely not available right now," which is exactly what the gate needs.

      **Mapping (verified against KSP's `PauseMenu.drawStockRevertOptions` at
      build time):** the pause menu shows the "Revert to Launch" button (which
      calls `FlightDriver.RevertToLaunch()`, restoring the `PostInitState`) when
      `FlightDriver.CanRevertToPostInit` is set, and the "Revert to VAB/SPH"
      buttons (which call `FlightDriver.RevertToPrelaunch(...)`, returning to the
      editor) when `FlightDriver.CanRevertToPrelaunch` is set. So
      `RevertAvailability.canRevertToLaunch` maps to `CanRevertToPostInit` and
      `RevertAvailability.canRevertToEditor` maps to `CanRevertToPrelaunch`: the
      KSP field names read backwards to their button labels, so the mapping is
      deliberately the inverse of a naive name-match.

      Same `system`-uplink convention as `SystemBodies`: a scene-side fact carried
      on its own Topic with no per-payload `Meta` (it rides the envelope),
      classified `DelayRole.TrueNow`: a ground-side game-state fact, not
      comms-derived vessel telemetry.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.ksp.revertAvailability"
      event:
        $ref: "#/components/messages/event"
  ksp.revertToEditor:
    address: ksp.revertToEditor
    description: |-
      Takes `RevertToEditorArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `ksp.revertToEditor`'s args, which editor the flight reverts back into.
      `RevertToEditorArgs.editor` is a small opaque string (`"vab"` or `"sph"`,
      case-insensitive) rather than the KSP `EditorFacility` enum, so the wire
      contract never leaks a native KSP type; the host bridges the string to the
      real facility (unrecognised value fails admission with
      `CommandErrorCode.Range` before the game is ever touched).

      `ksp.revertToLaunch`, `ksp.toTrackingStation` and `ksp.recover` take no args
      (they operate on the current flight / active vessel), so they have no arg
      type here.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.ksp.revertToEditor"
      commandResponse:
        $ref: "#/components/messages/commandResponse.ksp.revertToEditor"
  ksp.revertToLaunch:
    address: ksp.revertToLaunch
    description: |-
      Takes `NoCommandArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      The empty args shape, for the core commands that operate on the current
      flight or the active vessel and so take nothing: `vessel.control.stage`,
      `vessel.target.clear`, `ksp.recover`, `ksp.revertToLaunch` and
      `ksp.toTrackingStation`.

      **Send no `args` at all for these five.** The SDK's `send()` takes no second
      argument for a command typed this way, and anything you do put on the wire
      is read and discarded. Every other command requires its args.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.ksp.revertToLaunch"
      commandResponse:
        $ref: "#/components/messages/commandResponse.ksp.revertToLaunch"
  ksp.switchVessel:
    address: ksp.switchVessel
    description: |-
      Takes `SwitchVesselArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `ksp.switchVessel`'s args: the STABLE opaque vessel id
      (`vessel.id.ToString()`, the same id `SetTargetArgs.vesselId` uses),
      resolved server-side against `FlightGlobals.Vessels`. Never a live roster
      array index a client would have to track itself: the same index-vs-stable-id
      hazard the target commands already fixed (T-1). An empty id fails admission
      with `CommandErrorCode.NotFound` before the game is ever touched.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.ksp.switchVessel"
      commandResponse:
        $ref: "#/components/messages/commandResponse.ksp.switchVessel"
  ksp.toTrackingStation:
    address: ksp.toTrackingStation
    description: |-
      Takes `NoCommandArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      The empty args shape, for the core commands that operate on the current
      flight or the active vessel and so take nothing: `vessel.control.stage`,
      `vessel.target.clear`, `ksp.recover`, `ksp.revertToLaunch` and
      `ksp.toTrackingStation`.

      **Send no `args` at all for these five.** The SDK's `send()` takes no second
      argument for a command typed this way, and anything you do put on the wire
      is read and discarded. Every other command requires its args.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.ksp.toTrackingStation"
      commandResponse:
        $ref: "#/components/messages/commandResponse.ksp.toTrackingStation"
  parts.power:
    address: parts.power
    description: |-
      Carries a `PartsPower`.

      The `parts.power` channel payload: the active vessel's electric-charge
      production surface (solar panels, batteries, fuel cells, engine alternators,
      and a rolled-up production total). Unlike the bare-array `robotics.servos`
      and the `science.*` channels, this payload is a single WRAPPER OBJECT (or
      `null` when there is no active vessel / no power sub-group): so the Topic
      tag sits on this type directly with the default `IsArray = false`.

      **Typing-only mirror.** This reproduces, field-for-field, the exact
      serialized shape `Sitrep.Host.PartsViewProvider.BuildPower` already emits
      (same names, same camelCase wire keys via `RtConfig.CamelCaseForProperties`,
      same units). It is NOT serialized itself: the wire is written by
      `JsonWriter` walking the provider's dictionary: so adding it changes no
      bytes. The four arrays and the total are each nullable to mirror the
      provider (the arrays are always present in the emitted object, but the
      contract stays permissive; the total is `null` whenever
      `SnapshotDict.GetDouble` reads no finite value).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.parts.power"
      event:
        $ref: "#/components/messages/event"
  recovery.lastSummary:
    address: recovery.lastSummary
    description: |-
      Carries a `RecoveryReport`.

      The payload for the `recovery.lastSummary` channel: a single "last notable
      recovery" record for the current save, delivered on the
      Delivery.ReliableOrdered event lane. Mirrors the wire shape the consumer
      already parses (`FlightOutcomeBanner.parseRecovery`) field-for-field, the
      recovery-side counterpart of `CrashReport`.

      TYPING/codegen marker only. The producer (`Gonogo.KSP.RecoveryUplink`)
      hand-flattens the live-KSP recovery into a `Dictionary<string, object?>` via
      `Sitrep.Host.Recovery.RecoveryPayload.Build` before publishing, so
      JsonWriter only ever sees the dictionary: this POCO exists solely so the TS
      SDK has a concrete payload type to name (it is on
      `WirePayloadCoverageTests`'s producer-flatten allowlist for exactly that
      reason).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: reliable-ordered
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 3600
    messages:
      streamData:
        $ref: "#/components/messages/streamData.recovery.lastSummary"
      event:
        $ref: "#/components/messages/event"
  reliability.parts:
    address: reliability.parts
    description: |-
      Carries an array of `ReliabilityPartEntry`.

      Per-part reliability, in the terms a reliability model actually has: a
      condition, the provider's own word for it, an optional forward survival
      probability with its horizon, and any number of consumed budgets.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.reliability.parts"
      event:
        $ref: "#/components/messages/event"
  reliability.summary:
    address: reliability.summary
    description: |-
      Carries a `ReliabilitySummary`.

      Vessel-level reliability summary. Two facts and a bag, and deliberately no
      judgements: who is modelling, and whether they are modelling at all.

      There are no roll-ups here. A malfunction/critical count over the same part
      list published from the same capture at the same UT is a second authority
      for a derivable quantity, and a second authority is how two adjacent numbers
      come to disagree. The client derives what it needs from `reliability.parts`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.reliability.summary"
      event:
        $ref: "#/components/messages/event"
  robotics.available:
    address: robotics.available
    description: |-
      Carries a `RoboticsAvailability`.

      The `robotics.available` channel payload: a single wrapper object (or `null`
      when there is no active vessel) whose one field states whether the active
      vessel carries ANY Breaking Ground robotic servo (rotor / hinge / piston).
      This is deliberately its OWN Topic, not a field folded into the bare-array
      `parts.robotics`: an empty `ServoEntry[]` can't disambiguate "vessel has no
      robotic parts" (`available: false`) from "no snapshot / no active vessel"
      (payload `null`): the very ambiguity a widget like `RoboticsConsole` /
      `RotorTachometer` needs resolved to decide whether to render a "no robotics
      on this craft" empty state versus stay dark. It is DISTINCT from the
      Breaking-Ground DLC-presence fact (that is the `deployed.available` /
      `Meta.Dlc` build): this reflects parts present on THIS vessel, so it rides
      the delay clock (Delayed), whereas DLC presence is a ground-side TrueNow
      fact.

      `RoboticsAvailability.available` is nullable to mirror
      `SnapshotDict.GetBool`'s null-on-absence rule: a snapshot recorded before
      this field existed reads as `null`; a live snapshot always carries a
      concrete `true`/`false`.

      **Typing-only mirror** of
      `Sitrep.Host.BreakingGroundViewProvider.BuildRoboticsAvailable`: see
      `PartsPower` for the "no wire change" rationale.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.robotics.available"
      event:
        $ref: "#/components/messages/event"
  robotics.rotor.reverse:
    address: robotics.rotor.reverse
    description: |-
      Takes `RotorReverseArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args for `robotics.rotor.reverse`: flips the rotor's spin direction. This is
      the one robotics command that is genuinely a toggle (the widget's intent is
      "spin the other way" relative to whatever the rotor is doing now), so it
      carries no state field, only the `RotorReverseArgs.partId` to act on.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.rotor.reverse"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.rotor.reverse"
  robotics.rotor.setBrake:
    address: robotics.rotor.setBrake
    description: |-
      Takes `RotorSetValueArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args for the rotor scalar-limit commands
      (`robotics.rotor.setRpmLimit`/`setTorqueLimit`/`setBrake`), the ABSOLUTE
      value to apply, keyed by `RotorSetValueArgs.partId`. The bounded ones
      (torque 0–100, brake 0–200) are range-validated at the send gate; out of
      range yields `CommandResult.errorCode` `CommandErrorCode.Range`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.rotor.setBrake"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.rotor.setBrake"
  robotics.rotor.setLock:
    address: robotics.rotor.setLock
    description: |-
      Takes `ServoSetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every robotics boolean actuation
      (`robotics.servo.setMotor`/`setLock` and
      `robotics.rotor.setMotor`/`setLock`): an ABSOLUTE state to apply, never a
      toggle, matching every other actuation command in this contract (see
      `SetEnabledArgs`'s doc comment). Keyed by `ServoSetEnabledArgs.partId` (the
      read side's `flightID.ToString()`).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.rotor.setLock"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.rotor.setLock"
  robotics.rotor.setMotor:
    address: robotics.rotor.setMotor
    description: |-
      Takes `ServoSetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every robotics boolean actuation
      (`robotics.servo.setMotor`/`setLock` and
      `robotics.rotor.setMotor`/`setLock`): an ABSOLUTE state to apply, never a
      toggle, matching every other actuation command in this contract (see
      `SetEnabledArgs`'s doc comment). Keyed by `ServoSetEnabledArgs.partId` (the
      read side's `flightID.ToString()`).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.rotor.setMotor"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.rotor.setMotor"
  robotics.rotor.setRpmLimit:
    address: robotics.rotor.setRpmLimit
    description: |-
      Takes `RotorSetValueArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args for the rotor scalar-limit commands
      (`robotics.rotor.setRpmLimit`/`setTorqueLimit`/`setBrake`), the ABSOLUTE
      value to apply, keyed by `RotorSetValueArgs.partId`. The bounded ones
      (torque 0–100, brake 0–200) are range-validated at the send gate; out of
      range yields `CommandResult.errorCode` `CommandErrorCode.Range`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.rotor.setRpmLimit"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.rotor.setRpmLimit"
  robotics.rotor.setTorqueLimit:
    address: robotics.rotor.setTorqueLimit
    description: |-
      Takes `RotorSetValueArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args for the rotor scalar-limit commands
      (`robotics.rotor.setRpmLimit`/`setTorqueLimit`/`setBrake`), the ABSOLUTE
      value to apply, keyed by `RotorSetValueArgs.partId`. The bounded ones
      (torque 0–100, brake 0–200) are range-validated at the send gate; out of
      range yields `CommandResult.errorCode` `CommandErrorCode.Range`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.rotor.setTorqueLimit"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.rotor.setTorqueLimit"
  robotics.servo.setLock:
    address: robotics.servo.setLock
    description: |-
      Takes `ServoSetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every robotics boolean actuation
      (`robotics.servo.setMotor`/`setLock` and
      `robotics.rotor.setMotor`/`setLock`): an ABSOLUTE state to apply, never a
      toggle, matching every other actuation command in this contract (see
      `SetEnabledArgs`'s doc comment). Keyed by `ServoSetEnabledArgs.partId` (the
      read side's `flightID.ToString()`).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.servo.setLock"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.servo.setLock"
  robotics.servo.setMotor:
    address: robotics.servo.setMotor
    description: |-
      Takes `ServoSetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every robotics boolean actuation
      (`robotics.servo.setMotor`/`setLock` and
      `robotics.rotor.setMotor`/`setLock`): an ABSOLUTE state to apply, never a
      toggle, matching every other actuation command in this contract (see
      `SetEnabledArgs`'s doc comment). Keyed by `ServoSetEnabledArgs.partId` (the
      read side's `flightID.ToString()`).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.servo.setMotor"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.servo.setMotor"
  robotics.servo.setTarget:
    address: robotics.servo.setTarget
    description: |-
      Takes `ServoSetTargetArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args for the servo target commands (`robotics.servo.setTarget`), the
      ABSOLUTE angle (hinge) or extension (piston) to drive to, keyed by the
      part's `ServoSetTargetArgs.partId`. `ServoSetTargetArgs.partId` is the same
      `flightID.ToString()` the read side emits on each `parts.robotics` servo
      entry, so a widget round-trips the exact id it already displays. A rotor has
      no target (it spins continuously); a `setTarget` aimed at one comes back
      `CommandResult.errorCode` `CommandErrorCode.ModeUnavailable`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.robotics.servo.setTarget"
      commandResponse:
        $ref: "#/components/messages/commandResponse.robotics.servo.setTarget"
  robotics.servos:
    address: robotics.servos
    description: |-
      Carries an array of `ServoEntry`.

      One entry in the `robotics.servos` channel payload, a single Breaking Ground
      robotic servo on the active vessel. The channel payload is a BARE ARRAY of
      these (`ServoEntry[]`) or `null` (never a wrapper object) so the Topic tag
      sits on this element type with `IsArray = true`.

      `ServoEntry.type` is the servo kind as a plain string on the wire, NOT an
      enum, mirroring what the provider emits today; the enum cleanup is a later
      phase. The kinds are `"rotor"`, `"hinge"`, `"rotationServo"` and `"piston"`,
      plus `"servo"` for a `BaseServo` subclass the capture does not recognise (a
      part pack's own, or one a later KSP adds), which carries only the readings
      every servo has.

      **This list is a description, not a rule.** The capture derives the kinds
      from `BaseServo` itself rather than from any written-down set, which is the
      whole point: the set used to be written down, rotation servos were left out
      of it, and every one on every craft was dropped before it reached the wire.
      A consumer should switch on the kinds it can draw and ignore the rest, never
      assume this sentence is exhaustive.

      **Typing-only mirror** of
      `Sitrep.Host.BreakingGroundViewProvider.BuildServoEntry`: see `PartsPower`
      for the "no wire change, all fields nullable" rationale.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.robotics.servos"
      event:
        $ref: "#/components/messages/event"
  science.archive:
    address: science.archive
    description: |-
      Carries an array of `ArchiveEntry`.

      One entry in the `science.archive` channel payload: a single SUBJECT out of
      the whole-career R&D archive (`ResearchAndDevelopment.GetSubjects()`): every
      subject the player has ever collected or recovered, across every mission and
      every body, not scoped to the active vessel. The channel payload is a BARE
      ARRAY (`ArchiveEntry[]`) or `null` when the save has no R&D instance to walk
      (Sandbox mode), never an empty-vs-absent distinction beyond that (a Career
      save with an R&D instance but nothing collected yet emits an empty array).

      Distinct from `ExperimentBreakdownEntry`: that one rolls up the ACTIVE
      VESSEL's currently-stored `ScienceData` blobs and goes null with no vessel
      flying; this one is career-wide ground truth and streams at the Space Center
      with nothing flying at all.

      `ArchiveEntry.remainingPotential` is `ScienceCap - Science`, computed by
      `Gonogo.KSP.KspHost.BuildScienceArchive` the same way
      `ExperimentBreakdownEntry.remainingPotential` is; this layer only passes the
      already-computed figure through, matching that entry's own mapping idiom
      rather than re-deriving it here.

      **Typing-only mirror** of
      `Sitrep.Host.ScienceViewProvider.BuildArchiveEntry`: see `ExperimentEntry`
      for the "no wire change, all fields nullable" rationale.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.science.archive"
      event:
        $ref: "#/components/messages/event"
  science.experiment.deploy:
    address: science.experiment.deploy
    description: |-
      Takes `ExperimentActionArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every science-experiment actuation command
      (`science.experiment.deploy`/`science.experiment.transmit`): the experiment
      is addressed by `ExperimentActionArgs.partId`, the part's
      `flightID.ToString()`, the SAME opaque id the read side emits in
      `science.instruments` (one entry per `ModuleScienceExperiment`, keyed by
      `flightID`). The host resolves it against the active vessel's live parts; a
      client never supplies a live array index. An empty
      `ExperimentActionArgs.partId` resolves to nothing and yields
      `CommandResult.errorCode` `CommandErrorCode.NotFound`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.science.experiment.deploy"
      commandResponse:
        $ref: "#/components/messages/commandResponse.science.experiment.deploy"
  science.experiment.transmit:
    address: science.experiment.transmit
    description: |-
      Takes `ExperimentActionArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every science-experiment actuation command
      (`science.experiment.deploy`/`science.experiment.transmit`): the experiment
      is addressed by `ExperimentActionArgs.partId`, the part's
      `flightID.ToString()`, the SAME opaque id the read side emits in
      `science.instruments` (one entry per `ModuleScienceExperiment`, keyed by
      `flightID`). The host resolves it against the active vessel's live parts; a
      client never supplies a live array index. An empty
      `ExperimentActionArgs.partId` resolves to nothing and yields
      `CommandResult.errorCode` `CommandErrorCode.NotFound`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.science.experiment.transmit"
      commandResponse:
        $ref: "#/components/messages/commandResponse.science.experiment.transmit"
  science.experimentBreakdown:
    address: science.experimentBreakdown
    description: |-
      Carries an array of `ExperimentBreakdownEntry`.

      One entry in the `science.experimentBreakdown` channel payload, a
      per-SUBJECT rollup of the same stored ScienceData rows `science.experiments`
      lists one-row-per-blob, the new home for the old GonogoTelemetry-only
      `sci.experimentBreakdown` enrichment (which had no equivalent on the base
      wire until now). `ExperimentBreakdownEntry.biome`/
      `ExperimentBreakdownEntry.situation` are parsed straight off
      `ScienceData.subjectID` via KSP's own
      `ScienceUtil.GetExperimentFieldsFromScienceID` (confirmed via decompile:
      public static, splits the subject id it was built from rather than
      re-deriving from the vessel's CURRENT position, so a subject collected
      earlier in the flight keeps its own original biome/situation).
      `ExperimentBreakdownEntry.remainingPotential` is the ABSOLUTE science still
      recoverable from the subject (`ScienceSubject.scienceCap -
      ScienceSubject.science`, via `ResearchAndDevelopment.GetSubjectByID`),
      matching the old GonogoTelemetry semantics: `0` in Sandbox mode (no R&D
      instance, no subject caps to speak of). The channel payload is a BARE ARRAY
      (`ExperimentBreakdownEntry[]`) or `null`: never a wrapper object, and never
      an empty-vs-absent distinction beyond "the whole array is null when there's
      no active vessel / the vessel carries no stored science data" (mirrors
      `ExperimentEntry`'s convention). One row per DISTINCT subject id: multiple
      stored blobs for the same subject (e.g. two crew reports from the same
      biome) collapse into one entry with `ExperimentBreakdownEntry.dataMits`
      summed across them.

      **Typing-only mirror** of
      `Sitrep.Host.ScienceViewProvider.BuildExperimentBreakdownEntry`: see
      `ExperimentEntry` for the "no wire change, all fields nullable" rationale.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.science.experimentBreakdown"
      event:
        $ref: "#/components/messages/event"
  science.experiments:
    address: science.experiments
    description: |-
      Carries an array of `ExperimentEntry`.

      One entry in the `science.experiments` channel payload, a single science
      module (or a container holding stored results) on the ACTIVE vessel. The
      channel payload is a BARE ARRAY of these (`ExperimentEntry[]`) or `null`:
      never a wrapper object, and never an empty-vs-absent distinction beyond "the
      whole array is null when there is no active vessel / the sub-group could not
      be built" (see `Sitrep.Host.ScienceViewProvider`).

      **Typing-only mirror.** This type reproduces, field-for-field, the exact
      serialized shape `Sitrep.Host.ScienceViewProvider.BuildExperimentEntry`
      already emits (same names, same camelCase wire keys via
      `RtConfig.CamelCaseForProperties`, same units). It is NOT serialized itself:
      the wire is written by `JsonWriter` walking the provider's dictionary: so
      adding it changes no bytes. Every field is nullable because each is read
      through `SnapshotDict.Get*`, which yields `null` (not a sentinel) whenever
      the raw value is absent or non-finite.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.science.experiments"
      event:
        $ref: "#/components/messages/event"
  science.instruments:
    address: science.instruments
    description: |-
      Carries an array of `InstrumentEntry`.

      One entry in the `science.instruments` channel payload, a single
      `ModuleScienceExperiment` on the ACTIVE vessel, captured as an INVENTORY /
      status row keyed by `InstrumentEntry.partId` (the part's KSP `flightID`).
      This is distinct from `ExperimentEntry`: `science.experiments` walks the
      same modules but yields one row per STORED `ScienceData` result (a module
      with no data produces no row), whereas `science.instruments` yields one row
      per module regardless of whether it currently holds data, the operability
      picture (deployed / inoperable / rerunnable / resettable / collectable) an
      operator needs to decide what to run next. The channel payload is a BARE
      ARRAY (`InstrumentEntry[]`) or `null`. Typing-only mirror of
      `Sitrep.Host.ScienceViewProvider.BuildInstrumentEntry`: see
      `ExperimentEntry` for the "no wire change, all fields nullable" rationale.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.science.instruments"
      event:
        $ref: "#/components/messages/event"
  science.lab:
    address: science.lab
    description: |-
      Carries an array of `LabEntry`.

      One entry in the `science.lab` channel payload, a Mobile Processing Lab on
      the active vessel. The channel payload is a BARE ARRAY (`LabEntry[]`) or
      `null`. Typing-only mirror of
      `Sitrep.Host.ScienceViewProvider.BuildLabEntry`: see `ExperimentEntry` for
      the "no wire change, all fields nullable" rationale.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.science.lab"
      event:
        $ref: "#/components/messages/event"
  science.sensors:
    address: science.sensors
    description: |-
      Carries an array of `SensorEntry`.

      One entry in the `science.sensors` channel payload, a single
      environmental-sensor module (`ModuleEnviroSensor`: thermometer, barometer,
      gravioli detector, accelerometer, and any modded sensor sharing the module)
      on the ACTIVE vessel. The channel payload is a BARE ARRAY (`SensorEntry[]`)
      or `null`.

      Deliberately a GENERAL sensor group: one entry per sensor module, with
      `SensorEntry.type` carrying the raw `SensorType` enum name
      (`TEMP`/`PRES`/`GRAV`/`ACC`/...) as a string: rather than four fixed
      `temp/pres/grav/acc` Values. Modded sensor types and multiple instances of
      the same type both fall out naturally; the consumer (ScienceBench)
      groups/labels by `SensorEntry.type`.

      Typing-only mirror of `Sitrep.Host.ScienceViewProvider.BuildSensorEntry`:
      see `ExperimentEntry` for the "no wire change, all fields nullable"
      rationale.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.science.sensors"
      event:
        $ref: "#/components/messages/event"
  session:
    title: Connection session
    description: |-
      Frames that belong to the connection rather than to any one channel.

      `subscribe` and `unsubscribe` name a topic and are how a client opens and
      closes delivery on it: see the subscription gate in the document description.
      `set-vantage` selects the command centre this connection commands from and
      observes at, which governs both what it reads and how long its commands take.
      An `error` frame carries a code and, when it answers a dispatch, the
      `requestId` it refers to.

      This channel has no `address` because these frames are not routed by one: they
      are typed by their own `type` field and belong to the socket itself.
    servers:
      - $ref: "#/servers/mod"
    messages:
      subscribe:
        $ref: "#/components/messages/subscribe"
      unsubscribe:
        $ref: "#/components/messages/unsubscribe"
      setVantage:
        $ref: "#/components/messages/setVantage"
      error:
        $ref: "#/components/messages/error"
  spaceCenter.astronautComplex:
    address: spaceCenter.astronautComplex
    description: |-
      Carries a `AstronautComplexInfo`.

      The `spaceCenter.astronautComplex` channel payload: the Astronaut Complex
      hire tab, the rolling pool of applicants the operator can recruit, plus the
      roster-cap context a hire is gated on. Produced by
      `Sitrep.Host.SpaceCenterViewProvider.BuildAstronautComplex`.

      A wrapper object (not a bare array) because the applicant list rides
      alongside the facility-level cap and the current active-crew count, both of
      which the hire affordance needs: the current roster comes from the separate
      `spaceCenter.crewRoster` channel, this one carries the hire side. The whole
      payload is `null` in the SANDBOX / no-career / no-game case (no applicant
      pool exists), the provider's "no data" signal, distinct from a career save
      whose pool is genuinely empty (a non-null payload with an empty
      `AstronautComplexInfo.applicants` list).

      A TS-shape-only typing/codegen marker: the provider hand-builds the dict and
      `JsonWriter` walks that live tree, this POCO never serializes. Classified
      `DelayRole.TrueNow`: the Astronaut Complex is at KSC, known independent of
      any vessel's comms link, same class as `CrewRosterEntry`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.spaceCenter.astronautComplex"
      event:
        $ref: "#/components/messages/event"
  spaceCenter.crewRoster:
    address: spaceCenter.crewRoster
    description: |-
      Carries an array of `CrewRosterEntry`.

      One kerbal in the `spaceCenter.crewRoster` channel (the hired-crew roster:
      KSP's `KerbalRoster.Crew`, owned crew that is either available or currently
      assigned to a mission) and reused verbatim for every entry in
      `AstronautComplexInfo.applicants` - ONE shape for a kerbal whether hired or
      still a candidate. Produced by
      `Sitrep.Host.SpaceCenterViewProvider.BuildCrewRoster` /
      `BuildAstronautComplex`.

      The `spaceCenter.crewRoster` channel is a BARE ARRAY of these entries
      (tagged `isArray: true`, like `LaunchSiteEntry`), one per crew member keyed
      by `CrewRosterEntry.name`. The whole payload is `null` (not an empty array)
      when no sample has landed yet, the provider's "no data yet" vs. "zero crew"
      distinction.

      LaunchDirector reads `CrewRosterEntry.name`/`CrewRosterEntry.trait`/
      `CrewRosterEntry.experienceLevel`/`CrewRosterEntry.available`/
      `CrewRosterEntry.unavailableReason` (the original, folded pair); the
      Astronaut Complex additionally reads `CrewRosterEntry.standing`, the
      authoritative standing it groups by, and `CrewRosterEntry.situation`, its
      label, plus the full stat set
      (`CrewRosterEntry.courage`/`CrewRosterEntry.stupidity`/`CrewRosterEntry.experience`/
      `CrewRosterEntry.experienceLevelDelta`) and the role tooltip text
      (`CrewRosterEntry.roleDescription`/`CrewRosterEntry.descriptionEffects`). A
      TS-shape-only typing/codegen marker: the provider hand-builds the dict and
      `JsonWriter` walks that live tree, these POCOs never serialize. Classified
      `DelayRole.TrueNow`: a ground-side career fact, same class as
      `LaunchSiteEntry`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.spaceCenter.crewRoster"
      event:
        $ref: "#/components/messages/event"
  spaceCenter.launchSites:
    address: spaceCenter.launchSites
    description: |-
      Carries an array of `LaunchSiteEntry`.

      One launch site in the `spaceCenter.launchSites` channel, the union of the
      stock KSC pad + runway, any Making History sites, and any Kerbal Konstructs
      sites (KK registers its sites into `PSystemSetup.Instance.LaunchSites` via
      the public `AddLaunchSite` API, so enumerating that one list already covers
      all three, no reflection, no hard KK link). Produced by
      `Sitrep.Host.SpaceCenterViewProvider.BuildLaunchSites`.

      The channel is a BARE ARRAY of these entries (tagged `isArray: true`, like
      the `science.*` channels), NOT a wrapper object and NOT a KSC singleton: KSP
      has many launch sites, keyed by `LaunchSiteEntry.name`. The whole payload is
      `null` (not an empty array) when no sample has landed yet, the provider's
      "no data yet" vs. "zero sites" distinction.

      Mirrors the exact per-site dict the provider emits, same field names, casing
      and nullability; a TS-shape-only typing/codegen marker (no `Meta`, same
      `system`/`spaceCenter`-domain convention as `SystemBodies`: the provider
      hand-builds the dict and `JsonWriter` walks that live tree, these POCOs
      never serialize). Classified `DelayRole.TrueNow`: ground-side facts, known
      independent of any vessel's comms link, same class as `SystemBodies` /
      `GameDlc`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.spaceCenter.launchSites"
      event:
        $ref: "#/components/messages/event"
  spaceCenter.partsAvailable:
    address: spaceCenter.partsAvailable
    description: |-
      Carries a `SpaceCenterPartsAvailable`.

      The `spaceCenter.partsAvailable` channel payload: a wrapper carrying the
      count of parts the player can place right now (tech-unlocked AND purchased
      in career; the full `PartLoader` catalogue in sandbox). Produced by
      `Sitrep.Host.SpaceCenterViewProvider.BuildPartsAvailable`.

      A wrapper object (a bare scalar has no Topic shape); the SpaceCenterStatus
      widget reads `spaceCenter.partsAvailable.count`. The whole payload is `null`
      when no sample has landed yet. A TS-shape-only typing/codegen marker that
      never serializes. Classified `DelayRole.TrueNow`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.spaceCenter.partsAvailable"
      event:
        $ref: "#/components/messages/event"
  spaceCenter.pois:
    address: spaceCenter.pois
    description: |-
      Carries an array of `SpaceCenterPoiEntry`.

      One point of interest in the `spaceCenter.pois` channel: the union of every
      launch site (`ksc`/`launchSite` kinds, the same
      `PSystemSetup.Instance.LaunchSites` walk `LaunchSiteEntry` already does,
      filtered to sites with a set spawn-point coordinate) and every surface
      contract waypoint currently Active or Offered (`contractTarget` kind, from
      `FinePrint.WaypointManager`). Produced by
      `Sitrep.Host.SpaceCenterViewProvider.BuildPois`.

      The channel is a BARE ARRAY of these entries (tagged `isArray: true`, like
      `LaunchSiteEntry`), one per POI keyed by `SpaceCenterPoiEntry.id`. The whole
      payload is `null` (not an empty array) when no sample has landed yet: the
      provider's "no data yet" vs. "zero POIs" distinction. A TS-shape-only
      typing/codegen marker (the provider hand-builds the dict, this POCO never
      serializes). Classified `DelayRole.TrueNow` (ground-side facts, same class
      as `LaunchSiteEntry`).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.spaceCenter.pois"
      event:
        $ref: "#/components/messages/event"
  spaceCenter.savedShips:
    address: spaceCenter.savedShips
    description: |-
      Carries an array of `SavedShipEntry`.

      One craft file in the `spaceCenter.savedShips` channel, a saved VAB or SPH
      design the player can launch, read from the save's craft folders via the
      stock `CraftProfileInfo` metadata loader. Produced by
      `Sitrep.Host.SpaceCenterViewProvider.BuildSavedShips`.

      The channel is a BARE ARRAY of these entries (tagged `isArray: true`), one
      per `.craft` file keyed by `SavedShipEntry.name`. The whole payload is
      `null` (not an empty array) when no sample has landed yet. A TS-shape-only
      typing/codegen marker (the provider hand-builds the dict, these POCOs never
      serialize). Classified `DelayRole.TrueNow`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.spaceCenter.savedShips"
      event:
        $ref: "#/components/messages/event"
  spaceCenter.scene:
    address: spaceCenter.scene
    description: |-
      Carries a `SpaceCenterScene`.

      The `spaceCenter.scene` channel payload: the single current KSP game scene,
      produced by `Sitrep.Host.SpaceCenterViewProvider.BuildScene`. This is the
      migration target for the legacy `kc.scene` key: `SpaceCenterScene.scene`
      carries exactly one of the six strings
      `{"Flight","SpaceCenter","Editor","TrackingStation","MainMenu","Other"}`
      (the provider folds KSP's `GameScenes` enum onto that fixed set; any scene
      outside the five named ones: `LOADING`, `PSYSTEM`, `MISSIONBUILDER`, ...:
      maps to `"Other"`).

      Mirrors the exact serialized shape the provider emits (a wrapper object `{
      "scene": string }`); a TS-shape-only typing/codegen marker that does NOT
      participate in serialization. The whole payload is `null` when no sample has
      landed yet. No per-payload `Meta` (it rides the envelope), classified
      `DelayRole.TrueNow`: a ground-side game-state fact, same class as
      `SystemBodies`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.spaceCenter.scene"
      event:
        $ref: "#/components/messages/event"
  system.bodies:
    address: system.bodies
    description: |-
      Carries a `SystemBodies`.

      The `system.bodies` channel payload: the celestial-body tree, produced by
      `Sitrep.Host.SystemViewProvider.BuildSystemBodies`. This type MIRRORS that
      provider's existing hand-built serialized shape EXACTLY (a wrapper object `{
      "bodies": [ ... ] }`); it is a typing/codegen marker so a widget resolves a
      real payload type instead of `unknown`, and does NOT participate in
      serialization (the provider still emits the live value tree that
      `JsonWriter` walks; see SitrepTopicAttribute). The whole payload is `null`
      (not an empty-bodies object) when no sample has landed yet, the provider's
      "no data yet" vs. "zero bodies" distinction.

      Deliberately carries NO `Meta` field: unlike the `vessel.*` family, this
      `system`-domain snapshot has no per-payload provenance: its `Meta` rides the
      envelope (`StreamData.Meta`), never the payload body.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.system.bodies"
      event:
        $ref: "#/components/messages/event"
  system.frame:
    address: system.frame
    description: |-
      Carries a `ControlFrame`.

      The frame the game's own navigation view is expressed in: what the player is
      looking at, and what a burn expressed relative to the control frame is held
      fixed against.

      **Why this is not a widget's choice.** A widget picks a read frame for
      itself and nothing else sees it. This is the game's, it is one at a time,
      and it is written as well as read, so a command centre can put the player's
      view where a plan is being discussed.

      **Bodies travel by name.** Every other body table in this mod is keyed by
      `bodyName`, `system.bodies` included, so a frame named the same way needs no
      join to be understood and cannot disagree with the table beside it.

      **The pulsating frames carry SETS, not just a pair.** A rotating frame turns
      about two bodies; a pulsating one turns about two groups, and the origin is
      defined by the mass of the whole group. Publishing only the head of each
      side loses bodies out of the mass that decides where the origin is, and
      loses them silently, because the head is the name a reader recognises.
      `ControlFrame.primaryBodies` always leads with `ControlFrame.primaryBody` so
      a reader wanting the pair can take the heads and a reader computing the
      frame can take the sets.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: true-now
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.system.frame"
      event:
        $ref: "#/components/messages/event"
  system.frame.set:
    address: system.frame.set
    description: |-
      Takes `SetControlFrameArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      Whatever knows what frame the game's navigation view is currently in.

      A capability rather than a method on core, because the answer belongs to
      whichever mod owns the view. Stock's answer is real and simple, a body and
      inertial axes; an n-body producer's is one of five kinds over sets of
      bodies. Core resolves the interface and never learns which is installed,
      which is the whole of what makes this side stock-shaped rather than
      producer-shaped.

      Frame is null when nothing could be read. That is not a gap to fill with a
      default: a substituted frame draws a trajectory that looks exactly like one
      drawn in the frame the player is actually in, and nothing downstream could
      tell them apart.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.system.frame.set"
      commandResponse:
        $ref: "#/components/messages/commandResponse.system.frame.set"
  system.vessels:
    address: system.vessels
    description: |-
      Carries a `SystemVessels`.

      The `system.vessels` channel payload: the full known-vessel roster (every
      vessel, not just the active one, for TargetPicker-style "what could I
      target" listings), produced by `SystemViewProvider.BuildSystemVessels`.
      Mirrors that provider's existing serialized shape EXACTLY (a wrapper object
      `{ "vessels": [ ... ] }`). The whole payload is `null` when nothing is
      loaded (main menu), distinct from an empty roster (`{ "vessels": [] }`) when
      the game genuinely reports zero vessels. Same `system`-domain convention as
      `SystemBodies`: no per-payload `Meta` (it rides the envelope).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.system.vessels"
      event:
        $ref: "#/components/messages/event"
  target.available:
    address: target.available
    description: |-
      Carries a `TargetAvailable`.

      The `target.available` channel payload: the list of everything targetable
      from the active vessel. Wrapper object `{ "entries": [ ... ] }`, mirroring
      the provider's hand-built shape (like `system.vessels`). Emitted part-tree
      style: a full keyframe on subscribe (sticky-cached for late subscribers),
      then re-emitted on set-change (a target enters/leaves range, or the current
      target changes) plus a slow heartbeat re-key, per-entry
      `TargetListEntry.distance` rides that periodic re-key, deliberately NOT the
      change-gate.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.target.available"
      event:
        $ref: "#/components/messages/event"
  time.calendar:
    address: time.calendar
    description: |-
      Carries a `TimeCalendar`.

      The `time.calendar` channel payload: how long a day is, how long a year is,
      and what real-world instant UT 0 is (when the game has one), as the RUNNING
      GAME defines them rather than as anyone assumed.

      **Why this channel exists.** Every duration on the wire is SI seconds, so
      any consumer that wants to say "3 days" has to divide by something, and
      until this channel existed the only thing to divide by was a constant
      compiled into the client: 21,600, one Kerbin rotation. That is right for
      stock KSP on Kerbin time and wrong three ways otherwise.

      - **Stock, no mods.** `GameSettings.KERBIN_TIME` is a real setting a player
        can turn off, and KSP's own UI then reads in 24-hour days and 365-day
        years. A consumer holding 21,600 disagrees with the game on the same
        screen.
      - **A planet pack.** RSS and anything else built on Kopernicus replaces
        `KSPUtil.dateTimeFormatter` outright, so a day becomes 86,400s and a year
        365 days. A client dividing by 21,600 reports four times too many days, in
        a number that looks entirely plausible.
      - **Anything else.** The formatter is an interface with a public setter; a
        mod can put any calendar behind it. Reading the numbers off it is the only
        approach that does not need a list of which mods to know about.

      **Where the values come from.** Straight off `KSPUtil.dateTimeFormatter`,
      whose `Minute`, `Hour`, `Day` and `Year` are each a count of SECONDS
      (confirmed by decompiling `IDateTimeFormatter`). No arithmetic, no
      derivation, no per-mod special case: whatever the game is using to print its
      own clock is what this channel carries.

      **It can change mid-session**, which is why this is a channel and not a
      one-shot descriptor like `system.units`. The KERBIN_TIME setting is
      reachable from the in-game settings menu at any time.

      **Deliberately not derived here:** days-per-year. A consumer that wants it
      divides `TimeCalendar.yearSeconds` by `TimeCalendar.daySeconds`, which is
      exact and needs no second field to keep in step. Publishing both would
      create a pair that can disagree.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.time.calendar"
      event:
        $ref: "#/components/messages/event"
  time.setPaused:
    address: time.setPaused
    description: |-
      Takes `SetPausedArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      The contract carries no doc comment for `time.setPaused`'s args type. Add a `<summary>` to the C# and it will appear here.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.time.setPaused"
      commandResponse:
        $ref: "#/components/messages/commandResponse.time.setPaused"
  time.setWarpIndex:
    address: time.setWarpIndex
    description: |-
      Takes `SetWarpIndexArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `time.setWarpIndex`'s args: sim-meta, never delayed (light-time fiction
      doesn't apply to a ground-side simulation control).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.time.setWarpIndex"
      commandResponse:
        $ref: "#/components/messages/commandResponse.time.setWarpIndex"
  time.warp:
    address: time.warp
    description: |-
      Carries a `WarpState`.

      The `time.warp` channel payload: kills N-3 (the legacy `p.paused` conflates
      game-pause, no-power, off, antenna-not-found, and scene state into one
      undocumented int, with a doc/impl mismatch: the docs say `0..4` but
      `partPaused()` can return an undocumented `5`). This record is instead
      orthogonal typed fields, no single int can arrive with a meaning outside its
      own documented range.

      **Current UT is deliberately NOT a field here** (or anywhere in this
      contract): `meta.validAt` stamps every sample and the SDK's view-clock is
      the consumer-facing "what time is it" surface, polling `t.universalTime`
      over the wire (a tick-rate channel by definition) is not reproduced.

      **Decoupled from vessel presence (M1 Task 3 fold-in fix):** this record's
      `WarpState.meta` is stamped `Source = "game"`, NOT `"vessel:<guid>"`:
      warp/pause is genuinely GLOBAL game state (`Gonogo.KSP.KspHost.BuildTime`
      reads it unconditionally, with or without an active vessel), so it emits at
      the Space Center / tracking station too, not just in flight. An earlier
      draft gated this channel on active-vessel presence as a scoping
      simplification (reusing the vessel provenance/epoching mechanism uniformly);
      that gate silenced the channel exactly where warp control matters most
      (out-of-flight scenes), so it was removed: see
      `Sitrep.Host.VesselViewProvider.BuildWarp`'s doc comment for the emission
      rule now in force (present whenever `Values["time"]` itself is present,
      nothing else).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.time.warp"
      event:
        $ref: "#/components/messages/event"
  vessel.attitude:
    address: vessel.attitude
    description: |-
      Carries a `VesselAttitude`.

      The `vessel.attitude` channel payload: pitch/heading/roll in TWO named
      frames, both anchored to the same reference-transform ORIENTATION but
      measuring the surface up/north vectors from a different POSITION (see
      `Gonogo.KSP.KspHost.BuildAttitude`'s doc comment for the shared
      construction). Kills V-9: the legacy `n.heading`/
      `n.heading2`/`n.rawheading`/`n.rawheading2` quartet (root vs CoM, raw vs
      adjusted, no guidance which to use) is NOT reproduced by numeric suffix: per
      this class's original decision, a second frame is a new NAMED field with a
      frame tag: `VesselAttitude.pitch`/`VesselAttitude.heading`/
      `VesselAttitude.roll` are the CoM-referenced frame (up/north measured from
      `Vessel.CoM`: MechJeb's construction), and
      `VesselAttitude.pitchRootFrame`/`VesselAttitude.headingRootFrame`/
      `VesselAttitude.rollRootFrame` are the genuinely distinct
      ROOT-PART-referenced frame (up/north measured from `Vessel.rootPart`'s
      position instead, the two diverge whenever the root part sits away from the
      vessel's centre of mass). Not derivable from orbital elements (attitude
      depends on vessel orientation, not trajectory), hence streamed raw.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.attitude"
      event:
        $ref: "#/components/messages/event"
  vessel.comms:
    address: vessel.comms
    description: |-
      Carries a `VesselComms`.

      The `vessel.comms` channel payload: the raw CommNet VESSEL snapshot. Kills
      M-3 (one typed `VesselComms.controlState` enum replaces the magic-int
      `comm.controlState` + parallel `comm.controlStateName` string key) and M-4
      (no `0`/`0d` no-data sentinel, absence is the WHOLE channel being null when
      `vessel.connection` is null, R1(b), never a fake zero reading
      indistinguishable from "no telemetry at all").

      **Scope fence**: this is what the vessel itself reports. The delay authority
      and link modelling live in a future `comms.*` CAPABILITY channel
      (RemoteTech-default): the legacy `comm.signalDelay` does NOT get a field
      here; that successor is `comms.delay`, a different provider entirely.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.comms"
      event:
        $ref: "#/components/messages/event"
  vessel.control:
    address: vessel.control
    description: |-
      Carries a `VesselControl`.

      The `vessel.control` channel payload: the READ half of what the legacy
      vocabulary split across `f.` (toggle/action) and `v.` (value-read) prefixes
      for the same concept (N-1's read half; the WRITE half is a future
      typed-command task). Every field is individually nullable, R1(a): a null
      field is a normal, meaningful "this input isn't available this tick" (e.g.
      no `ctrlState`/no action-group data), never a sentinel default: while the
      record ITSELF is present whenever a vessel is (KspHost's `BuildControl`
      always returns a group, never a null one).

      **V-3 documented, not silently "fixed":** `VesselControl.throttle` is 0..1
      NOMINALLY, but KSP's own `FlightInputHandler.state.mainThrottle` isn't
      clamped upstream: a kOS/mod-driven throttle can genuinely read > 1 (the
      "200% throttle" phantom). Silently clamping it here would be a NEW wart
      (lying about upstream game truth); the range is documented, reader beware.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.control"
      event:
        $ref: "#/components/messages/event"
  vessel.control.setAbort:
    address: vessel.control.setAbort
    description: |-
      Takes `SetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every plain boolean actuation command (`setSas`/
      `setRcs`/`setGear`/`setBrakes`/`setLights`): an ABSOLUTE state to apply,
      never a toggle. Under light-time delay a toggle arriving after unknown
      intervening state is a race by construction, the `toggleActionGroup`
      footgun. Every M1 actuation command is set-semantics only, so it does not
      exist here at all.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setAbort"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setAbort"
  vessel.control.setActionGroup:
    address: vessel.control.setActionGroup
    description: |-
      Takes `SetActionGroupArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      `vessel.control.stage`'s result is `CommandResult<int>`, a real value comes
      back (the new current stage index in `Payload`), unlike the legacy `f.stage`
      void fire-and-forget. See `CommandResult`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setActionGroup"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setActionGroup"
  vessel.control.setAxes:
    address: vessel.control.setAxes
    description: |-
      Takes `SetControlAxesArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      `vessel.control.setAxes`'s args: a partial update of the held fly-by-wire
      override. Every field is nullable so the client can drive ONE axis at a time
      (set-pitch alone) without clobbering the others: only non-null fields
      overwrite their stored value. Rotation
      (`SetControlAxesArgs.pitch`/`SetControlAxesArgs.yaw`/`SetControlAxesArgs.roll`)
      and translation
      (`SetControlAxesArgs.x`/`SetControlAxesArgs.y`/`SetControlAxesArgs.z`) are
      −1..1; the analog value is preserved end-to-end (a mapped analog stick gives
      proportional RCS rather than the legacy fork's −1/0/1 quantisation). Trim
      (`SetControlAxesArgs.pitchTrim`/`SetControlAxesArgs.yawTrim`/`SetControlAxesArgs.rollTrim`)
      is applied from inside the callback each frame alongside the axes, so it
      stays durable while armed instead of being stomped by SAS. Out-of-range
      values are clamped to −1..1 at the admission gate (a hardware stick reading
      slightly past full is a routine quirk, not an error).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setAxes"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setAxes"
  vessel.control.setBrakes:
    address: vessel.control.setBrakes
    description: |-
      Takes `SetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every plain boolean actuation command (`setSas`/
      `setRcs`/`setGear`/`setBrakes`/`setLights`): an ABSOLUTE state to apply,
      never a toggle. Under light-time delay a toggle arriving after unknown
      intervening state is a race by construction, the `toggleActionGroup`
      footgun. Every M1 actuation command is set-semantics only, so it does not
      exist here at all.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setBrakes"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setBrakes"
  vessel.control.setFlyByWire:
    address: vessel.control.setFlyByWire
    description: |-
      Takes `SetFlyByWireArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      `vessel.control.setFlyByWire`'s args: arm/disarm the persistent fly-by-wire
      override. FBW is the one `vessel.control.*` command that is NOT a one-shot
      actuation: a raw control axis (pitch/yaw/roll/translation) is re-zeroed by
      KSP every physics frame, so the mod holds an override struct and re-applies
      it from a `Vessel.OnFlyByWire` callback while armed. This command flips that
      armed flag: `SetFlyByWireArgs.enabled` `true` attaches the callback (axes
      resume from their last-set values, or 0 on first arm), `false` detaches it
      and neutralizes the stored axes/trims so control is fully handed back to the
      player/SAS with no residual override.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setFlyByWire"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setFlyByWire"
  vessel.control.setGear:
    address: vessel.control.setGear
    description: |-
      Takes `SetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every plain boolean actuation command (`setSas`/
      `setRcs`/`setGear`/`setBrakes`/`setLights`): an ABSOLUTE state to apply,
      never a toggle. Under light-time delay a toggle arriving after unknown
      intervening state is a race by construction, the `toggleActionGroup`
      footgun. Every M1 actuation command is set-semantics only, so it does not
      exist here at all.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setGear"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setGear"
  vessel.control.setLights:
    address: vessel.control.setLights
    description: |-
      Takes `SetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every plain boolean actuation command (`setSas`/
      `setRcs`/`setGear`/`setBrakes`/`setLights`): an ABSOLUTE state to apply,
      never a toggle. Under light-time delay a toggle arriving after unknown
      intervening state is a race by construction, the `toggleActionGroup`
      footgun. Every M1 actuation command is set-semantics only, so it does not
      exist here at all.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setLights"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setLights"
  vessel.control.setRcs:
    address: vessel.control.setRcs
    description: |-
      Takes `SetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every plain boolean actuation command (`setSas`/
      `setRcs`/`setGear`/`setBrakes`/`setLights`): an ABSOLUTE state to apply,
      never a toggle. Under light-time delay a toggle arriving after unknown
      intervening state is a race by construction, the `toggleActionGroup`
      footgun. Every M1 actuation command is set-semantics only, so it does not
      exist here at all.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setRcs"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setRcs"
  vessel.control.setSas:
    address: vessel.control.setSas
    description: |-
      Takes `SetEnabledArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args shared by every plain boolean actuation command (`setSas`/
      `setRcs`/`setGear`/`setBrakes`/`setLights`): an ABSOLUTE state to apply,
      never a toggle. Under light-time delay a toggle arriving after unknown
      intervening state is a race by construction, the `toggleActionGroup`
      footgun. Every M1 actuation command is set-semantics only, so it does not
      exist here at all.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setSas"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setSas"
  vessel.control.setSasMode:
    address: vessel.control.setSasMode
    description: |-
      Takes `SetSasModeArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      The contract carries no doc comment for `vessel.control.setSasMode`'s args type. Add a `<summary>` to the C# and it will appear here.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setSasMode"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setSasMode"
  vessel.control.setThrottle:
    address: vessel.control.setThrottle
    description: |-
      Takes `SetThrottleArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      The contract carries no doc comment for `vessel.control.setThrottle`'s args type. Add a `<summary>` to the C# and it will appear here.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.setThrottle"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.setThrottle"
  vessel.control.stage:
    address: vessel.control.stage
    description: |-
      Takes `NoCommandArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      The empty args shape, for the core commands that operate on the current
      flight or the active vessel and so take nothing: `vessel.control.stage`,
      `vessel.target.clear`, `ksp.recover`, `ksp.revertToLaunch` and
      `ksp.toTrackingStation`.

      **Send no `args` at all for these five.** The SDK's `send()` takes no second
      argument for a command typed this way, and anything you do put on the wire
      is read and discarded. Every other command requires its args.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.control.stage"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.control.stage"
  vessel.crew:
    address: vessel.crew
    description: |-
      Carries a `VesselCrew`.

      The `vessel.crew` channel payload. Started count-only for M1 (G-13: grows to
      a full roster later WITHOUT a topic rename, per the "misc junk drawer split"
      ruling). The roster (`VesselCrew.crew`) and `VesselCrew.capacity` are that
      additive growth, new fields on the same record, same topic. Splitting this
      out of KspHost's `misc` group into its own coherent, independently-growable
      channel is itself part of the wart-fix.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.crew"
      event:
        $ref: "#/components/messages/event"
  vessel.dock:
    address: vessel.dock
    description: |-
      Carries a `DockAlignment`.

      The `vessel.dock` channel payload: the docking/rendezvous capture-add (M3
      R3): relative position/velocity + coarse orientation between the active
      vessel's nearest FREE (undocked) docking port and the currently-targeted
      docking port, for docking-alignment widgets. Whole- channel absence means
      "not docking-relevant right now", no target targeted, the target isn't
      itself a docking port, or the active vessel has no free port of its own;
      never a stale/zero-distance sentinel record (same R1(b) convention
      `VesselTarget` already established).

      Reuses the ONE canonical `Vec3` shape (never a second vector encoding).
      `DockAlignment.forwardDot` is the dot product of the two ports' forward
      (docking-axis) vectors: -1.0 means the ports face each other head-on (the
      alignment a successful dock needs), +1.0 means they point the same direction
      (facing away from each other), a widget maps this to a 0..100% "facing"
      readout however it likes; this contract intentionally ships the raw dot
      product rather than a pre-baked percentage so the mapping stays a client
      concern.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.dock"
      event:
        $ref: "#/components/messages/event"
  vessel.flight:
    address: vessel.flight
    description: |-
      Carries a `VesselFlight`.

      The `vessel.flight` channel payload: MEASUREMENTS, not evaluations:
      quantities the game measures that aren't derivable from orbital elements
      (terrain height, aero state) or that serve as off-rails ground truth
      (speeds). Kills V-10 (no (0,0) lat/long sentinel, the channel is simply
      absent when there's no vessel, never a fake origin point) and V-12 (one
      canonical field per quantity: the srfSpeed/speed/surfaceSpeed triplet and
      kPa/Pa variants collapse to `VesselFlight.surfaceSpeed` and
      `VesselFlight.dynamicPressureKPa`). `missionTime` deliberately does NOT
      appear here: see `VesselIdentity.launchUt`'s doc comment.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.flight"
      event:
        $ref: "#/components/messages/event"
  vessel.identity:
    address: vessel.identity
    description: |-
      Carries a `VesselIdentity`.

      The `vessel.identity` channel payload: kills V-13 (one typed
      `VesselIdentity.situation` enum replaces the v.situation/v.situationString/
      v.landedAt triplet) and moves `missionTime` off the wire entirely:
      `VesselIdentity.launchUt` is static after liftoff (sampleUt - missionTime),
      so MET (mission elapsed time) is a consumer-side derivation (viewUt -
      launchUt) rather than a tick-rate field that would force this whole record
      to re-emit every tick.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.identity"
      event:
        $ref: "#/components/messages/event"
  vessel.inventory:
    address: vessel.inventory
    description: |-
      Carries a `VesselInventory`.

      The stock cargo a vessel's PARTS are carrying: the supply aboard.

      **Why the crew's own inventories are not here.** A kerbal carries the same
      KSP module, `ModuleInventoryPart`, so one topic was the obvious shape and it
      is the wrong one. These answer different questions. This channel answers
      "what is aboard, and where", which is SUPPLY. `vessel.crew` answers "who is
      here, are they qualified, and what are they holding", which is the ACTOR
      list, and that is where a kerbal's two slots belong: beside the trait and
      experience level that decide whether they may do the job at all.

      **Why location is carried rather than a per-vessel total.** A kerbal has two
      slots, forty volume and a 65kg limit, one slot of which defaults to a
      parachute; a cargo container has far more. A craft can be carrying plenty of
      something while the kerbal who needs it has none, and a single total reports
      the reassuring half of that.

      This is STOCK, and deliberately not any Uplink's. Stock KSP's own repair
      mechanic consumes stock cargo, so a stock-career player has inventories
      worth showing whether or not a modelling mod is installed.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.inventory"
      event:
        $ref: "#/components/messages/event"
  vessel.invokePartAction:
    address: vessel.invokePartAction
    description: |-
      Takes `InvokePartActionArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Args for `vessel.invokePartAction`: fire one button of one part's
      right-click Part Action Window, the remote-control equivalent of the player
      clicking it in-game.

      This is an actuation of a part ON the craft, so the command rides light-time
      (`Delayed = true`) exactly like `vessel.control.*` and the robotics commands
      it is modelled on.

      **No state field, unlike every other actuation command.** The contract's
      usual discipline is "absolute set, never toggle" (see
      `ServoSetEnabledArgs`), but a `BaseEvent` has no settable value: KSP models
      these as fire-this-button, and the button's own label is what changes
      ("Deploy" becomes "Retract"). So this command is a pure invoke, in the same
      position as `robotics.rotor.reverse`: the lone stateless member of its
      family, for a reason that comes from KSP rather than from convenience. The
      operator's read-back is the `vessel.partActions.<flightId>` channel
      re-reporting the new button set one light-time later.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.invokePartAction"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.invokePartAction"
  vessel.landing:
    address: vessel.landing
    description: |-
      Carries a `VesselLanding`.

      The `vessel.landing` channel payload: terrain-informed landing data that
      needs KSP's PQS heightmap, which no client-side derivation can source (no
      client-side height grid is anywhere near fine-grained enough for a
      lander-scale slope), plus an atmosphere-aware descent estimate that needs
      per-part drag the client does not have.

      Distinct from the vacuum ballistic scalars a client solves for itself, which
      need no terrain and stay client-side.

      Whole-channel absence means "not descending toward a solid surface",
      relevance-gated at the source on situation + a descent test +
      `CelestialBody.hasSolidSurface` / a non-null `pqsController`, so this never
      carries a stale reading from orbit or a fabricated 0.0 from a body with no
      PQS. This is the third instance of the CaptureCrash house pattern (one
      source-gated channel published to every screen), with a continuous numeric
      gate rather than a categorical event.

      Every field is nullable: a field is null when its input is unavailable this
      tick (e.g. no PQS, no touchdown solution, not in atmosphere). Never ship a
      0.0 that was not verified.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.landing"
      event:
        $ref: "#/components/messages/event"
  vessel.maneuver:
    address: vessel.maneuver
    description: |-
      Carries a `VesselManeuver`.

      The `vessel.maneuver` channel payload. `VesselManeuver.nodes` is ALWAYS an
      array: kills R2's empty-vs-null inconsistency (KspHost's
      `BuildManeuverNodes` returns `null` for "no nodes queued," the common case;
      this mapper normalizes that to `[]`, never a null collection). *Derived,
      SDK-side, NOT streamed here:* the post-burn orbit preview (elements + node →
      new elements, consumer-side math).

      **`VesselManeuver.nodes` is ordered by execution**, earliest
      `ManeuverNode.ut` first, and that ordering IS the plan: burn N is flown
      after burn N-1 and acts on what burn N-1 left behind. No separate ordinal or
      predecessor field is carried, because array position already says it and a
      second expression of the same fact is a second thing that can be wrong. The
      per-burn patch chain expresses the same linkage a third time, in a form only
      a patched-conic planner can produce; see `ManeuverNode.patches`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.maneuver"
      event:
        $ref: "#/components/messages/event"
  vessel.maneuver.add:
    address: vessel.maneuver.add
    description: |-
      Takes `AddManeuverNodeArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      `vessel.maneuver.add`'s args: NAMED delta-v components in the node's own
      radial/normal/prograde frame, exactly like the wire's `ManeuverNode` shape.
      Kills O-4: there is no positional `[ut,x,y,z]` array to mis-order (raw KSP
      `ManeuverNode.DeltaV` is `x=radialOut, y=normal, z=prograde`) for why the
      actuator seam must preserve this exact component assignment rather than
      "helpfully" reordering it.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.maneuver.add"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.maneuver.add"
  vessel.maneuver.plan.send:
    address: vessel.maneuver.plan.send
    description: |-
      Takes `SendManeuverPlanArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      A whole flight plan, composed at a command centre and transmitted to be
      instantiated aboard.

      **Why a whole plan rather than per-burn edits.** Five burn edits are five
      messages, each with its own light-time, each able to arrive late, out of
      order, or not at all. A craft that received three of them would fly a plan
      nobody composed and nobody approved. One plan is one message, applied whole
      or not at all.

      **The burns are transmitted, never re-derived.** The receiving side installs
      these numbers rather than re-solving toward a goal. A plan re-solved on
      arrival would be solved against the craft's true state, which is ahead of
      everything the operator could see, so the craft would fly something nobody
      at the command centre ever looked at.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.maneuver.plan.send"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.maneuver.plan.send"
  vessel.maneuver.remove:
    address: vessel.maneuver.remove
    description: |-
      Takes `RemoveManeuverNodeArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      The contract carries no doc comment for `vessel.maneuver.remove`'s args type. Add a `<summary>` to the C# and it will appear here.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.maneuver.remove"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.maneuver.remove"
  vessel.maneuver.update:
    address: vessel.maneuver.update
    description: |-
      Takes `UpdateManeuverNodeArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      Result of `vessel.maneuver.add` is `CommandResult<string>`, O-6 fixed: the
      created node's opaque id is actually returned in `Payload`. See
      `CommandResult`.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.maneuver.update"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.maneuver.update"
  vessel.orbit:
    address: vessel.orbit
    description: |-
      Carries a `VesselOrbit`.

      The `vessel.orbit` channel payload: elements are the CAUSE; every kinematic
      quantity (position/velocity/apsides/anomalies/period) is a consumer-side
      derivation at view-UT via the propagation capability, never streamed here
      (the "elements-not-position" ruling). Kills O-1 (there is no
      `eccentricAnomaly` field at all, the copy-paste-bug class can't exist on a
      wire that never carries one), O-8 (spelled-out, unit-annotated fields, UT
      always `double`), O-9 (`VesselOrbit.encounter` is a typed nullable record,
      never the -1/0/1 + "" + NaN sentinel spray of o.encounterExists/Time/Body),
      O-10 (no duplicate apsis keys). Units: `VesselOrbit.sma` in metres;
      `VesselOrbit.inc`/`VesselOrbit.lan`/ `VesselOrbit.argPe` in DEGREES
      (KSP-native); `VesselOrbit.meanAnomalyAtEpoch` in RADIANS (also KSP-native):
      this degrees/radians split is an inherited KSP inconsistency deliberately
      KEPT, not "fixed": converting would desync from every KSP reference and the
      recorder's own raw values).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.orbit"
      event:
        $ref: "#/components/messages/event"
  vessel.orbit.truth:
    address: vessel.orbit.truth
    description: |-
      Carries a `VesselOrbitTruth`.

      The `vessel.orbit.truth` channel payload: KSP's own maintained ground-truth
      state vector, parent-body-relative. DEV-GATED, not a product channel: exists
      so the propagator-diff harness / a debug widget can verify element->position
      math against KSP's own state, never as a widget-facing altitude/velocity
      source (that would rebuild the elements-not-position discipline's failure
      mode / V-12). `VesselOrbitTruth.frameRotating` gates whether
      `VesselOrbitTruth.position`/`VesselOrbitTruth.velocity` are directly
      comparable to a fixed-frame Kepler propagator's output (false) or sit in a
      frame co-rotating with the body's spin instead (true); see
      `Gonogo.KSP.KspHost.BuildOrbit`'s doc comment for the full derivation. There
      is no engine-level "hide from the data picker" flag yet (that's a future
      SDK/picker concern): this channel is dev-only BY CONVENTION today, enforced
      by never binding it from a widget, not by engine-level gating.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.orbit.truth"
      event:
        $ref: "#/components/messages/event"
  vessel.parts:
    address: vessel.parts
    description: |-
      Carries a `VesselParts`.

      The `vessel.parts` channel payload: the active vessel's full part-tree
      topology (P1b slice 2), the foundation ShipMap / PowerSystems topology /
      ThermalStatus all build on. A SINGLE WRAPPER OBJECT (or `null` when there is
      no active vessel / no topology group this tick), so the Topic tag sits on
      this type directly with the default `IsArray = false`: same posture as
      `VesselStructure` and the sibling structured `vessel.*` channels, NOT the
      bare-array `parts.robotics`.

      **Thermal folds in here.** Per-part temperatures ride each `VesselPart`
      (`VesselPart.currentTemp`/ `VesselPart.maxTemp`/`VesselPart.skinTemp`/
      `VesselPart.skinMaxTemp`), so the hottest-part / engine / heat-shield
      rollups are SDK-DERIVABLE on top of this channel, there is no separate
      `therm.hottestPart*` Topic (v-topology-redesign.md). The existing
      `vessel.thermal` rollup channel is NOT removed by this build; that is a
      later cleanup.

      **Typing-only mirror.** This reproduces, field-for-field, the exact
      serialized shape `Sitrep.Host.VesselPartsViewProvider.ToWire` already emits
      (same names, same camelCase wire keys via `RtConfig.CamelCaseForProperties`,
      same units). It is NOT serialized itself: the wire is written by
      `JsonWriter` walking the provider's dictionary: so adding it changes no
      bytes.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.parts"
      event:
        $ref: "#/components/messages/event"
  vessel.physics.mode:
    address: vessel.physics.mode
    description: |-
      Carries a `VesselPhysicsMode`.

      The `vessel.physics.mode` Topic payload: the active vessel's physics regime
      (`PhysicsMode`). Its own Topic Value per the 2026-07-09 decision that
      reverses the earlier "fold physics mode into stream meta" call:
      `PayloadMeta.quality` was a bad stand-in for a discrete enum).
      DelayRole-Delayed like every other vessel-derived channel: it describes the
      vessel itself, so ground learns about it at UT+delay, not as a ground-side
      fact.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.physics.mode"
      event:
        $ref: "#/components/messages/event"
  vessel.propulsion:
    address: vessel.propulsion
    description: |-
      Carries a `VesselPropulsion`.

      The `vessel.propulsion` channel payload: the TWR/burn-time derivation inputs
      (G-4). `VesselPropulsion.totalMass`/`VesselPropulsion.dryMass` in tonnes,
      `VesselPropulsion.currentThrust`/`VesselPropulsion.availableThrust` in kN
      (dimensionally consistent for TWR: kN/(t·m/s²): see kN/(t·m/s²)).
      `VesselPropulsion.availableThrust` already excludes shut-down/flamed-out
      engines at capture (only `EngineIgnited && !flameout` engines contribute):
      it is "what this vessel can produce RIGHT NOW," not its rated maximum.
      *Derived, SDK-side, NOT streamed here:* TWR (`currentThrust / (totalMass ·
      g)`), max-TWR, and a crude vessel-level burn-time estimate (retiring
      `dv.currentTWR`/`dv.*` until a stage sim exists, G-14).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.propulsion"
      event:
        $ref: "#/components/messages/event"
  vessel.repair:
    address: vessel.repair
    description: |-
      Takes `RepairPartArgs`.

      **Delayed.** This command rides the uplink: it takes effect at UT plus light-time to the craft, not when it is sent.

      `vessel.repair`'s args: which part, and which crew member does it.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: true
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.repair"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.repair"
  vessel.resources:
    address: vessel.resources
    description: |-
      Carries a `VesselResources`.

      The `vessel.resources` channel payload: a keyframed map, keyed by resource
      name. Kills R-1 (`SumResources`'s `-1` sentinel for an absent/empty
      resource: never reproduced here), R-3 (row-vanishing ambiguity), R-4
      (`{}`-for-dead-id).

      **Three-way typed absence (R1):**

      - **Key ABSENT** from `VesselResources.resources`: structural: this vessel
        does not carry the resource at all (KspHost omits any resource with
        `maxAmount <= 0`). Changes only on staging/docking.
      - **Key present, `{current: 0, max: > 0}`**, carried but currently empty (a
        real, meaningful reading, not an error).
      - **Whole channel absent/stale**: no vessel at all (R1(b), same convention
        as every other `vessel.*` channel).

      Because every emission is the FULL map (a structured, keyframed channel,
      never a delta), a key disappearing between two emissions is itself a real
      structural statement (the vessel stopped carrying that resource, e.g. a tank
      was staged away), never an ambiguous "did it change or did the stream just
      drop it" (R-3's ambiguity).

      **Deliberately deferred**: flow/rates (R-2/R-5), those belong to a future
      parts/power channel family with per-module provenance; bolting a
      vessel-total `flow` on now would reproduce R-6 (a "truth" number that isn't
      the game's truth).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.resources"
      event:
        $ref: "#/components/messages/event"
  vessel.structure:
    address: vessel.structure
    description: |-
      Carries a `VesselStructure`.

      The `vessel.structure` channel payload: the other half of KspHost's `misc`
      junk-drawer split (see `VesselCrew`'s doc comment).
      `VesselStructure.currentStage` uses KSP's own (P-4-flagged "inverted vs.
      visible staging") numbering UNCHANGED: documented here, not silently
      renumbered, so this contract doesn't invent a second numbering scheme to
      reconcile. `VesselStructure.stageCount` is already `maxInverseStage + 1`
      (KspHost's own normalization). A future part-tree/topology channel
      (`vessel.parts`) is a SIBLING of this record, not a growth of it (R-8's
      "bulk topology is its own ASSET-class design" lesson).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.structure"
      event:
        $ref: "#/components/messages/event"
  vessel.surface:
    address: vessel.surface
    description: |-
      Carries a `VesselSurface`.

      The `vessel.surface` channel payload: the landing capture-add (M3 R3): data
      a LandingStatus widget needs that `vessel.flight` doesn't already carry.
      `vessel.flight.AltitudeTerrain` (KSP's `radarAltitude`, measured from the
      vessel's centre of mass) already ships: `VesselSurface.heightFromTerrain`
      here is a DIFFERENT, additional reading (KSP's own `heightFromTerrain`,
      which accounts for the vessel's physical extent: effectively "how far is my
      LOWEST point from the ground," the number a landing-gear/suicide-burn widget
      actually cares about, not the CoM-to-ground distance).

      Whole-channel absence means "not near any surface right now", guarded on
      `Situation.Orbiting`/ `Situation.Escaping` on the capture side
      (`Gonogo.KSP.KspHost.BuildSurface`), never a stale/garbage AGL reading from
      deep space (KSP keeps whatever it last computed for `heightFromTerrain` even
      when there's no meaningful "terrain" underneath at all).
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.surface"
      event:
        $ref: "#/components/messages/event"
  vessel.target:
    address: vessel.target
    description: |-
      Carries a `VesselTarget`.

      The `vessel.target` channel payload: the active vessel's CURRENT target only
      (no roster; `system.vessels`/`tar.availableVessels`'s replacement is a
      deferred M1.5 add). Kills V-8:
      `VesselTarget.relativePosition`/`VesselTarget.relativeVelocity` both use the
      ONE canonical `Vec3` shape, replacing the legacy vocabulary's two
      incompatible vector encodings (bare `[x,y,z]` array vs. `{x,y,z}` object)
      that coexisted across different key families.

      `VesselTarget.orbit` reuses `VesselOrbit` itself (not a separate "target
      orbit" shape), and that is load-bearing: it lets the SDK propagate a target
      with the EXACT SAME code path as the self vessel, so both are evaluated at
      the same view-UT by the same propagation logic (the single-view-time
      invariant). Its nested `VesselTarget.meta` is stamped with the SAME subject
      (the active vessel producing this sample), not a separate target-vessel
      identity, `VesselTarget.vesselId`/`VesselTarget.bodyIndex` below (M3 R3) now
      DO carry the target's own identity, closing the gap this doc comment used to
      flag as deferred.

      Whole-channel absence (the outer `VesselTarget?` being null) means nothing
      is targeted, the common case, R1(b), never a sentinel
      zero-distance/zero-vector record.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.target"
      event:
        $ref: "#/components/messages/event"
  vessel.target.clear:
    address: vessel.target.clear
    description: |-
      Takes `NoCommandArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      The empty args shape, for the core commands that operate on the current
      flight or the active vessel and so take nothing: `vessel.control.stage`,
      `vessel.target.clear`, `ksp.recover`, `ksp.revertToLaunch` and
      `ksp.toTrackingStation`.

      **Send no `args` at all for these five.** The SDK's `send()` takes no second
      argument for a command typed this way, and anything you do put on the wire
      is read and discarded. Every other command requires its args.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.target.clear"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.target.clear"
  vessel.target.set:
    address: vessel.target.set
    description: |-
      Takes `SetTargetArgs`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      `vessel.target.set`'s args: a discriminated union expressed as
      `SetTargetArgs.kind` + the one field that kind actually uses (C# has no
      native union type; this mirrors `TargetKind`'s existing vessel/body/other
      split rather than inventing a parallel shape). T-1 fixed:
      `SetTargetArgs.vesselId` is the STABLE opaque vessel id (resolved
      server-side against `FlightGlobals.Vessels`), never a live array index a
      client would have to track itself. T-2 fixed: vessel id and body index are
      separate fields in separate namespaces, so they can never be confused for
      one another.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.target.set"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.target.set"
  vessel.thermal:
    address: vessel.thermal
    description: |-
      Carries a `VesselThermal`.

      The `vessel.thermal` channel payload: kills P-5 (the int-where-
      object-expected "partless-paused" sentinel, and the divide-by-zero/NaN risk
      of a part with `maxTemp <= 0`): both ratios are typed `double?`, null
      meaning "no part had a valid `maxTemp`/ `skinMaxTemp` this tick": a
      distinct, typed state, never an indistinguishable-from-real-data `0.0` ("no
      valid part" vs. "coldest possible part").

      Whole-channel absence (the outer `VesselThermal?` being null) means the
      vessel currently has no parts at all (KspHost's `BuildThermal` returns no
      group in that case), a DIFFERENT, coarser absence than an individual null
      ratio.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delivery: lossy-latest
    x-sitrep-delay-role: delayed
    x-sitrep-keyframe-interval-ut: 30
    messages:
      streamData:
        $ref: "#/components/messages/streamData.vessel.thermal"
      event:
        $ref: "#/components/messages/event"
  vessel.trajectory.forVantage:
    address: vessel.trajectory.forVantage
    description: |-
      Takes `VantagePlanRequest`.

      **Not delayed.** This command takes effect immediately, because it is ground-side bookkeeping rather than a signal to a craft.

      Args for `vessel.trajectory.forVantage`: where does this craft go, given
      what my command centre has been told.

      There is deliberately no vantage field. The answer depends on who is asking,
      and a client that could name its own vantage could name somebody else's and
      be shown what they can see. It is resolved where the command enters instead.
    servers:
      - $ref: "#/servers/mod"
    x-sitrep-delayed: false
    messages:
      commandRequest:
        $ref: "#/components/messages/commandRequest.vessel.trajectory.forVantage"
      commandResponse:
        $ref: "#/components/messages/commandResponse.vessel.trajectory.forVantage"
operations:
  dispatch:alarm.scet.arm:
    action: send
    channel:
      $ref: "#/channels/alarm.scet.arm"
    title: Dispatch alarm.scet.arm
    tags:
      - name: alarm
    messages:
      - $ref: "#/channels/alarm.scet.arm/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/alarm.scet.arm"
      messages:
        - $ref: "#/channels/alarm.scet.arm/messages/commandResponse"
  dispatch:alarm.scet.disarm:
    action: send
    channel:
      $ref: "#/channels/alarm.scet.disarm"
    title: Dispatch alarm.scet.disarm
    tags:
      - name: alarm
    messages:
      - $ref: "#/channels/alarm.scet.disarm/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/alarm.scet.disarm"
      messages:
        - $ref: "#/channels/alarm.scet.disarm/messages/commandResponse"
  dispatch:career.contract.accept:
    action: send
    channel:
      $ref: "#/channels/career.contract.accept"
    title: Dispatch career.contract.accept
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.contract.accept/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.contract.accept"
      messages:
        - $ref: "#/channels/career.contract.accept/messages/commandResponse"
  dispatch:career.contract.cancel:
    action: send
    channel:
      $ref: "#/channels/career.contract.cancel"
    title: Dispatch career.contract.cancel
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.contract.cancel/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.contract.cancel"
      messages:
        - $ref: "#/channels/career.contract.cancel/messages/commandResponse"
  dispatch:career.contract.decline:
    action: send
    channel:
      $ref: "#/channels/career.contract.decline"
    title: Dispatch career.contract.decline
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.contract.decline/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.contract.decline"
      messages:
        - $ref: "#/channels/career.contract.decline/messages/commandResponse"
  dispatch:career.crew.fire:
    action: send
    channel:
      $ref: "#/channels/career.crew.fire"
    title: Dispatch career.crew.fire
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.crew.fire/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.crew.fire"
      messages:
        - $ref: "#/channels/career.crew.fire/messages/commandResponse"
  dispatch:career.crew.hire:
    action: send
    channel:
      $ref: "#/channels/career.crew.hire"
    title: Dispatch career.crew.hire
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.crew.hire/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.crew.hire"
      messages:
        - $ref: "#/channels/career.crew.hire/messages/commandResponse"
  dispatch:career.facility.upgrade:
    action: send
    channel:
      $ref: "#/channels/career.facility.upgrade"
    title: Dispatch career.facility.upgrade
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.facility.upgrade/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.facility.upgrade"
      messages:
        - $ref: "#/channels/career.facility.upgrade/messages/commandResponse"
  dispatch:career.strategy.activate:
    action: send
    channel:
      $ref: "#/channels/career.strategy.activate"
    title: Dispatch career.strategy.activate
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.strategy.activate/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.strategy.activate"
      messages:
        - $ref: "#/channels/career.strategy.activate/messages/commandResponse"
  dispatch:career.strategy.deactivate:
    action: send
    channel:
      $ref: "#/channels/career.strategy.deactivate"
    title: Dispatch career.strategy.deactivate
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.strategy.deactivate/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.strategy.deactivate"
      messages:
        - $ref: "#/channels/career.strategy.deactivate/messages/commandResponse"
  dispatch:career.tech.unlock:
    action: send
    channel:
      $ref: "#/channels/career.tech.unlock"
    title: Dispatch career.tech.unlock
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.tech.unlock/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/career.tech.unlock"
      messages:
        - $ref: "#/channels/career.tech.unlock/messages/commandResponse"
  dispatch:comms.setSimulationDelayPolicy:
    action: send
    channel:
      $ref: "#/channels/comms.setSimulationDelayPolicy"
    title: Dispatch comms.setSimulationDelayPolicy
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.setSimulationDelayPolicy/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/comms.setSimulationDelayPolicy"
      messages:
        - $ref: "#/channels/comms.setSimulationDelayPolicy/messages/commandResponse"
  dispatch:ksp.launch:
    action: send
    channel:
      $ref: "#/channels/ksp.launch"
    title: Dispatch ksp.launch
    tags:
      - name: ksp
    messages:
      - $ref: "#/channels/ksp.launch/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/ksp.launch"
      messages:
        - $ref: "#/channels/ksp.launch/messages/commandResponse"
  dispatch:ksp.recover:
    action: send
    channel:
      $ref: "#/channels/ksp.recover"
    title: Dispatch ksp.recover
    tags:
      - name: ksp
    messages:
      - $ref: "#/channels/ksp.recover/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/ksp.recover"
      messages:
        - $ref: "#/channels/ksp.recover/messages/commandResponse"
  dispatch:ksp.revertToEditor:
    action: send
    channel:
      $ref: "#/channels/ksp.revertToEditor"
    title: Dispatch ksp.revertToEditor
    tags:
      - name: ksp
    messages:
      - $ref: "#/channels/ksp.revertToEditor/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/ksp.revertToEditor"
      messages:
        - $ref: "#/channels/ksp.revertToEditor/messages/commandResponse"
  dispatch:ksp.revertToLaunch:
    action: send
    channel:
      $ref: "#/channels/ksp.revertToLaunch"
    title: Dispatch ksp.revertToLaunch
    tags:
      - name: ksp
    messages:
      - $ref: "#/channels/ksp.revertToLaunch/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/ksp.revertToLaunch"
      messages:
        - $ref: "#/channels/ksp.revertToLaunch/messages/commandResponse"
  dispatch:ksp.switchVessel:
    action: send
    channel:
      $ref: "#/channels/ksp.switchVessel"
    title: Dispatch ksp.switchVessel
    tags:
      - name: ksp
    messages:
      - $ref: "#/channels/ksp.switchVessel/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/ksp.switchVessel"
      messages:
        - $ref: "#/channels/ksp.switchVessel/messages/commandResponse"
  dispatch:ksp.toTrackingStation:
    action: send
    channel:
      $ref: "#/channels/ksp.toTrackingStation"
    title: Dispatch ksp.toTrackingStation
    tags:
      - name: ksp
    messages:
      - $ref: "#/channels/ksp.toTrackingStation/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/ksp.toTrackingStation"
      messages:
        - $ref: "#/channels/ksp.toTrackingStation/messages/commandResponse"
  dispatch:robotics.rotor.reverse:
    action: send
    channel:
      $ref: "#/channels/robotics.rotor.reverse"
    title: Dispatch robotics.rotor.reverse
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.rotor.reverse/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.rotor.reverse"
      messages:
        - $ref: "#/channels/robotics.rotor.reverse/messages/commandResponse"
  dispatch:robotics.rotor.setBrake:
    action: send
    channel:
      $ref: "#/channels/robotics.rotor.setBrake"
    title: Dispatch robotics.rotor.setBrake
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.rotor.setBrake/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.rotor.setBrake"
      messages:
        - $ref: "#/channels/robotics.rotor.setBrake/messages/commandResponse"
  dispatch:robotics.rotor.setLock:
    action: send
    channel:
      $ref: "#/channels/robotics.rotor.setLock"
    title: Dispatch robotics.rotor.setLock
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.rotor.setLock/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.rotor.setLock"
      messages:
        - $ref: "#/channels/robotics.rotor.setLock/messages/commandResponse"
  dispatch:robotics.rotor.setMotor:
    action: send
    channel:
      $ref: "#/channels/robotics.rotor.setMotor"
    title: Dispatch robotics.rotor.setMotor
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.rotor.setMotor/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.rotor.setMotor"
      messages:
        - $ref: "#/channels/robotics.rotor.setMotor/messages/commandResponse"
  dispatch:robotics.rotor.setRpmLimit:
    action: send
    channel:
      $ref: "#/channels/robotics.rotor.setRpmLimit"
    title: Dispatch robotics.rotor.setRpmLimit
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.rotor.setRpmLimit/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.rotor.setRpmLimit"
      messages:
        - $ref: "#/channels/robotics.rotor.setRpmLimit/messages/commandResponse"
  dispatch:robotics.rotor.setTorqueLimit:
    action: send
    channel:
      $ref: "#/channels/robotics.rotor.setTorqueLimit"
    title: Dispatch robotics.rotor.setTorqueLimit
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.rotor.setTorqueLimit/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.rotor.setTorqueLimit"
      messages:
        - $ref: "#/channels/robotics.rotor.setTorqueLimit/messages/commandResponse"
  dispatch:robotics.servo.setLock:
    action: send
    channel:
      $ref: "#/channels/robotics.servo.setLock"
    title: Dispatch robotics.servo.setLock
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.servo.setLock/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.servo.setLock"
      messages:
        - $ref: "#/channels/robotics.servo.setLock/messages/commandResponse"
  dispatch:robotics.servo.setMotor:
    action: send
    channel:
      $ref: "#/channels/robotics.servo.setMotor"
    title: Dispatch robotics.servo.setMotor
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.servo.setMotor/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.servo.setMotor"
      messages:
        - $ref: "#/channels/robotics.servo.setMotor/messages/commandResponse"
  dispatch:robotics.servo.setTarget:
    action: send
    channel:
      $ref: "#/channels/robotics.servo.setTarget"
    title: Dispatch robotics.servo.setTarget
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.servo.setTarget/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/robotics.servo.setTarget"
      messages:
        - $ref: "#/channels/robotics.servo.setTarget/messages/commandResponse"
  dispatch:science.experiment.deploy:
    action: send
    channel:
      $ref: "#/channels/science.experiment.deploy"
    title: Dispatch science.experiment.deploy
    tags:
      - name: science
    messages:
      - $ref: "#/channels/science.experiment.deploy/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/science.experiment.deploy"
      messages:
        - $ref: "#/channels/science.experiment.deploy/messages/commandResponse"
  dispatch:science.experiment.transmit:
    action: send
    channel:
      $ref: "#/channels/science.experiment.transmit"
    title: Dispatch science.experiment.transmit
    tags:
      - name: science
    messages:
      - $ref: "#/channels/science.experiment.transmit/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/science.experiment.transmit"
      messages:
        - $ref: "#/channels/science.experiment.transmit/messages/commandResponse"
  dispatch:system.frame.set:
    action: send
    channel:
      $ref: "#/channels/system.frame.set"
    title: Dispatch system.frame.set
    tags:
      - name: system
    messages:
      - $ref: "#/channels/system.frame.set/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/system.frame.set"
      messages:
        - $ref: "#/channels/system.frame.set/messages/commandResponse"
  dispatch:time.setPaused:
    action: send
    channel:
      $ref: "#/channels/time.setPaused"
    title: Dispatch time.setPaused
    tags:
      - name: time
    messages:
      - $ref: "#/channels/time.setPaused/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/time.setPaused"
      messages:
        - $ref: "#/channels/time.setPaused/messages/commandResponse"
  dispatch:time.setWarpIndex:
    action: send
    channel:
      $ref: "#/channels/time.setWarpIndex"
    title: Dispatch time.setWarpIndex
    tags:
      - name: time
    messages:
      - $ref: "#/channels/time.setWarpIndex/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/time.setWarpIndex"
      messages:
        - $ref: "#/channels/time.setWarpIndex/messages/commandResponse"
  dispatch:vessel.control.setAbort:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setAbort"
    title: Dispatch vessel.control.setAbort
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setAbort/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setAbort"
      messages:
        - $ref: "#/channels/vessel.control.setAbort/messages/commandResponse"
  dispatch:vessel.control.setActionGroup:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setActionGroup"
    title: Dispatch vessel.control.setActionGroup
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setActionGroup/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setActionGroup"
      messages:
        - $ref: "#/channels/vessel.control.setActionGroup/messages/commandResponse"
  dispatch:vessel.control.setAxes:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setAxes"
    title: Dispatch vessel.control.setAxes
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setAxes/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setAxes"
      messages:
        - $ref: "#/channels/vessel.control.setAxes/messages/commandResponse"
  dispatch:vessel.control.setBrakes:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setBrakes"
    title: Dispatch vessel.control.setBrakes
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setBrakes/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setBrakes"
      messages:
        - $ref: "#/channels/vessel.control.setBrakes/messages/commandResponse"
  dispatch:vessel.control.setFlyByWire:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setFlyByWire"
    title: Dispatch vessel.control.setFlyByWire
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setFlyByWire/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setFlyByWire"
      messages:
        - $ref: "#/channels/vessel.control.setFlyByWire/messages/commandResponse"
  dispatch:vessel.control.setGear:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setGear"
    title: Dispatch vessel.control.setGear
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setGear/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setGear"
      messages:
        - $ref: "#/channels/vessel.control.setGear/messages/commandResponse"
  dispatch:vessel.control.setLights:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setLights"
    title: Dispatch vessel.control.setLights
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setLights/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setLights"
      messages:
        - $ref: "#/channels/vessel.control.setLights/messages/commandResponse"
  dispatch:vessel.control.setRcs:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setRcs"
    title: Dispatch vessel.control.setRcs
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setRcs/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setRcs"
      messages:
        - $ref: "#/channels/vessel.control.setRcs/messages/commandResponse"
  dispatch:vessel.control.setSas:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setSas"
    title: Dispatch vessel.control.setSas
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setSas/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setSas"
      messages:
        - $ref: "#/channels/vessel.control.setSas/messages/commandResponse"
  dispatch:vessel.control.setSasMode:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setSasMode"
    title: Dispatch vessel.control.setSasMode
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setSasMode/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setSasMode"
      messages:
        - $ref: "#/channels/vessel.control.setSasMode/messages/commandResponse"
  dispatch:vessel.control.setThrottle:
    action: send
    channel:
      $ref: "#/channels/vessel.control.setThrottle"
    title: Dispatch vessel.control.setThrottle
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.setThrottle/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.setThrottle"
      messages:
        - $ref: "#/channels/vessel.control.setThrottle/messages/commandResponse"
  dispatch:vessel.control.stage:
    action: send
    channel:
      $ref: "#/channels/vessel.control.stage"
    title: Dispatch vessel.control.stage
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control.stage/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.control.stage"
      messages:
        - $ref: "#/channels/vessel.control.stage/messages/commandResponse"
  dispatch:vessel.invokePartAction:
    action: send
    channel:
      $ref: "#/channels/vessel.invokePartAction"
    title: Dispatch vessel.invokePartAction
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.invokePartAction/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.invokePartAction"
      messages:
        - $ref: "#/channels/vessel.invokePartAction/messages/commandResponse"
  dispatch:vessel.maneuver.add:
    action: send
    channel:
      $ref: "#/channels/vessel.maneuver.add"
    title: Dispatch vessel.maneuver.add
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.maneuver.add/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.maneuver.add"
      messages:
        - $ref: "#/channels/vessel.maneuver.add/messages/commandResponse"
  dispatch:vessel.maneuver.plan.send:
    action: send
    channel:
      $ref: "#/channels/vessel.maneuver.plan.send"
    title: Dispatch vessel.maneuver.plan.send
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.maneuver.plan.send/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.maneuver.plan.send"
      messages:
        - $ref: "#/channels/vessel.maneuver.plan.send/messages/commandResponse"
  dispatch:vessel.maneuver.remove:
    action: send
    channel:
      $ref: "#/channels/vessel.maneuver.remove"
    title: Dispatch vessel.maneuver.remove
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.maneuver.remove/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.maneuver.remove"
      messages:
        - $ref: "#/channels/vessel.maneuver.remove/messages/commandResponse"
  dispatch:vessel.maneuver.update:
    action: send
    channel:
      $ref: "#/channels/vessel.maneuver.update"
    title: Dispatch vessel.maneuver.update
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.maneuver.update/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.maneuver.update"
      messages:
        - $ref: "#/channels/vessel.maneuver.update/messages/commandResponse"
  dispatch:vessel.repair:
    action: send
    channel:
      $ref: "#/channels/vessel.repair"
    title: Dispatch vessel.repair
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.repair/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.repair"
      messages:
        - $ref: "#/channels/vessel.repair/messages/commandResponse"
  dispatch:vessel.target.clear:
    action: send
    channel:
      $ref: "#/channels/vessel.target.clear"
    title: Dispatch vessel.target.clear
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.target.clear/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.target.clear"
      messages:
        - $ref: "#/channels/vessel.target.clear/messages/commandResponse"
  dispatch:vessel.target.set:
    action: send
    channel:
      $ref: "#/channels/vessel.target.set"
    title: Dispatch vessel.target.set
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.target.set/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.target.set"
      messages:
        - $ref: "#/channels/vessel.target.set/messages/commandResponse"
  dispatch:vessel.trajectory.forVantage:
    action: send
    channel:
      $ref: "#/channels/vessel.trajectory.forVantage"
    title: Dispatch vessel.trajectory.forVantage
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.trajectory.forVantage/messages/commandRequest"
    reply:
      channel:
        $ref: "#/channels/vessel.trajectory.forVantage"
      messages:
        - $ref: "#/channels/vessel.trajectory.forVantage/messages/commandResponse"
  read:alarm.scet:
    action: receive
    channel:
      $ref: "#/channels/alarm.scet"
    title: Read alarm.scet
    description: Delivered only after `subscribe` names `alarm.scet` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: alarm
    messages:
      - $ref: "#/channels/alarm.scet/messages/streamData"
      - $ref: "#/channels/alarm.scet/messages/event"
  read:alarm.scet.fired:
    action: receive
    channel:
      $ref: "#/channels/alarm.scet.fired"
    title: Read alarm.scet.fired
    description: Delivered only after `subscribe` names `alarm.scet.fired` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: alarm
    messages:
      - $ref: "#/channels/alarm.scet.fired/messages/streamData"
      - $ref: "#/channels/alarm.scet.fired/messages/event"
  read:career.facilities:
    action: receive
    channel:
      $ref: "#/channels/career.facilities"
    title: Read career.facilities
    description: Delivered only after `subscribe` names `career.facilities` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.facilities/messages/streamData"
      - $ref: "#/channels/career.facilities/messages/event"
  read:career.mode:
    action: receive
    channel:
      $ref: "#/channels/career.mode"
    title: Read career.mode
    description: Delivered only after `subscribe` names `career.mode` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.mode/messages/streamData"
      - $ref: "#/channels/career.mode/messages/event"
  read:career.status:
    action: receive
    channel:
      $ref: "#/channels/career.status"
    title: Read career.status
    description: Delivered only after `subscribe` names `career.status` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: career
    messages:
      - $ref: "#/channels/career.status/messages/streamData"
      - $ref: "#/channels/career.status/messages/event"
  read:commandCentre.roster:
    action: receive
    channel:
      $ref: "#/channels/commandCentre.roster"
    title: Read commandCentre.roster
    description: Delivered only after `subscribe` names `commandCentre.roster` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: commandCentre
    messages:
      - $ref: "#/channels/commandCentre.roster/messages/streamData"
      - $ref: "#/channels/commandCentre.roster/messages/event"
  read:commandCentre.separation:
    action: receive
    channel:
      $ref: "#/channels/commandCentre.separation"
    title: Read commandCentre.separation
    description: Delivered only after `subscribe` names `commandCentre.separation` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: commandCentre
    messages:
      - $ref: "#/channels/commandCentre.separation/messages/streamData"
      - $ref: "#/channels/commandCentre.separation/messages/event"
  read:comms.commandCentre:
    action: receive
    channel:
      $ref: "#/channels/comms.commandCentre"
    title: Read comms.commandCentre
    description: Delivered only after `subscribe` names `comms.commandCentre` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.commandCentre/messages/streamData"
      - $ref: "#/channels/comms.commandCentre/messages/event"
  read:comms.connectivity:
    action: receive
    channel:
      $ref: "#/channels/comms.connectivity"
    title: Read comms.connectivity
    description: Delivered only after `subscribe` names `comms.connectivity` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.connectivity/messages/streamData"
      - $ref: "#/channels/comms.connectivity/messages/event"
  read:comms.controlState:
    action: receive
    channel:
      $ref: "#/channels/comms.controlState"
    title: Read comms.controlState
    description: Delivered only after `subscribe` names `comms.controlState` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.controlState/messages/streamData"
      - $ref: "#/channels/comms.controlState/messages/event"
  read:comms.degrade:
    action: receive
    channel:
      $ref: "#/channels/comms.degrade"
    title: Read comms.degrade
    description: Delivered only after `subscribe` names `comms.degrade` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.degrade/messages/streamData"
      - $ref: "#/channels/comms.degrade/messages/event"
  read:comms.delay:
    action: receive
    channel:
      $ref: "#/channels/comms.delay"
    title: Read comms.delay
    description: Delivered only after `subscribe` names `comms.delay` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.delay/messages/streamData"
      - $ref: "#/channels/comms.delay/messages/event"
  read:comms.link:
    action: receive
    channel:
      $ref: "#/channels/comms.link"
    title: Read comms.link
    description: Delivered only after `subscribe` names `comms.link` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.link/messages/streamData"
      - $ref: "#/channels/comms.link/messages/event"
  read:comms.network:
    action: receive
    channel:
      $ref: "#/channels/comms.network"
    title: Read comms.network
    description: Delivered only after `subscribe` names `comms.network` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.network/messages/streamData"
      - $ref: "#/channels/comms.network/messages/event"
  read:comms.occlusion:
    action: receive
    channel:
      $ref: "#/channels/comms.occlusion"
    title: Read comms.occlusion
    description: Delivered only after `subscribe` names `comms.occlusion` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.occlusion/messages/streamData"
      - $ref: "#/channels/comms.occlusion/messages/event"
  read:comms.path:
    action: receive
    channel:
      $ref: "#/channels/comms.path"
    title: Read comms.path
    description: Delivered only after `subscribe` names `comms.path` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.path/messages/streamData"
      - $ref: "#/channels/comms.path/messages/event"
  read:comms.signalStrength:
    action: receive
    channel:
      $ref: "#/channels/comms.signalStrength"
    title: Read comms.signalStrength
    description: Delivered only after `subscribe` names `comms.signalStrength` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: comms
    messages:
      - $ref: "#/channels/comms.signalStrength/messages/streamData"
      - $ref: "#/channels/comms.signalStrength/messages/event"
  read:crash.lastCrash:
    action: receive
    channel:
      $ref: "#/channels/crash.lastCrash"
    title: Read crash.lastCrash
    description: Delivered only after `subscribe` names `crash.lastCrash` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: crash
    messages:
      - $ref: "#/channels/crash.lastCrash/messages/streamData"
      - $ref: "#/channels/crash.lastCrash/messages/event"
  read:deployed.bases:
    action: receive
    channel:
      $ref: "#/channels/deployed.bases"
    title: Read deployed.bases
    description: Delivered only after `subscribe` names `deployed.bases` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: deployed
    messages:
      - $ref: "#/channels/deployed.bases/messages/streamData"
      - $ref: "#/channels/deployed.bases/messages/event"
  read:dv.stages:
    action: receive
    channel:
      $ref: "#/channels/dv.stages"
    title: Read dv.stages
    description: Delivered only after `subscribe` names `dv.stages` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: dv
    messages:
      - $ref: "#/channels/dv.stages/messages/streamData"
      - $ref: "#/channels/dv.stages/messages/event"
  read:dv.summary:
    action: receive
    channel:
      $ref: "#/channels/dv.summary"
    title: Read dv.summary
    description: Delivered only after `subscribe` names `dv.summary` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: dv
    messages:
      - $ref: "#/channels/dv.summary/messages/streamData"
      - $ref: "#/channels/dv.summary/messages/event"
  read:fleet.silence:
    action: receive
    channel:
      $ref: "#/channels/fleet.silence"
    title: Read fleet.silence
    description: Delivered only after `subscribe` names `fleet.silence` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: fleet
    messages:
      - $ref: "#/channels/fleet.silence/messages/streamData"
      - $ref: "#/channels/fleet.silence/messages/event"
  read:flight.current:
    action: receive
    channel:
      $ref: "#/channels/flight.current"
    title: Read flight.current
    description: Delivered only after `subscribe` names `flight.current` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: flight
    messages:
      - $ref: "#/channels/flight.current/messages/streamData"
      - $ref: "#/channels/flight.current/messages/event"
  read:flight.ended:
    action: receive
    channel:
      $ref: "#/channels/flight.ended"
    title: Read flight.ended
    description: Delivered only after `subscribe` names `flight.ended` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: flight
    messages:
      - $ref: "#/channels/flight.ended/messages/streamData"
      - $ref: "#/channels/flight.ended/messages/event"
  read:flight.simulation:
    action: receive
    channel:
      $ref: "#/channels/flight.simulation"
    title: Read flight.simulation
    description: Delivered only after `subscribe` names `flight.simulation` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: flight
    messages:
      - $ref: "#/channels/flight.simulation/messages/streamData"
      - $ref: "#/channels/flight.simulation/messages/event"
  read:flight.started:
    action: receive
    channel:
      $ref: "#/channels/flight.started"
    title: Read flight.started
    description: Delivered only after `subscribe` names `flight.started` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: flight
    messages:
      - $ref: "#/channels/flight.started/messages/streamData"
      - $ref: "#/channels/flight.started/messages/event"
  read:flight.vesselChanged:
    action: receive
    channel:
      $ref: "#/channels/flight.vesselChanged"
    title: Read flight.vesselChanged
    description: Delivered only after `subscribe` names `flight.vesselChanged` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: flight
    messages:
      - $ref: "#/channels/flight.vesselChanged/messages/streamData"
      - $ref: "#/channels/flight.vesselChanged/messages/event"
  read:game.dlc:
    action: receive
    channel:
      $ref: "#/channels/game.dlc"
    title: Read game.dlc
    description: Delivered only after `subscribe` names `game.dlc` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: game
    messages:
      - $ref: "#/channels/game.dlc/messages/streamData"
      - $ref: "#/channels/game.dlc/messages/event"
  read:isru.converters:
    action: receive
    channel:
      $ref: "#/channels/isru.converters"
    title: Read isru.converters
    description: Delivered only after `subscribe` names `isru.converters` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: isru
    messages:
      - $ref: "#/channels/isru.converters/messages/streamData"
      - $ref: "#/channels/isru.converters/messages/event"
  read:isru.drills:
    action: receive
    channel:
      $ref: "#/channels/isru.drills"
    title: Read isru.drills
    description: Delivered only after `subscribe` names `isru.drills` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: isru
    messages:
      - $ref: "#/channels/isru.drills/messages/streamData"
      - $ref: "#/channels/isru.drills/messages/event"
  read:ksp.revertAvailability:
    action: receive
    channel:
      $ref: "#/channels/ksp.revertAvailability"
    title: Read ksp.revertAvailability
    description: Delivered only after `subscribe` names `ksp.revertAvailability` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: ksp
    messages:
      - $ref: "#/channels/ksp.revertAvailability/messages/streamData"
      - $ref: "#/channels/ksp.revertAvailability/messages/event"
  read:parts.power:
    action: receive
    channel:
      $ref: "#/channels/parts.power"
    title: Read parts.power
    description: Delivered only after `subscribe` names `parts.power` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: parts
    messages:
      - $ref: "#/channels/parts.power/messages/streamData"
      - $ref: "#/channels/parts.power/messages/event"
  read:recovery.lastSummary:
    action: receive
    channel:
      $ref: "#/channels/recovery.lastSummary"
    title: Read recovery.lastSummary
    description: Delivered only after `subscribe` names `recovery.lastSummary` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: recovery
    messages:
      - $ref: "#/channels/recovery.lastSummary/messages/streamData"
      - $ref: "#/channels/recovery.lastSummary/messages/event"
  read:reliability.parts:
    action: receive
    channel:
      $ref: "#/channels/reliability.parts"
    title: Read reliability.parts
    description: Delivered only after `subscribe` names `reliability.parts` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: reliability
    messages:
      - $ref: "#/channels/reliability.parts/messages/streamData"
      - $ref: "#/channels/reliability.parts/messages/event"
  read:reliability.summary:
    action: receive
    channel:
      $ref: "#/channels/reliability.summary"
    title: Read reliability.summary
    description: Delivered only after `subscribe` names `reliability.summary` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: reliability
    messages:
      - $ref: "#/channels/reliability.summary/messages/streamData"
      - $ref: "#/channels/reliability.summary/messages/event"
  read:robotics.available:
    action: receive
    channel:
      $ref: "#/channels/robotics.available"
    title: Read robotics.available
    description: Delivered only after `subscribe` names `robotics.available` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.available/messages/streamData"
      - $ref: "#/channels/robotics.available/messages/event"
  read:robotics.servos:
    action: receive
    channel:
      $ref: "#/channels/robotics.servos"
    title: Read robotics.servos
    description: Delivered only after `subscribe` names `robotics.servos` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: robotics
    messages:
      - $ref: "#/channels/robotics.servos/messages/streamData"
      - $ref: "#/channels/robotics.servos/messages/event"
  read:science.archive:
    action: receive
    channel:
      $ref: "#/channels/science.archive"
    title: Read science.archive
    description: Delivered only after `subscribe` names `science.archive` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: science
    messages:
      - $ref: "#/channels/science.archive/messages/streamData"
      - $ref: "#/channels/science.archive/messages/event"
  read:science.experimentBreakdown:
    action: receive
    channel:
      $ref: "#/channels/science.experimentBreakdown"
    title: Read science.experimentBreakdown
    description: Delivered only after `subscribe` names `science.experimentBreakdown` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: science
    messages:
      - $ref: "#/channels/science.experimentBreakdown/messages/streamData"
      - $ref: "#/channels/science.experimentBreakdown/messages/event"
  read:science.experiments:
    action: receive
    channel:
      $ref: "#/channels/science.experiments"
    title: Read science.experiments
    description: Delivered only after `subscribe` names `science.experiments` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: science
    messages:
      - $ref: "#/channels/science.experiments/messages/streamData"
      - $ref: "#/channels/science.experiments/messages/event"
  read:science.instruments:
    action: receive
    channel:
      $ref: "#/channels/science.instruments"
    title: Read science.instruments
    description: Delivered only after `subscribe` names `science.instruments` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: science
    messages:
      - $ref: "#/channels/science.instruments/messages/streamData"
      - $ref: "#/channels/science.instruments/messages/event"
  read:science.lab:
    action: receive
    channel:
      $ref: "#/channels/science.lab"
    title: Read science.lab
    description: Delivered only after `subscribe` names `science.lab` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: science
    messages:
      - $ref: "#/channels/science.lab/messages/streamData"
      - $ref: "#/channels/science.lab/messages/event"
  read:science.sensors:
    action: receive
    channel:
      $ref: "#/channels/science.sensors"
    title: Read science.sensors
    description: Delivered only after `subscribe` names `science.sensors` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: science
    messages:
      - $ref: "#/channels/science.sensors/messages/streamData"
      - $ref: "#/channels/science.sensors/messages/event"
  read:spaceCenter.astronautComplex:
    action: receive
    channel:
      $ref: "#/channels/spaceCenter.astronautComplex"
    title: Read spaceCenter.astronautComplex
    description: Delivered only after `subscribe` names `spaceCenter.astronautComplex` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: spaceCenter
    messages:
      - $ref: "#/channels/spaceCenter.astronautComplex/messages/streamData"
      - $ref: "#/channels/spaceCenter.astronautComplex/messages/event"
  read:spaceCenter.crewRoster:
    action: receive
    channel:
      $ref: "#/channels/spaceCenter.crewRoster"
    title: Read spaceCenter.crewRoster
    description: Delivered only after `subscribe` names `spaceCenter.crewRoster` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: spaceCenter
    messages:
      - $ref: "#/channels/spaceCenter.crewRoster/messages/streamData"
      - $ref: "#/channels/spaceCenter.crewRoster/messages/event"
  read:spaceCenter.launchSites:
    action: receive
    channel:
      $ref: "#/channels/spaceCenter.launchSites"
    title: Read spaceCenter.launchSites
    description: Delivered only after `subscribe` names `spaceCenter.launchSites` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: spaceCenter
    messages:
      - $ref: "#/channels/spaceCenter.launchSites/messages/streamData"
      - $ref: "#/channels/spaceCenter.launchSites/messages/event"
  read:spaceCenter.partsAvailable:
    action: receive
    channel:
      $ref: "#/channels/spaceCenter.partsAvailable"
    title: Read spaceCenter.partsAvailable
    description: Delivered only after `subscribe` names `spaceCenter.partsAvailable` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: spaceCenter
    messages:
      - $ref: "#/channels/spaceCenter.partsAvailable/messages/streamData"
      - $ref: "#/channels/spaceCenter.partsAvailable/messages/event"
  read:spaceCenter.pois:
    action: receive
    channel:
      $ref: "#/channels/spaceCenter.pois"
    title: Read spaceCenter.pois
    description: Delivered only after `subscribe` names `spaceCenter.pois` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: spaceCenter
    messages:
      - $ref: "#/channels/spaceCenter.pois/messages/streamData"
      - $ref: "#/channels/spaceCenter.pois/messages/event"
  read:spaceCenter.savedShips:
    action: receive
    channel:
      $ref: "#/channels/spaceCenter.savedShips"
    title: Read spaceCenter.savedShips
    description: Delivered only after `subscribe` names `spaceCenter.savedShips` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: spaceCenter
    messages:
      - $ref: "#/channels/spaceCenter.savedShips/messages/streamData"
      - $ref: "#/channels/spaceCenter.savedShips/messages/event"
  read:spaceCenter.scene:
    action: receive
    channel:
      $ref: "#/channels/spaceCenter.scene"
    title: Read spaceCenter.scene
    description: Delivered only after `subscribe` names `spaceCenter.scene` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: spaceCenter
    messages:
      - $ref: "#/channels/spaceCenter.scene/messages/streamData"
      - $ref: "#/channels/spaceCenter.scene/messages/event"
  read:system.bodies:
    action: receive
    channel:
      $ref: "#/channels/system.bodies"
    title: Read system.bodies
    description: Delivered only after `subscribe` names `system.bodies` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: system
    messages:
      - $ref: "#/channels/system.bodies/messages/streamData"
      - $ref: "#/channels/system.bodies/messages/event"
  read:system.frame:
    action: receive
    channel:
      $ref: "#/channels/system.frame"
    title: Read system.frame
    description: Delivered only after `subscribe` names `system.frame` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: system
    messages:
      - $ref: "#/channels/system.frame/messages/streamData"
      - $ref: "#/channels/system.frame/messages/event"
  read:system.vessels:
    action: receive
    channel:
      $ref: "#/channels/system.vessels"
    title: Read system.vessels
    description: Delivered only after `subscribe` names `system.vessels` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: system
    messages:
      - $ref: "#/channels/system.vessels/messages/streamData"
      - $ref: "#/channels/system.vessels/messages/event"
  read:target.available:
    action: receive
    channel:
      $ref: "#/channels/target.available"
    title: Read target.available
    description: Delivered only after `subscribe` names `target.available` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: target
    messages:
      - $ref: "#/channels/target.available/messages/streamData"
      - $ref: "#/channels/target.available/messages/event"
  read:time.calendar:
    action: receive
    channel:
      $ref: "#/channels/time.calendar"
    title: Read time.calendar
    description: Delivered only after `subscribe` names `time.calendar` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: time
    messages:
      - $ref: "#/channels/time.calendar/messages/streamData"
      - $ref: "#/channels/time.calendar/messages/event"
  read:time.warp:
    action: receive
    channel:
      $ref: "#/channels/time.warp"
    title: Read time.warp
    description: Delivered only after `subscribe` names `time.warp` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: time
    messages:
      - $ref: "#/channels/time.warp/messages/streamData"
      - $ref: "#/channels/time.warp/messages/event"
  read:vessel.attitude:
    action: receive
    channel:
      $ref: "#/channels/vessel.attitude"
    title: Read vessel.attitude
    description: Delivered only after `subscribe` names `vessel.attitude` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.attitude/messages/streamData"
      - $ref: "#/channels/vessel.attitude/messages/event"
  read:vessel.comms:
    action: receive
    channel:
      $ref: "#/channels/vessel.comms"
    title: Read vessel.comms
    description: Delivered only after `subscribe` names `vessel.comms` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.comms/messages/streamData"
      - $ref: "#/channels/vessel.comms/messages/event"
  read:vessel.control:
    action: receive
    channel:
      $ref: "#/channels/vessel.control"
    title: Read vessel.control
    description: Delivered only after `subscribe` names `vessel.control` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.control/messages/streamData"
      - $ref: "#/channels/vessel.control/messages/event"
  read:vessel.crew:
    action: receive
    channel:
      $ref: "#/channels/vessel.crew"
    title: Read vessel.crew
    description: Delivered only after `subscribe` names `vessel.crew` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.crew/messages/streamData"
      - $ref: "#/channels/vessel.crew/messages/event"
  read:vessel.dock:
    action: receive
    channel:
      $ref: "#/channels/vessel.dock"
    title: Read vessel.dock
    description: Delivered only after `subscribe` names `vessel.dock` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.dock/messages/streamData"
      - $ref: "#/channels/vessel.dock/messages/event"
  read:vessel.flight:
    action: receive
    channel:
      $ref: "#/channels/vessel.flight"
    title: Read vessel.flight
    description: Delivered only after `subscribe` names `vessel.flight` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.flight/messages/streamData"
      - $ref: "#/channels/vessel.flight/messages/event"
  read:vessel.identity:
    action: receive
    channel:
      $ref: "#/channels/vessel.identity"
    title: Read vessel.identity
    description: Delivered only after `subscribe` names `vessel.identity` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.identity/messages/streamData"
      - $ref: "#/channels/vessel.identity/messages/event"
  read:vessel.inventory:
    action: receive
    channel:
      $ref: "#/channels/vessel.inventory"
    title: Read vessel.inventory
    description: Delivered only after `subscribe` names `vessel.inventory` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.inventory/messages/streamData"
      - $ref: "#/channels/vessel.inventory/messages/event"
  read:vessel.landing:
    action: receive
    channel:
      $ref: "#/channels/vessel.landing"
    title: Read vessel.landing
    description: Delivered only after `subscribe` names `vessel.landing` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.landing/messages/streamData"
      - $ref: "#/channels/vessel.landing/messages/event"
  read:vessel.maneuver:
    action: receive
    channel:
      $ref: "#/channels/vessel.maneuver"
    title: Read vessel.maneuver
    description: Delivered only after `subscribe` names `vessel.maneuver` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.maneuver/messages/streamData"
      - $ref: "#/channels/vessel.maneuver/messages/event"
  read:vessel.orbit:
    action: receive
    channel:
      $ref: "#/channels/vessel.orbit"
    title: Read vessel.orbit
    description: Delivered only after `subscribe` names `vessel.orbit` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.orbit/messages/streamData"
      - $ref: "#/channels/vessel.orbit/messages/event"
  read:vessel.orbit.truth:
    action: receive
    channel:
      $ref: "#/channels/vessel.orbit.truth"
    title: Read vessel.orbit.truth
    description: Delivered only after `subscribe` names `vessel.orbit.truth` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.orbit.truth/messages/streamData"
      - $ref: "#/channels/vessel.orbit.truth/messages/event"
  read:vessel.parts:
    action: receive
    channel:
      $ref: "#/channels/vessel.parts"
    title: Read vessel.parts
    description: Delivered only after `subscribe` names `vessel.parts` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.parts/messages/streamData"
      - $ref: "#/channels/vessel.parts/messages/event"
  read:vessel.physics.mode:
    action: receive
    channel:
      $ref: "#/channels/vessel.physics.mode"
    title: Read vessel.physics.mode
    description: Delivered only after `subscribe` names `vessel.physics.mode` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.physics.mode/messages/streamData"
      - $ref: "#/channels/vessel.physics.mode/messages/event"
  read:vessel.propulsion:
    action: receive
    channel:
      $ref: "#/channels/vessel.propulsion"
    title: Read vessel.propulsion
    description: Delivered only after `subscribe` names `vessel.propulsion` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.propulsion/messages/streamData"
      - $ref: "#/channels/vessel.propulsion/messages/event"
  read:vessel.resources:
    action: receive
    channel:
      $ref: "#/channels/vessel.resources"
    title: Read vessel.resources
    description: Delivered only after `subscribe` names `vessel.resources` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.resources/messages/streamData"
      - $ref: "#/channels/vessel.resources/messages/event"
  read:vessel.structure:
    action: receive
    channel:
      $ref: "#/channels/vessel.structure"
    title: Read vessel.structure
    description: Delivered only after `subscribe` names `vessel.structure` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.structure/messages/streamData"
      - $ref: "#/channels/vessel.structure/messages/event"
  read:vessel.surface:
    action: receive
    channel:
      $ref: "#/channels/vessel.surface"
    title: Read vessel.surface
    description: Delivered only after `subscribe` names `vessel.surface` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.surface/messages/streamData"
      - $ref: "#/channels/vessel.surface/messages/event"
  read:vessel.target:
    action: receive
    channel:
      $ref: "#/channels/vessel.target"
    title: Read vessel.target
    description: Delivered only after `subscribe` names `vessel.target` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.target/messages/streamData"
      - $ref: "#/channels/vessel.target/messages/event"
  read:vessel.thermal:
    action: receive
    channel:
      $ref: "#/channels/vessel.thermal"
    title: Read vessel.thermal
    description: Delivered only after `subscribe` names `vessel.thermal` on the session channel, which the mod answers here with an `event` named `subscribed`.
    tags:
      - name: vessel
    messages:
      - $ref: "#/channels/vessel.thermal/messages/streamData"
      - $ref: "#/channels/vessel.thermal/messages/event"
  session.control:
    action: send
    channel:
      $ref: "#/channels/session"
    title: Open or close delivery, or change vantage
    tags:
      - name: session
    messages:
      - $ref: "#/channels/session/messages/subscribe"
      - $ref: "#/channels/session/messages/unsubscribe"
      - $ref: "#/channels/session/messages/setVantage"
  session.notices:
    action: receive
    channel:
      $ref: "#/channels/session"
    title: Connection-scoped errors
    description: An `event` frame is NOT here. It names a topic and is only ever emitted for one this connection has subscribed to, so it sits on the topic channels alongside their `stream-data`.
    tags:
      - name: session
    messages:
      - $ref: "#/channels/session/messages/error"
components:
  correlationIds:
    requestId:
      description: "A dispatch and its answer are correlated on `requestId`, never on order: responses may arrive out of the order the requests were sent, and a delayed command's answer can be minutes behind it."
      location: $message.payload#/requestId
  messages:
    commandRequest.alarm.scet.arm:
      name: command-request
      title: Dispatch alarm.scet.arm
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `alarm.scet.arm`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: alarm.scet.arm
              args:
                $ref: "#/components/schemas/ScetAlarmArmArgs"
    commandRequest.alarm.scet.disarm:
      name: command-request
      title: Dispatch alarm.scet.disarm
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `alarm.scet.disarm`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: alarm.scet.disarm
              args:
                $ref: "#/components/schemas/ScetAlarmDisarmArgs"
    commandRequest.career.contract.accept:
      name: command-request
      title: Dispatch career.contract.accept
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.contract.accept`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.contract.accept
              args:
                $ref: "#/components/schemas/ContractActionArgs"
    commandRequest.career.contract.cancel:
      name: command-request
      title: Dispatch career.contract.cancel
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.contract.cancel`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.contract.cancel
              args:
                $ref: "#/components/schemas/ContractActionArgs"
    commandRequest.career.contract.decline:
      name: command-request
      title: Dispatch career.contract.decline
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.contract.decline`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.contract.decline
              args:
                $ref: "#/components/schemas/ContractActionArgs"
    commandRequest.career.crew.fire:
      name: command-request
      title: Dispatch career.crew.fire
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.crew.fire`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.crew.fire
              args:
                $ref: "#/components/schemas/FireCrewArgs"
    commandRequest.career.crew.hire:
      name: command-request
      title: Dispatch career.crew.hire
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.crew.hire`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.crew.hire
              args:
                $ref: "#/components/schemas/HireApplicantArgs"
    commandRequest.career.facility.upgrade:
      name: command-request
      title: Dispatch career.facility.upgrade
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.facility.upgrade`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.facility.upgrade
              args:
                $ref: "#/components/schemas/UpgradeFacilityArgs"
    commandRequest.career.strategy.activate:
      name: command-request
      title: Dispatch career.strategy.activate
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.strategy.activate`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.strategy.activate
              args:
                $ref: "#/components/schemas/ActivateStrategyArgs"
    commandRequest.career.strategy.deactivate:
      name: command-request
      title: Dispatch career.strategy.deactivate
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.strategy.deactivate`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.strategy.deactivate
              args:
                $ref: "#/components/schemas/DeactivateStrategyArgs"
    commandRequest.career.tech.unlock:
      name: command-request
      title: Dispatch career.tech.unlock
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `career.tech.unlock`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: career.tech.unlock
              args:
                $ref: "#/components/schemas/UnlockTechArgs"
    commandRequest.comms.setSimulationDelayPolicy:
      name: command-request
      title: Dispatch comms.setSimulationDelayPolicy
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `comms.setSimulationDelayPolicy`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: comms.setSimulationDelayPolicy
              args:
                $ref: "#/components/schemas/SetSimulationDelayPolicyArgs"
    commandRequest.ksp.launch:
      name: command-request
      title: Dispatch ksp.launch
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `ksp.launch`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: ksp.launch
              args:
                $ref: "#/components/schemas/LaunchArgs"
    commandRequest.ksp.recover:
      name: command-request
      title: Dispatch ksp.recover
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `ksp.recover`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: ksp.recover
              args:
                $ref: "#/components/schemas/NoCommandArgs"
    commandRequest.ksp.revertToEditor:
      name: command-request
      title: Dispatch ksp.revertToEditor
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `ksp.revertToEditor`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: ksp.revertToEditor
              args:
                $ref: "#/components/schemas/RevertToEditorArgs"
    commandRequest.ksp.revertToLaunch:
      name: command-request
      title: Dispatch ksp.revertToLaunch
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `ksp.revertToLaunch`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: ksp.revertToLaunch
              args:
                $ref: "#/components/schemas/NoCommandArgs"
    commandRequest.ksp.switchVessel:
      name: command-request
      title: Dispatch ksp.switchVessel
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `ksp.switchVessel`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: ksp.switchVessel
              args:
                $ref: "#/components/schemas/SwitchVesselArgs"
    commandRequest.ksp.toTrackingStation:
      name: command-request
      title: Dispatch ksp.toTrackingStation
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `ksp.toTrackingStation`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: ksp.toTrackingStation
              args:
                $ref: "#/components/schemas/NoCommandArgs"
    commandRequest.robotics.rotor.reverse:
      name: command-request
      title: Dispatch robotics.rotor.reverse
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.rotor.reverse`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.rotor.reverse
              args:
                $ref: "#/components/schemas/RotorReverseArgs"
    commandRequest.robotics.rotor.setBrake:
      name: command-request
      title: Dispatch robotics.rotor.setBrake
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.rotor.setBrake`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.rotor.setBrake
              args:
                $ref: "#/components/schemas/RotorSetValueArgs"
    commandRequest.robotics.rotor.setLock:
      name: command-request
      title: Dispatch robotics.rotor.setLock
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.rotor.setLock`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.rotor.setLock
              args:
                $ref: "#/components/schemas/ServoSetEnabledArgs"
    commandRequest.robotics.rotor.setMotor:
      name: command-request
      title: Dispatch robotics.rotor.setMotor
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.rotor.setMotor`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.rotor.setMotor
              args:
                $ref: "#/components/schemas/ServoSetEnabledArgs"
    commandRequest.robotics.rotor.setRpmLimit:
      name: command-request
      title: Dispatch robotics.rotor.setRpmLimit
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.rotor.setRpmLimit`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.rotor.setRpmLimit
              args:
                $ref: "#/components/schemas/RotorSetValueArgs"
    commandRequest.robotics.rotor.setTorqueLimit:
      name: command-request
      title: Dispatch robotics.rotor.setTorqueLimit
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.rotor.setTorqueLimit`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.rotor.setTorqueLimit
              args:
                $ref: "#/components/schemas/RotorSetValueArgs"
    commandRequest.robotics.servo.setLock:
      name: command-request
      title: Dispatch robotics.servo.setLock
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.servo.setLock`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.servo.setLock
              args:
                $ref: "#/components/schemas/ServoSetEnabledArgs"
    commandRequest.robotics.servo.setMotor:
      name: command-request
      title: Dispatch robotics.servo.setMotor
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.servo.setMotor`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.servo.setMotor
              args:
                $ref: "#/components/schemas/ServoSetEnabledArgs"
    commandRequest.robotics.servo.setTarget:
      name: command-request
      title: Dispatch robotics.servo.setTarget
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `robotics.servo.setTarget`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: robotics.servo.setTarget
              args:
                $ref: "#/components/schemas/ServoSetTargetArgs"
    commandRequest.science.experiment.deploy:
      name: command-request
      title: Dispatch science.experiment.deploy
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `science.experiment.deploy`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: science.experiment.deploy
              args:
                $ref: "#/components/schemas/ExperimentActionArgs"
    commandRequest.science.experiment.transmit:
      name: command-request
      title: Dispatch science.experiment.transmit
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `science.experiment.transmit`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: science.experiment.transmit
              args:
                $ref: "#/components/schemas/ExperimentActionArgs"
    commandRequest.system.frame.set:
      name: command-request
      title: Dispatch system.frame.set
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `system.frame.set`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: system.frame.set
              args:
                $ref: "#/components/schemas/SetControlFrameArgs"
    commandRequest.time.setPaused:
      name: command-request
      title: Dispatch time.setPaused
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `time.setPaused`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: time.setPaused
              args:
                $ref: "#/components/schemas/SetPausedArgs"
    commandRequest.time.setWarpIndex:
      name: command-request
      title: Dispatch time.setWarpIndex
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `time.setWarpIndex`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: time.setWarpIndex
              args:
                $ref: "#/components/schemas/SetWarpIndexArgs"
    commandRequest.vessel.control.setAbort:
      name: command-request
      title: Dispatch vessel.control.setAbort
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setAbort`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setAbort
              args:
                $ref: "#/components/schemas/SetEnabledArgs"
    commandRequest.vessel.control.setActionGroup:
      name: command-request
      title: Dispatch vessel.control.setActionGroup
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setActionGroup`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setActionGroup
              args:
                $ref: "#/components/schemas/SetActionGroupArgs"
    commandRequest.vessel.control.setAxes:
      name: command-request
      title: Dispatch vessel.control.setAxes
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setAxes`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setAxes
              args:
                $ref: "#/components/schemas/SetControlAxesArgs"
    commandRequest.vessel.control.setBrakes:
      name: command-request
      title: Dispatch vessel.control.setBrakes
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setBrakes`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setBrakes
              args:
                $ref: "#/components/schemas/SetEnabledArgs"
    commandRequest.vessel.control.setFlyByWire:
      name: command-request
      title: Dispatch vessel.control.setFlyByWire
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setFlyByWire`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setFlyByWire
              args:
                $ref: "#/components/schemas/SetFlyByWireArgs"
    commandRequest.vessel.control.setGear:
      name: command-request
      title: Dispatch vessel.control.setGear
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setGear`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setGear
              args:
                $ref: "#/components/schemas/SetEnabledArgs"
    commandRequest.vessel.control.setLights:
      name: command-request
      title: Dispatch vessel.control.setLights
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setLights`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setLights
              args:
                $ref: "#/components/schemas/SetEnabledArgs"
    commandRequest.vessel.control.setRcs:
      name: command-request
      title: Dispatch vessel.control.setRcs
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setRcs`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setRcs
              args:
                $ref: "#/components/schemas/SetEnabledArgs"
    commandRequest.vessel.control.setSas:
      name: command-request
      title: Dispatch vessel.control.setSas
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setSas`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setSas
              args:
                $ref: "#/components/schemas/SetEnabledArgs"
    commandRequest.vessel.control.setSasMode:
      name: command-request
      title: Dispatch vessel.control.setSasMode
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setSasMode`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setSasMode
              args:
                $ref: "#/components/schemas/SetSasModeArgs"
    commandRequest.vessel.control.setThrottle:
      name: command-request
      title: Dispatch vessel.control.setThrottle
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.setThrottle`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.setThrottle
              args:
                $ref: "#/components/schemas/SetThrottleArgs"
    commandRequest.vessel.control.stage:
      name: command-request
      title: Dispatch vessel.control.stage
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.control.stage`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.control.stage
              args:
                $ref: "#/components/schemas/NoCommandArgs"
    commandRequest.vessel.invokePartAction:
      name: command-request
      title: Dispatch vessel.invokePartAction
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.invokePartAction`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.invokePartAction
              args:
                $ref: "#/components/schemas/InvokePartActionArgs"
    commandRequest.vessel.maneuver.add:
      name: command-request
      title: Dispatch vessel.maneuver.add
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.maneuver.add`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.maneuver.add
              args:
                $ref: "#/components/schemas/AddManeuverNodeArgs"
    commandRequest.vessel.maneuver.plan.send:
      name: command-request
      title: Dispatch vessel.maneuver.plan.send
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.maneuver.plan.send`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.maneuver.plan.send
              args:
                $ref: "#/components/schemas/SendManeuverPlanArgs"
    commandRequest.vessel.maneuver.remove:
      name: command-request
      title: Dispatch vessel.maneuver.remove
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.maneuver.remove`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.maneuver.remove
              args:
                $ref: "#/components/schemas/RemoveManeuverNodeArgs"
    commandRequest.vessel.maneuver.update:
      name: command-request
      title: Dispatch vessel.maneuver.update
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.maneuver.update`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.maneuver.update
              args:
                $ref: "#/components/schemas/UpdateManeuverNodeArgs"
    commandRequest.vessel.repair:
      name: command-request
      title: Dispatch vessel.repair
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.repair`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.repair
              args:
                $ref: "#/components/schemas/RepairPartArgs"
    commandRequest.vessel.target.clear:
      name: command-request
      title: Dispatch vessel.target.clear
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.target.clear`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.target.clear
              args:
                $ref: "#/components/schemas/NoCommandArgs"
    commandRequest.vessel.target.set:
      name: command-request
      title: Dispatch vessel.target.set
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.target.set`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.target.set
              args:
                $ref: "#/components/schemas/SetTargetArgs"
    commandRequest.vessel.trajectory.forVantage:
      name: command-request
      title: Dispatch vessel.trajectory.forVantage
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-request` envelope for `vessel.trajectory.forVantage`, with its typed `args`.
        allOf:
          - $ref: "#/components/schemas/CommandRequest"
          - properties:
              command:
                const: vessel.trajectory.forVantage
              args:
                $ref: "#/components/schemas/VantagePlanRequest"
    commandResponse.alarm.scet.arm:
      name: command-response
      title: Reply to alarm.scet.arm
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `alarm.scet.arm`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.alarm.scet.disarm:
      name: command-response
      title: Reply to alarm.scet.disarm
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `alarm.scet.disarm`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.contract.accept:
      name: command-response
      title: Reply to career.contract.accept
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.contract.accept`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.contract.cancel:
      name: command-response
      title: Reply to career.contract.cancel
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.contract.cancel`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.contract.decline:
      name: command-response
      title: Reply to career.contract.decline
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.contract.decline`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.crew.fire:
      name: command-response
      title: Reply to career.crew.fire
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.crew.fire`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.crew.hire:
      name: command-response
      title: Reply to career.crew.hire
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.crew.hire`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.facility.upgrade:
      name: command-response
      title: Reply to career.facility.upgrade
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.facility.upgrade`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.strategy.activate:
      name: command-response
      title: Reply to career.strategy.activate
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.strategy.activate`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.strategy.deactivate:
      name: command-response
      title: Reply to career.strategy.deactivate
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.strategy.deactivate`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.career.tech.unlock:
      name: command-response
      title: Reply to career.tech.unlock
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `career.tech.unlock`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.comms.setSimulationDelayPolicy:
      name: command-response
      title: Reply to comms.setSimulationDelayPolicy
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `comms.setSimulationDelayPolicy`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.ksp.launch:
      name: command-response
      title: Reply to ksp.launch
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `ksp.launch`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.ksp.recover:
      name: command-response
      title: Reply to ksp.recover
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `ksp.recover`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.ksp.revertToEditor:
      name: command-response
      title: Reply to ksp.revertToEditor
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `ksp.revertToEditor`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.ksp.revertToLaunch:
      name: command-response
      title: Reply to ksp.revertToLaunch
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `ksp.revertToLaunch`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.ksp.switchVessel:
      name: command-response
      title: Reply to ksp.switchVessel
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `ksp.switchVessel`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.ksp.toTrackingStation:
      name: command-response
      title: Reply to ksp.toTrackingStation
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `ksp.toTrackingStation`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.rotor.reverse:
      name: command-response
      title: Reply to robotics.rotor.reverse
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.rotor.reverse`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.rotor.setBrake:
      name: command-response
      title: Reply to robotics.rotor.setBrake
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.rotor.setBrake`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.rotor.setLock:
      name: command-response
      title: Reply to robotics.rotor.setLock
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.rotor.setLock`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.rotor.setMotor:
      name: command-response
      title: Reply to robotics.rotor.setMotor
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.rotor.setMotor`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.rotor.setRpmLimit:
      name: command-response
      title: Reply to robotics.rotor.setRpmLimit
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.rotor.setRpmLimit`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.rotor.setTorqueLimit:
      name: command-response
      title: Reply to robotics.rotor.setTorqueLimit
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.rotor.setTorqueLimit`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.servo.setLock:
      name: command-response
      title: Reply to robotics.servo.setLock
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.servo.setLock`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.servo.setMotor:
      name: command-response
      title: Reply to robotics.servo.setMotor
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.servo.setMotor`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.robotics.servo.setTarget:
      name: command-response
      title: Reply to robotics.servo.setTarget
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `robotics.servo.setTarget`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.science.experiment.deploy:
      name: command-response
      title: Reply to science.experiment.deploy
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `science.experiment.deploy`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.science.experiment.transmit:
      name: command-response
      title: Reply to science.experiment.transmit
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `science.experiment.transmit`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.system.frame.set:
      name: command-response
      title: Reply to system.frame.set
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `system.frame.set`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.time.setPaused:
      name: command-response
      title: Reply to time.setPaused
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `time.setPaused`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.time.setWarpIndex:
      name: command-response
      title: Reply to time.setWarpIndex
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `time.setWarpIndex`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setAbort:
      name: command-response
      title: Reply to vessel.control.setAbort
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setAbort`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setActionGroup:
      name: command-response
      title: Reply to vessel.control.setActionGroup
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setActionGroup`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setAxes:
      name: command-response
      title: Reply to vessel.control.setAxes
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setAxes`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setBrakes:
      name: command-response
      title: Reply to vessel.control.setBrakes
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setBrakes`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setFlyByWire:
      name: command-response
      title: Reply to vessel.control.setFlyByWire
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setFlyByWire`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setGear:
      name: command-response
      title: Reply to vessel.control.setGear
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setGear`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setLights:
      name: command-response
      title: Reply to vessel.control.setLights
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setLights`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setRcs:
      name: command-response
      title: Reply to vessel.control.setRcs
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setRcs`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setSas:
      name: command-response
      title: Reply to vessel.control.setSas
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setSas`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setSasMode:
      name: command-response
      title: Reply to vessel.control.setSasMode
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setSasMode`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.setThrottle:
      name: command-response
      title: Reply to vessel.control.setThrottle
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.setThrottle`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.control.stage:
      name: command-response
      title: Reply to vessel.control.stage
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.control.stage`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                description: |-
                  R7 Fix 1: the payload-carrying result, `CommandResult` plus a typed
                  CommandResult.Payload. `vessel.control.stage` returns `CommandResult<int>`
                  (the new current stage index, rather than a void fire-and-forget);
                  `vessel.maneuver.add` returns `CommandResult<string>` (the created node's
                  opaque id, O-6 fixed). CommandResult.Payload is default (null for reference
                  types) when `CommandResult.success` is false.
                allOf:
                  - $ref: "#/components/schemas/CommandResult"
                  - type: object
                    properties:
                      payload:
                        type:
                          - number
                          - "null"
    commandResponse.vessel.invokePartAction:
      name: command-response
      title: Reply to vessel.invokePartAction
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.invokePartAction`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.maneuver.add:
      name: command-response
      title: Reply to vessel.maneuver.add
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.maneuver.add`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                description: |-
                  R7 Fix 1: the payload-carrying result, `CommandResult` plus a typed
                  CommandResult.Payload. `vessel.control.stage` returns `CommandResult<int>`
                  (the new current stage index, rather than a void fire-and-forget);
                  `vessel.maneuver.add` returns `CommandResult<string>` (the created node's
                  opaque id, O-6 fixed). CommandResult.Payload is default (null for reference
                  types) when `CommandResult.success` is false.
                allOf:
                  - $ref: "#/components/schemas/CommandResult"
                  - type: object
                    properties:
                      payload:
                        type:
                          - string
                          - "null"
    commandResponse.vessel.maneuver.plan.send:
      name: command-response
      title: Reply to vessel.maneuver.plan.send
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.maneuver.plan.send`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.maneuver.remove:
      name: command-response
      title: Reply to vessel.maneuver.remove
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.maneuver.remove`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.maneuver.update:
      name: command-response
      title: Reply to vessel.maneuver.update
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.maneuver.update`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.repair:
      name: command-response
      title: Reply to vessel.repair
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.repair`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                description: |-
                  R7 Fix 1: the payload-carrying result, `CommandResult` plus a typed
                  CommandResult.Payload. `vessel.control.stage` returns `CommandResult<int>`
                  (the new current stage index, rather than a void fire-and-forget);
                  `vessel.maneuver.add` returns `CommandResult<string>` (the created node's
                  opaque id, O-6 fixed). CommandResult.Payload is default (null for reference
                  types) when `CommandResult.success` is false.
                allOf:
                  - $ref: "#/components/schemas/CommandResult"
                  - type: object
                    properties:
                      payload:
                        anyOf:
                          - $ref: "#/components/schemas/RepairOutcome"
                          - type: "null"
    commandResponse.vessel.target.clear:
      name: command-response
      title: Reply to vessel.target.clear
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.target.clear`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.target.set:
      name: command-response
      title: Reply to vessel.target.set
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.target.set`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/CommandResult"
    commandResponse.vessel.trajectory.forVantage:
      name: command-response
      title: Reply to vessel.trajectory.forVantage
      contentType: application/json
      correlationId:
        $ref: "#/components/correlationIds/requestId"
      payload:
        description: The `command-response` envelope for `vessel.trajectory.forVantage`, with its typed `result`.
        allOf:
          - $ref: "#/components/schemas/CommandResponse"
          - properties:
              result:
                $ref: "#/components/schemas/VantagePlanReply"
    error:
      name: error
      title: A dispatch that could not be carried, or a bad session request
      payload:
        $ref: "#/components/schemas/ErrorMsg"
      correlationId:
        description: Present when the error answers a dispatch, absent when it is about the connection or a topic.
        location: $message.payload#/requestId
    event:
      name: event
      title: A named occurrence on a topic, carrying no payload of its own
      payload:
        $ref: "#/components/schemas/EventMsg"
    setVantage:
      name: set-vantage
      title: Select this connection's command centre
      payload:
        $ref: "#/components/schemas/SetVantage"
    streamData.alarm.scet:
      name: stream-data
      title: A frame on alarm.scet
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `alarm.scet`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: alarm.scet
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/ScetAlarm"
    streamData.alarm.scet.fired:
      name: stream-data
      title: A frame on alarm.scet.fired
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `alarm.scet.fired`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: alarm.scet.fired
              payload:
                $ref: "#/components/schemas/ScetAlarmFired"
    streamData.career.facilities:
      name: stream-data
      title: A frame on career.facilities
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `career.facilities`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: career.facilities
              payload:
                $ref: "#/components/schemas/CareerFacilities"
    streamData.career.mode:
      name: stream-data
      title: A frame on career.mode
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `career.mode`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: career.mode
              payload:
                $ref: "#/components/schemas/CareerMode"
    streamData.career.status:
      name: stream-data
      title: A frame on career.status
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `career.status`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: career.status
              payload:
                $ref: "#/components/schemas/CareerStatus"
    streamData.commandCentre.roster:
      name: stream-data
      title: A frame on commandCentre.roster
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `commandCentre.roster`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: commandCentre.roster
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/CommandCentreEntry"
    streamData.commandCentre.separation:
      name: stream-data
      title: A frame on commandCentre.separation
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `commandCentre.separation`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: commandCentre.separation
              payload:
                $ref: "#/components/schemas/CommandCentreSeparation"
    streamData.comms.commandCentre:
      name: stream-data
      title: A frame on comms.commandCentre
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.commandCentre`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.commandCentre
              payload:
                $ref: "#/components/schemas/CommsCommandCentre"
    streamData.comms.connectivity:
      name: stream-data
      title: A frame on comms.connectivity
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.connectivity`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.connectivity
              payload:
                $ref: "#/components/schemas/CommsConnectivity"
    streamData.comms.controlState:
      name: stream-data
      title: A frame on comms.controlState
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.controlState`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.controlState
              payload:
                $ref: "#/components/schemas/CommsControlState"
    streamData.comms.degrade:
      name: stream-data
      title: A frame on comms.degrade
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.degrade`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.degrade
              payload:
                $ref: "#/components/schemas/CommsDegrade"
    streamData.comms.delay:
      name: stream-data
      title: A frame on comms.delay
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.delay`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.delay
              payload:
                $ref: "#/components/schemas/CommsDelay"
    streamData.comms.link:
      name: stream-data
      title: A frame on comms.link
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.link`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.link
              payload:
                $ref: "#/components/schemas/CommsLink"
    streamData.comms.network:
      name: stream-data
      title: A frame on comms.network
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.network`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.network
              payload:
                $ref: "#/components/schemas/CommsNetwork"
    streamData.comms.occlusion:
      name: stream-data
      title: A frame on comms.occlusion
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.occlusion`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.occlusion
              payload:
                $ref: "#/components/schemas/CommsOcclusion"
    streamData.comms.path:
      name: stream-data
      title: A frame on comms.path
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.path`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.path
              payload:
                $ref: "#/components/schemas/CommsPath"
    streamData.comms.signalStrength:
      name: stream-data
      title: A frame on comms.signalStrength
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `comms.signalStrength`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: comms.signalStrength
              payload:
                $ref: "#/components/schemas/CommsSignalStrength"
    streamData.crash.lastCrash:
      name: stream-data
      title: A frame on crash.lastCrash
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `crash.lastCrash`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: crash.lastCrash
              payload:
                $ref: "#/components/schemas/CrashReport"
    streamData.deployed.bases:
      name: stream-data
      title: A frame on deployed.bases
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `deployed.bases`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: deployed.bases
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/DeployedEntry"
    streamData.dv.stages:
      name: stream-data
      title: A frame on dv.stages
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `dv.stages`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: dv.stages
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/StageDeltaVEntry"
    streamData.dv.summary:
      name: stream-data
      title: A frame on dv.summary
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `dv.summary`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: dv.summary
              payload:
                $ref: "#/components/schemas/StageDeltaVSummary"
    streamData.fleet.silence:
      name: stream-data
      title: A frame on fleet.silence
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `fleet.silence`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: fleet.silence
              payload:
                $ref: "#/components/schemas/FleetSilence"
    streamData.flight.current:
      name: stream-data
      title: A frame on flight.current
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `flight.current`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: flight.current
              payload:
                $ref: "#/components/schemas/FlightCurrent"
    streamData.flight.ended:
      name: stream-data
      title: A frame on flight.ended
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `flight.ended`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: flight.ended
              payload:
                $ref: "#/components/schemas/FlightEnded"
    streamData.flight.simulation:
      name: stream-data
      title: A frame on flight.simulation
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `flight.simulation`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: flight.simulation
              payload:
                $ref: "#/components/schemas/FlightSimulation"
    streamData.flight.started:
      name: stream-data
      title: A frame on flight.started
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `flight.started`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: flight.started
              payload:
                $ref: "#/components/schemas/FlightStarted"
    streamData.flight.vesselChanged:
      name: stream-data
      title: A frame on flight.vesselChanged
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `flight.vesselChanged`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: flight.vesselChanged
              payload:
                $ref: "#/components/schemas/FlightVesselChanged"
    streamData.game.dlc:
      name: stream-data
      title: A frame on game.dlc
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `game.dlc`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: game.dlc
              payload:
                $ref: "#/components/schemas/GameDlc"
    streamData.isru.converters:
      name: stream-data
      title: A frame on isru.converters
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `isru.converters`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: isru.converters
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/IsruConverterEntry"
    streamData.isru.drills:
      name: stream-data
      title: A frame on isru.drills
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `isru.drills`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: isru.drills
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/IsruDrillEntry"
    streamData.ksp.revertAvailability:
      name: stream-data
      title: A frame on ksp.revertAvailability
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `ksp.revertAvailability`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: ksp.revertAvailability
              payload:
                $ref: "#/components/schemas/RevertAvailability"
    streamData.parts.power:
      name: stream-data
      title: A frame on parts.power
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `parts.power`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: parts.power
              payload:
                $ref: "#/components/schemas/PartsPower"
    streamData.recovery.lastSummary:
      name: stream-data
      title: A frame on recovery.lastSummary
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `recovery.lastSummary`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: recovery.lastSummary
              payload:
                $ref: "#/components/schemas/RecoveryReport"
    streamData.reliability.parts:
      name: stream-data
      title: A frame on reliability.parts
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `reliability.parts`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: reliability.parts
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/ReliabilityPartEntry"
    streamData.reliability.summary:
      name: stream-data
      title: A frame on reliability.summary
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `reliability.summary`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: reliability.summary
              payload:
                $ref: "#/components/schemas/ReliabilitySummary"
    streamData.robotics.available:
      name: stream-data
      title: A frame on robotics.available
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `robotics.available`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: robotics.available
              payload:
                $ref: "#/components/schemas/RoboticsAvailability"
    streamData.robotics.servos:
      name: stream-data
      title: A frame on robotics.servos
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `robotics.servos`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: robotics.servos
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/ServoEntry"
    streamData.science.archive:
      name: stream-data
      title: A frame on science.archive
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `science.archive`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: science.archive
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/ArchiveEntry"
    streamData.science.experimentBreakdown:
      name: stream-data
      title: A frame on science.experimentBreakdown
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `science.experimentBreakdown`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: science.experimentBreakdown
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/ExperimentBreakdownEntry"
    streamData.science.experiments:
      name: stream-data
      title: A frame on science.experiments
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `science.experiments`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: science.experiments
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/ExperimentEntry"
    streamData.science.instruments:
      name: stream-data
      title: A frame on science.instruments
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `science.instruments`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: science.instruments
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/InstrumentEntry"
    streamData.science.lab:
      name: stream-data
      title: A frame on science.lab
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `science.lab`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: science.lab
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/LabEntry"
    streamData.science.sensors:
      name: stream-data
      title: A frame on science.sensors
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `science.sensors`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: science.sensors
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/SensorEntry"
    streamData.spaceCenter.astronautComplex:
      name: stream-data
      title: A frame on spaceCenter.astronautComplex
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `spaceCenter.astronautComplex`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: spaceCenter.astronautComplex
              payload:
                $ref: "#/components/schemas/AstronautComplexInfo"
    streamData.spaceCenter.crewRoster:
      name: stream-data
      title: A frame on spaceCenter.crewRoster
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `spaceCenter.crewRoster`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: spaceCenter.crewRoster
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/CrewRosterEntry"
    streamData.spaceCenter.launchSites:
      name: stream-data
      title: A frame on spaceCenter.launchSites
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `spaceCenter.launchSites`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: spaceCenter.launchSites
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/LaunchSiteEntry"
    streamData.spaceCenter.partsAvailable:
      name: stream-data
      title: A frame on spaceCenter.partsAvailable
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `spaceCenter.partsAvailable`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: spaceCenter.partsAvailable
              payload:
                $ref: "#/components/schemas/SpaceCenterPartsAvailable"
    streamData.spaceCenter.pois:
      name: stream-data
      title: A frame on spaceCenter.pois
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `spaceCenter.pois`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: spaceCenter.pois
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/SpaceCenterPoiEntry"
    streamData.spaceCenter.savedShips:
      name: stream-data
      title: A frame on spaceCenter.savedShips
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `spaceCenter.savedShips`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: spaceCenter.savedShips
              payload:
                type: array
                items:
                  $ref: "#/components/schemas/SavedShipEntry"
    streamData.spaceCenter.scene:
      name: stream-data
      title: A frame on spaceCenter.scene
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `spaceCenter.scene`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: spaceCenter.scene
              payload:
                $ref: "#/components/schemas/SpaceCenterScene"
    streamData.system.bodies:
      name: stream-data
      title: A frame on system.bodies
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `system.bodies`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: system.bodies
              payload:
                $ref: "#/components/schemas/SystemBodies"
    streamData.system.frame:
      name: stream-data
      title: A frame on system.frame
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `system.frame`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: system.frame
              payload:
                $ref: "#/components/schemas/ControlFrame"
    streamData.system.vessels:
      name: stream-data
      title: A frame on system.vessels
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `system.vessels`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: system.vessels
              payload:
                $ref: "#/components/schemas/SystemVessels"
    streamData.target.available:
      name: stream-data
      title: A frame on target.available
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `target.available`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: target.available
              payload:
                $ref: "#/components/schemas/TargetAvailable"
    streamData.time.calendar:
      name: stream-data
      title: A frame on time.calendar
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `time.calendar`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: time.calendar
              payload:
                $ref: "#/components/schemas/TimeCalendar"
    streamData.time.warp:
      name: stream-data
      title: A frame on time.warp
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `time.warp`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: time.warp
              payload:
                $ref: "#/components/schemas/WarpState"
    streamData.vessel.attitude:
      name: stream-data
      title: A frame on vessel.attitude
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.attitude`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.attitude
              payload:
                $ref: "#/components/schemas/VesselAttitude"
    streamData.vessel.comms:
      name: stream-data
      title: A frame on vessel.comms
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.comms`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.comms
              payload:
                $ref: "#/components/schemas/VesselComms"
    streamData.vessel.control:
      name: stream-data
      title: A frame on vessel.control
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.control`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.control
              payload:
                $ref: "#/components/schemas/VesselControl"
    streamData.vessel.crew:
      name: stream-data
      title: A frame on vessel.crew
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.crew`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.crew
              payload:
                $ref: "#/components/schemas/VesselCrew"
    streamData.vessel.dock:
      name: stream-data
      title: A frame on vessel.dock
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.dock`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.dock
              payload:
                $ref: "#/components/schemas/DockAlignment"
    streamData.vessel.flight:
      name: stream-data
      title: A frame on vessel.flight
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.flight`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.flight
              payload:
                $ref: "#/components/schemas/VesselFlight"
    streamData.vessel.identity:
      name: stream-data
      title: A frame on vessel.identity
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.identity`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.identity
              payload:
                $ref: "#/components/schemas/VesselIdentity"
    streamData.vessel.inventory:
      name: stream-data
      title: A frame on vessel.inventory
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.inventory`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.inventory
              payload:
                $ref: "#/components/schemas/VesselInventory"
    streamData.vessel.landing:
      name: stream-data
      title: A frame on vessel.landing
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.landing`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.landing
              payload:
                $ref: "#/components/schemas/VesselLanding"
    streamData.vessel.maneuver:
      name: stream-data
      title: A frame on vessel.maneuver
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.maneuver`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.maneuver
              payload:
                $ref: "#/components/schemas/VesselManeuver"
    streamData.vessel.orbit:
      name: stream-data
      title: A frame on vessel.orbit
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.orbit`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.orbit
              payload:
                $ref: "#/components/schemas/VesselOrbit"
    streamData.vessel.orbit.truth:
      name: stream-data
      title: A frame on vessel.orbit.truth
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.orbit.truth`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.orbit.truth
              payload:
                $ref: "#/components/schemas/VesselOrbitTruth"
    streamData.vessel.parts:
      name: stream-data
      title: A frame on vessel.parts
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.parts`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.parts
              payload:
                $ref: "#/components/schemas/VesselParts"
    streamData.vessel.physics.mode:
      name: stream-data
      title: A frame on vessel.physics.mode
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.physics.mode`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.physics.mode
              payload:
                $ref: "#/components/schemas/VesselPhysicsMode"
    streamData.vessel.propulsion:
      name: stream-data
      title: A frame on vessel.propulsion
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.propulsion`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.propulsion
              payload:
                $ref: "#/components/schemas/VesselPropulsion"
    streamData.vessel.resources:
      name: stream-data
      title: A frame on vessel.resources
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.resources`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.resources
              payload:
                $ref: "#/components/schemas/VesselResources"
    streamData.vessel.structure:
      name: stream-data
      title: A frame on vessel.structure
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.structure`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.structure
              payload:
                $ref: "#/components/schemas/VesselStructure"
    streamData.vessel.surface:
      name: stream-data
      title: A frame on vessel.surface
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.surface`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.surface
              payload:
                $ref: "#/components/schemas/VesselSurface"
    streamData.vessel.target:
      name: stream-data
      title: A frame on vessel.target
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.target`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.target
              payload:
                $ref: "#/components/schemas/VesselTarget"
    streamData.vessel.thermal:
      name: stream-data
      title: A frame on vessel.thermal
      contentType: application/json
      payload:
        description: The `stream-data` envelope carrying `vessel.thermal`.
        allOf:
          - $ref: "#/components/schemas/StreamData"
          - properties:
              topic:
                const: vessel.thermal
              payload:
                $ref: "#/components/schemas/VesselThermal"
    subscribe:
      name: subscribe
      title: Open delivery on a topic
      payload:
        $ref: "#/components/schemas/Subscribe"
      examples:
        - name: open-vessel-flight
          summary: The first frame a client sends. Until it does the socket stays silent, and the mod answers this one with an `event` named `subscribed` on the `vessel.flight` channel.
          payload:
            type: subscribe
            topic: vessel.flight
    unsubscribe:
      name: unsubscribe
      title: Close delivery on a topic
      payload:
        $ref: "#/components/schemas/Unsubscribe"
  schemas:
    ActionBinding:
      type: object
      description: |-
        One action-group binding in `VesselPart.actionBindings`: a single part
        action and the named action groups it fires with. `ActionBinding.groups` are
        the `KSPActionGroup` enum member names (`SAS`/`RCS`/
        `Brakes`/`Gear`/`Light`/`Abort`/`Stage`/ `Custom01`...) the action's Flags
        bitmask decodes to (`None` excluded).
      required:
        - action
        - groups
        - groupsMask
      properties:
        action:
          type: string
          x-sitrep-unit: text
          description: |-
            The action's PAW label: `BaseAction.guiName` (e.g. "Toggle", "Extend
            Panel").
        groups:
          type: array
          items:
            type: string
            x-sitrep-unit: text
          description: |-
            Named KSPActionGroup groups this action is bound to (e.g.
            `["SAS","Custom01"]`). Never empty, an action bound to no group isn't
            emitted. Display labels; see `ActionBinding.groupsMask`.
        groupsMask:
          type: number
          x-sitrep-unit: enum
          description: |-
            `BaseAction.actionGroup`'s raw `[Flags]` BITMASK, the whole of it.
            `KspActionGroup` names the bits.

            A mask rather than an ordinal because `KSPActionGroup` is a flags enum: one
            action can fire with several groups, which is exactly what
            `ActionBinding.groups` already carries as names. The mask is here because
            the NAME list cannot be trusted to be complete - it is built by intersecting
            the mask against the groups the capture knows about, so a group KSP adds is
            dropped before the wire and the client cannot tell that from a group nothing
            is bound to. The mask has no such ceiling.
    ActionGroupState:
      type: object
      description: |-
        One custom action group's IDENTITY plus its live state. Deliberately NOT a
        positional `bool[]` indexed `[ag1..ag10]`: such an array can carry state but
        never a NAME, and a name is the whole point. Stock KSP's ten customs are
        anonymous, but Action Groups Extended (AGX) gives the player up to 250
        groups they name themselves ("Solar Panels", "Science Bay"). A positional
        array cannot express that, and forces the client to hardcode "AG1".."AG10"
        labels.

        Scope: this list carries the CUSTOM (extensible) groups only. The stock
        singletons (SAS/RCS/Gear/Brakes/Lights/Abort) keep their own dedicated
        `VesselControl` fields and their own dedicated commands
        (`vessel.control.setGear` etc.), because they are fixed stock concepts that
        no mod extends: AGX adds custom groups, it does not add a second SAS.
        Folding them into this list would trade a typed field for a string match and
        gain nothing.
      required:
        - index
        - name
      properties:
        index:
          type: number
          x-sitrep-unit: id
          description: |-
            1-based group number: the same number `vessel.control.setActionGroup` takes.
            Stock KSP: 1..10 (`KSPActionGroup.Custom01..Custom10`). An AGX backend may
            report indices up to 250. Consumers must NOT assume 10, nor assume the list
            is dense or sorted.
        name:
          type: string
          x-sitrep-unit: text
          description: |-
            Human display name. Stock KSP has no per-group naming, so the stock backend
            reports `"AG1".."AG10"`: exactly what the UI already showed, now sourced
            from the mod rather than hardcoded client-side. An AGX backend reports the
            player's own names instead.
        state:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the group is currently engaged. `null` means the backend knows this
            group exists (it has an index and a name) but could not read whether it is
            engaged: NOT that the group is disengaged. A client that collapses the two
            draws an OFF toggle for a group whose state nobody knows, and inverting that
            reading commands the wrong way.
    ActivateStrategyArgs:
      type: object
      description: |-
        `career.strategy.activate`'s args: the strategy's stable id plus the slider
        fraction to activate it at. `ActivateStrategyArgs.strategyId` is
        `StrategyConfig.Name` (e.g. `"OutsourceRnDStrategy"`): the exact same id the
        READ side emits for each strategy (`career.status`'s `strategies[].id`), so
        a client activates using the id it already read.
        `ActivateStrategyArgs.factor` is the 0..1 slider fraction the strategy is
        committed at (its up-front funds/science/reputation cost scales with it); it
        is best-effort: a strategy with no factor slider ignores it and activates at
        its fixed factor.
      required:
        - strategyId
        - factor
      properties:
        strategyId:
          type: string
          x-sitrep-unit: id
        factor:
          type: number
          x-sitrep-unit: ratio
          description: 0..1 slider fraction; ignored by strategies without a factor slider.
    AddManeuverNodeArgs:
      type: object
      description: |-
        `vessel.maneuver.add`'s args: NAMED delta-v components in the node's own
        radial/normal/prograde frame, exactly like the wire's `ManeuverNode` shape.
        Kills O-4: there is no positional `[ut,x,y,z]` array to mis-order (raw KSP
        `ManeuverNode.DeltaV` is `x=radialOut, y=normal, z=prograde`) for why the
        actuator seam must preserve this exact component assignment rather than
        "helpfully" reordering it.
      required:
        - ut
        - prograde
        - normal
        - radialOut
      properties:
        ut:
          type: number
          x-sitrep-unit: ut
        prograde:
          type: number
          x-sitrep-unit: m/s
        normal:
          type: number
          x-sitrep-unit: m/s
        radialOut:
          type: number
          x-sitrep-unit: m/s
    AlternatorEntry:
      type: object
      description: |-
        One engine alternator in the `parts.power` payload's `alternators` array.
        Typing-only mirror of `Sitrep.Host.PartsViewProvider.BuildAlternatorEntry`.
      properties:
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        outputRate:
          type:
            - number
            - "null"
          x-sitrep-unit: units/s
    ArchiveEntry:
      type: object
      description: |-
        One entry in the `science.archive` channel payload: a single SUBJECT out of
        the whole-career R&D archive (`ResearchAndDevelopment.GetSubjects()`): every
        subject the player has ever collected or recovered, across every mission and
        every body, not scoped to the active vessel. The channel payload is a BARE
        ARRAY (`ArchiveEntry[]`) or `null` when the save has no R&D instance to walk
        (Sandbox mode), never an empty-vs-absent distinction beyond that (a Career
        save with an R&D instance but nothing collected yet emits an empty array).

        Distinct from `ExperimentBreakdownEntry`: that one rolls up the ACTIVE
        VESSEL's currently-stored `ScienceData` blobs and goes null with no vessel
        flying; this one is career-wide ground truth and streams at the Space Center
        with nothing flying at all.

        `ArchiveEntry.remainingPotential` is `ScienceCap - Science`, computed by
        `Gonogo.KSP.KspHost.BuildScienceArchive` the same way
        `ExperimentBreakdownEntry.remainingPotential` is; this layer only passes the
        already-computed figure through, matching that entry's own mapping idiom
        rather than re-deriving it here.

        **Typing-only mirror** of
        `Sitrep.Host.ScienceViewProvider.BuildArchiveEntry`: see `ExperimentEntry`
        for the "no wire change, all fields nullable" rationale.
      properties:
        subjectId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        experimentId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        experimentTitle:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        body:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        situation:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        biome:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        science:
          type:
            - number
            - "null"
          x-sitrep-unit: science
          description: Science banked for this subject so far.
        scienceCap:
          type:
            - number
            - "null"
          x-sitrep-unit: science
          description: Max science this subject can ever yield.
        remainingPotential:
          type:
            - number
            - "null"
          x-sitrep-unit: science
          description: |-
            Absolute science still recoverable from this subject (`scienceCap -
            science`).
        subjectValue:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Region multiplier KSP applies to this subject's yield.
    AstronautComplexInfo:
      type: object
      description: |-
        The `spaceCenter.astronautComplex` channel payload: the Astronaut Complex
        hire tab, the rolling pool of applicants the operator can recruit, plus the
        roster-cap context a hire is gated on. Produced by
        `Sitrep.Host.SpaceCenterViewProvider.BuildAstronautComplex`.

        A wrapper object (not a bare array) because the applicant list rides
        alongside the facility-level cap and the current active-crew count, both of
        which the hire affordance needs: the current roster comes from the separate
        `spaceCenter.crewRoster` channel, this one carries the hire side. The whole
        payload is `null` in the SANDBOX / no-career / no-game case (no applicant
        pool exists), the provider's "no data" signal, distinct from a career save
        whose pool is genuinely empty (a non-null payload with an empty
        `AstronautComplexInfo.applicants` list).

        A TS-shape-only typing/codegen marker: the provider hand-builds the dict and
        `JsonWriter` walks that live tree, this POCO never serializes. Classified
        `DelayRole.TrueNow`: the Astronaut Complex is at KSC, known independent of
        any vessel's comms link, same class as `CrewRosterEntry`.
      required:
        - applicants
      properties:
        applicants:
          type: array
          items:
            $ref: "#/components/schemas/CrewRosterEntry"
          description: |-
            The hireable applicant pool (`KerbalRoster.Applicants`), sharing
            `CrewRosterEntry`'s full shape (with `CrewRosterEntry.situation` always
            `"Applicant"`) so the Astronaut Complex's applicant and active-crew rows
            render off one type. Always present (empty, never null) once the payload
            itself is non-null.
        activeCrew:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Current active (Crew-type) count (`KerbalRoster.GetActiveCrewCount`): the
            number counted against `AstronautComplexInfo.crewCapacity`. Null when the
            roster isn't queryable.
        crewCapacity:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Active-crew cap set by the Astronaut Complex facility tier
            (`GameVariables.GetActiveCrewLimit` over the facility's NORMALISED level). A
            hire is blocked once `AstronautComplexInfo.activeCrew` reaches it.
            `int.MaxValue` at the top facility tier (unlimited); preserved as-is on the
            wire, never clamped, so the client can render "unlimited". Null when the
            facility isn't queryable.
        nextHireCost:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
          description: |-
            Funds cost to hire the next applicant (`GameVariables.GetRecruitHireCost`):
            one figure for the whole pool, the same for every applicant this tick and
            rising with the current roster size.
    AtmosphereEntry:
      type: object
      description: |-
        A body's atmosphere, present on a `BodyEntry` only when the body actually
        has one (null otherwise; never an all-null placeholder, matching the
        payload's null-not-sentinel discipline). Mirrors the exact nested dict
        `SystemViewProvider.BuildAtmosphere` emits.
      properties:
        depth:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Atmosphere height, metres (`CelestialBody.atmosphereDepth`); null when
            absent.
        hasOxygen:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the atmosphere is breathable / oxygenated
            (`CelestialBody.atmosphereContainsOxygen`); null when absent.
        seaLevelPressure:
          type:
            - number
            - "null"
          x-sitrep-unit: kPa
          description: |-
            Sea-level pressure, kPa (`CelestialBody.atmospherePressureSeaLevel`); null
            when absent.
        pressureAltitudes:
          type:
            - array
            - "null"
          items:
            type: number
            x-sitrep-unit: m
          description: |-
            Altitudes of the `AtmosphereEntry.pressures` samples, metres above sea
            level, ascending from 0; null when the stream does not report a profile.
            Same length as `AtmosphereEntry.pressures`.

            Spacing is chosen per body rather than fixed, because the shape varies
            enormously: RSS Earth's table runs to 94 km and Saturn's to 1,270 km, and a
            grid uniform in altitude spends most of its points on near-vacuum for the
            second while undersampling the first. The producer bisects until every
            segment's interior sits within 1% of the log-linear chord through its ends,
            which is the space a reader sees (the profile is drawn on a log pressure
            axis), and stops at 48 points. Measured against the ten real pressure curves
            the RSS install ships, the worst reconstruction error is 1.51%, and 1.12% on
            every body but Pluto, whose near-vacuum air runs the point cap out.

            It costs 16 to 48 points per atmospheric body, which on a real RO install
            (33 bodies, 11 with air) is 5.2 kB added to a 23.3 kB `system.bodies` emit.
            That channel re-sends itself every second, so this is a fifth again on the
            largest thing on the wire, for a table that is fixed for the session. It is
            carried here anyway because it is a physical fact about a body and belongs
            with the rest of them; if the channel is ever given a change-gate that can
            see a payload has not moved, this is the field that gains most from it.

            The table ends six decades below sea level, not at `AtmosphereEntry.depth`.
            Above that the game's own curve is a cubic plunging into a hard zero at the
            ceiling, which no interpolation in log space can follow and which carries no
            pressure worth stating. A consumer draws to the last sample and takes
            `AtmosphereEntry.depth` as where the air formally ends.
        pressures:
          type:
            - array
            - "null"
          items:
            type: number
            x-sitrep-unit: kPa
          description: |-
            Pressure at each `AtmosphereEntry.pressureAltitudes` entry, kPa, as the
            game's own `CelestialBody.GetPressure` answers it; null when the stream does
            not report a profile.

            Sampled rather than modelled because the exponential `P0·exp(-h/H)` a client
            can build from sea-level pressure and a scale height is not what KSP
            evaluates, and there is no scale-height field on `CelestialBody` to build it
            from honestly. A body with `atmosphereUsePressureCurve` set follows a
            tabulated curve, which is what stock's own atmospheres and every
            RealAtmospheres-style pack use; against the real RSS Earth curve the
            exponential is out by a factor of sixteen at altitude. Sampling the game's
            answer is correct for stock, for a planet pack and for a curve nobody has
            written yet, without the client modelling anything.

            Rounded to six significant figures. The curve path evaluates in float32
            inside Unity's own `AnimationCurve`, so more digits would be inventing
            precision, and six is far below the 1% spacing tolerance.
    BatteryEntry:
      type: object
      description: |-
        One battery in the `parts.power` payload's `batteries` array. Typing-only
        mirror of `Sitrep.Host.PartsViewProvider.BuildBatteryEntry`.
      properties:
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        current:
          type:
            - number
            - "null"
          x-sitrep-unit: units
        max:
          type:
            - number
            - "null"
          x-sitrep-unit: units
    BodyEntry:
      type: object
      description: |-
        One celestial body in the `SystemBodies` tree. Mirrors the exact per-body
        dict `SystemViewProvider.BuildBody` emits: same field names, casing and
        nullability. Shaped to make the classic orbit warts unspellable: an explicit
        parent-index tree rather than flat indexed keys, no numeric sentinels for
        missing data, and no `eccentricAnomaly` field at all, because an orbit-patch
        formatter that carries one tends to fill it with the body's ECCENTRICITY
        instead.
      required:
        - index
      properties:
        name:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: Body name (e.g. "Kerbin"); null when the live game hasn't populated it.
        index:
          type: number
          x-sitrep-unit: id
          description: |-
            This body's position in the list: stable per session. Always present (the
            provider falls back to the list index when the raw field is missing), never
            null.
        parentIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index of the body this one orbits; null ONLY for the root star (no parent),
            never a sentinel like -1.
        radius:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Mean radius, metres; null when the live game doesn't have it yet (never 0/-1
            as a stand-in).
        orbit:
          description: |-
            Orbital elements; null ONLY for the root star (orbit is meaningless without
            a parent), the "sun has a bogus orbit" wart suppressed at the source.
          anyOf:
            - $ref: "#/components/schemas/OrbitEntry"
            - type: "null"
        gravParameter:
          type:
            - number
            - "null"
          x-sitrep-unit: m³/s²
          description: |-
            Standard gravitational parameter μ = G·M, m³/s² (KSP
            `CelestialBody.gravParameter`). Null when the live game hasn't populated it.
        mass:
          type:
            - number
            - "null"
          x-sitrep-unit: kg
          description: Body mass, kilograms (`CelestialBody.Mass`).
        surfaceGravity:
          type:
            - number
            - "null"
          x-sitrep-unit: g
          description: |-
            Surface gravity in multiples of g₀ (`CelestialBody.GeeASL`), verbatim. This
            is the CONFIG PRIMITIVE, not a derived quantity: KSP computes mass and
            gravParameter FROM it (`Mass = Radius² · (GeeASL ·
            PhysicsGlobals.GravitationalAcceleration) / G`), so a client reconstructing
            it as μ/r²/g₀ is running the game's own arithmetic backwards and can only
            lose precision doing it.
        hillSphere:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Hill-sphere radius, metres (`CelestialBody.hillSphere`). Null for the root
            star, where KSP's own value is `double.PositiveInfinity` and there is no
            parent to be bound by. On the wire because the textbook expression and KSP's
            disagree, and we shipped the textbook one. KSP computes
            `a·(1−e)·(m/M)^(1/3)`; the standard form carries a factor of three under the
            root, `a·(1−e)·∛(m/3M)`. Ours had the three, so every hill sphere the app
            has ever drawn was ∛(1/3) ≈ 0.693 of the game's, about 31% too small, in two
            widgets that render it as a fact.
        sphereOfInfluence:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Sphere-of-influence radius, metres (`CelestialBody.sphereOfInfluence`); null
            when absent.
        rotationPeriod:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: |-
            Sidereal rotation period, seconds (`CelestialBody.rotationPeriod`); a
            NEGATIVE value denotes retrograde rotation. Null when absent. Carries "does
            this body rotate" on its own (a body rotates iff this is finite and
            non-zero), so no separate bool is emitted for it.
        tidallyLocked:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the body is tidally locked to its parent
            (`CelestialBody.tidallyLocked`); null when absent.
        atmosphere:
          description: |-
            Atmosphere descriptor; null when the body has no atmosphere
            (`!CelestialBody.atmosphere`), the "airless vs. no-data" distinction the
            whole payload's null-not-sentinel rule preserves.
          anyOf:
            - $ref: "#/components/schemas/AtmosphereEntry"
            - type: "null"
        hasOcean:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the body has a liquid ocean (`CelestialBody.ocean`); null when
            absent.
        description:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            KSP's per-body flavour text (`CelestialBody.bodyDescription`); null when
            absent. May be a raw `#autoLOC...` localization tag the client suppresses.
        isHome:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether KSC and the launch sites sit on this body
            (`CelestialBody.isHomeWorld`); true on exactly one body, null when absent.
            The authoritative home-body marker: a client locates home by this flag,
            never by index.
    CareerContract:
      type: object
      description: One contract in `CareerContracts.active` / `CareerContracts.offered`.
      required:
        - parameters
      properties:
        id:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        agent:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        state:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        fundsAdvance:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
        fundsCompletion:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
        fundsFailure:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
        scienceCompletion:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        reputationCompletion:
          type:
            - number
            - "null"
          x-sitrep-unit: rep
        reputationFailure:
          type:
            - number
            - "null"
          x-sitrep-unit: rep
        dateAccepted:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
        dateDeadline:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
        dateExpire:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
        parameters:
          type: array
          items:
            $ref: "#/components/schemas/CareerContractParameter"
    CareerContractParameter:
      type: object
      description: One parameter (objective) of a `CareerContract`.
      properties:
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        state:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            `Contracts.ParameterState`'s enum NAME (`Incomplete`/`Complete`/`Failed`): a
            display label. `CareerContractParameter.stateOrdinal` is the field to branch
            on.
        stateOrdinal:
          description: |-
            `CareerContractParameter.state`'s KSP ORDINAL, typed to `KspParameterState`.

            Whether an objective reads as DONE was decided by comparing
            `CareerContractParameter.state` against `"Complete"`, and an unrecognised
            spelling collapsed onto `Incomplete`. That is the pessimistic arm: a
            completed objective showing as outstanding, and a contract-parameter ALARM
            set on "Complete" that simply never fires. An alarm that never fires is the
            failure mode this whole exercise is about.

            `null` when the capture carried no state, which is a third answer and must
            not be read as either arm.
          anyOf:
            - $ref: "#/components/schemas/KspParameterState"
              x-sitrep-unit: enum
            - type: "null"
    CareerContracts:
      type: object
      description: |-
        Contracts sub-group of `CareerStatus`. All three lists are always present
        (empty, never null).
      required:
        - active
        - offered
        - completedRecent
      properties:
        active:
          type: array
          items:
            $ref: "#/components/schemas/CareerContract"
        offered:
          type: array
          items:
            $ref: "#/components/schemas/CareerContract"
        completedRecent:
          type: array
          items:
            $ref: "#/components/schemas/CareerContract"
          description: |-
            BOUNDED recently-completed list: the last N (currently 10) `State.Completed`
            contracts from `ContractSystem.Instance.ContractsFinished`, sorted
            newest-first by `Contract.DateFinished` (see
            `Gonogo.KSP.KspHost.BuildCareerContracts`). Same `CareerContract` element
            shape as `CareerContracts.active` / `CareerContracts.offered`: no extra
            fields; `State` is always `"Completed"` here. Rides `career.status`
            (TrueNow).
    CareerEconomy:
      type: object
      description: |-
        Economy sub-group of `CareerStatus`: funds/reputation/science, each null
        when absent.
      properties:
        funds:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
        reputation:
          type:
            - number
            - "null"
          x-sitrep-unit: rep
          description: |-
            The stock reputation field, unchanged. Under a career overhaul it is the
            most consequential number in the save (it IS the income) and the value was
            never wrong: what was missing is the context below, which is why that
            arrived as an elected interpretation rather than as a replacement here.
        science:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        economyModel:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Which money model answered the four fields below, e.g. `"stock"`. Provenance
            only: a client reads the interpretation, never branches on who produced it.
        reputationDecayPerDay:
          type:
            - number
            - "null"
          x-sitrep-unit: rep/day
          description: |-
            Reputation lost per day at the current reputation. Zero on stock, which
            genuinely has no decay, and that zero is a statement rather than a
            placeholder.
        subsidyPerDay:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: Funding the current reputation earns, per day. Zero on stock.
        subsidyMinPerDay:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: "The subsidy at zero reputation: the floor nothing takes away."
        subsidyMaxPerDay:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: |-
            The subsidy reputation cannot beat. With the minimum it says how much of the
            range the current reputation has bought, which is what turns a bare
            reputation number into something an operator can act on.
        upkeepPerDay:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: |-
            Total ongoing cost per day. This is why `CareerEconomy.funds` is the right
            balance and the wrong affordability test under an overhaul: a balance that
            covers a purchase today may not cover it plus next month's salaries.
        upkeep:
          description: |-
            Where the upkeep goes: the parts `CareerEconomy.upkeepPerDay` is made of,
            and they sum to it. ABSENT on stock, which has no per-source model at all:
            seven zeros would claim stock levies seven kinds of nothing, where the truth
            is that it levies none of them.

            Also absent when the model can state its costs but cannot price them, in
            which case `CareerEconomy.upkeepBeforeModifiers` stands alone. A set that
            did not add up to the total beside it would be worse than no set: a reader
            has no way to tell which of the two lied.
          anyOf:
            - $ref: "#/components/schemas/CareerUpkeep"
            - type: "null"
        upkeepBeforeModifiers:
          description: |-
            The same sources, priced BEFORE whatever the model does to them at
            transaction time: leaders, strategies, standing discounts. ABSENT when the
            model applies nothing, so the difference between this and
            `CareerEconomy.upkeep` is what the career's current arrangements are worth.
          anyOf:
            - $ref: "#/components/schemas/CareerUpkeep"
            - type: "null"
        unlockCredit:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
          description: |-
            A prepaid allowance the elected money model spends BEFORE
            `CareerEconomy.funds` on the purchases it covers. In funds, because that is
            what it discounts. ABSENT on stock, which has no such pool.

            The second reason `CareerEconomy.funds` alone is not an affordability test:
            where this exists, part of a price is already paid. It is a BALANCE and not
            a per-purchase answer, so a surface that offers such a purchase shows this
            and the funds balance together rather than deriving the split itself.
    CareerFacilities:
      type: object
      description: |-
        The `career.facilities` channel payload: the space centre's buildings, each
        with the tier it stands at and the ladder it stands on.

        **It arrives only while the game can answer, and stops otherwise.** A
        facility's tier count is readable from the live building objects, which KSP
        instantiates at the space centre, in the editor and in flight near the KSC,
        and nowhere else. From the tracking station or from orbit there is no
        reading to take, so this channel goes SILENT rather than reporting a row of
        nulls. The last reading stands, dated, through the client's ordinary
        staleness machinery: a whole channel can be held and said to be held, where
        a nullable field on a channel that keeps ticking cannot.

        A tier count does not change during a save, so a ladder held from an earlier
        scene is still true. The tier standing on it can move, and does when the
        player buys an upgrade, so both travel together and are dated together
        rather than one being trusted further than the other.
      properties:
        facilities:
          type:
            - object
            - "null"
          additionalProperties:
            $ref: "#/components/schemas/CareerFacility"
          description: |-
            DYNAMIC-KEY MAP keyed by `SpaceCenterFacility` name (e.g. `"LaunchPad"`,
            `"VehicleAssemblyBuilding"`): not a fixed record, so enumerate the keys
            rather than reaching for one you expect to be there. Never empty on the
            wire: the channel is absent instead.
    CareerFacility:
      type: object
      description: |-
        One facility entry in `CareerFacilities.facilities`. All three fields share
        one live-facility gate on the KSP side, so they are null together when the
        facility isn't queryable in the current scene.
      properties:
        facilityOrdinal:
          description: |-
            Which facility this entry is, as KSP's `SpaceCenterFacility` ORDINAL, typed
            to `KspSpaceCenterFacility`.

            `career.status.facilities` is keyed by the enum NAME and stays that way:
            rekeying the map to a number would be a breaking retype and would change the
            shape of every consumer's key walk. So the identity rides INSIDE the entry
            instead, and a client no longer has to recognise the key it arrived under.
            It used to have to: the key was matched against a hand-written nine-entry
            name table, and a facility whose name missed was skipped outright, so it
            simply vanished from the display.

            `null` from a producer that predates this field.
          anyOf:
            - $ref: "#/components/schemas/KspSpaceCenterFacility"
              x-sitrep-unit: enum
            - type: "null"
        currentTier:
          type:
            - number
            - "null"
          x-sitrep-unit: count
        maxTier:
          type:
            - number
            - "null"
          x-sitrep-unit: count
        upgradeCost:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
    CareerMode:
      type: object
      description: |-
        The `career.mode` channel payload: a single `GameMode`, the active save's
        mode. Produced by `Sitrep.Host.CareerViewProvider.BuildCareerMode`, which
        reads the raw `Game.Modes.ToString()` string `Gonogo.KSP.KspHost` captures
        each tick. The whole payload is `null` only when no game is loaded at all
        (main menu / no save): a "no data yet" absence, never a fabricated mode;
        once a save is loaded the mode is always one of the four `GameMode` members.

        **Typing-only mirror.** This type reproduces the EXACT serialized shape
        `CareerViewProvider.BuildCareerMode` emits (`{ "mode": <int> }`, the enum's
        integer ordinal, matching every other enum in this codec; see
        `Sitrep.Core.Serialization.JsonWriter`). It is a codegen marker, not
        serialized itself.
      required:
        - mode
      properties:
        mode:
          $ref: "#/components/schemas/GameMode"
          x-sitrep-unit: enum
    CareerStatus:
      type: object
      description: |-
        The `career.status` channel payload: the KSC and career-mode snapshot, in
        four groups (economy, contracts, strategies, tech). The space centre's
        buildings are NOT here: they ride `CareerFacilities`, for the staleness
        reason that type's own doc gives.

        **Three states, and they mean different things.** The whole payload is
        `null` in SANDBOX, where there is no career at all. A non-null payload with
        a sub-group `null` means career mode is running and that group is genuinely
        unavailable this tick. All four top-level keys are ALWAYS present, each
        nullable, never omitted, so a missing key is a protocol error rather than an
        absent group.

        Every number is nullable and `null` is never a sentinel: a field is `null`
        whenever the raw value is absent or non-finite. The two counts
        (`CareerStrategies.activeCount`, `CareerTech.unlockedCount`) are the only
        non-nullable numbers, because an empty list counts as zero rather than as
        unknown.
      properties:
        economy:
          anyOf:
            - $ref: "#/components/schemas/CareerEconomy"
            - type: "null"
        contracts:
          anyOf:
            - $ref: "#/components/schemas/CareerContracts"
            - type: "null"
        strategies:
          anyOf:
            - $ref: "#/components/schemas/CareerStrategies"
            - type: "null"
        tech:
          anyOf:
            - $ref: "#/components/schemas/CareerTech"
            - type: "null"
    CareerStrategies:
      type: object
      description: |-
        Strategies sub-group of `CareerStatus`. `CareerStrategies.activeCount` is
        NON-nullable, the provider defaults it to `Active.Count` when the raw value
        is absent.
      required:
        - active
        - all
        - activeCount
      properties:
        active:
          type: array
          items:
            $ref: "#/components/schemas/CareerStrategy"
        all:
          type: array
          items:
            $ref: "#/components/schemas/CareerStrategy"
        activeCount:
          type: number
          x-sitrep-unit: count
    CareerStrategy:
      type: object
      description: One strategy in `CareerStrategies.active` / `CareerStrategies.all`.
      properties:
        id:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        description:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        department:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        isActive:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        factor:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
        dateActivated:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
        requiredReputation:
          type:
            - number
            - "null"
          x-sitrep-unit: rep
        initialCostFunds:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
        initialCostScience:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        initialCostReputation:
          type:
            - number
            - "null"
          x-sitrep-unit: rep
        hasFactorSlider:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        factorSliderDefault:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
        factorSliderSteps:
          type:
            - number
            - "null"
          x-sitrep-unit: count
        canActivate:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        activateBlockedReason:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        canDeactivate:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        deactivateBlockedReason:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        effect:
          type:
            - string
            - "null"
          x-sitrep-unit: text
    CareerTech:
      type: object
      description: |-
        Tech sub-group of `CareerStatus`. `CareerTech.unlockedCount` is
        NON-nullable, the provider defaults it to `UnlockedIds.Count` when the raw
        value is absent.
      required:
        - unlockedCount
        - unlockedIds
        - nodes
      properties:
        unlockedCount:
          type: number
          x-sitrep-unit: count
        unlockedIds:
          type: array
          items:
            type: string
            x-sitrep-unit: id
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/CareerTechNode"
    CareerTechNode:
      type: object
      description: One node in `CareerTech.nodes`.
      required:
        - parents
      properties:
        id:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        description:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The node's flavour line, as the tech tree itself writes it ("How hard can
            Rocket Science be anyway?").

            It comes from the tree's own config rather than from `RDTech`, whose
            `description` field only exists while the R&D Building scene is open. A tech
            tree a mod has replaced (RP-1) is read the same way, so this is the node's
            description in whatever tree the save is playing.
        scienceCost:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        unlocked:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        parents:
          type: array
          items:
            type: string
            x-sitrep-unit: id
    CareerUpkeep:
      type: object
      description: |-
        Ongoing cost by source, per day, from the elected economy model. Every
        member is absent when that model does not have the concept, never zero: an
        unmodelled source and a source costing nothing are different facts.
      properties:
        facilities:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: "Buildings: the standing cost of having a space centre at all."
        launchComplexes:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: |-
            Launch complexes and their pads, which cost whether or not anything is
            building.
        researchSalary:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: Researcher salaries, which an idle research queue does not stop.
        training:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: Crew training in progress.
        crewBase:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: "Standing crew costs: everyone on the roster, flying or not."
        crewInFlight:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: The extra a crew in flight costs over a crew on the ground.
        integrationSalary:
          type:
            - number
            - "null"
          x-sitrep-unit: f/day
          description: Engineer salaries on the integration teams.
    CentreSeparationEntry:
      type: object
      description: |-
        One ordered pair in the `commandCentre.separation` channel: how far
        `CentreSeparationEntry.from` is from `CentreSeparationEntry.to`, in one-way
        seconds along the routed CommNet path.

        Both ends are command-centre ids from `commandCentre.roster`, so a pair is a
        ground station against another ground station, a crewed craft against a
        ground station, or two crewed craft: a crewed control-source vessel IS a
        centre, so no separate vocabulary is needed for it.
      required:
        - from
        - to
        - oneWaySeconds
      properties:
        from:
          type: string
          x-sitrep-unit: id
          description: The centre the separation is measured FROM, as a roster `Id`.
        to:
          type: string
          x-sitrep-unit: id
          description: The centre the separation is measured TO, as a roster `Id`.
        oneWaySeconds:
          type: number
          x-sitrep-unit: s
          description: One-way signal time along the routed path between the two.
    ClosestApproach:
      type: object
      description: |-
        Next closest approach between the active vessel and its current target,
        computed MOD-side by the elected IPropagationProvider (stock two-body Kepler
        by default, an n-body provider when elected over it). Replaces the SDK's
        former client-side `o.closestTgtApprUT` two-body solve: the authority moves
        into the mod so an n-body physics mod can supply the true encounter instead
        of a Kepler approximation that is simply wrong under n-body.

        It comes from the propagation provider rather than a solver of its own so
        that the encounter and the trajectory it is an encounter ON are always the
        same physics. Null on `VesselTarget` when there is no target, no frame the
        provider can reach both objects in, or no encounter inside the window it was
        asked about.
      required:
        - time
        - distance
      properties:
        time:
          type: number
          x-sitrep-unit: ut
          description: |-
            Universal Time (seconds) of the minimum separation at or after the sample's
            UT.
        distance:
          type: number
          x-sitrep-unit: m
          description: Separation (metres) at `ClosestApproach.time`.
    CommandCentreEntry:
      type: object
      description: |-
        One command centre in the `commandCentre.roster` channel: a vantage/
        authority the operator can command from and observe at (Plan 3). The union
        of the stock CommNet home nodes (KSC, Extra Ground Stations, Kerbal
        Konstructs sites) and crewed control-source vessels. Produced by the mod's
        command-centre enumeration pass.

        The channel is a BARE ARRAY of these entries (tagged `isArray: true`, like
        `SpaceCenterPoiEntry`), one per active centre keyed by
        `CommandCentreEntry.id`.
      required:
        - active
      properties:
        id:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Stable authority/vantage key: `"ksc"` | `"ground:<name>"` | `"kk:<site>"` |
            `"vessel:<guid>"`.
        displayName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: Human-facing name.
        kind:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            One of `GroundStation` / `CrewedVessel` / `Colony` / `Custom` (the
            `CommandCentreKind` name).
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index into `SystemBodies` of the body this centre sits on; null when unknown
            or not surface-anchored.
        latitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Body-fixed surface latitude of the centre in degrees, when surface-anchored;
            null for a moving vessel centre.

            **Null is "not applicable", not "not computed".** A `GroundStation` always
            reports coordinates. A `CrewedVessel` reports them only while landed,
            splashed or pre-launch: off the ground the only thing derivable is a
            sub-vessel ground point that sweeps at orbital rate, which is not a place
            the centre occupies. So null says the centre is airborne or in space, and a
            client may act on that rather than treating it as missing data. The one case
            where an anchored centre reports null is a body that could not be read at
            all, and then `CommandCentreEntry.bodyIndex` is null too: the two travel
            together, so a null coordinate never appears beside a known body.

            Always null or non-null together with `CommandCentreEntry.longitude`.
        longitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Body-fixed surface longitude of the centre in degrees, wrapped to (-180,
            180], matching every other geographic value on the wire. Null under exactly
            the rule `CommandCentreEntry.latitude` documents, and always null together
            with it.
        active:
          type: boolean
          x-sitrep-unit: flag
          description: Whether this centre is a valid command source right now.
        delayQuality:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Whether this centre can be routed to: `"routed"` (a CommNode ControlPath
            exists, occlusion-aware) or `"unroutable"` (no CommNode, so no command path
            and no delay). There is deliberately no position-only approximation:
            commands ride the relay network, and a pair with no route has no delay to
            quote.
    CommandCentreSeparation:
      type: object
      description: |-
        How far every active command centre is from every other, one-way, along the
        routed CommNet path. The number a human at one vantage needs to know how
        long their words take to reach a human at another.

        **Sparse, and that is the contract.** A pair with no route has NO entry
        rather than a zero or a sentinel: commands and messages ride the relay
        network, so an unroutable pair has no separation to quote, and inventing one
        would make an unreachable correspondent look merely distant. A reader that
        finds no entry for a pair knows the separation is unavailable, which is a
        different fact from it being large.

        Each centre against ITSELF is always present as an explicit zero: a node is
        exactly no distance from itself, and without the row a reader would fall
        through to "unavailable" for the one pair it is most certain about.

        DELAYED, on the same reasoning as `comms.delay`: a separation is measured
        between two places a signal has to cross, so the figure a reader holds is
        the one their own light has brought them. It is the separation as OBSERVED,
        never as it is at this instant, and a pair whose geometry has changed since
        says so one light-time later.
      required:
        - pairs
      properties:
        pairs:
          type: array
          items:
            $ref: "#/components/schemas/CentreSeparationEntry"
          description: Every ordered pair with a routed path, plus each centre's own zero.
    CommandErrorCode:
      type: integer
      description: |-
        The typed, machine-readable failure code every command result carries, R7
        Fix 1's replacement for the bare `string` error codes
        (`"E_RANGE"`/`"E_NOT_FOUND"`/`"E_MODE_UNAVAILABLE"`/ `"E_NO_VESSEL"`) the
        three hand-rolled result records used to return. A string code forces the
        client to string-match a magic value that the compiler can neither check nor
        enumerate. This enum makes the failure surface a closed, typed set instead.

        `CommandErrorCode.None` is the success sentinel (paired with
        `CommandResult.success` = true); `CommandErrorCode.Unknown` is the
        forward-compatible fallback for any code a newer producer emits that an
        older consumer doesn't recognise: the same `Unknown`-style read-fallback
        convention every other enum in this contract uses.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: None
          description: "No error: the success sentinel, paired with `CommandResult.success` = true."
        - const: 1
          title: Unknown
          description: |-
            Forward-compat fallback: a code a newer producer emitted that this consumer
            doesn't recognise.
        - const: 2
          title: NoVessel
          description: No active vessel to act on (was `"E_NO_VESSEL"`).
        - const: 3
          title: ModeUnavailable
          description: |-
            The requested mode/state isn't currently available (was
            `"E_MODE_UNAVAILABLE"`).
        - const: 4
          title: Range
          description: An argument was out of its valid range (was `"E_RANGE"`).
        - const: 5
          title: NotFound
          description: |-
            The referenced entity (node id, vessel/body target) didn't resolve (was
            `"E_NOT_FOUND"`).
        - const: 6
          title: Timeout
          description: |-
            F2-fix backstop: the command was marshaled onto the host's main-thread pump
            but that pump did not drain it within the bounded wait (a scene-load /
            loading-screen stall). A synthetic failure returned by the host so the
            Courier thread can never park indefinitely, not emitted by any uplink
            handler. Additive (Major 2, Minor 0 -> 1).
        - const: 7
          title: PlanNotOwned
          description: |-
            The elected maneuver-plan provider is not the one that reads stock's
            `patchedConicSolver`, so a write there would never be seen.

            Refused rather than attempted, because attempting it produces a GHOST NODE:
            we mutate stock's solver, the owning planner never reads it (an n-body
            backend clears that list every frame and writes its own guidance node into
            it), and the operator sees a maneuver node on the board that does precisely
            nothing. A silent wrong answer with a confident presentation.

            The code says WHY. It deliberately does not say WHO: this enum is typed
            precisely so a client never string-matches, and the owner is already on the
            wire as `VesselManeuver.Planner` for a readout to name. Additive (Major 5).
        - const: 8
          title: LimitReached
          description: |-
            A capacity is full: the Astronaut Complex holds its cap of active crew, a
            facility holds its cap of anything else countable.

            Split out of `CommandErrorCode.ModeUnavailable`, which was carrying five
            unrelated causes at once (crew cap, facility maxed, no roster, no Funding,
            wrong scene) and so could not tell a permanent refusal from a transient one.
            This arm says the cap is reached and the world has to change before a retry
            means anything; freeing a slot is a thing an operator can actually do.

            The arm chooses the sentence, `CommandResult.breach` supplies the numbers in
            it. Neither is worth sending without the other: a code with no payload
            cannot say "16 of 16".
        - const: 9
          title: AlreadyAtMaximum
          description: |-
            Already at the top of an upgradeable scale, so there is nothing above this
            to move to. The Launch Pad at tier 3 of 3.

            Deliberately NOT `CommandErrorCode.LimitReached`. A cap that is full can be
            freed; a maximum tier cannot be exceeded by any action at all, and an
            operator reads those two differently.
        - const: 10
          title: InsufficientFunds
          description: |-
            The command costs more than the funds on hand.

            Was `CommandErrorCode.Range`, which documents "an argument was out of its
            valid range" and is not what happened: affordability is not about an
            argument, and a client reading the enum name aloud got it wrong.

            `CommandResult.breach` carries the cost as `Actual` against the balance as
            `Limit`, so the client can say how short and in the operator's own currency
            rendering.
        - const: 11
          title: InsufficientScience
          description: |-
            The command costs more science than is banked.
            `CommandErrorCode.InsufficientFunds`'s twin, and separate for the same
            reason the game keeps `Currency` as three members: an operator short of
            science does something entirely different about it from one short of funds.

            Authority: `CurrencyModifierQuery.RunQuery(reason,
            ...).CanAfford(Currency.Science)`, which is what `RDTech.ResearchTech` asks.
            NOT `ResearchAndDevelopment.CanAfford`, which skips the modifier chain and
            so answers a different question from the one the game acts on.
        - const: 12
          title: CareerModeRequired
          description: |-
            The save is not a career save, so this command's whole subsystem does not
            exist here.

            Authority: `HighLogic.CurrentGame.Mode`, and in practice the null `Instance`
            of the `ScenarioModule` that would have answered (`Funding`,
            `ContractSystem`, `StrategySystem`, `ResearchAndDevelopment`,
            `ScenarioUpgradeableFacilities`).

            This is a PERMANENT property of the save, not a state that may change, which
            is exactly what `CommandErrorCode.ModeUnavailable` could not say. An
            operator should see the control absent rather than refused; a client that
            can tell this arm from the others can do that.
        - const: 13
          title: WrongScene
          description: |-
            The game is in a scene this command cannot run from.

            Authority: `HighLogic.LoadedScene` (`GameScenes`). `CommandResult.detail`
            names the scene when the producer had one.
        - const: 14
          title: WrongState
          description: |-
            The entity is not in a state this transition applies to: an already-active
            strategy asked to activate, an already-researched node asked to unlock, a
            contract asked to accept when it is not offered, an assigned kerbal asked to
            be sacked, a spent experiment asked to deploy.

            Authority: the entity's own state enum. `Strategies.Strategy.IsActive`,
            `RDTech.State`, `Contract.State`, `ProtoCrewMember.RosterStatus`,
            `ModuleScienceExperiment.Deployed`/`Inoperable`. Every one of those is
            `[Description]`-tagged or otherwise nameable, so `CommandResult.detail` can
            carry the state in the game's own words.
        - const: 15
          title: NotClearToProceed
          description: |-
            Right command, wrong moment: the flight is not in a state that permits it
            yet, and will be later.

            Authority: `FlightGlobals.ClearToSave()`, which returns one of five refusals
            (in atmosphere, under acceleration, moving over the surface, about to crash,
            on a ladder), plus `FlightDriver.CanRevertToPostInit`/`CanRevertToPrelaunch`
            and the `GameParameters` flags for leaving to the space center and to the
            tracking station. The arm rides on `CommandResult.detail`.

            Distinct from `CommandErrorCode.WrongState`, which is about the entity and
            does not resolve by waiting.
        - const: 16
          title: CapabilityMismatch
          description: |-
            The part or vessel does not have the capability this command needs: a rotor
            asked for a target angle, an unmotorised servo asked to drive, a part with
            no such action, an action present but inert, an autopilot mode this craft
            cannot hold.

            Authority: the part's own module list and fields
            (`ModuleRoboticServoRotor`/`Hinge`/`Piston`, `servoIsMotorized`,
            `BaseEvent.active`, `BaseEvent.EventIsDisabledByVariant`) and
            `VesselAutopilot.CanSetMode`.

            Nothing an operator waits for. The craft would have to be different for this
            to work, which is why it is not `CommandErrorCode.NotClearToProceed` and not
            `CommandErrorCode.WrongState`.
        - const: 17
          title: NoConnection
          description: |-
            There is no usable link for what this command needs to send.

            Authority: `ScienceUtil.GetBestTransmitter(Vessel)` and
            `IScienceDataTransmitter.CanTransmit()`. Deliberately NOT the Courier's own
            comms-loss gate, which refuses the dispatch before a handler ever runs; this
            is the vessel finding it has no antenna that can carry the payload.
        - const: 18
          title: NotUnlocked
          description: |-
            The capability exists in the game but this save has not unlocked it: fuel
            transfer, custom action groups, flight planning, EVA, the maneuver tool.

            Authority: `GameVariables.UnlockedFuelTransfer`,
            `UnlockedActionGroupsStock`/`Custom`, `UnlockedFlightPlanning`,
            `UnlockedEVA`/`Flags`/`Clamber`, `ManeuverToolAvailable`, each read at the
            owning facility's normalised level.

            Distinct from `CommandErrorCode.LimitReached`, which is a number against a
            number. This is a switch that is off, and the fix is an upgrade rather than
            freeing a slot.
        - const: 19
          title: SiteOccupied
          description: |-
            Another vessel is on the launch site.

            Authority: `PreFlightTests.LaunchSiteClear`, whose
            `GetWarningTitle()`/`GetWarningDescription()` are the game's own words for
            it and ride on `CommandResult.detail`.
        - const: 20
          title: FacilityDamaged
          description: |-
            The facility this command needs is destroyed or damaged.

            Authority: `PreFlightTests.FacilityOperational`, over
            `PSystemSetup.Instance.GetSpaceCenterFacility(name).GetFacilityDamage()`.
        - const: 21
          title: NotReady
          description: |-
            The vehicle is not a launchable article yet: an install's build and
            logistics model has work outstanding on it. Nothing is over a limit and
            nothing is broken, the thing simply has not been made ready.

            Authority: whichever Uplink CONTRIBUTED the readiness requirement that
            refused (see IUplinkHost.AddCommandRequirement), never a stock KSP read:
            stock has no build step, so it contributes no readiness requirements and
            this code never arrives on a stock install. Under RP-1 it is a vehicle that
            was never integrated, one still integrating, one finished but not rolled
            out, or one rolled out to a pad still being reconditioned.
            `CommandResult.detail` says which.

            Deliberately NOT `CommandErrorCode.LimitReached`, which is the launch
            refusal an operator already gets for a craft that is too heavy or too large
            for the site, and which is fixed by changing the craft or upgrading the pad.
            This one is fixed by doing the outstanding work, and the two want entirely
            different next moves.

            Deliberately NOT `CommandErrorCode.NotFound` either, which `ksp.launch`
            already returns when no craft file answers to the name. A craft that exists
            on disk and has never been built is a different situation from one that does
            not exist, and collapsing them tells an operator to go looking for a file
            that is sitting right there.
        - const: 22
          title: InsufficientResource
          description: |-
            The command consumes a countable ITEM and there are not enough of them
            aboard: an EVA repair kit for a repair, on a provider that charges one.

            Authority: the provider's own charge, read back from the same function that
            STATES the cost on `ReliabilityPartEntry.repairCost`. The two come from one
            place precisely so a console cannot show one number while the repair takes
            another, and the ITEM is always the provider's to name: this code never
            asserts which one, only that there were too few.

            `CommandErrorCode.InsufficientFunds`'s and
            `CommandErrorCode.InsufficientScience`'s third sibling, and separate for the
            same reason those two are separate from each other: an operator short of a
            physical item does something entirely different about it from one short of a
            currency, and nothing can be bought to fix it.

            Deliberately NOT `CommandErrorCode.LimitReached`, which is a capacity that
            is FULL. This is a store that is empty, and the two read as opposites.
        - const: 23
          title: Unreadable
          description: |-
            The provider was ASKED and COULD NOT ANSWER. Nothing about the craft, the
            save or the moment was established, so the one fact this refusal carries is
            that the question went unanswered.

            None of its three neighbours, and folding it into any of them states
            something that was never established rather than merely stating it coarsely.
            "The answer is no" (a genuine omni antenna asked to aim) is a FACT about the
            craft, and that is `CommandErrorCode.CapabilityMismatch`. "Not applicable"
            (asked of a craft that carries nothing this command could act on) is a
            refusal about what is there at all, and that is `CommandErrorCode.NotFound`.
            "Not yet" resolves by waiting, which is
            `CommandErrorCode.NotClearToProceed`; this does not resolve by waiting, and
            a retry is a second attempt at the same question rather than a later one.

            `CommandResult.detail` names WHAT could not be read when the producer had a
            name for it, and never says what the answer would have been. A surface has
            nothing to tell the operator about their vehicle here, because nothing was
            learned about it; offering the command again is the only honest next move.
    CommandRequest:
      type: object
      description: |-
        The envelope every dispatch is sent in.

        `command` is the channel's `address` and `args` is that command's own args
        type; each command's `command-request` message pins both. The answer comes back
        as a `command-response` correlated on `requestId`, or as an `error` frame if
        the dispatch was never carried.
      required:
        - type
        - requestId
        - command
        - label
        - topic
        - args
        - sentAt
      properties:
        type:
          type: string
          const: command-request
          x-sitrep-unit: id
        requestId:
          type: string
          x-sitrep-unit: id
        command:
          type: string
          x-sitrep-unit: id
        label:
          type: string
          x-sitrep-unit: text
          description: |-
            Caller-supplied, generic display label for this dispatch, carried verbatim
            into the corresponding `PendingUplink.label` entry on
            `system.uplink.pending`. Empty ⇒ the renderer falls back to
            `CommandRequest.command`. Never inspected/parsed by the engine.
        topic:
          type: string
          x-sitrep-unit: id
          description: |-
            Dispatch-time addressing: carried verbatim into the corresponding
            `PendingUplink.topic` entry on `system.uplink.pending`. Never
            inspected/parsed by the engine.
        vantage:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Per-call vantage override (Plan 3 / delay-UX): the command centre this
            specific command dispatches from, governing its delay via `DelayTo(vantage,
            node)`. Empty ⇒ the server uses the connection's session `SelectedVantage`
            (the default). A program-meta command (tech/strategy/contract) sends
            `"meta"` so it stays instant (`DelayTo("meta", *) = 0`) regardless of which
            centre the operator has selected. Nullable/optional: a pre-Vantage client
            omits it (codegen emits vantage?: string), and the server treats null/empty
            as the session vantage.
        args:
          description: The command's own arguments. Each command's `command-request` message narrows this to that command's args schema.
        sentAt:
          type: number
          x-sitrep-unit: ut
          description: |-
            When the client dispatched, in UT seconds (KSP universal time), the same
            base as `Meta.validAt`. The declaration reaches a client through the units
            map rather than through the emitted type: the `Value<"ut">` retyping pass
            runs over wire PAYLOAD types only, so every command-args and envelope field
            stays a bare number in `contract.ts` and carries its unit in `units.json`.
            **Every client sends 0 today.** The dispatching client has no UT to hand at
            that point that the server would not know better, and the server stamps the
            response's `Meta.deliveredAt` off its own clock, so a caller wanting a
            round-trip measures against its own view time rather than reading this back.
            The field is carried onto a response's `Meta.validAt`, which therefore reads
            0 on a command response: nothing consumes that today, and a consumer that
            starts to must make the client fill this in first.
      examples:
        - type: command-request
          requestId: 0f3c9a12
          command: vessel.control.setThrottle
          label: Throttle 40%
          topic: vessel.control
          vantage: ksc
          args:
            value: 0.4
          sentAt: 0
    CommandResponse:
      type: object
      description: |-
        The envelope every answer to a dispatch arrives in.

        `result` is the command's own reply type, pinned by that command's
        `command-response` message. A resolved response is a command that RAN: a
        refusal arrives here with `success: false` and an `errorCode`, and a fault
        arrives as an `error` frame on the session channel instead.

        There is no `command` field. `requestId` is the only link back to the
        dispatch, and a delayed command's answer can be minutes behind it.
      required:
        - type
        - requestId
        - result
        - meta
      properties:
        type:
          type: string
          const: command-response
          x-sitrep-unit: id
        requestId:
          type: string
          x-sitrep-unit: id
        result:
          description: The command's own reply. Each command's `command-response` message narrows this to that command's reply schema.
        meta:
          $ref: "#/components/schemas/Meta"
      examples:
        - type: command-response
          requestId: 0f3c9a12
          result:
            success: true
            errorCode: 0
          meta: &a5
            source: gonogo
            validAt: 1842006.5
            seq: 918
            deliveredAt: 1842006.5
            vantage: ksc
            quality: 1
            active: true
            staleness: 0
            timelineEpoch: 3
    CommandResult:
      type: object
      description: |-
        R7 Fix 1: the ONE result shape every command returns, replacing the three
        hand-rolled records (`Ack`/`StageResult`/`AddManeuverNodeResult`) that each
        re-declared `Success` + `ErrorCode`. `CommandResult.success` false pairs
        with a typed `CommandResult.errorCode` (never a free-text message a client
        has to string-match), following a `Result<T, CommandError>` ruling: results
        are always delivered (never a fire-and-forget void), and failure is
        structured data, not a thrown exception.

        This non-generic base is the "no payload" case (every plain actuation
        command: the former `Ack`). Commands that return a real value use
        `CommandResult`, whose `Payload` carries it (`vessel.control.stage`'s new
        stage index, `vessel.maneuver.add`'s created node id).
      required:
        - success
        - errorCode
      properties:
        success:
          type: boolean
          x-sitrep-unit: flag
        errorCode:
          $ref: "#/components/schemas/CommandErrorCode"
          x-sitrep-unit: enum
        breach:
          description: |-
            The numbers behind the refusal, when the refusal has any: the cap and the
            count, the tier and the top tier, the price and the balance. Null on success
            and on every refusal that is not a comparison.

            `CommandResult.errorCode` alone cannot say "16 of 16 active crew", and the
            code and the numbers only mean anything together: the code picks the
            sentence, this fills the gaps in it. Every number here was already in scope
            on the line that refused, and used to be discarded there.

            The SAME `LimitBreach` the declared-gate path carries on
            `GateVerdict.breach`, deliberately, so an operator reads one sentence shape
            whether the refusal came from a gate or from an actuator that got far enough
            to look.
          anyOf:
            - $ref: "#/components/schemas/LimitBreach"
            - type: "null"
        detail:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The refusal in the GAME's own words, when the game had any: the arm of
            `ClearToSaveStatus` it came back with,
            `Strategies.Strategy.CanBeActivated(out string reason)`'s reason,
            `GameVariables.GetEVALockedReason`'s sentence, a
            `PreFlightTests.IPreFlightTest`'s `GetWarningTitle()`, a
            `[Description]`-tagged state member's name. Empty when the refusal had
            nothing to quote.

            Interpolating what the game says beats inferring a cause from the mechanism
            that produced it, and it means this mod does not maintain an English table
            of KSP's own vocabulary that goes stale on every update and is wrong in
            every other language.

            Prose for a human, never parsed: `CommandResult.errorCode` is the
            machine-readable half and this is the readable one. The same split, and the
            same field name, as `GateVerdict.detail`.

            Nullable rather than empty-defaulted, so it lands on the wire as an OPTIONAL
            property: an existing consumer that builds a `CommandResult` is not made to
            supply a field it has nothing to put in, which is what makes this additive
            rather than a Major.
    CommsCommandCentre:
      type: object
      description: |-
        The `comms.commandCentre` payload: identifies WHICH command centre the
        active vessel's control path currently terminates at, vanilla KSC or a
        crewed control-source vessel (the stock "6-kerbal command center" mechanic),
        so a client can show its own stats against the right name instead of
        assuming KSC. Shares its id/kind scheme with `CommandCentreEntry` (the
        `commandCentre.roster` union): it names ONE entry from that same set,
        whichever one the vessel's own `ControlPath` resolved to this tick. Every
        field is null when there is no live remote centre right now (no connection,
        or the terminal node matches neither a ground station nor a crewed control
        source), the existing comms.link/comms.connectivity "No signal" case already
        covers that for a reader.
      required:
        - meta
      properties:
        id:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Stable authority/vantage key, same scheme as `CommandCentreEntry.id`: "ksc"
            | "ground:<name>" | "kk:<site>" | "vessel:<guid>". Null when no remote
            centre resolved.
        displayName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: Human-facing name.
        kind:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            One of `GroundStation` / `CrewedVessel` / `Colony` / `Custom` (the
            `CommandCentreKind` name), same as `CommandCentreEntry.kind`.
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index into system.bodies of the body this centre sits on; null when unknown,
            not surface-anchored, or the centre is a moving vessel.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsConnectivity:
      type: object
      description: |-
        The `comms.connectivity` payload: always-present, sourced from the elected
        backend. Ground-side truth about whether the active vessel has a control
        link home right now.
      required:
        - connected
        - controlSource
        - hasLocalControl
        - meta
      properties:
        connected:
          type: boolean
          x-sitrep-unit: flag
        controlSource:
          $ref: "#/components/schemas/CommsControlSource"
          x-sitrep-unit: enum
        hasLocalControl:
          type: boolean
          x-sitrep-unit: flag
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsControlSource:
      type: integer
      description: |-
        Degree of vessel control the link currently affords, the `controlSource`
        axis of `CommsConnectivity`. Mirrors stock `CommNet.VesselControlState`'s
        partial/full distinction without leaking a KSP enum onto the wire.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: None
        - const: 1
          title: Partial
        - const: 2
          title: Full
    CommsControlState:
      type: object
      description: |-
        The `comms.controlState` payload: always-present, elected backend.
        `CommsControlState.reason` is a nullable annotation (absent = no
        annotation), never an empty-string sentinel.
      required:
        - state
        - meta
      properties:
        state:
          $ref: "#/components/schemas/CommsControlStateKind"
          x-sitrep-unit: enum
        reason:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsControlStateKind:
      type: integer
      description: Control-state kind for `CommsControlState`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: None
        - const: 1
          title: PartialManoeuvre
        - const: 2
          title: Full
    CommsDegrade:
      type: object
      description: |-
        The `comms.degrade` payload: how degraded the active vessel's link home is
        right now, on one fixed scale, as the comms backend in force grades it.

        `CommsDegrade.level` runs from 0, nothing wrong, to 1, nothing usable
        getting through. It is ABSENT when nothing graded the link, and absent is a
        third answer rather than a low one: "nobody rated this" and "this link is
        perfect" are opposite instructions to anything choosing a quality, so a
        consumer must branch on the absence rather than default it to a number.

        **Read this rather than deriving a quality from `comms.signalStrength`.**
        That field is 0..1 too, and it is a different quantity on a stock install
        than on a RealAntennas one: a range fraction against an antenna curve versus
        spare room on a data-rate ladder. Nothing on the wire distinguishes them, so
        `1 - signalStrength` is two different quality curves on two saves. This
        channel names its rule, so a consumer acting on the number can see which
        grading produced it.

        DELAYED, like the link observations it grades and unlike its always-live
        `comms.delay` sibling. A rating is an observation of the craft's link, so an
        operator should learn of a degradation one light-time after it happened, at
        the same instant the telemetry that suffered it arrives. It is delay-gated
        on the ordinary terms, so through a blackout it holds at last-known; the
        disconnect edge itself reaches a client on `comms.link`, which is the
        connectivity authority and is exempt from that freeze precisely so it can
        report it.
      required:
        - modelId
        - modelName
        - meta
      properties:
        modelId:
          type: string
          x-sitrep-unit: id
          description: The grading rule's id; `"unknown"` when nothing graded the link.
        modelName:
          type: string
          x-sitrep-unit: text
          description: |-
            The grading rule's display name, so a surface can say which grading is in
            play.
        level:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            The rating: 0 pristine, 1 unusable, absent when unrated. Never outside that
            range, and never a NaN: an arithmetic that overshot is clamped to the end it
            overshot, and one that failed to resolve arrives absent.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsDelay:
      type: object
      description: |-
        The `comms.delay` payload: the CORE SignalDelay capability's output, gated
        by the `comms.signalDelay.enabled` config flag. `CommsDelay.oneWaySeconds`
        distinguishes two DIFFERENT "no delay" cases by value (R7: typed absence,
        never a single overloaded sentinel):

        - **null**: no measurable `CommsPath` (no path home, or incomplete hop
          geometry). There is nothing to measure, so nothing is reported.
          `CommsDelay.source` is `CommsDelaySource.None`.
        - **0**: the delay feature is disabled (`comms.signalDelay.enabled = false`)
          but the vessel IS connected. A genuine "zero delay applied", not an
          absence. `CommsDelay.source` is also `CommsDelaySource.None` here: the two
          cases share the same `Source` and are told apart only by whether the value
          is null.
        - a real number: `CommsDelay.source` is `CommsDelaySource.SignalDelay`;
          gonogo's own light-time math over the elected backend's hop geometry.

        Two of the zeroes name their own reason instead of sharing
        `CommsDelaySource.None`: `CommsDelaySource.Simulation` and
        `CommsDelaySource.NoCommsModel`. The second is what tells a client that a
        board showing no path and no relay graph is a save with the CommNet
        difficulty option off, not a craft in a permanent blackout: the blackout
        reports `null` here and `connected:false` on `CommsLink`, and this reports
        `0` and `connected:true`.

        DELAYED, like the telemetry it describes. A light-time is measured over the
        route a signal actually took, so the figure that reaches an operator is the
        delay as it WAS when the light left, and a craft whose delay has grown says
        so one light-time after it grew. Read it as an observation rather than as
        the current state of the link.
      required:
        - source
        - meta
      properties:
        oneWaySeconds:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: |-
            The one-way light-time to the command centre, seconds. See this type's own
            summary for the null/zero split, which is the whole discriminator: null is
            "nothing measurable", 0 is a measured or applied zero.
        source:
          $ref: "#/components/schemas/CommsDelaySource"
          x-sitrep-unit: enum
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsDelaySource:
      type: integer
      description: Where a `CommsDelay` value came from.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: None
        - const: 1
          title: SignalDelay
        - const: 2
          title: Simulation
          description: |-
            Zero, because the flight on screen is a SIMULATION and the operator has not
            asked for delay during one. A rehearsal has no spacecraft, so it has no
            light-time, and modelling a distance to a craft that is not there is a
            fiction rather than a measurement.

            Its own member rather than `CommsDelaySource.None` because a live board is a
            claim, and an operator is entitled to know which claim it is: "no delay is
            configured" and "delay is off because this is a rehearsal" call for
            different reactions. See `flight.simulation`.
        - const: 3
          title: NoCommsModel
          description: |-
            Zero, because this save models no comms network AT ALL: the stock CommNet
            difficulty option is off, so there are no ground stations, no relay graph
            and no path to measure a light-time over. Control reaches a craft directly,
            from anywhere, instantly.

            A POSITIVE FACT, and the reason it is a member here rather than a null
            `CommsDelay.oneWaySeconds`: null means "there is a comms model and it can
            measure nothing right now", which is a permanent blackout and the exact
            opposite prognosis. This is what distinguishes the two ON THE WIRE. A save
            with no comms model reports this alongside `connected:true`, so the channel
            keeps arriving with nothing held back, where a real blackout reports null
            and stops.
    CommsHop:
      type: object
      description: |-
        One ordered hop toward KSC in the control path. `CommsHop.distanceMeters` is
        the geometry SignalDelay consumes for light-time; it is nullable, absent
        when the backend cannot supply per-hop geometry (typed absence, never 0).
        Per-hop RealAntennas rate is NOT a field on this shared shape: the forward
        band rate rides the RA uplink's own `realantennas.hopRates` channel (a thin
        per-hop annotation keyed by these same node ids, joined onto the route
        client-side by a `comm-signal.hop-rates` contribution), and the other RA
        per-hop facts ride `CommsHop.extensions` under `"realantennas"`. The core
        hop stays RA-agnostic.

        `CommsHop.from`/`CommsHop.to` name the endpoints. Ground stations carry
        their OWN name (RSS/RealAntennas fly a dozen of them), not a single shared
        "home" label: two consecutive samples both showing a one-hop direct link,
        one to Kourou and one to Canberra, are a STATION HANDOFF, and under a shared
        label they were indistinguishable from one station whose range simply
        changed. That ambiguity is what made a relay handoff readable as an
        occlusion blackout.

        `CommsHop.fromIsHome`/`CommsHop.toIsHome` carry that home-ness per endpoint,
        so it survives without parsing a name. `CommsHop.kind` cannot serve: it is
        one value for the whole hop, so it says a ground station is involved but
        never which end.
      required:
        - from
        - to
        - fromIsHome
        - toIsHome
        - kind
      properties:
        from:
          type: string
          x-sitrep-unit: id
        to:
          type: string
          x-sitrep-unit: id
        fromIsHome:
          type: boolean
          x-sitrep-unit: flag
        toIsHome:
          type: boolean
          x-sitrep-unit: flag
        kind:
          $ref: "#/components/schemas/CommsHopKind"
          x-sitrep-unit: enum
        distanceMeters:
          type:
            - number
            - "null"
          x-sitrep-unit: m
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag: how the elected comms backend carries
            per-hop facts this shared shape does not declare, WITHOUT a PR against core
            (see ProviderExtensionBagAttribute for the whole mechanism). Null under the
            vanilla CommNet backend, which has nothing stock does not already say; a
            RealAntennas install fills `Extensions["realantennas"]` with band, tech
            level, modulation, encoder, required Eb/N0, beamwidth, EC draw and the
            reverse-direction rate, typed by the RA client's own `RealAntennasHopExt`.
            It rides `comms.path`, so it inherits that channel's Delayed classification.
          additionalProperties: true
    CommsHopKind:
      type: integer
      description: Kind of a node participating in a `CommsHop`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Home
        - const: 1
          title: Relay
        - const: 2
          title: Vessel
    CommsLink:
      type: object
      description: |-
        The `comms.link` connectivity MetaTopic: the ONE client-facing answer to "is
        there a control link home right now?", carried as a **Delayed,
        freeze-EXEMPT** channel (see `ChannelEngine.ConnectivityMetaTopic`). It is
        the delayed successor to the de-publicised TrueNow `CommsConnectivity`
        observation channel: clients (the app's SignalLossIndicator/CameraFeed, the
        kOS terminal's line-mode gate) read `comms.link.connected` instead of any
        raw `comms.*` observation.

        **Why its own topic, freeze-exempt:** the link state is what REPORTS the
        freeze, so: exactly parallel to `comms.delay` being exempt from its own
        delay: it must be exempt from the freeze it drives. It reveals the
        disconnect edge at `T+delay` (you learn of the outage one light-time after
        it happens) and keeps reporting `connected:false` through the blackout, so
        the client's "NO SIGNAL" flips at the correct delayed instant. The
        `VesselComms` observation struct (signalStrength/controlState) stays Delayed
        AND freeze-gated: it freezes at last-known through the outage.
      required:
        - connected
        - meta
      properties:
        connected:
          type: boolean
          x-sitrep-unit: flag
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsNetwork:
      type: object
      description: |-
        The `comms.network` payload: always-emitted, but its richness tracks the
        elected backend (a "backend-dependent detail"). Under bare CommNet this may
        be a single home-edge; under RealAntennas it enumerates the relay graph.
      required:
        - nodes
        - edges
        - meta
      properties:
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/CommsNetworkNode"
        edges:
          type: array
          items:
            $ref: "#/components/schemas/CommsNetworkEdge"
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsNetworkEdge:
      type: object
      description: One edge in the `CommsNetwork` relay graph.
      required:
        - a
        - b
        - active
      properties:
        a:
          type: string
          x-sitrep-unit: id
        b:
          type: string
          x-sitrep-unit: id
        active:
          type: boolean
          x-sitrep-unit: flag
    CommsNetworkNode:
      type: object
      description: |-
        One node in the `CommsNetwork` relay graph. `CommsNetworkNode.id` is a
        UNIQUE, stable join key in the same id space `CommsHop.from`/`CommsHop.to`
        use: a vessel's persistent id for a craft, the station's own name for a
        ground station. Never a vessel's display name, which two craft can share,
        which made it unsafe as a graph or roster key.
        `CommsNetworkNode.displayName` carries the label, and
        `CommsNetworkNode.kind` carries home-ness, so nothing has to read meaning
        out of the id.
      required:
        - id
        - displayName
        - kind
      properties:
        id:
          type: string
          x-sitrep-unit: id
        displayName:
          type: string
          x-sitrep-unit: text
        kind:
          $ref: "#/components/schemas/CommsHopKind"
          x-sitrep-unit: enum
    CommsOcclusion:
      type: object
      description: |-
        The `comms.occlusion` payload: always-present, sourced from the elected
        backend (the PROVIDER axis `Comms.cs`'s header describes). The declared
        occlusion model, named, with its rule already applied to every celestial
        body the game knows about.

        TRUE-NOW like the rest of the comms family, and for a stronger reason than
        most: this is not an observation of the vessel at all, it is a statement
        about the universe's geometry and the rule the elected backend applies to
        it. Delaying it would mean a predictor computing tomorrow's blackout from
        yesterday's model.

        Effectively static within a session: the body set does not change and the
        multipliers change only if the player edits the difficulty settings. The
        producer republishes an unchanged instance, which the emitter's change-gate
        suppresses, so the channel costs a keyframe and nothing else.
      required:
        - modelId
        - modelName
        - bodies
        - meta
      properties:
        modelId:
          type: string
          x-sitrep-unit: id
          description: |-
            The elected model's ICommsOcclusionModel.ModelId; `"unknown"` when no
            backend is elected.
        modelName:
          type: string
          x-sitrep-unit: text
          description: The elected model's display name, so a UI can say which geometry is in play.
        bodies:
          type: array
          items:
            $ref: "#/components/schemas/CommsOcclusionBody"
          description: |-
            Every known celestial body, with the model applied. Empty before the game
            has populated a body list, never null.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsOcclusionBody:
      type: object
      description: |-
        One body's occlusion geometry, as resolved by the elected model.
        `CommsOcclusionBody.index` matches `BodyEntry.Index` on `system.bodies`
        (both are `CelestialBody.flightGlobalsIndex`), so a consumer joins the two
        without name-matching.

        Both radii ride the wire: `CommsOcclusionBody.radiusMeters` is what the rock
        measures and `CommsOcclusionBody.occludingRadiusMeters` is what the backend
        treats it as. Carrying both keeps the difference visible (and the multiplier
        derivable) without asking any consumer to apply one.
      required:
        - index
        - radiusMeters
        - hasAtmosphere
        - occludingRadiusMeters
      properties:
        index:
          type: number
          x-sitrep-unit: id
        name:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        radiusMeters:
          type: number
          x-sitrep-unit: m
          description: The body's own mean radius (`CelestialBody.Radius`).
        hasAtmosphere:
          type: boolean
          x-sitrep-unit: flag
        occludingRadiusMeters:
          type: number
          x-sitrep-unit: m
          description: What the elected backend treats as blocking a radio path through this body.
    CommsPath:
      type: object
      description: |-
        The `comms.path` payload: always-present, elected backend. Ordered hops from
        the active vessel to KSC. Empty `CommsPath.hops` = no path home (a real,
        control-loss state, not absence-of-data).

        DELAYED, and NEVER RECKONABLE. The route is the one the arriving signal
        took, so it reveals with the telemetry that came down it. It also carries no
        forward model and cannot be given one: a route changes DISCRETELY, a relay
        drops below the horizon and the whole chain re-solves to different hops, and
        every basis a reckoner could declare (Kepler propagation, dead reckoning,
        rate integration) moves a continuous quantity. What you are shown is the
        topology AS OBSERVED; nothing may extrapolate it forward.
      required:
        - hops
        - meta
      properties:
        hops:
          type: array
          items:
            $ref: "#/components/schemas/CommsHop"
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    CommsSignalStrength:
      type: object
      description: |-
        The `comms.signalStrength` payload: always-present, elected backend. 0..1.
        CommNet gives a coarse range-fraction; RealAntennas gives a
        link-budget-derived value.

        A save with the stock CommNet difficulty option off models no link budget at
        all and reports 1 here: nothing attenuates a link that is not modelled. A
        reader that needs to know this is not a grading has
        `CommsDelaySource.NoCommsModel` on `comms.delay`, which is the one
        discriminator for the whole family rather than a second one per field.
      required:
        - value
        - meta
      properties:
        value:
          type: number
          x-sitrep-unit: ratio
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    ComposedBurn:
      type: object
      description: |-
        One burn as a command centre SPECIFIED it, which is a different thing from
        one as the craft reports it.

        **Inputs only.** A `ManeuverNode` carries what a burn turned out to be: its
        cutoff, its final mass, the patch chain it puts the craft on. None of those
        is something an operator states, they are what a planner works out, and
        putting them in a command would invite a caller to state a conclusion and
        have it quietly ignored.

        **Anchored to ignition.** A burn starts when it starts. The half-delta-v
        instant a node reports is derived from a solved burn, so it cannot be the
        thing that specifies one.
      required:
        - ignitionUt
        - frame
        - dvRadial
        - dvNormal
        - dvPrograde
        - inertiallyFixed
      properties:
        ignitionUt:
          type: number
          x-sitrep-unit: ut
        frame:
          description: |-
            The basis the three components below are in.

            Stated rather than assumed, because the same three numbers are a different
            burn in each basis and both bases are in use: stock plans in
            radial/normal/prograde, an integrating planner in the Frenet trihedron. A
            default here would silently reinterpret every burn sent by the other one.
          allOf:
            - $ref: "#/components/schemas/ManeuverFrame"
              x-sitrep-unit: enum
        dvRadial:
          type: number
          x-sitrep-unit: m/s
          description: |-
            The basis's first, second and third component, in the basis's own order,
            exactly as `ManeuverNode.frame` describes for the reported shape. So these
            are radial/normal/prograde under `ManeuverFrame.RadialNormalPrograde` and
            tangent/normal/binormal under `ManeuverFrame.TangentNormalBinormal`.
        dvNormal:
          type: number
          x-sitrep-unit: m/s
        dvPrograde:
          type: number
          x-sitrep-unit: m/s
        inertiallyFixed:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            Hold the burn's direction against the stars rather than against the craft's
            moving frame.
        thrust:
          type:
            - number
            - "null"
          x-sitrep-unit: kN
          description: |-
            The engine to burn with. BOTH absent means "leave whatever the plan already
            holds", which is the ordinary case for editing an existing burn.

            Stated as real numbers rather than as a named preset. A preset is one
            planner's idea of a placeholder engine, and core naming it would put that
            planner's numbers in every other planner's contract; a caller that wants a
            placeholder states the placeholder.
        specificImpulse:
          type:
            - number
            - "null"
          x-sitrep-unit: isp
    ContractActionArgs:
      type: object
      description: |-
        Args shared by `career.contract.accept`/`decline`/`cancel`: the contract's
        stable `ContractID` (stringified; the same id the READ side emits for each
        contract, `career.status`'s `contracts[].id`). Which of the three verbs is
        valid depends on the contract's current state (accept/decline require an
        offered contract, cancel an active one); an out-of-state request comes back
        `CommandErrorCode.ModeUnavailable`.
      required:
        - contractId
      properties:
        contractId:
          type: string
          x-sitrep-unit: id
    ControlFrame:
      type: object
      description: |-
        The frame the game's own navigation view is expressed in: what the player is
        looking at, and what a burn expressed relative to the control frame is held
        fixed against.

        **Why this is not a widget's choice.** A widget picks a read frame for
        itself and nothing else sees it. This is the game's, it is one at a time,
        and it is written as well as read, so a command centre can put the player's
        view where a plan is being discussed.

        **Bodies travel by name.** Every other body table in this mod is keyed by
        `bodyName`, `system.bodies` included, so a frame named the same way needs no
        join to be understood and cannot disagree with the table beside it.

        **The pulsating frames carry SETS, not just a pair.** A rotating frame turns
        about two bodies; a pulsating one turns about two groups, and the origin is
        defined by the mass of the whole group. Publishing only the head of each
        side loses bodies out of the mass that decides where the origin is, and
        loses them silently, because the head is the name a reader recognises.
        `ControlFrame.primaryBodies` always leads with `ControlFrame.primaryBody` so
        a reader wanting the pair can take the heads and a reader computing the
        frame can take the sets.
      required:
        - kind
      properties:
        kind:
          $ref: "#/components/schemas/ControlFrameKind"
          x-sitrep-unit: enum
        centreBody:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The body the frame is centred on, when it has one. The rotating frames are
            defined by their pair rather than by a centre.
        primaryBody:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: The body a rotating frame turns about. Null for the centred frames.
        secondaryBody:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: The body a rotating frame is anchored to. Null for the centred frames.
        primaryBodies:
          type:
            - array
            - "null"
          items:
            type: string
            x-sitrep-unit: text
          description: |-
            Every body on the primary side, leading with `ControlFrame.primaryBody`. See
            this type's own doc for why the set travels rather than the head.
        secondaryBodies:
          type:
            - array
            - "null"
          items:
            type: string
            x-sitrep-unit: text
          description: Every body on the secondary side, leading with `ControlFrame.secondaryBody`.
        targetFrameSelected:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            The frame is defined against the current target rather than against a body,
            which sits orthogonally to `ControlFrame.kind` rather than inside it.
            Closest approach is computed only in this frame, and apsides do not exist in
            it at all.
        targetId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: The target the frame is defined against, when it is a target frame.
    ControlFrameKind:
      type: integer
      description: |-
        The kinds of reference frame a control frame can be.

        Taken from what an n-body producer actually offers rather than from what
        this app would find convenient: the five members below are the five frame
        types the shipped native build constructs. A vocabulary invented here would
        name frames nothing can select, and would have no name for frames a player
        is looking at right now.

        **A superset of what a widget can FOLLOW.** The read frames a widget may
        draw in cover three of these. A control frame outside that subset is a real
        state and not an error: a widget set to follow the control frame resolves to
        nothing, which is the behaviour that side already documents.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Unspecified
          description: Nothing stated one. Distinct from a frame we could not name.
        - const: 1
          title: BodyCentredInertial
          description: Centred on a body, axes fixed against the stars.
        - const: 2
          title: BodyCentredBodyDirection
          description: |-
            Centred on a body, one axis held towards another body. What "parent
            direction" names on the read side.
        - const: 3
          title: BarycentricRotating
          description: Turning with the barycentre of two bodies.
        - const: 4
          title: RotatingPulsating
          description: |-
            Turning with a pair of bodies AND holding the separation of their two mass
            centres fixed, so a transfer between them draws the same shape whatever
            their current distance.
        - const: 5
          title: BodySurface
          description: Centred on a body and turning with its surface.
    ControlState:
      type: integer
      description: |-
        Mirrors KSP's own `VesselControlState` enum by name (its underlying int
        values collide by design in stock KSP, e.g. `Probe == ProbeNone == 2`, which
        is KSP's own ambiguity, not one this contract introduces; we simply consume
        whichever name `.ToString()` already commits to). `ControlState.Unknown` is
        the graceful fallback for an unrecognized raw value.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: None
        - const: 1
          title: Probe
        - const: 2
          title: Kerbal
        - const: 3
          title: Partial
        - const: 4
          title: Full
        - const: 5
          title: ProbeNone
        - const: 6
          title: ProbePartial
        - const: 7
          title: ProbeFull
        - const: 8
          title: KerbalNone
        - const: 9
          title: KerbalPartial
        - const: 10
          title: KerbalFull
        - const: 11
          title: Unknown
    CrashFlightStats:
      type: object
      description: |-
        Per-flight statistics accumulated across the whole flight up to the crash,
        `CrashReport.flightStats`. See `crash-payloads.ts` for the wire shape.
      required:
        - kerbalsKilled
        - partsLost
        - flightEndMode
        - highestSpeedOverLand
        - missionEnd
        - highestGee
        - highestAltitude
        - totalDistance
        - missionTime
        - highestSpeed
        - groundDistance
        - liftOff
      properties:
        kerbalsKilled:
          type: number
          x-sitrep-unit: count
          description: Kerbals killed earlier in the flight (before the final crash).
        partsLost:
          type: number
          x-sitrep-unit: count
          description: Cumulative parts destroyed across the flight.
        flightEndMode:
          type: string
          x-sitrep-unit: text
          description: How the flight ended (e.g. `"CATASTROPHIC_FAILURE"`).
        highestSpeedOverLand:
          type: number
          x-sitrep-unit: m/s
        missionEnd:
          type: boolean
          x-sitrep-unit: flag
        highestGee:
          type: number
          x-sitrep-unit: g
        highestAltitude:
          type: number
          x-sitrep-unit: m
        totalDistance:
          type: number
          x-sitrep-unit: m
        missionTime:
          type: number
          x-sitrep-unit: s
          description: Mission time (seconds since launch) at the crash.
        highestSpeed:
          type: number
          x-sitrep-unit: m/s
        groundDistance:
          type: number
          x-sitrep-unit: m
        liftOff:
          type: boolean
          x-sitrep-unit: flag
    CrashPartLost:
      type: object
      description: |-
        One part lost in a crash: an entry of `CrashReport.partsLost`. See
        `crash-payloads.ts` for the wire shape.
      required:
        - partId
        - partName
        - partTitle
        - msg
      properties:
        partId:
          type: number
          x-sitrep-unit: id
          description: The part's `flightID`.
        partName:
          type: string
          x-sitrep-unit: text
          description: The part's `partInfo.name` (e.g. `"mk1pod.v2"`).
        partTitle:
          type: string
          x-sitrep-unit: text
          description: The part's `partInfo.title` (e.g. `"Mk1 Command Pod"`).
        msg:
          type: string
          x-sitrep-unit: text
          description: "Destruction message for this part: often empty."
    CrashReport:
      type: object
      description: |-
        The payload for the `crash.lastCrash` channel: a single "last notable crash"
        record for the current save, delivered on the Delivery.ReliableOrdered event
        lane. Mirrors the wire shape the consumers already parse
        (`FlightOutcomeBanner.parseCrash`, `LaunchDirector`) field-for-field; the
        frozen captures in `packages/app/src/__tests__/fixtures/crash-payloads.ts`
        are the wire ground truth this type names.

        TYPING/codegen marker only. The producer (`Gonogo.KSP.CrashUplink`)
        hand-flattens the live-KSP crash into a `Dictionary<string, object?>` via
        `Sitrep.Host.Crash.CrashPayload.Build` before publishing, so JsonWriter only
        ever sees the dictionary: this POCO exists solely so the TS SDK has a
        concrete payload type to name (it is on `WirePayloadCoverageTests`'s
        producer-flatten allowlist for exactly that reason).
      required:
        - vesselId
        - eventKind
        - what
        - vesselType
        - msg
        - latitude
        - longitude
        - partsLost
        - body
        - flightStats
        - vesselName
        - events
        - kerbalsKilled
        - situation
        - crewAboard
        - altitude
        - ut
      properties:
        vesselId:
          type: string
          x-sitrep-unit: id
          description: The crashed vessel's stable id (`Vessel.id` as a string GUID).
        eventKind:
          type: string
          x-sitrep-unit: id
          description: "Which detector fired: `CrashSplashdown` / `Destroyed` / `Crash`."
        what:
          type: string
          x-sitrep-unit: text
          description: |-
            The colliding object's name (`EventReport.other`): empty for a non-collision
            death.
        vesselType:
          type: string
          x-sitrep-unit: id
          description: The crashed vessel's `VesselType` name (e.g. `"Ship"`).
        msg:
          type: string
          x-sitrep-unit: text
          description: "The detector's message (`EventReport.msg`): often empty."
        latitude:
          type: number
          x-sitrep-unit: °
        longitude:
          type: number
          x-sitrep-unit: °
        partsLost:
          type: array
          items:
            $ref: "#/components/schemas/CrashPartLost"
          description: Parts lost in the destroying event.
        body:
          type: string
          x-sitrep-unit: text
          description: Name of the body the crash happened on (`mainBody.bodyName`).
        flightStats:
          description: Per-flight statistics accumulated up to the crash.
          allOf:
            - $ref: "#/components/schemas/CrashFlightStats"
        vesselName:
          type: string
          x-sitrep-unit: text
        events:
          type: array
          items:
            type: string
            x-sitrep-unit: text
          description: Timestamped flight-event log (liftoff, staging, the crash line).
        kerbalsKilled:
          type: array
          items:
            type: string
            x-sitrep-unit: text
          description: Names of the kerbals lost in this crash.
        situation:
          type: string
          x-sitrep-unit: text
          description: |-
            The vessel's flight situation at the crash (`Vessel.Situations` name, e.g.
            `"FLYING"`).
        crewAboard:
          type: array
          items:
            type: string
            x-sitrep-unit: text
          description: Names of the crew aboard at the crash.
        altitude:
          type: number
          x-sitrep-unit: m
        ut:
          type: number
          x-sitrep-unit: ut
          description: Universal time of the crash capture.
    CrewMember:
      type: object
      description: |-
        One crew member in the `vessel.crew` payload's `crew` roster. Typing-only
        mirror of the entry `Sitrep.Host.VesselViewProvider` reads out of the
        snapshot's `crew` group: every field nullable because each is read through
        `SnapshotDict.Get*`, which yields `null` (not a sentinel) on absence.
        Sourced from KSP's `ProtoCrewMember`: `name`/`trait`/`experienceLevel` plus
        the `type` (`KerbalType`) and `rosterStatus` (`RosterStatus`) enums,
        captured as their string names.
      properties:
        name:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        trait:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        experienceLevel:
          type:
            - number
            - "null"
          x-sitrep-unit: count
        type:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        rosterStatus:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        carrying:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/InventoryItem"
          description: |-
            What this kerbal is personally carrying, from their own
            `ModuleInventoryPart`.

            Here rather than on `vessel.inventory` because it answers a different
            question. That channel is SUPPLY, what is aboard and where. This is the
            ACTOR: whether THIS kerbal, whose trait and experience level sit two fields
            up, can do a job right now without anything being fetched first. A consumer
            deciding who should perform a task reads one payload, not a join.

            Null when the crew source could not read inventories at all, which is not
            the same as an empty list, meaning they are carrying nothing.
        slots:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            The kerbal's own slot count, stock default 2, one of which usually holds a
            parachute.
        packedVolumeLimit:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: |-
            Their packed-volume limit, stock default 40 in KSP's own cargo-volume unit.
            With a repair kit at 5, this is what actually bounds how many they can
            carry.
        packedVolumeUsed:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Packed volume they are currently using, same unit as the limit.
    CrewRosterEntry:
      type: object
      description: |-
        One kerbal in the `spaceCenter.crewRoster` channel (the hired-crew roster:
        KSP's `KerbalRoster.Crew`, owned crew that is either available or currently
        assigned to a mission) and reused verbatim for every entry in
        `AstronautComplexInfo.applicants` - ONE shape for a kerbal whether hired or
        still a candidate. Produced by
        `Sitrep.Host.SpaceCenterViewProvider.BuildCrewRoster` /
        `BuildAstronautComplex`.

        The `spaceCenter.crewRoster` channel is a BARE ARRAY of these entries
        (tagged `isArray: true`, like `LaunchSiteEntry`), one per crew member keyed
        by `CrewRosterEntry.name`. The whole payload is `null` (not an empty array)
        when no sample has landed yet, the provider's "no data yet" vs. "zero crew"
        distinction.

        LaunchDirector reads `CrewRosterEntry.name`/`CrewRosterEntry.trait`/
        `CrewRosterEntry.experienceLevel`/`CrewRosterEntry.available`/
        `CrewRosterEntry.unavailableReason` (the original, folded pair); the
        Astronaut Complex additionally reads `CrewRosterEntry.standing`, the
        authoritative standing it groups by, and `CrewRosterEntry.situation`, its
        label, plus the full stat set
        (`CrewRosterEntry.courage`/`CrewRosterEntry.stupidity`/`CrewRosterEntry.experience`/
        `CrewRosterEntry.experienceLevelDelta`) and the role tooltip text
        (`CrewRosterEntry.roleDescription`/`CrewRosterEntry.descriptionEffects`). A
        TS-shape-only typing/codegen marker: the provider hand-builds the dict and
        `JsonWriter` walks that live tree, these POCOs never serialize. Classified
        `DelayRole.TrueNow`: a ground-side career fact, same class as
        `LaunchSiteEntry`.
      properties:
        name:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Kerbal name (`ProtoCrewMember.name`): the id the hire command resolves
            against the live applicant pool.
        trait:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Specialisation (`ProtoCrewMember.trait`):
            `"Pilot"`/`"Engineer"`/`"Scientist"`/`"Tourist"`.
        experienceLevel:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Experience level (`ProtoCrewMember.experienceLevel`), 0–5 (0 for a fresh
            applicant).
        available:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the kerbal can be assigned to a flight today.

            **The field an old client should branch on.** It is derived from EVERY axis
            the derivation knows about, by CrewStandings.CanFly, which is a whitelist:
            only `Available` and `Applicant` are free, so a standing added to
            `CrewStanding` later reads as unavailable here without anybody editing a
            consumer. A widget that has never heard of training therefore still refuses
            to crew a kerbal who is mid-course.

            A backend may override it outright.
        unavailableReason:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Why the kerbal can't fly, in prose: `Assigned` reads "On mission",
            `Training` reads "In training", `Resting` reads "Standing down", and every
            other blocking standing reads its own name, so a retiree reads "Retired" and
            not "Dead". Empty string when `CrewRosterEntry.available` is true. A backend
            may override the wording.

            **No date, ever.** The when rides `CrewRosterEntry.standingEndsAtUt` and
            `CrewRosterEntry.retiresAtUt` as `ut` values, because a date formatted here
            would be formatted in the mod's idea of a calendar and an RSS save does not
            count years the way a stock one does. This is the only string on the payload
            a client could not re-render.
        standing:
          description: |-
            The kerbal's standing, as the dashboard means it: the field to BRANCH on.
            The elected ICrewStandingBackend's answer where it has one, otherwise
            derived from KSP's roster status by the stock backend.

            This exists because the roster status alone is NOT the answer under a career
            overhaul. RP-1 retires a kerbal by writing stock's `Dead` into the roster
            status, so `CrewRosterEntry.situationOrdinal` below reads `Dead` for a
            living retiree and no reading of it can recover the difference. See
            `CrewStanding` for the whole account.
          anyOf:
            - $ref: "#/components/schemas/CrewStanding"
              x-sitrep-unit: enum
            - type: "null"
        standingSource:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Which provider decided `CrewRosterEntry.standing`: the elected backend's
            `ProviderId`, e.g. `"stock"` or `"rp1"`. Absent when no backend was
            reachable at capture time.

            Carried so a surface can attribute a correction rather than merely apply it.
            A retiree shown as retired is a claim about a save that stock KSP would
            report as a fatality, and an operator is entitled to see which mod is making
            it.
        standingEndsAtUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            When the CURRENT `CrewRosterEntry.standing` lapses, as universal time: the
            course ETA for `Training`, the rest period's end for `Resting`. Absent for a
            standing with no scheduled end, which is most of them.

            Read with `CrewRosterEntry.unavailableReason` to say why a kerbal cannot fly
            AND until when. The two are separate fields so the client formats the date
            in its own calendar.
        retiresAtUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            When this kerbal is scheduled to become `Retired`, as universal time. Absent
            under any backend that does not schedule retirements, stock included, and
            absent rather than zero when a backend holds no date for this kerbal: a
            career overhaul's own getter answers 0 for "no record", and 0 would retire
            the whole roster at the epoch.

            Live at the same time as `CrewRosterEntry.standingEndsAtUt` and not a
            substitute for it: a kerbal is Available or Training for years while a
            retirement date sits in the future.
        situation:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            `CrewRosterEntry.standing`'s display LABEL: its enum name, or `"Applicant"`
            for a hireable candidate. Text for an operator, never a branch: compare
            `CrewRosterEntry.standing` instead.
        situationOrdinal:
          description: |-
            KSP's OWN ordinal: `(int)ProtoCrewMember.rosterStatus`, typed to
            `KspRosterStatus`, whose members mirror KSP's numbering.

            A truthful read of the game field and nothing more, kept because what KSP
            itself holds is worth knowing and because a command core dispatches is
            arbitrated against this value. It is NOT the field to branch on: under RP-1
            it reads `Dead` for a living retiree. `CrewRosterEntry.standing` is the
            answer.

            `null` for an APPLICANT, and that is a real distinction rather than a
            missing value: an applicant is not in the roster, so it has no
            `RosterStatus` at all, and `CrewRosterEntry.standing` carries
            `CrewStanding.Applicant` instead. Use `CrewRosterEntry.isApplicant` to tell
            the two apart. Also `null` when the capture carried no status.
          anyOf:
            - $ref: "#/components/schemas/KspRosterStatus"
              x-sitrep-unit: enum
            - type: "null"
        inactive:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the kerbal is standing down rather than on duty
            (`ProtoCrewMember.inactive`): KSP's own field, published beside the derived
            answer the way `CrewRosterEntry.situationOrdinal` is.

            **Not the field to branch on.** It is an INPUT to the derivation: a kerbal
            standing down has roster status `Available`, and this flag is what turns
            that into `CrewStanding.Resting` with `CrewRosterEntry.available` false. It
            reached the wire with nothing deriving from it, and a resting kerbal read as
            free to fly the whole time.

            Stock leaves it false. A career overhaul's post-flight R&R is what actually
            sets it, and it goes on the wire whether or not one is installed: the field
            is KSP's, so reading it costs a stock install nothing.
        inactiveUntilUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            When the stand-down ends (`ProtoCrewMember.inactiveTimeEnd`), as universal
            time. Absent when `CrewRosterEntry.inactive` is false: KSP leaves the field
            at whatever the last rest period set, so quoting it for a kerbal on duty
            would date a rest that is already over.
        isApplicant:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether this entry is a hireable candidate (`ProtoCrewMember.type ==
            KerbalType.Applicant`) rather than owned crew. Carried so a client never has
            to recognise the `"Applicant"` spelling of `CrewRosterEntry.situation` to
            know which channel it is reading.
        courage:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: Courage, 0–1 (`ProtoCrewMember.courage`).
        stupidity:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: Stupidity, 0–1 (`ProtoCrewMember.stupidity`).
        experience:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: |-
            Raw experience points (`ProtoCrewMember.experience`), 0 for a fresh
            applicant.
        experienceLevelDelta:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            Progress toward the next rank, 0–1 (the computed
            `ProtoCrewMember.ExperienceLevelDelta`); `1` at max rank (5).
        roleDescription:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The role's stock tooltip description
            (`ProtoCrewMember.experienceTrait.Description`): the exact string the
            in-game Astronaut Complex shows, sourced from `Traits.cfg`.
        descriptionEffects:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The role's current-rank effects text
            (`ProtoCrewMember.experienceTrait.DescriptionEffects`): rank-aware, it
            changes as the kerbal is promoted. Reflects this kerbal's own rank today
            only; there is no fetchable "effects at another rank" preview.
    CrewStanding:
      type: integer
      description: |-
        What a kerbal's place on the books IS, as the dashboard means it: this
        contract's own vocabulary, not a mirror of any game enum.

        The first four members line up with `KspRosterStatus` in meaning but
        deliberately NOT in numbering: a mirror would tie growth here to Squad
        shipping a new roster status, which is the assumption that let a retiree
        read as a fatality. `CrewStanding.Applicant` is a standing KSP expresses as
        a KerbalType rather than a RosterStatus, and it belongs in one enumeration
        with the rest because a client asking "what is this kerbal's standing" wants
        one answer.

        Behind `spaceCenter.crewRoster[].standing`, and it is the field to branch
        on; see `CrewRosterEntry.situationOrdinal` for what the raw KSP ordinal
        beside it is still good for.

        **The numbering IS the reading order**, and members are inserted rather than
        appended for that reason. The SDK's `CREW_STANDING_ORDER` sorts by value so
        a crew surface reads free to fly, then committed, then off the books, and
        derives that from the enum so nobody has to maintain a second list.
        Appending `CrewStanding.Training` would have filed it after
        `CrewStanding.Dead`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Unknown
          description: |-
            No backend could say. Distinct from every answer below, and never a stand-in
            for one: a capture that read no roster status at all reports this rather
            than guessing at Available.
        - const: 1
          title: Applicant
          description: A hireable candidate, not yet on the books.
        - const: 2
          title: Available
          description: On the books and free to fly.
        - const: 3
          title: Assigned
          description: On the books and currently crewing a vessel.
        - const: 4
          title: Training
          description: |-
            On the books, committed to a training course, and not assignable until it
            finishes. CrewStandingReading.StandingEndsAtUt carries the course's own ETA.

            Reachable only through a backend that models training. Stock has no courses,
            so a stock install never reports it, and KSP's roster status for a kerbal
            mid-course is `Available`: the same shape as the retiree, where the game
            field is not the answer.
        - const: 5
          title: Resting
          description: |-
            On the books, standing down after a flight, and not assignable until the
            rest period ends. CrewStandingReading.StandingEndsAtUt carries its end.

            Derived from KSP's own `ProtoCrewMember.inactive`, so the stock backend
            answers it and every install gets it. Stock rarely sets the field; a career
            overhaul's post-flight R&R is what usually does.
        - const: 6
          title: Retired
          description: |-
            Finished flying, alive, off the flight roster for good. Reachable only
            through a backend that models a career ending well; stock has no such
            concept and never reports it.
        - const: 7
          title: Dead
          description: Killed.
        - const: 8
          title: Missing
          description: "Missing: KSP's own separate answer, kept separate."
    DeactivateStrategyArgs:
      type: object
      description: |-
        `career.strategy.deactivate`'s args: the strategy's stable
        `StrategyConfig.Name` id (see `ActivateStrategyArgs.strategyId`).
      required:
        - strategyId
      properties:
        strategyId:
          type: string
          x-sitrep-unit: id
    DeployedEntry:
      type: object
      description: |-
        One entry in the `deployed.bases` channel payload, a Breaking Ground
        deployed-science experiment. The channel payload is a BARE ARRAY
        (`DeployedEntry[]`) or `null`. Unlike the other two channels,
        `deployed.bases` is captured GLOBALLY across every loaded vessel: a deployed
        cluster is its own ground vessel, so an entry normally describes a vessel
        OTHER than the active one, distinguished by `DeployedEntry.vesselName`.
        Typing-only mirror of
        `Sitrep.Host.BreakingGroundViewProvider.BuildDeployedEntry`; see
        `ExperimentEntry` for the "no wire change, all fields nullable" rationale.
      properties:
        vesselName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        body:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        situation:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        biome:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        experimentId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        scienceCompletedPercentage:
          type:
            - number
            - "null"
          x-sitrep-unit: "%"
        scienceTransmittedPercentage:
          type:
            - number
            - "null"
          x-sitrep-unit: "%"
        scienceValue:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        scienceLimit:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        powerState:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            `ModuleGroundSciencePart.PowerState` verbatim: KSP's own words for the power
            state, for display. DO NOT BRANCH ON THIS.

            It is not an enum name, it is LOCALISED PROSE. The module assigns it from
            `Localizer` in `UpdateModuleUI()`, so the value is whatever language the
            player runs KSP in, and it is only written when the part-action window
            refreshes, so it can also be stale. `DeployedEntry.power` is the field to
            read.
        connectionState:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            `ModuleGroundSciencePart.ConnectionState` verbatim, for display. Localised
            prose on the same terms as `DeployedEntry.powerState`; read
            `DeployedEntry.controllerConnected` instead.
        power:
          description: |-
            The cluster's power state, DERIVED from the same facts `UpdateModuleUI()`
            itself branches on
            (`DeployedScienceCluster.IsPowered`/`.ControllerPartEnabled`, the module's
            `Enabled` and `DeployedOnGround`) rather than parsed back out of the
            sentence that method writes.

            This exists because branching on `DeployedEntry.powerState` could not be
            made correct. The client compared it against `"Powered"` and against
            `"NoPower"`, a string KSP has never emitted, so `Unpowered`, `Disabled`,
            `Controller Disabled` and `N/A` all fell through to POWERED: an unpowered
            cluster painted as a working one, in English, before any question of
            translation.

            `null` when the cluster could not be read at all, which is a third answer
            and must not be read as either powered or unpowered.
          anyOf:
            - $ref: "#/components/schemas/DeployedPowerState"
              x-sitrep-unit: enum
            - type: "null"
        controllerConnected:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the experiment is attached to a controller cluster at all, the fact
            behind `DeployedEntry.connectionState`'s "Connected"/"Not Connected". `null`
            when it could not be determined.
        powerAvailable:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Power units the cluster's parts PRODUCE
            (`DeployedScienceCluster.PowerAvailable`), against
            `DeployedEntry.powerRequired`. Breaking Ground's own integral power scale
            summed over the cluster's parts: NOT electric charge, and not a rate.
            Available at or above required is what makes the cluster powered.

            `null` when the cluster could not be read, on the same terms as
            `DeployedEntry.power`. Zero is a real reading a live cluster holds while its
            panels are dark, so it must not stand in for absence.
        powerRequired:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Power units the cluster's parts REQUIRE
            (`DeployedScienceCluster.PowerRequired`). Same scale and same null rule as
            `DeployedEntry.powerAvailable`, the demand side of the balance.
        deployedOnGround:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
    DeployedPowerState:
      type: integer
      description: |-
        A deployed-science cluster's power state: OUR enum, not KSP's.

        KSP has no enum for this. `ModuleGroundSciencePart` carries the answer as a
        localised sentence, so this reproduces the five outcomes `UpdateModuleUI()`
        distinguishes, derived from the booleans it reads rather than from the
        sentence it writes. Being ours, it is an ordinal on the wire and a closed
        union on the client like every other enum in this contract, and it needs no
        mirror test: nobody else owns its numbering.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Powered
          description: "Powered and working: the cluster reports `IsPowered`."
        - const: 1
          title: Unpowered
          description: Deployed and switched on, but the cluster has no power.
        - const: 2
          title: ControllerDisabled
          description: The cluster's CONTROLLER is switched off, so nothing is powered.
        - const: 3
          title: Disabled
          description: This experiment is switched off, or is not deployed on the ground.
        - const: 4
          title: NotConnected
          description: |-
            Attached to no cluster at all. Distinct from `DeployedPowerState.Unpowered`:
            there is nothing to supply power, rather than a supply that is empty.
    DockAlignment:
      type: object
      description: |-
        The `vessel.dock` channel payload: the docking/rendezvous capture-add (M3
        R3): relative position/velocity + coarse orientation between the active
        vessel's nearest FREE (undocked) docking port and the currently-targeted
        docking port, for docking-alignment widgets. Whole- channel absence means
        "not docking-relevant right now", no target targeted, the target isn't
        itself a docking port, or the active vessel has no free port of its own;
        never a stale/zero-distance sentinel record (same R1(b) convention
        `VesselTarget` already established).

        Reuses the ONE canonical `Vec3` shape (never a second vector encoding).
        `DockAlignment.forwardDot` is the dot product of the two ports' forward
        (docking-axis) vectors: -1.0 means the ports face each other head-on (the
        alignment a successful dock needs), +1.0 means they point the same direction
        (facing away from each other), a widget maps this to a 0..100% "facing"
        readout however it likes; this contract intentionally ships the raw dot
        product rather than a pre-baked percentage so the mapping stays a client
        concern.
      required:
        - relativePosition
        - relativeVelocity
        - distance
        - meta
      properties:
        relativePosition:
          type: object
          description: Metres, own-port-relative (target port minus own port).
          required:
            - x
            - y
            - z
          properties:
            x: &a1
              type: number
              x-sitrep-unit: m
            y: *a1
            z: *a1
        relativeVelocity:
          type: object
          description: m/s, own-port-relative.
          required:
            - x
            - y
            - z
          properties:
            x: &a2
              type: number
              x-sitrep-unit: m/s
            y: *a2
            z: *a2
        distance:
          type: number
          x-sitrep-unit: m
          description: |-
            Metres: `DockAlignment.relativePosition`'s magnitude, provided directly so a
            widget doesn't have to re-derive it every frame.
        forwardDot:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: |-
            Dot product of the own port's and target port's forward vectors; see the
            class doc comment. Null only if either port's transform was unavailable this
            tick.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    ErrorMsg:
      type: object
      description: |-
        A fault, never a refusal, and the difference decides how a client handles it.

        An `error` frame means the request was never CARRIED: an unknown command, a
        command whose provider has fail-softed, a result the codec could not serialise,
        or a `set-vantage` naming a command centre that is not active. No handler ran,
        so there is nothing to report about what the game thought.

        A command the game REFUSED ran and said no. That arrives as a
        `command-response` with `success: false` and an `errorCode`, correlated on
        `requestId` like any other answer, and it is not an error frame.

        `code` is an open vocabulary rather than an enumeration, and the mod emits
        three values into it: `unknown-vantage`, a `set-vantage` naming a centre that
        is not active; `E_UNAVAILABLE`, a dispatch that reached no handler; and
        `result-serialization-error`, a command that ran and whose result could not be
        written. A client's own transport mints codes into the same field, so an
        unrecognised code is still a fault and `message` is what to show for it.
        `CommandErrorCode`, enumerated in full under `components.schemas`, is a
        different vocabulary: it names REFUSALS and never appears here.
      required:
        - type
        - code
        - message
      properties:
        type:
          type: string
          const: error
          x-sitrep-unit: id
        requestId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        topic:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        code:
          type: string
          x-sitrep-unit: id
        message:
          type: string
          x-sitrep-unit: text
    EventMsg:
      type: object
      description: |-
        Something that happened to a SUBSCRIPTION, on the topic it happened to.

        Two names are emitted, and each names one topic this connection is subscribed
        to:

        - `subscribed` acknowledges a `subscribe`, once per subscribe, on the
          reliable lane. It is the frame to wait for rather than the first
          `stream-data`: a channel with nothing to say yet sends the ack and then
          nothing. A `subscribe` naming a topic no declared channel and no registered
          dynamic namespace owns is answered with NOTHING, not an error, so a missing
          ack is how a client learns a topic is unowned
        - `timeline-reset` says the game quickloaded and UT rewound. It is sent to
          every session for every topic it holds, and the delayed view built from
          frames before it is abandoned rather than reconciled

        An event carries no payload of its own: `name` is the whole of what happened,
        and `meta` describes the delivery as it does on any other frame. Events on a
        DYNAMIC topic are emitted the same way and are absent here for the reason the
        document description gives.
      required:
        - type
        - topic
        - name
        - meta
      properties:
        type:
          type: string
          const: event
          x-sitrep-unit: id
        topic:
          type: string
          x-sitrep-unit: id
        name:
          type: string
          x-sitrep-unit: text
        meta:
          $ref: "#/components/schemas/Meta"
    ExperimentActionArgs:
      type: object
      description: |-
        Args shared by every science-experiment actuation command
        (`science.experiment.deploy`/`science.experiment.transmit`): the experiment
        is addressed by `ExperimentActionArgs.partId`, the part's
        `flightID.ToString()`, the SAME opaque id the read side emits in
        `science.instruments` (one entry per `ModuleScienceExperiment`, keyed by
        `flightID`). The host resolves it against the active vessel's live parts; a
        client never supplies a live array index. An empty
        `ExperimentActionArgs.partId` resolves to nothing and yields
        `CommandResult.errorCode` `CommandErrorCode.NotFound`.
      required:
        - partId
      properties:
        partId:
          type: string
          x-sitrep-unit: id
    ExperimentBreakdownEntry:
      type: object
      description: |-
        One entry in the `science.experimentBreakdown` channel payload, a
        per-SUBJECT rollup of the same stored ScienceData rows `science.experiments`
        lists one-row-per-blob, the new home for the old GonogoTelemetry-only
        `sci.experimentBreakdown` enrichment (which had no equivalent on the base
        wire until now). `ExperimentBreakdownEntry.biome`/
        `ExperimentBreakdownEntry.situation` are parsed straight off
        `ScienceData.subjectID` via KSP's own
        `ScienceUtil.GetExperimentFieldsFromScienceID` (confirmed via decompile:
        public static, splits the subject id it was built from rather than
        re-deriving from the vessel's CURRENT position, so a subject collected
        earlier in the flight keeps its own original biome/situation).
        `ExperimentBreakdownEntry.remainingPotential` is the ABSOLUTE science still
        recoverable from the subject (`ScienceSubject.scienceCap -
        ScienceSubject.science`, via `ResearchAndDevelopment.GetSubjectByID`),
        matching the old GonogoTelemetry semantics: `0` in Sandbox mode (no R&D
        instance, no subject caps to speak of). The channel payload is a BARE ARRAY
        (`ExperimentBreakdownEntry[]`) or `null`: never a wrapper object, and never
        an empty-vs-absent distinction beyond "the whole array is null when there's
        no active vessel / the vessel carries no stored science data" (mirrors
        `ExperimentEntry`'s convention). One row per DISTINCT subject id: multiple
        stored blobs for the same subject (e.g. two crew reports from the same
        biome) collapse into one entry with `ExperimentBreakdownEntry.dataMits`
        summed across them.

        **Typing-only mirror** of
        `Sitrep.Host.ScienceViewProvider.BuildExperimentBreakdownEntry`: see
        `ExperimentEntry` for the "no wire change, all fields nullable" rationale.
      properties:
        subjectId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        biome:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        situation:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        expTitle:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        dataMits:
          type:
            - number
            - "null"
          x-sitrep-unit: Mit
          description: |-
            Summed `ScienceData.dataAmount` (mits) across every stored blob for this
            subject.
        remainingPotential:
          type:
            - number
            - "null"
          x-sitrep-unit: science
          description: |-
            Absolute science still recoverable from this subject (`scienceCap -
            science`); `0` outside Career/Science mode.
        valueModel:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Which value model produced `ExperimentBreakdownEntry.remainingPotential`,
            and which unit `ExperimentBreakdownEntry.dataMits` is really in. See
            ScienceValueModels.

            Stock's rollup is a snapshot: one summed data figure and one "how much is
            left". A provider with a full per-subject ledger (collected vs retrieved,
            in-flight split, times completed) projects it down to those two and carries
            the ledger in `ExperimentBreakdownEntry.extensions`: stock's pair is a lossy
            VIEW of the richer set, never the other way round.
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag, per-subject-rollup half. Same
            mechanism and same rule as `ExperimentEntry.extensions`.
          additionalProperties: true
    ExperimentEntry:
      type: object
      description: |-
        One entry in the `science.experiments` channel payload, a single science
        module (or a container holding stored results) on the ACTIVE vessel. The
        channel payload is a BARE ARRAY of these (`ExperimentEntry[]`) or `null`:
        never a wrapper object, and never an empty-vs-absent distinction beyond "the
        whole array is null when there is no active vessel / the sub-group could not
        be built" (see `Sitrep.Host.ScienceViewProvider`).

        **Typing-only mirror.** This type reproduces, field-for-field, the exact
        serialized shape `Sitrep.Host.ScienceViewProvider.BuildExperimentEntry`
        already emits (same names, same camelCase wire keys via
        `RtConfig.CamelCaseForProperties`, same units). It is NOT serialized itself:
        the wire is written by `JsonWriter` walking the provider's dictionary: so
        adding it changes no bytes. Every field is nullable because each is read
        through `SnapshotDict.Get*`, which yields `null` (not a sentinel) whenever
        the raw value is absent or non-finite.
      properties:
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        location:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            "experiment" (a live science module) or "container" (a part storing
            collected results).
        experimentId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        subjectId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        dataAmount:
          type:
            - number
            - "null"
          x-sitrep-unit: Mit
        scienceValueRatio:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
        baseTransmitValue:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        transmitBonus:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            Transmit-value multiplier, 0..1. Every `xmitDataScalar` across the installed
            part cfgs is at most 1.0, so this is a bounded ratio, not an open-ended
            dimensionless number.
        labValue:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        deployed:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        inoperable:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        situation:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        valueModel:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Which value model produced `ExperimentEntry.scienceValueRatio` /
            `ExperimentEntry.baseTransmitValue` / `ExperimentEntry.labValue`, and which
            unit `ExperimentEntry.dataAmount` is really in. See ScienceValueModels for
            why this exists and why the vocabulary is open.

            **The one field on this payload that is not simply "what the game says".** A
            provider whose data is not in mits leaves `ExperimentEntry.dataAmount` null
            rather than putting a megabyte figure in a mits-typed field: a field's unit
            is compile-time-baked here and cannot vary by elected provider, so the
            honest move is absence plus the real figure in the provider's own
            `ExperimentEntry.extensions` namespace.
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag: how a science backend carries a
            per-experiment field this shared shape does not declare, WITHOUT a PR
            against this file. See ProviderExtensionBagAttribute for the whole
            mechanism.

            This is where everything a richer science model knows that stock has no
            concept of belongs: storage capacity, file-vs-sample, transmit rate,
            per-unit science rate. Adding those as nullable members here instead is the
            hand-curated-superset anti-pattern the bag replaces
            (`Sitrep.Host.Tests.ScienceProviderExtensionRatchetTests` holds that line).
          additionalProperties: true
    FireCrewArgs:
      type: object
      description: |-
        `career.crew.fire`'s args: a hired kerbal's `ProtoCrewMember.name`, the same
        id the READ side emits for each roster row (`spaceCenter.crewRoster`'s
        entries). Firing (`KerbalRoster.SackAvailable`) costs nothing and simply
        returns the kerbal to the applicant pool, so it is reversible (a re-hire
        brings them back with the same stats). Valid only on a kerbal whose current
        roster standing is Available; a name that doesn't resolve on the hired-crew
        roster comes back `CommandErrorCode.NotFound`, one that resolves but isn't
        Available (Assigned/Dead/Missing) comes back
        `CommandErrorCode.ModeUnavailable`.
      required:
        - kerbalName
      properties:
        kerbalName:
          type: string
          x-sitrep-unit: id
    FleetSilence:
      type: object
      description: |-
        The fleet-wide silence roster on `fleet.silence`: every vessel the tracker
        holds a reckoning for, in one payload.

        **Why this exists when `silence.<guid>.state` already does.** A per-vessel
        topic can only be read by something that already knows which vessel to ask
        for, which makes it unusable as the input to anything that has to work the
        fleet out for itself. Concretely: a contribution declares its dependencies
        STATICALLY at module load, so no contribution can name a per-guid topic, and
        the client-side bridge that reaches those topics only holds vessels some
        component is ALREADY subscribed to. A fan-out over that bridge sees exactly
        the vessels a widget had already rendered, which is circular. One static
        topic carrying every entry breaks the circle: a Processor declares it once,
        derives once per frame, and a contribution fans out over entries that
        genuinely exist.

        **Delayed on the MAIN node, and that is a real difference.**
        `FleetVesselSilence` rides the per-vessel node, so each vessel's reckoning
        arrives on that vessel's own light-time and is freeze-exempt. A single
        aggregate cannot do that: one payload has one node and one delay. So this
        rides the main node's delay, exactly as `SystemVessels` does while carrying
        per-vessel `CommsConnected` alongside the per-subject-delayed
        `FleetVesselContact`. The per-vessel topic stays authoritative for one
        vessel on that vessel's own clock; this is the fleet-wide index. A consumer
        that needs the former must not substitute the latter.
      required:
        - vessels
      properties:
        vessels:
          type: array
          items:
            $ref: "#/components/schemas/FleetSilenceEntry"
    FleetSilenceEntry:
      type: object
      description: |-
        One vessel's reckoning inside the fleet-wide `FleetSilence` roster: the same
        fields `FleetVesselSilence` carries, plus the vessel id that the per-vessel
        topic gets from its own topic string.
      required:
        - vesselId
        - state
      properties:
        vesselId:
          type: string
          x-sitrep-unit: id
          description: |-
            Stable subject id (KSP vessel GUID), the same id the `fleet.` and `silence.`
            namespaces key on.
        state:
          type: string
          x-sitrep-unit: enum
          description: One of `Nominal` / `Silent` / `Lost`.
        silenceSinceUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: UT the current silence run began. Null while Nominal.
        deadlineUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            UT at which this silence run becomes eligible to be declared Lost. Null
            while Nominal, or for a destroyed vessel.
        deadlineBasis:
          type:
            - string
            - "null"
          x-sitrep-unit: enum
          description: One of `Sitrep.Host.Comms.SilenceDeadlineBasis`. Null while Nominal.
        predictedReacquisitionUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            UT the radio path is predicted to re-open. Null is a prediction WITHHELD,
            never an emergence of "now".
        predictionGraceSeconds:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: |-
            The error budget the deadline was armed with, seconds: how long past the
            predicted return this craft may stay quiet before its silence is something
            other than a late reappearance.

            It is the only thing on the wire that says how much confidence to place in
            `FleetSilenceEntry.predictedReacquisitionUt` beside it. Without it, "back in
            15 min" and "back in 15 min, and we would not call it late for another 5"
            render identically.

            ONE-SIDED, and not a symmetric uncertainty: it is an allowance after the
            predicted moment, so render "allowing 5 min of slack" and never "+/- 5 min".
            Null wherever the prediction is null, since a budget quoted next to a
            withheld prediction is an error bar around nothing.
    FlightCurrent:
      type: object
      description: |-
        The `flight.current` channel payload: a UT-indexed **Value** (LossyLatest +
        DelayRole.Delayed, mirroring every `vessel.*` channel): the authoritative
        "what flight is this, and what phase is it in" reading for whichever vessel
        gonogo is presently reporting as active. That is the vessel the game is
        flying, with one exception: a kerbal on EVA does not become the subject of
        this reading, the craft they stepped out of stays it for as long as that
        craft is in the world. `FlightCurrent.phase` reuses `Situation` rather than
        inventing a parallel enum; see FlightLifecycleSampler doc reference in
        `Sitrep.Host.Flight` for the exact phase source.
      required:
        - flightId
        - vesselId
        - vesselName
        - phase
      properties:
        flightId:
          type: string
          x-sitrep-unit: id
          description: |-
            The mod-minted stable flight id: KSP's `Vessel.id` GUID as a string, the
            same currency `VesselIdentity.VesselId`/`CrashReport.VesselId` already use.
        vesselId:
          type: string
          x-sitrep-unit: id
        vesselName:
          type: string
          x-sitrep-unit: text
        phase:
          description: |-
            The vessel's current flight phase: reuses `Situation`
            (PreLaunch/Flying/Landed/...), not a parallel enum.
          allOf:
            - $ref: "#/components/schemas/Situation"
              x-sitrep-unit: enum
    FlightEndReason:
      type: integer
      description: |-
        The flight-lifecycle domain: retires the client-side `FlightDetector`
        heuristic that reconstructed flight boundaries from `vesselName` +
        `missionTime` + a revert-threshold guess. The producer
        (`Gonogo.KSP.FlightUplink` + `Sitrep.Host.Flight.FlightLifecycleSampler`)
        hooks KSP's flight GameEvents internally and translates them into this clean
        contract: no KSP names ever cross the wire.

        **Crash/recovery stayed separate** (the smaller-blast-radius pick, per the
        spec's build-time TBD): `crash.lastCrash`/ `recovery.lastSummary` keep their
        own rich detail payloads unmodified; `FlightEnded` only carries the coarse
        `FlightEndReason`. `FlightUplink` hooks the SAME
        `onCrash`/`onCrashSplashdown`/`onVesselWillDestroy`/
        `onVesselRecoveryProcessingComplete` GameEvents `CrashUplink`/
        `RecoveryUplink` already hook, independently: zero coupling, zero risk to
        the existing detail streams.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Recovered
        - const: 1
          title: Crashed
        - const: 2
          title: Reverted
        - const: 3
          title: Destroyed
    FlightEnded:
      type: object
      description: |-
        The `flight.ended` channel payload: a Delivery.ReliableOrdered +
        DelayRole.Delayed event, fired once per flight when it stops being trackable
        (recovered, crashed/destroyed, or reverted). Rides the SAME delay class as
        `crash.lastCrash`/`recovery.lastSummary`, so it inherits the already-proven
        revert-before-reveal erasure invariant
        (`RevertBeforeRevealErasesAReliableOrderedDelayedEventForever`, commit
        `82132a08`) for free: no new reveal-gate work needed.
      required:
        - flightId
        - vesselId
        - vesselName
        - reason
        - ut
      properties:
        flightId:
          type: string
          x-sitrep-unit: id
        vesselId:
          type: string
          x-sitrep-unit: id
        vesselName:
          type: string
          x-sitrep-unit: text
        reason:
          $ref: "#/components/schemas/FlightEndReason"
          x-sitrep-unit: enum
        ut:
          type: number
          x-sitrep-unit: ut
          description: |-
            Universal time the flight ended. For `FlightEndReason.Reverted` this is the
            revert-TARGET UT (see `FlightLifecycleSampler`'s revert-epoch-consistency
            doc), not the wall-clock moment the player hit revert.
    FlightSimulation:
      type: object
      description: |-
        The `flight.simulation` channel payload: is this a rehearsal, and is signal
        delay being applied to it.

        **TrueNow, and it has to be.** This is meta about the stream rather than a
        reading from a craft, the same disposition `comms.delay` takes: a channel
        that told an operator "this is a simulation" only after the light-time had
        elapsed would be describing the board they were looking at four minutes ago.

        **Absence is data.** A stock install publishes nothing here, because it has
        nothing to say; see `FlightSimulation.simulated`.
      required:
        - delayApplied
        - delayInSimulation
        - meta
      properties:
        simulated:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the flight on screen is a simulation. Null when the install has no
            such concept; see ISimulationBackend.IsSimulatedFlight for why that is
            different from false.
        delayApplied:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            Whether signal delay is currently being applied to this flight.

            A rehearsal has no spacecraft, so it has no light-time, and by default a
            simulation cuts the delay outright rather than modelling a distance to a
            craft that is not there. A controller may still want the delay on, to
            rehearse under the conditions the real flight will have, which is why it is
            `FlightSimulation.delayInSimulation` below rather than a rule. This field is
            the OUTCOME of those two, so a client can say why the board is live without
            re-deriving it.
        delayInSimulation:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            The operator's standing choice: apply signal delay during a simulation
            anyway. Off by default, for the reason above.

            Carried here so the settings row that changes it can READ what the mod is
            actually doing rather than what a console once asked for. The mod owns this
            value: it is what enforces the delay, and a console preference the enforcer
            never heard would be a switch wired to nothing.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    FlightStarted:
      type: object
      description: |-
        The `flight.started` channel payload: a Delivery.ReliableOrdered +
        DelayRole.Delayed event, fired the moment a genuinely NEW flight begins
        (first-ever observation of a vessel id, or a switch onto a vessel this
        session has never tracked before; see `FlightLifecycleSampler`'s doc comment
        for the exact started-vs- vesselChanged distinction).
      required:
        - flightId
        - vesselId
        - vesselName
        - ut
      properties:
        flightId:
          type: string
          x-sitrep-unit: id
        vesselId:
          type: string
          x-sitrep-unit: id
        vesselName:
          type: string
          x-sitrep-unit: text
        ut:
          type: number
          x-sitrep-unit: ut
          description: |-
            Universal time this flight began: the UUT the sampler first observed the
            vessel active (or the revert-target UT, for a flight started as a revert's
            counterpart).
    FlightVesselChanged:
      type: object
      description: |-
        The `flight.vesselChanged` channel payload: a Delivery.ReliableOrdered +
        DelayRole.Delayed event, fired whenever the operator's active-vessel focus
        moves to a DIFFERENT, already-known vessel
        (docking/undocking/EVA/tracking-station reselect): decoupled from
        `FlightStarted`/`FlightEnded`: switching focus away from a still-flying
        vessel does not end its flight.
      required:
        - flightId
        - vesselId
        - vesselName
        - ut
      properties:
        flightId:
          type: string
          x-sitrep-unit: id
        vesselId:
          type: string
          x-sitrep-unit: id
        vesselName:
          type: string
          x-sitrep-unit: text
        previousVesselId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            The vessel id the operator's focus moved FROM, null on the very first
            observation (nothing to switch away from).
        ut:
          type: number
          x-sitrep-unit: ut
    FuelCellEntry:
      type: object
      description: |-
        One fuel cell in the `parts.power` payload's `fuelCells` array. Typing-only
        mirror of `Sitrep.Host.PartsViewProvider.BuildFuelCellEntry`.
      properties:
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        active:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        status:
          type:
            - string
            - "null"
          x-sitrep-unit: text
    GameDlc:
      type: object
      description: |-
        The `game.dlc` channel payload, which KSP expansions ("DLC") are installed,
        produced by `Sitrep.Host.SystemViewProvider.BuildGameDlc`. This is the
        `Meta.Dlc` path: a ground-side, scene-independent game fact (the install has
        the expansion or it doesn't), NOT a per-tick capture flag. It lets a widget
        distinguish "the player has no DLC" from "the DLC is present but nothing is
        deployed yet", chiefly `DeployedScience`, which reads Breaking Ground.

        Mirrors the exact serialized shape `SystemViewProvider.BuildGameDlc` emits
        (a wrapper object `{ "breakingGround": bool, "makingHistory": bool }`); it
        is a typing/codegen marker so a widget resolves a real payload type instead
        of `unknown`, and does NOT participate in serialization (the provider emits
        the live value tree that `JsonWriter` walks; see SitrepTopicAttribute). The
        whole payload is `null` (not an all-false object) when no sample has landed
        yet, the provider's "no data yet" vs. "DLC genuinely absent" distinction.

        Same `game`/`system`-domain convention as `SystemBodies`: no per-payload
        `Meta` field, its `Meta` rides the envelope (`StreamData.Meta`), never the
        payload body. This is a ground-side fact, so its Topic is
        `DelayRole.TrueNow`: DLC presence is known independent of any vessel's comms
        link.
      required:
        - breakingGround
        - makingHistory
      properties:
        breakingGround:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            Whether the Breaking Ground expansion ("Serenity") is installed, deployed
            science, robotics, surface features.
        makingHistory:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            Whether the Making History expansion is installed, mission builder, extra
            parts.
    GameMode:
      type: integer
      description: |-
        The game's save mode, mirroring KSP's `Game.Modes`, the ground-side fact
        that decides which career surfaces (funds, tech tree, contracts, strategies,
        facility upgrades) are even meaningful. Distinct from `CareerStatus`: that
        payload is `null` in sandbox/science (no `Funding`/`ContractSystem` to
        read), so it can't carry the mode, a save can be in `GameMode.Sandbox` or
        `GameMode.Science` and still need widgets to know which one. Hence
        `career.mode` is its OWN topic, emitted in ALL modes.

        KSP's `Game.Modes` also has `SCENARIO`, `SCENARIO_NON_RESUMABLE`, `MISSION`
        and `MISSION_BUILDER`; none map to a distinct player-career surface, so
        `Sitrep.Host.CareerViewProvider.ParseGameMode` folds them (and any future
        KSP addition) into `GameMode.Unknown` rather than the mapper throwing.
        `SCIENCE_SANDBOX` maps to `GameMode.Science`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Sandbox
        - const: 1
          title: Career
        - const: 2
          title: Science
        - const: 3
          title: Unknown
    HireApplicantArgs:
      type: object
      description: |-
        `career.crew.hire`'s args: the applicant's `ProtoCrewMember.name`, the same
        id the READ side emits for each applicant (`spaceCenter.astronautComplex`'s
        `applicants[].name`), so a client hires the applicant it read. Hiring debits
        the current recruit cost from funds and moves the applicant into the crew
        roster. An applicant that has left the pool since (a stale pool, someone
        else hired, KSP refreshed it) comes back `CommandErrorCode.NotFound`; an
        unaffordable hire `CommandErrorCode.Range`; a full roster (Astronaut Complex
        cap) or a non-career save `CommandErrorCode.ModeUnavailable`.
      required:
        - applicantName
      properties:
        applicantName:
          type: string
          x-sitrep-unit: id
    InboundFrame:
      description: |-
        Every frame the mod sends, discriminated on `type`.

        The first thing a client writes is a router over these four, and they are the
        whole of the inbound surface: `stream-data` carries a subscribed topic's value,
        `command-response` answers a dispatch, `event` reports something that happened
        to a subscription, and `error` reports a request that could not be carried.
        Every per-channel message below narrows one of these; none of them adds a fifth
        `type`.

        Each arm pins `type` with a `const`, and that is what the discrimination is
        made of: a plain JSON Schema validator picks the right arm from the constants
        alone. The `discriminator` beside this is the AsyncAPI Schema keyword, a bare
        string naming that property, and it is a LABEL on the constants rather than the
        mechanism. Two things follow. It is not OpenAPI 3's `discriminator` object, so
        `propertyName` and `mapping` do not belong here and the object form does not
        validate. And a JSON Schema tool that has never heard of it loses nothing,
        because the constants are still there.
      discriminator: type
      oneOf:
        - $ref: "#/components/schemas/StreamData"
        - $ref: "#/components/schemas/CommandResponse"
        - $ref: "#/components/schemas/EventMsg"
        - $ref: "#/components/schemas/ErrorMsg"
    InstrumentEntry:
      type: object
      description: |-
        One entry in the `science.instruments` channel payload, a single
        `ModuleScienceExperiment` on the ACTIVE vessel, captured as an INVENTORY /
        status row keyed by `InstrumentEntry.partId` (the part's KSP `flightID`).
        This is distinct from `ExperimentEntry`: `science.experiments` walks the
        same modules but yields one row per STORED `ScienceData` result (a module
        with no data produces no row), whereas `science.instruments` yields one row
        per module regardless of whether it currently holds data, the operability
        picture (deployed / inoperable / rerunnable / resettable / collectable) an
        operator needs to decide what to run next. The channel payload is a BARE
        ARRAY (`InstrumentEntry[]`) or `null`. Typing-only mirror of
        `Sitrep.Host.ScienceViewProvider.BuildInstrumentEntry`: see
        `ExperimentEntry` for the "no wire change, all fields nullable" rationale.
      properties:
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            The part's KSP `flightID` (stringified): the stable join key for this
            instrument.
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        experimentId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        deployed:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        inoperable:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        rerunnable:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        resettable:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        dataIsCollectable:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag, instrument half. Same mechanism and
            same rule as `ExperimentEntry.extensions`. This payload carries no
            value-model-dependent number (it is pure operability), so it takes the bag
            but no `valueModel` tag.

            Stock's operability picture is a flat pair of bools
            (`InstrumentEntry.deployed`/`InstrumentEntry.inoperable`). A provider that
            models running as a state machine with a reason ("shrouded", "no EC",
            "sample depleted") projects it down to those bools and carries the state and
            the reason here.
          additionalProperties: true
    InventoryItem:
      type: object
      description: One kind of thing stored in an `InventoryStore`, with how many of it.
      required:
        - name
        - quantity
      properties:
        name:
          type: string
          x-sitrep-unit: id
          description: |-
            `AvailablePart.name`, the config id (e.g. `"evaRepairKit"`). The id a
            consumer matches on, never the title, which is localised.
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: The part's display title, already localised by KSP.
        quantity:
          type: number
          x-sitrep-unit: count
          description: How many of this kind are in this store, summed across slots.
        packedVolume:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Packed volume of ONE of these, same unit as the store's limits.
    InventoryStore:
      type: object
      description: "One part's `ModuleInventoryPart`: a cargo hold aboard."
      required:
        - partId
        - partName
        - items
      properties:
        partId:
          type: string
          x-sitrep-unit: id
          description: |-
            `Part.flightID` stringified, so a store id-joins to `vessel.parts` and the
            other per-part channels.
        partName:
          type: string
          x-sitrep-unit: text
          description: The part's display title.
        items:
          type: array
          items:
            $ref: "#/components/schemas/InventoryItem"
          description: |-
            What is actually in it. Empty is meaningful and is NOT the same as an absent
            store: an empty hold is a place to put something.
        slots:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            `ModuleInventoryPart.InventorySlots`. Null when the module did not report
            one.
        slotsUsed:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Slots with something in them, so a consumer can say "2 of 4" without summing
            quantities that share a slot.
        packedVolumeLimit:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: |-
            `packedVolumeLimit`, in KSP's own cargo-volume unit rather than cubic
            metres: it is a config number (a kerbal is 40, a repair kit is 5) and
            presenting it as a physical volume would be inventing a dimension the game
            does not attach to it.
        packedVolumeUsed:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: |-
            Packed volume currently used, same unit as
            `InventoryStore.packedVolumeLimit`.
        massLimit:
          type:
            - number
            - "null"
          x-sitrep-unit: t
          description: |-
            `massLimit` in tonnes. A kerbal's is 0.065, which is the binding constraint
            on how much they can carry long before slots are.
    InvokePartActionArgs:
      type: object
      description: |-
        Args for `vessel.invokePartAction`: fire one button of one part's
        right-click Part Action Window, the remote-control equivalent of the player
        clicking it in-game.

        This is an actuation of a part ON the craft, so the command rides light-time
        (`Delayed = true`) exactly like `vessel.control.*` and the robotics commands
        it is modelled on.

        **No state field, unlike every other actuation command.** The contract's
        usual discipline is "absolute set, never toggle" (see
        `ServoSetEnabledArgs`), but a `BaseEvent` has no settable value: KSP models
        these as fire-this-button, and the button's own label is what changes
        ("Deploy" becomes "Retract"). So this command is a pure invoke, in the same
        position as `robotics.rotor.reverse`: the lone stateless member of its
        family, for a reason that comes from KSP rather than from convenience. The
        operator's read-back is the `vessel.partActions.<flightId>` channel
        re-reporting the new button set one light-time later.
      required:
        - partId
        - eventName
      properties:
        partId:
          type: string
          x-sitrep-unit: id
          description: |-
            The part's `flightID.ToString()`: the same id the read side stamps on
            `PartActions.partId` and `VesselPart.id`, so a widget round-trips the exact
            id it already holds with no correlation step. An id that no longer resolves
            (the part was staged away, undocked, or the vessel unloaded) comes back
            `CommandResult.errorCode` `CommandErrorCode.NotFound` rather than silently
            doing nothing.
        eventName:
          type: string
          x-sitrep-unit: id
          description: |-
            The `PartActionEntry.name` of the button to fire (`BaseEvent.name`, the
            stable code id, NEVER the localized `PartActionEntry.label`). An event name
            the resolved part no longer exposes comes back
            `CommandErrorCode.ModeUnavailable`: the part is there but that button is
            not, which is a genuinely different failure from an unresolvable part.
    IsruConverterEntry:
      type: object
      description: |-
        One chemical converter on the active vessel. Field set matches stock's
        surface: whether it is running, and the recipe it is running, at live rates.
      required:
        - inputs
        - outputs
      properties:
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        partTitle:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        running:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: Actively converting this tick.
        inputs:
          type: array
          items:
            $ref: "#/components/schemas/IsruResourceFlow"
          description: |-
            Recipe inputs at their live rate. Empty list, not null, when the converter
            carries no recipe.
        outputs:
          type: array
          items:
            $ref: "#/components/schemas/IsruResourceFlow"
          description: Recipe outputs at their live rate.
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag, converter half. Same mechanism and
            same rule as `IsruDrillEntry.extensions`.

            Note what does NOT belong here: a blocking-reason string for a starved
            recipe. A converter that is on but moving nothing is already fully described
            by `IsruConverterEntry.running` true alongside zero rates, so a reader
            derives that condition from the shared fields. Inventing an issue field
            would mean fabricating a diagnostic no engine actually reports.
          additionalProperties: true
    IsruDrillEntry:
      type: object
      description: |-
        One drill (resource harvester) on the active vessel. The field set is
        deliberately exactly what stock ISRU has: resource, abundance, rate, deploy,
        running, plus the two identification fields every list-shaped payload in
        this contract carries. It is not "stock's fields with nulls for what a
        richer mod does", it is the literal intersection, and the intersection
        happens to be everything stock has. Anything one provider knows and another
        does not goes in `IsruDrillEntry.extensions`.
      properties:
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Part.flightID stringified: the same join key
            vessel.parts/parts.power/reliability.parts use.
        partTitle:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: Part.partInfo.title, for display without a vessel.parts join.
        resource:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Resource this drill extracts (e.g. "Ore"). Free text, not a closed enum:
            whatever the running install's configs and profiles name, the same posture
            every other resource-identity field in this contract takes.
        deployed:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Drill head deployed. Null for a harvester with no deploy animation, e.g.
            some asteroid drills.
        running:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: Actively extracting this tick.
        abundance:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            Local abundance of `IsruDrillEntry.resource` at the drill's current
            position, 0..1. Stock reads the same resource map the right-click PAW does.
            A mod that samples its own abundance reports that instead, and for asteroid
            or comet mining the remaining-mass ratio of the source rock lands here: the
            same 0..1 shape from a different source. Null when the backend has no
            abundance concept for this harvest type.
        rate:
          type:
            - number
            - "null"
          x-sitrep-unit: units/s
          description: |-
            EFFECTIVE current extraction rate, already abundance- and
            efficiency-adjusted rather than the static config rate, so it matches what
            the part's own readout shows. Zero rather than null when
            `IsruDrillEntry.running` is false: the drill genuinely extracts nothing,
            which is a number, not an absence.
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag: how an ISRU backend carries a
            per-drill field this shared shape does not declare, WITHOUT a PR against
            this file. See ProviderExtensionBagAttribute for the whole mechanism. Null
            for the vanilla backend, which has nothing stock does not already say. A
            blocking-reason string, an EC draw, an asteroid's remaining mass: all of
            those belong here rather than as nullable members above.
          additionalProperties: true
    IsruResourceFlow:
      type: object
      description: |-
        One resource flow in a converter's recipe, input or output side. The rate is
        live (already scaled by whatever the part's current efficiency or capacity
        multiplier is), not the raw recipe ratio, so an operator reads what is
        actually moving rather than what the config asked for.
      properties:
        resource:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        rate:
          type:
            - number
            - "null"
          x-sitrep-unit: units/s
    KspEditorFacility:
      type: integer
      description: |-
        KSP's `EditorFacility`: which editor a craft was built in. Behind
        `spaceCenter.savedShips[].facilityOrdinal`, beside the name in
        `SavedShipEntry.facility`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: None
        - const: 1
          title: VAB
        - const: 2
          title: SPH
    KspParameterState:
      type: integer
      description: |-
        KSP's `Contracts.ParameterState`: whether one objective of a contract is
        done. Behind `career.status.contracts[].parameters[].stateOrdinal`, beside
        the name in `CareerContractParameter.state`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Incomplete
        - const: 1
          title: Complete
        - const: 2
          title: Failed
    KspPartCategory:
      type: integer
      description: |-
        KSP's `PartCategories`: the editor category a part filters into. Behind
        `vessel.parts[].categoryOrdinal`, beside the name in `VesselPart.category`.

        `KspPartCategory.none` is `-1`, not `0`, so this enum is NOT dense from zero
        and the client cannot resolve it with the array-walking `namesOf`. The
        lower-case spelling is KSP's; `.ToString()` on that member yields `"none"`
        and the wire carries exactly that.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Propulsion
        - const: 1
          title: Control
        - const: 2
          title: Structural
        - const: 3
          title: Aero
        - const: 4
          title: Utility
        - const: 5
          title: Science
        - const: 6
          title: Pods
        - const: 7
          title: FuelTank
        - const: 8
          title: Engine
        - const: 9
          title: Communication
        - const: 10
          title: Electrical
        - const: 11
          title: Ground
        - const: 12
          title: Thermal
        - const: 13
          title: Payload
        - const: 14
          title: Coupling
        - const: 15
          title: Cargo
        - const: 16
          title: Robotics
        - const: -1
          title: none
    KspRosterStatus:
      type: integer
      description: |-
        KSP's `ProtoCrewMember.RosterStatus`: a kerbal's standing in the roster.
        Behind `spaceCenter.crewRoster[].situationOrdinal`, beside the name in
        `CrewRosterEntry.situation`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Available
        - const: 1
          title: Assigned
        - const: 2
          title: Dead
        - const: 3
          title: Missing
    KspSpaceCenterFacility:
      type: integer
      description: |-
        KSP's `SpaceCenterFacility`: one building at the space centre. Behind
        `career.status.facilities[].facilityOrdinal` and
        `LimitBreach.facilityOrdinal`.

        `career.status.facilities` is keyed by the NAME rather than the ordinal, and
        stays that way: rekeying the map would be a breaking retype and would change
        the shape of every consumer's key walk. The ordinal rides inside each entry
        instead, so a client can branch on it without trusting the key it arrived
        under.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Administration
        - const: 1
          title: AstronautComplex
        - const: 2
          title: LaunchPad
        - const: 3
          title: MissionControl
        - const: 4
          title: ResearchAndDevelopment
        - const: 5
          title: Runway
        - const: 6
          title: TrackingStation
        - const: 7
          title: SpaceplaneHangar
        - const: 8
          title: VehicleAssemblyBuilding
    LabEntry:
      type: object
      description: |-
        One entry in the `science.lab` channel payload, a Mobile Processing Lab on
        the active vessel. The channel payload is a BARE ARRAY (`LabEntry[]`) or
        `null`. Typing-only mirror of
        `Sitrep.Host.ScienceViewProvider.BuildLabEntry`: see `ExperimentEntry` for
        the "no wire change, all fields nullable" rationale.
      properties:
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        dataStored:
          type:
            - number
            - "null"
          x-sitrep-unit: Mit
        dataStorage:
          type:
            - number
            - "null"
          x-sitrep-unit: Mit
        storedScience:
          type:
            - number
            - "null"
          x-sitrep-unit: science
        processingData:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        statusText:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        scientistCount:
          type:
            - number
            - "null"
          x-sitrep-unit: count
        scienceRate:
          type:
            - number
            - "null"
          x-sitrep-unit: science/day
          description: |-
            Science generated per GAME-DAY, not per second. The host reads this off
            `ModuleScienceConverter.CalculateScienceRate`, whose decompile multiplies
            the per-tick rate by a full day.
        isOperational:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        valueModel:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Which value model produced `LabEntry.scienceRate` and
            `LabEntry.storedScience`, and which unit `LabEntry.dataStored` /
            `LabEntry.dataStorage` are really in. See ScienceValueModels.

            A lab is not the same KIND of thing under every model. Stock's is terminal:
            it turns stored data into science per game-day and you are done. A provider
            whose lab is an intermediate pipeline stage (analysing a sample into a
            transmissible file, which then still has to be sent) produces NO science
            itself, leaves `LabEntry.scienceRate` null, and carries its own rate in
            `LabEntry.extensions`. Without this tag a widget cannot tell that null apart
            from "idle".
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag, lab half. Same mechanism and same
            rule as `ExperimentEntry.extensions`.
          additionalProperties: true
    LaunchArgs:
      type: object
      description: |-
        `ksp.launch`'s args: load a saved craft onto a launch site. The craft is
        identified by `LaunchArgs.shipName` plus the `LaunchArgs.facility` it was
        saved from (`"VAB"`/`"SPH"`, case-insensitive, the host bridges it to KSP's
        `EditorFacility` and rebuilds the on-disk `.craft` path server-side, so the
        wire never carries a native KSP type or an absolute path). An empty ship
        name or an unrecognised facility fails admission
        (`CommandErrorCode.NotFound`/`CommandErrorCode.Range`) before the game is
        ever touched.

        `LaunchArgs.crew` is a real array of kerbal names (empty = launch unmanned),
        NOT the legacy semicolon-joined blob the old action string used: the command
        surface is JSON, so the client unwinds its `;`-encoded crew list back into
        an array before dispatching and the host assigns each name into a free craft
        seat.
      required:
        - shipName
        - facility
        - site
        - crew
      properties:
        shipName:
          type: string
          x-sitrep-unit: text
        facility:
          type: string
          x-sitrep-unit: text
          description: |-
            `"VAB"` or `"SPH"` (case-insensitive). Any other value yields
            `CommandResult.errorCode` `CommandErrorCode.Range`.
        site:
          type: string
          x-sitrep-unit: text
        crew:
          type: array
          items:
            type: string
            x-sitrep-unit: text
          description: Kerbal names to seat, in order. Empty = launch unmanned.
    LaunchSiteEntry:
      type: object
      description: |-
        One launch site in the `spaceCenter.launchSites` channel, the union of the
        stock KSC pad + runway, any Making History sites, and any Kerbal Konstructs
        sites (KK registers its sites into `PSystemSetup.Instance.LaunchSites` via
        the public `AddLaunchSite` API, so enumerating that one list already covers
        all three, no reflection, no hard KK link). Produced by
        `Sitrep.Host.SpaceCenterViewProvider.BuildLaunchSites`.

        The channel is a BARE ARRAY of these entries (tagged `isArray: true`, like
        the `science.*` channels), NOT a wrapper object and NOT a KSC singleton: KSP
        has many launch sites, keyed by `LaunchSiteEntry.name`. The whole payload is
        `null` (not an empty array) when no sample has landed yet, the provider's
        "no data yet" vs. "zero sites" distinction.

        Mirrors the exact per-site dict the provider emits, same field names, casing
        and nullability; a TS-shape-only typing/codegen marker (no `Meta`, same
        `system`/`spaceCenter`-domain convention as `SystemBodies`: the provider
        hand-builds the dict and `JsonWriter` walks that live tree, these POCOs
        never serialize). Classified `DelayRole.TrueNow`: ground-side facts, known
        independent of any vessel's comms link, same class as `SystemBodies` /
        `GameDlc`.
      properties:
        name:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Internal launch-site id (`LaunchSite.name`): the stable key used with
            `PSystemSetup`'s lookup APIs; null when the live game hasn't populated it.
        displayName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Human-facing display name (`PSystemSetup.GetLaunchSiteDisplayName`, falling
            back to `LaunchSite.launchSiteName`).
        editorFacility:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Which editor this site launches from (the pad-vs-runway distinction) as the
            `EditorFacility` enum name (`"None"`/`"VAB"`/`"SPH"`); a VAB site is a pad,
            an SPH site a runway.
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index into `SystemBodies` of the body this site sits on; null when absent or
            unresolved (never a sentinel like -1).
        latitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Latitude of the site's spawn point on its body
            (`LaunchSite.SpawnPoint.latlonaltSet`); null when the site has no set spawn
            coordinate (never a fabricated `0`). Pairs with `LaunchSiteEntry.longitude`
            to give the site a location for the command-delay geometry (a launch is a
            command to this location).
        longitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Longitude of the site's spawn point on its body; null when the site has no
            set spawn coordinate. Pairs with `LaunchSiteEntry.latitude`.
        isStock:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether this is a stock KSP launch site (`PSystemSetup.IsStockLaunchSite`),
            false for Making History / Kerbal Konstructs sites.
        padOccupied:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether a vessel is currently sitting on this pad. There is no clean stock
            per-site occupancy API, so for now this is populated ONLY on the stock KSC
            pad, derived from the active vessel being in the PRELAUNCH situation; every
            other site carries null (per-site true occupancy is a follow-up).
        padVesselTitle:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Name of the vessel occupying this pad, when derivable (depends on
            `LaunchSiteEntry.padOccupied`); null until per-site occupancy exists beyond
            the stock-pad PRELAUNCH derivation.
    LimitBreach:
      type: object
      description: |-
        The comparison behind a `GateOutcome.Fail`: the limit and the actual value,
        never a verdict on its own.

        "Too heavy" does not tell an operator whether to shed 200 kg or redesign.
        Carrying both numbers lets the CLIENT compose "1.4 t over the 18 t Launch
        Pad limit" through its own unit rendering, rather than the mod baking an
        English sentence in one unit system.
      required:
        - facility
        - facilityName
        - facilityLevel
        - quantity
        - unit
      properties:
        facility:
          type: string
          x-sitrep-unit: id
        facilityName:
          type: string
          x-sitrep-unit: text
          description: |-
            The facility's name as the GAME writes it ("Astronaut Complex"), for the
            sentence an operator reads. Empty when the producer had no display name to
            hand.

            `LimitBreach.facility` beside it is the raw `SpaceCenterFacility` member
            name, which is an id and reads like one. Nothing else on the wire publishes
            the display name, so without this the client would have to keep its own
            English mapping of KSP's enum: a second source of truth, wrong in every
            other language, and stale the moment KSP adds a facility.
        facilityLevel:
          type: number
          x-sitrep-unit: ratio
          description: Normalised facility level, as KSP reports it. Not a tier index.
        quantity:
          type: string
          x-sitrep-unit: id
        limit:
          type:
            - number
            - "null"
          x-sitrep-unit: n/a
          description: |-
            The limit, in whatever unit `LimitBreach.quantity` implies. NULL when the
            facility is unlimited.

            Never the sentinel. KSP returns `float.MaxValue` (and `int.MaxValue`, and a
            `Vector3` of them) at maximum level, and 3.4e38 rendered beside a craft mass
            is not "unlimited", it is a bug that reads as a units error. No limit is the
            ABSENCE of a limit. A breach with no limit should be unreachable, since
            nothing can exceed an unlimited limit.
        actual:
          type:
            - number
            - "null"
          x-sitrep-unit: n/a
          description: What the call actually asked for, same unit as `LimitBreach.limit`.
        unit:
          type: string
          x-sitrep-unit: id
          description: |-
            The unit token `LimitBreach.limit` and `LimitBreach.actual` are in, e.g. `t`
            for a mass limit, `count` for a part count.

            Carried as DATA because one breach type serves limits with different
            dimensions: a static `[SitrepUnit]` on those two properties cannot be right
            for all of them, and the unit gate says plainly that a wrong unit is worse
            than a bare readout because the client will confidently mislabel it. So they
            declare `NotApplicable` and their real unit travels here, which is also what
            lets the client render the comparison in the operator's own units instead of
            the mod composing a sentence.
    ManeuverFrame:
      type: integer
      description: |-
        The basis a planned burn's delta-v components are expressed in. On the wire
        because the two in use are similar enough to be mistaken for each other and
        different enough to be wrong, and the distinction previously lived only in
        `ManeuverNode`'s prose.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: RadialNormalPrograde
          description: |-
            KSP's own maneuver-node basis: radial-out, normal, prograde, taken against
            the patch the node sits on at its own UT.
        - const: 1
          title: TangentNormalBinormal
          description: |-
            The Frenet trihedron of the trajectory at the burn point: tangent, normal,
            binormal. Not a renaming of `ManeuverFrame.RadialNormalPrograde`: the axes
            differ, and for an eccentric orbit they differ by an amount that matters.
        - const: 2
          title: Unknown
          description: Graceful fallback, same role as `TransitionType.Unknown`.
    ManeuverFrameReference:
      type: integer
      description: |-
        What a burn's basis is measured relative to.

        The same four the read-frame side names, deliberately. A frame an operator
        picked to READ a trajectory in and a frame a burn was PLANNED in are the
        same kind of thing, and giving them separate vocabularies would make "is
        this burn in the frame I am looking at" a question nobody could answer
        without a translation table.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Unspecified
          description: |-
            Not stated. Zero so a planner that says nothing is not read as having
            claimed a frame.
        - const: 1
          title: FollowControlFrame
          description: Whatever the craft's own control frame currently is.
        - const: 2
          title: BodyCentredInertial
          description: Non-rotating, centred on a body.
        - const: 3
          title: ParentDirection
          description: Aligned with the direction to a body's parent.
        - const: 4
          title: RotatingPulsating
          description: Rotating with two primaries, with lengths that pulsate.
    ManeuverNode:
      type: object
      description: |-
        One planned BURN. NAMED delta-v components in a NAMED frame
        (`ManeuverNode.frame`), with the impulsive case as the one where
        `ManeuverNode.ignitionUt` and `ManeuverNode.cutoffUt` are absent rather than
        equal.

        Kills O-4: the legacy `o.addManeuverNode[ut, x, y, z]` (where `[x,y,z]` is
        secretly `[radialOut, normal, prograde]`, with `updateManeuverNode`
        prepending an `id` that shifts every subsequent index by one, and a THIRD,
        different display order) is the textbook arg-order footgun this named shape
        makes impossible to mis-order.

        **Why this is a burn and not a stock node.** A stock node is an
        instantaneous impulse and real burns are not, which stock KSP itself
        concedes by computing `DeltaVStageInfo.stageBurnTime` and by carrying a
        burn-time readout on its own navball. Every serious maneuver mod in the
        ecosystem then reimplements the same correction independently, because the
        stock type has nowhere to put it. The three instants here are that
        nowhere-to-put-it, filled in.

        **The impulsive case is absent duration, never zero duration.** A
        zero-duration burn with a thrust implies infinite acceleration, so any
        consumer computing thrust times duration over mass gets nonsense instead of
        an impulse. Absence says "not modelled", which is the true statement.
      required:
        - id
        - ut
        - patches
      properties:
        id:
          type: string
          x-sitrep-unit: id
          description: |-
            Stable, opaque id: the M3 R3 fix for the read/write correlation gap
            (`packages/sitrep-client/src/map-command.ts`'s `KNOWN_COMMAND_GAPS`
            comment): assigned by `Gonogo.KSP.KspHost` via a shared
            `ReferenceIdRegistry<global::ManeuverNode>` (see that class's doc comment
            for the full scheme), the SAME instance `KspVesselActuator` uses to resolve
            `vessel.maneuver.update`/ `.remove`'s `nodeId` argument: so a node's id
            round-trips into those commands whether the node was created through
            `vessel.maneuver.add` or placed by hand in the map view. Empty string only
            for a node read off a recording captured BEFORE this field existed (replay
            of old data; never a live capture).
        ut:
          type: number
          x-sitrep-unit: ut
          description: |-
            The instant the burn's IMPULSIVE EQUIVALENT occurs: the one instant a
            zero-duration model has, and the one every countdown in the app has always
            shown. Stock's `ManeuverNode.UT` is exactly this.

            **It is not the ignition time, and the difference is a real defect elsewhere
            in the ecosystem.** A finite burn starts before this and ends after it,
            which is why every serious KSP maneuver mod independently reimplements
            "start at UT minus half the burn time". `ManeuverNode.ignitionUt` and
            `ManeuverNode.cutoffUt` carry those two instants directly instead of leaving
            each consumer to guess a convention.
        ignitionUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            When the engines light, or null when nothing supplies a burn-duration model
            for this craft.

            Null is a real answer and not a failure, on the same terms as
            `IPropagationProvider.CharacteristicCycleSeconds`. Stock computes a burn
            time only for a LOADED vessel (`VesselDeltaV.CheckDirtyAndRun` early-returns
            on `!loaded`), so an unloaded craft's queued burn honestly has no ignition
            time rather than a guessed one.

            **Never a sentinel equal to `ManeuverNode.ut`.** Collapsing an unmodelled
            duration onto the impulsive instant would make "we do not know when to light
            the engines" indistinguishable from "this burn is instantaneous", and only
            one of those is ever true.
        cutoffUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            When the engines cut, or null on the same terms as
            `ManeuverNode.ignitionUt`. Burn duration is `CutoffUt - IgnitionUt`.

            Carried as an instant rather than as a separate duration field on purpose: a
            duration alongside two instants is a third number that can disagree with the
            other two, and there is no reading of a disagreement that helps anybody.

            Not derivable as `Ut` plus half a duration in general. That symmetry holds
            only while the craft's mass is constant, and a burn long enough to be worth
            modelling is long enough to change it.
        frame:
          description: |-
            The basis `ManeuverNode.dvRadial`/`ManeuverNode.dvNormal`/
            `ManeuverNode.dvPrograde` are expressed in. Null only on a node read off a
            recording captured BEFORE this field existed, on the same terms as
            `ManeuverNode.id`.

            Previously this lived only in this class's prose, which was safe exactly as
            long as one basis existed. Nullable rather than defaulted because
            `ManeuverFrame.RadialNormalPrograde` is index 0, so a defaulted value would
            assert the stock basis for components that might be in another one.

            **The three fields are POSITIONAL slots, and this names what they hold.**
            They are the basis's first, second and third component in the basis's own
            declared order: `ManeuverFrame.RadialNormalPrograde` puts radial, normal and
            prograde in them, and `ManeuverFrame.TangentNormalBinormal` puts tangent,
            normal and binormal. So on a Frenet burn `ManeuverNode.dvRadial` carries the
            TANGENT and `ManeuverNode.dvPrograde` carries the BINORMAL, which the field
            names actively work against and is why it is written down here rather than
            left to be inferred. Saying so is the difference between a reader that
            renders a Frenet burn correctly and one that silently rotates every burn an
            integrating planner produces while looking right.
          anyOf:
            - $ref: "#/components/schemas/ManeuverFrame"
              x-sitrep-unit: enum
            - type: "null"
        dvRadial:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: |-
            Null only if KSP's own dv component was non-finite (NaN/Infinity) this tick:
            the NODE is still preserved (never silently dropped just because one
            component came back bad); see `VesselViewProvider.BuildManeuver`.
        dvNormal:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: |-
            Null only if KSP's own dv component was non-finite this tick; see
            `ManeuverNode.dvRadial`'s doc comment.
        dvPrograde:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: |-
            Null only if KSP's own dv component was non-finite this tick; see
            `ManeuverNode.dvRadial`'s doc comment.
        dvTotal:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: |-
            Null only if KSP's own dv magnitude was non-finite this tick; see
            `ManeuverNode.dvRadial`'s doc comment.
        frameReference:
          description: |-
            What `ManeuverNode.frame`'s basis is measured RELATIVE TO.

            `ManeuverFrame` names a BASIS and not a frame, and for stock that is enough
            because there is only ever one thing the basis can be relative to. A planner
            that lets an operator choose the reference frame breaks that assumption: the
            same tangent/normal/binormal triple means a different burn depending on what
            it is tangent TO, and a client shown the numbers without this is being shown
            a burn it cannot identify.

            **A kind and a body, not a name.** A string would be a second vocabulary for
            something the app already has one of: the read-frame side names exactly
            these four kinds, and every widget that draws a frame already resolves them.
            Two ways of naming one concept is how a compatibility shim starts.

            Null when the planner has only one frame, which is the stock case and not a
            gap.
          anyOf:
            - $ref: "#/components/schemas/ManeuverFrameReference"
              x-sitrep-unit: enum
            - type: "null"
        frameReferenceBodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            The body `ManeuverNode.frameReference` is about, as a `system.bodies` index.
            Unused by a frame that needs no body.
        inertiallyFixed:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether the craft holds a fixed inertial attitude through the burn rather
            than following the frame as it rotates.

            Not a nicety: over a long burn the two steer differently, so a plan shown
            without it is a plan whose execution cannot be predicted from what is on
            screen. Null when the planner has no such concept, which is stock.
        thrust:
          type:
            - number
            - "null"
          x-sitrep-unit: kN
          description: |-
            Thrust the plan was computed against.

            Stock CAN fill this and today does not: the impulsive model has no use for
            it, so nothing asked. It is here rather than on a planner-specific channel
            because "what thrust was this planned against" is a question about the burn,
            and the answer differs between a plan made at full throttle and one made on
            a single engine whatever computed it.
        specificImpulse:
          type:
            - number
            - "null"
          x-sitrep-unit: isp
          description: Specific impulse the plan was computed against.
        initialMass:
          type:
            - number
            - "null"
          x-sitrep-unit: t
          description: Craft mass at ignition, as the plan assumed it.
        finalMass:
          type:
            - number
            - "null"
          x-sitrep-unit: t
          description: Craft mass at cutoff, as the plan assumed it.
        patches:
          type: array
          items:
            $ref: "#/components/schemas/OrbitPatch"
          description: |-
            This node's post-burn future-orbit patch chain: element 0 is the orbit the
            vessel is on IMMEDIATELY after the burn (KSP's own
            `ManeuverNode.nextPatch`), followed by any subsequent SOI-transition
            patches. ALWAYS an array (R2): empty when the solver hasn't produced a
            post-burn patch yet (a just-added node mid-tick). See
            `Gonogo.KSP.KspHost.BuildOrbitPatchChain` for the walk (same helper
            `VesselOrbit.patches` uses, started from the node's own `nextPatch` instead
            of the vessel's current orbit).

            **How one burn links to the next, measured on the Deck 2026-08-18.** A
            burn's INPUT trajectory is the patch in the PREVIOUS burn's chain whose
            `PatchEndTransition` is `TransitionType.Maneuver`, equivalently the one
            whose `EndUt` equals this burn's `ManeuverNode.ut`. For the first burn it is
            the craft's own `vessel.orbit`. Every chain is a suffix of the previous one,
            but the number of patches skipped varies with how many SOI crossings fall
            between the two burns, so counting positions is not the rule and gets it
            wrong the first time a crossing appears.

            KSP re-parents strictly sequentially, also measured: inserting a burn ahead
            of an existing one re-derives every later chain, so a burn's input is always
            the previous burn's result.

            **This whole field is a PATCHED-CONIC encoding.** It exists because a stock
            plan IS a sequence of conics joined at SOI boundaries. A planner that
            integrates has no such boundaries and will leave this empty while still
            describing a perfectly good burn, so nothing may treat an empty chain as a
            malformed node.
    Meta:
      type: object
      required:
        - source
        - validAt
        - seq
        - deliveredAt
        - vantage
        - quality
        - active
        - staleness
        - timelineEpoch
      properties:
        source:
          type: string
          x-sitrep-unit: id
        validAt:
          type: number
          x-sitrep-unit: ut
          description: |-
            When the payload was TRUE in the game, in UT seconds (KSP universal time),
            the same base every `*Ut` field on every payload uses. This is the instant a
            reading is "as of", and the one a client compares against its view time to
            decide currency. The unit IS declared, and reaches a client through the
            units map rather than through the emitted type: the `Value<"ut">` retyping
            pass runs over wire PAYLOAD types only, so this stays a bare number in
            `contract.ts` and carries `"ut"` in `units.json`. Every command-args field
            does the same; reading only the type under-reports the declaration. Keeping
            the envelope out of that pass is deliberate: nothing renders these, ten
            transport and timeline files do arithmetic on them, and the envelope rides
            every message, so a wrapper would allocate twice per message on the hottest
            path for a quantity no readout shows.
        seq:
          type: number
          x-sitrep-unit: id
        deliveredAt:
          type: number
          x-sitrep-unit: ut
          description: |-
            When the server handed the message to the transport, in the same UT seconds
            as `Meta.validAt`. The two differ by the signal delay the vantage is under,
            so subtracting one from the other is how old the payload was when it
            arrived, and they are equal on a live (zero-delay) link. Declared, and bare
            in the emitted type, as `Meta.validAt` is.
        vantage:
          type: string
          x-sitrep-unit: id
        quality:
          $ref: "#/components/schemas/Quality"
          x-sitrep-unit: enum
        active:
          type: boolean
          x-sitrep-unit: flag
        staleness:
          $ref: "#/components/schemas/Staleness"
          x-sitrep-unit: enum
        timelineEpoch:
          type: number
          x-sitrep-unit: id
          description: |-
            Generation counter for the current timeline: 0 at boot, incremented once for
            every quickload/rewind (ResetTimeline). Stamped on EVERY envelope `Meta`
            (streams AND command responses) by `Courier.MakeMeta`: see that method's doc
            comment for why this had to be added now rather than retrofitted later: once
            recordings/stations exist, a sample with no epoch can never be told apart
            from one on an abandoned pre-rewind timeline. A client compares this against
            its own last-seen epoch to detect a rewind atomically, without re-deriving
            it from a backward `validAt` jump (which a reordered/coalesced delivery
            could mask).
        gapSinceUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            The `Meta.validAt` of the last sample on this topic that precedes a KNOWN
            break in the record, set only on the first sample delivered after that break
            and `null` on every other sample.

            Non-null is a positive claim, not an absence: data existed between this UT
            and the carrying sample's own `Meta.validAt`, and it is gone. Two things
            produce one. A blackout recording that overran its storage bound had its
            oldest span dropped, so the replay resumes mid-hole. A channel that does not
            record at all (a session fact, never aboard the craft: see
            `ChannelDeclaration.Recordable`) has no replay, so its first post-blackout
            sample carries the whole outage as the gap.

            A client draws it as a break rather than joining across it. Without it a
            chart interpolates a straight line through an outage it has no readings for,
            which is the one thing an operator must not be shown: the line looks like
            data.
    NoCommandArgs:
      type: object
      description: |-
        The empty args shape, for the core commands that operate on the current
        flight or the active vessel and so take nothing: `vessel.control.stage`,
        `vessel.target.clear`, `ksp.recover`, `ksp.revertToLaunch` and
        `ksp.toTrackingStation`.

        **Send no `args` at all for these five.** The SDK's `send()` takes no second
        argument for a command typed this way, and anything you do put on the wire
        is read and discarded. Every other command requires its args.
      properties: {}
      x-sitrep-empty: true
    OrbitEncounter:
      type: object
      description: "One upcoming SOI patch transition: see `VesselOrbit.encounter`."
      required:
        - transitionType
        - transitionUt
      properties:
        transitionType:
          $ref: "#/components/schemas/TransitionType"
          x-sitrep-unit: enum
        transitionUt:
          type: number
          x-sitrep-unit: ut
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index into `system.bodies` of the body being transitioned INTO; null if that
            body couldn't be resolved.
    OrbitEntry:
      type: object
      description: |-
        A body's Keplerian orbital elements, as emitted by
        `SystemViewProvider.BuildOrbit` (present on every `BodyEntry` except the
        root star). Each element is independently nullable: KSP's own `lan`/`argPe`
        are NaN for a near-equatorial/near-circular orbit (a routine case) and the
        provider maps that (and any genuinely-absent value) to null via the shared
        non-finite-is-absent rule, never a NaN token on the wire.

        Units mirror the KSP-native inconsistency deliberately KEPT upstream:
        `OrbitEntry.sma` in metres; `OrbitEntry.inc`/`OrbitEntry.lan`/
        `OrbitEntry.argPe` in DEGREES; `OrbitEntry.meanAnomalyAtEpoch` in RADIANS;
        `OrbitEntry.epoch` in UT seconds. No `eccentricAnomaly` field (see
        `BodyEntry`).
      properties:
        sma:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: Semi-major axis, metres.
        ecc:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Eccentricity.
        inc:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: Inclination, degrees.
        lan:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Longitude of ascending node, degrees; null for an undefined node
            (near-equatorial orbit).
        argPe:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Argument of periapsis, degrees; null for an undefined periapsis
            (near-circular orbit).
        meanAnomalyAtEpoch:
          type:
            - number
            - "null"
          x-sitrep-unit: rad
          description: Mean anomaly at epoch, radians.
        epoch:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: Epoch UT, seconds.
    OrbitPatch:
      type: object
      description: |-
        One conic segment of a vessel's future trajectory, a patched-conic "patch"
        in KSP's own sense (`Orbit.nextPatch`/`previousPatch`). Unlike `VesselOrbit`
        (which is deliberately elements-only, see its own doc comment), a patch
        chain exists purely so the CLIENT can propagate/render a forward trajectory,
        so it carries the same already-computed apsis/shape fields KSP's own `Orbit`
        exposes (`OrbitPatch.peA`/`OrbitPatch.apA`/`OrbitPatch.semiLatusRectum`/
        `OrbitPatch.semiMinorAxis`) rather than forcing the client to re-derive them
        per patch. `OrbitPatch.referenceBody`/`OrbitPatch.closestEncounterBody` are
        body NAME strings, because the client's existing patch-consuming math
        (`packages/core/src/calc/trajectory.ts`, which predates this Topic and
        already expects body names) needs zero reshaping to use them directly.
        `OrbitPatch.referenceBodyIndex`/`OrbitPatch.closestEncounterBodyIndex` sit
        beside them and are the IDENTITY, matching `VesselOrbit.referenceBodyIndex`
        and every other body reference in this contract. Both are carried on
        purpose: the names were once described here as "the one deliberate
        departure" from the index convention, which held only while nothing needed
        to resolve a patch's body to anything. Propagating a patch does, and a
        display name is the wrong key for that. `OrbitPatch.mu` completes the same
        thought: a patch now carries everything needed to propagate it, so it is no
        longer the only orbit on the wire that requires a `system.bodies` join
        before it can be used. `OrbitPatch.lan`/`OrbitPatch.argPe` are plain
        (non-nullable) doubles here, UNLIKE `VesselOrbit.lan`/`VesselOrbit.argPe`: a
        deliberate, narrower exception to this codebase's usual R1 "never NaN, never
        a fake 0" rule: the propagation math that consumes a patch already
        hard-assumes a finite number for both (no null-handling branch), matching
        the historical behaviour for a near-circular/near-equatorial patch.
        Capturing them nullable here would silently break every consumer without a
        matching client-side rewrite: out of scope for this Topic. See
        `Gonogo.KSP.KspHost.BuildOrbitPatchChain`'s doc comment for how a NaN is
        substituted with 0 at capture time, preserving that pre-existing (imperfect
        but non-breaking) behaviour.
      required:
        - sma
        - ecc
        - inc
        - lan
        - argPe
        - meanAnomalyAtEpoch
        - epoch
        - period
        - startUt
        - endUt
        - patchStartTransition
        - patchEndTransition
        - peA
        - apA
        - semiLatusRectum
        - semiMinorAxis
        - referenceBody
      properties:
        sma:
          type: number
          x-sitrep-unit: m
        ecc:
          type: number
          x-sitrep-unit: "1"
        inc:
          type: number
          x-sitrep-unit: °
        lan:
          type: number
          x-sitrep-unit: °
        argPe:
          type: number
          x-sitrep-unit: °
        meanAnomalyAtEpoch:
          type: number
          x-sitrep-unit: rad
        epoch:
          type: number
          x-sitrep-unit: ut
        period:
          type: number
          x-sitrep-unit: s
          description: |-
            Orbital period, seconds. Non-finite (hyperbolic/parabolic patches) is
            carried as-is, the client's `isPatchElliptical` guard is what filters those,
            not this field.
        startUt:
          type: number
          x-sitrep-unit: ut
        endUt:
          type: number
          x-sitrep-unit: ut
        patchStartTransition:
          $ref: "#/components/schemas/TransitionType"
          x-sitrep-unit: enum
        patchEndTransition:
          $ref: "#/components/schemas/TransitionType"
          x-sitrep-unit: enum
        peA:
          type: number
          x-sitrep-unit: m
          description: |-
            Periapsis altitude above `OrbitPatch.referenceBody`'s mean radius, metres,
            `Orbit.PeA`.
        apA:
          type: number
          x-sitrep-unit: m
          description: |-
            Apoapsis altitude above `OrbitPatch.referenceBody`'s mean radius, metres,
            `Orbit.ApA`.
        semiLatusRectum:
          type: number
          x-sitrep-unit: m
        semiMinorAxis:
          type: number
          x-sitrep-unit: m
        referenceBody:
          type: string
          x-sitrep-unit: text
          description: |-
            Body this patch orbits: matches `system.bodies`' NAME, not its index (see
            class doc).
        closestEncounterBody:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Body this patch's trajectory most closely encounters, if any, null when
            there is none. Same "name, not index" convention as
            `OrbitPatch.referenceBody`.
        mu:
          type:
            - number
            - "null"
          x-sitrep-unit: m³/s²
          description: |-
            Parent body's standard gravitational parameter (GM), so a patch is
            self-sufficient to propagate exactly as `VesselOrbit.mu` makes a vessel's
            own orbit self-sufficient.

            Without it a patch was the only orbit on the wire that could not be
            propagated from what it carries: a consumer had to resolve
            `OrbitPatch.referenceBody` through `system.bodies` to find the number. That
            asymmetry made an A/B between a vessel's own orbit and a maneuver patch
            measure the lookup as well as the arithmetic.

            Null only on a patch read off a recording captured BEFORE this field
            existed, on the same terms as `ManeuverNode.id`. Nullable rather than 0
            because a zero GM is not a body, and every consumer of it divides.
        referenceBodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Body this patch orbits, as its `system.bodies` INDEX. The identity, where
            `OrbitPatch.referenceBody` is the display name: index is what every other
            body reference in this contract is keyed on
            (`VesselOrbit.referenceBodyIndex`, `VesselTarget`, `TargetAvailable`,
            `VesselIdentity.ParentBodyIndex`) and what `Sitrep.Propagation`'s
            `PropagationTarget` and `PropagationFrame` name a body by.

            Carried ALONGSIDE the name rather than replacing it: the name is
            load-bearing in `orbit-patches.ts`'s SOI-change detection and in
            `trajectory.ts`, which predates this Topic (see the class doc), so dropping
            it is a client migration and not a contract edit.

            Null only on a pre-existing recording, per `OrbitPatch.mu`. Nullable rather
            than 0 specifically because 0 is a REAL body index (the star), so a
            defaulted value here would read as a confident wrong answer rather than as
            an absent one.
        closestEncounterBodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            `OrbitPatch.closestEncounterBody`'s `system.bodies` index, on the same
            index-is-identity terms as `OrbitPatch.referenceBodyIndex`. Null when there
            is no encounter at all, and also null on a pre-existing recording: the two
            are indistinguishable here, which is acceptable only because
            `OrbitPatch.closestEncounterBody` already carries the distinction.
    PartBounds:
      type: object
      description: |-
        A `VesselPart`'s local bounding box: `PartBounds.size` is the part's
        `prefabSize` (a cheap, per-part-constant proxy for the renderer bounds
        ShipMap could refine later), `PartBounds.center` the mesh-centre offset from
        the part's own origin (`Part.boundsCentroidOffset`). Fuel-line parts report
        a whole-conduit-wrapping bounds, a carried-forward KSP quirk the consumer
        handles, not this capture.

        Both are in the PART's own frame (`part-local`), never the vessel's: they
        are authored fields of the part's config node, so one value has to serve
        every instance of that part however it was assembled. A consumer placing the
        box on a ship rotates both by that part's `orgRot` before adding
        `VesselPart.position`, which is `vessel-local`.
      required:
        - size
      properties:
        size:
          type: object
          description: "`Part.prefabSize`: the part's untransformed bounding-box extents (metres)."
          required:
            - x
            - y
            - z
          properties:
            x: &a3
              type: number
              x-sitrep-unit: m
            y: *a3
            z: *a3
        center:
          type:
            - object
            - "null"
          description: |-
            `Part.boundsCentroidOffset`: mesh-centre offset from the part's own origin
            (metres, PART-local); `null` when absent.

            Part-local, not vessel-local: an authored per-part-type constant like
            `PartBounds.size` beside it, so one value serves every instance of the part
            at whatever attitude it was assembled at. `VesselPart.position` is
            `vessel-local`, so adding the two raw mixes frames and lands the box
            somewhere the part is not; rotate this one by the part's `orgRot` first.
          required:
            - x
            - y
            - z
          properties:
            x: &a4
              type: number
              x-sitrep-unit: m
            y: *a4
            z: *a4
    PartModuleState:
      type: object
      description: |-
        One module's behavioural state in `VesselPart.moduleStates`.
        `PartModuleState.type` discriminates the module and `PartModuleState.state`
        carries the standardised deploy/activation word, whose vocabulary is on that
        property.
      required:
        - type
        - state
      properties:
        type:
          type: string
          x-sitrep-unit: id
          description: |-
            Discriminator: `solarPanel` / `radiator` / `antenna` / `parachute` /
            `engine` / `drill` / `landingGear`. (`cargoBay` is a defined vocabulary
            value with no module here; see `KspHost.BuildPartModuleStates`'s doc comment
            for why.)
        state:
          type: string
          x-sitrep-unit: text
          description: |-
            The standardised deploy/activation state, one closed vocabulary across every
            `PartModuleState.type` rather than each module's own enum spelling.

            - `extended` / `retracted` / `deploying` / `retracting`, for anything that
              animates: solar panels, radiators, antennas, landing gear.
            - `stowed` / `armed` / `extended` / `broken`, the parachute lifecycle.
              `armed` is armed and waiting for its atmospheric trigger, which is not the
              same as deployed.
            - `active` / `inactive`, for engines and drills.
            - `unknown` when the underlying game enum maps to none of the above, which
              is a statement that the state was read and not recognised rather than a
              stand-in for retracted.
        tracking:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: "Solar-panel-only: sun-tracking gimbal active. `null` for every other type."
        flameout:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Engine-only: fuel-starved. `null` unless `true` (never emitted as a bare
            `false`).
    PartResourceFlow:
      type: object
      description: |-
        One resource row in `VesselPart.resources`: storage
        (`PartResourceFlow.amount`/`PartResourceFlow.maxAmount`) plus live flow
        (`PartResourceFlow.flow`/`PartResourceFlow.nominalFlow`).

        **Flow scope.** `PartResourceFlow.flow`/`PartResourceFlow.nominalFlow` are
        populated only for the module types whose live rate is CHEAPLY derivable
        from public fields without hand-simulating KSP's resource solver: solar
        panels (`ModuleDeployableSolarPanel.flowRate`/ `chargeRate`), alternators
        (`ModuleAlternator.outputRate`), and engine propellant consumption
        (`Propellant.currentRequirement`, signed negative). This is the SAME "if
        cheap" scoping `KspHost.BuildPartsPower`'s doc comment already establishes
        for `totalProductionEc`: resource converters / fuel cells / drills report
        storage only (no computed rate; not cheaply derivable from static fields),
        matching that precedent rather than inventing a shaky approximation.
        `PartResourceFlow.nominalFlow` is omitted (left `null`) whenever it would
        equal `PartResourceFlow.flow`, per the SDK contract.
      required:
        - amount
        - maxAmount
      properties:
        amount:
          type: number
          x-sitrep-unit: units
          description: "`PartResource.amount`: current stored amount."
        maxAmount:
          type: number
          x-sitrep-unit: units
          description: "`PartResource.maxAmount`: storage capacity."
        flow:
          type:
            - number
            - "null"
          x-sitrep-unit: units/s
          description: |-
            Signed units/sec: positive = producing, negative = consuming. `null` when no
            cheaply-derivable module contributes.
        nominalFlow:
          type:
            - number
            - "null"
          x-sitrep-unit: units/s
          description: |-
            Same-sign 100%-efficiency cap (rated solar output). `null` when no module
            supports a nominal, or when it would equal `PartResourceFlow.flow`.
    PartsPower:
      type: object
      description: |-
        The `parts.power` channel payload: the active vessel's electric-charge
        production surface (solar panels, batteries, fuel cells, engine alternators,
        and a rolled-up production total). Unlike the bare-array `robotics.servos`
        and the `science.*` channels, this payload is a single WRAPPER OBJECT (or
        `null` when there is no active vessel / no power sub-group): so the Topic
        tag sits on this type directly with the default `IsArray = false`.

        **Typing-only mirror.** This reproduces, field-for-field, the exact
        serialized shape `Sitrep.Host.PartsViewProvider.BuildPower` already emits
        (same names, same camelCase wire keys via `RtConfig.CamelCaseForProperties`,
        same units). It is NOT serialized itself: the wire is written by
        `JsonWriter` walking the provider's dictionary: so adding it changes no
        bytes. The four arrays and the total are each nullable to mirror the
        provider (the arrays are always present in the emitted object, but the
        contract stays permissive; the total is `null` whenever
        `SnapshotDict.GetDouble` reads no finite value).
      properties:
        solarPanels:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/SolarPanelEntry"
        batteries:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/BatteryEntry"
        fuelCells:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/FuelCellEntry"
        alternators:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/AlternatorEntry"
        totalProductionEc:
          type:
            - number
            - "null"
          x-sitrep-unit: units/s
    PayloadMeta:
      type: object
      description: |-
        The slim, payload-specific sibling of `Meta`, carried on every `vessel.*`
        and `time.warp` PAYLOAD (`VesselOrbit.Meta`, `VesselIdentity.Meta` and so
        on).

        It says what the payload is ABOUT, and nothing about its delivery. The real
        `seq`, `deliveredAt`, `vantage` and `validAt` are on the ENVELOPE `Meta`,
        one per `stream-data` frame: read those there, never here.

        `PayloadMeta.source` is the subject's provenance, and takes one of two
        forms: `"vessel:<guid>"` when the payload describes one craft, or `"game"`
        when it describes the session. `PayloadMeta.quality` says whether that craft
        is on rails or fully loaded. Those two are the whole of this type.
      required:
        - source
        - quality
      properties:
        source:
          type: string
          x-sitrep-unit: id
        quality:
          $ref: "#/components/schemas/Quality"
          x-sitrep-unit: enum
    PhysicsMode:
      type: integer
      description: |-
        The active vessel's physics-simulation regime, derived from KSP's own
        `Vessel.loaded`/`Vessel.packed` flags (confirmed via decompile: both are
        public `bool` fields on `Vessel`). This is the proper Value that replaces
        the old "read stream meta" stand-in, physics mode is a discrete enum in its
        own right, NOT a quality band on `PayloadMeta.quality` (a 2026-07-09
        reversal). Widgets that switch propagation/dead-reckoning strategy
        (a.physicsMode consumers) read this to know whether the craft is on-rails
        conics, a packed cluster, or a fully physics-simulated vessel.

        Mapping (see `Gonogo.KSP.KspHost.BuildPhysics` and
        `Sitrep.Host.VesselViewProvider.BuildPhysicsMode`):

        - `!loaded` ⇒ `PhysicsMode.OnRails`: the vessel is unloaded, its motion is
          pure on-rails conic propagation, no PhysX at all.
        - `loaded && packed` ⇒ `PhysicsMode.Packed`: loaded into the scene but still
          packed (rails-following near the active vessel, not yet unpacked into full
          physics).
        - `loaded && !packed` ⇒ `PhysicsMode.Unpacked`: fully physics-simulated
          (off-rails).

        `PhysicsMode.Unknown` is the graceful fallback for a raw value this contract
        doesn't recognize (same convention as `SasMode`/`VesselType`).
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: OnRails
        - const: 1
          title: Packed
        - const: 2
          title: Unpacked
        - const: 3
          title: Unknown
    PropagationHorizon:
      type: object
      description: |-
        The window over which an element set is authoritative, as stated by
        whichever propagation provider produced it.

        Measured from the sample's OBSERVATION instant, not from
        `VesselOrbit.epoch`. `Epoch` is the mean-anomaly reference epoch and can sit
        far from when the sample was taken, so subtracting it would answer a
        different question with the same units and no type could catch it.
      required:
        - kind
        - trajectoryKind
      properties:
        kind:
          $ref: "#/components/schemas/PropagationHorizonKind"
          x-sitrep-unit: enum
        trajectoryKind:
          description: |-
            What KIND of answer these elements are, which is the client's real question.
            Replaced a provider id, and the difference matters.

            The horizon answers REACH: how far may I extrapolate. It does not answer
            SHAPE: is a conic the right renderer at all. A client cannot infer the
            second from the first, and the failure case is concrete rather than
            principled: an analytic provider reports `PropagationHorizonKind.Unbounded`,
            and so may an INTEGRATING provider in a low-perturbation regime, where the
            horizon is genuinely long. A client reasoning "unbounded, therefore
            analytic, therefore an ellipse is fine" then draws a closed conic for an
            integrated trajectory: faithful at the sample instant, wrong as a path, and
            confident.

            An earlier draft carried the provider's literal id instead. That answered
            "who computed this" where the client needed "what is this like", and it put
            a vendor's name in a standard payload. An enumeration answers the real
            question completely, and every provider can state it, stock included, which
            is what makes it belong on the standard shape at all.

            Diagnostics keep their own home: `system.uplinks` already carries each
            Uplink's id, version and availability once per session, and a version is
            what a bug report wants more than a name.
          allOf:
            - $ref: "#/components/schemas/TrajectoryKind"
              x-sitrep-unit: enum
        untilUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            The last UT these elements answer for. Set if and only if
            `PropagationHorizon.kind` is `PropagationHorizonKind.Until`; null otherwise,
            never a sentinel standing in for "forever".
    PropagationHorizonKind:
      type: integer
      description: |-
        Deliberately THREE arms, and the ordering is the point.

        `PropagationHorizonKind.Unspecified` is 0, so a producer that forgets the
        horizon gets the REFUSING answer rather than the permissive one. Had
        `PropagationHorizonKind.Unbounded` been the default, a provider that failed
        to populate it would have read as "trust this conic forever", which is the
        most dangerous available reading and would have failed silently.

        `PropagationHorizonKind.Unbounded` is a CLAIM, made by a provider that
        genuinely has no limit (an analytic two-body solver), not a default nobody
        made. It is its own arm rather than an infinite `PropagationHorizon.untilUt`
        so that "forever" never has to be recognised as an extreme number.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Unspecified
          description: No provider stated one. Treat as unpropagatable.
        - const: 1
          title: Unbounded
          description: "Authoritative for all future UT: an analytic solver with no horizon."
        - const: 2
          title: Until
          description: Authoritative until `PropagationHorizon.untilUt`.
    Quality:
      type: integer
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: OnRails
        - const: 1
          title: Loaded
    RecoveryCrewEntry:
      type: object
      description: |-
        One crew member aboard at recovery: an entry of
        `RecoveryReport.crewBreakdown`.
      required:
        - name
        - trait
        - isTourist
        - xpGained
        - levelsGained
        - newLevel
      properties:
        name:
          type: string
          x-sitrep-unit: text
        trait:
          type: string
          x-sitrep-unit: text
          description: The kerbal's career trait (e.g. `"Pilot"`).
        isTourist:
          type: boolean
          x-sitrep-unit: flag
        xpGained:
          type: number
          x-sitrep-unit: count
        levelsGained:
          type: number
          x-sitrep-unit: count
        newLevel:
          type: number
          x-sitrep-unit: count
    RecoveryPartEntry:
      type: object
      description: |-
        One recovered-part group: an entry of `RecoveryReport.partBreakdown`.
        Identically-named parts are grouped, hence `RecoveryPartEntry.count`.
      required:
        - partName
        - partTitle
        - count
        - partValue
        - resourcesValue
        - totalValue
      properties:
        partName:
          type: string
          x-sitrep-unit: text
          description: The part's `partInfo.name` (e.g. `"mk1pod.v2"`).
        partTitle:
          type: string
          x-sitrep-unit: text
          description: The part's `partInfo.title` (e.g. `"Mk1 Command Pod"`).
        count:
          type: number
          x-sitrep-unit: count
        partValue:
          type: number
          x-sitrep-unit: funds
        resourcesValue:
          type: number
          x-sitrep-unit: funds
        totalValue:
          type: number
          x-sitrep-unit: funds
    RecoveryReport:
      type: object
      description: |-
        The payload for the `recovery.lastSummary` channel: a single "last notable
        recovery" record for the current save, delivered on the
        Delivery.ReliableOrdered event lane. Mirrors the wire shape the consumer
        already parses (`FlightOutcomeBanner.parseRecovery`) field-for-field, the
        recovery-side counterpart of `CrashReport`.

        TYPING/codegen marker only. The producer (`Gonogo.KSP.RecoveryUplink`)
        hand-flattens the live-KSP recovery into a `Dictionary<string, object?>` via
        `Sitrep.Host.Recovery.RecoveryPayload.Build` before publishing, so
        JsonWriter only ever sees the dictionary: this POCO exists solely so the TS
        SDK has a concrete payload type to name (it is on
        `WirePayloadCoverageTests`'s producer-flatten allowlist for exactly that
        reason).
      required:
        - capturedAtUT
        - vesselName
        - recoveryLocation
        - recoveryFactor
        - scienceEarned
        - totalScience
        - fundsEarned
        - totalFunds
        - reputationEarned
        - totalReputation
        - displayReputation
        - scienceBreakdown
        - partBreakdown
        - resourceBreakdown
        - crewBreakdown
      properties:
        capturedAtUT:
          type: number
          x-sitrep-unit: ut
          description: Universal time of the recovery capture.
        vesselName:
          type: string
          x-sitrep-unit: text
        recoveryLocation:
          type: string
          x-sitrep-unit: text
          description: |-
            Where the vessel came down: KSP's own recovery-location string (e.g.
            `"KSC"`, `"Water"`).
        recoveryFactor:
          type: string
          x-sitrep-unit: text
          description: |-
            KSP's own recovery-factor display string (e.g. `"100%"`), the payout
            multiplier for landing precision.
        scienceEarned:
          type: number
          x-sitrep-unit: science
        totalScience:
          type: number
          x-sitrep-unit: science
        fundsEarned:
          type: number
          x-sitrep-unit: funds
        totalFunds:
          type: number
          x-sitrep-unit: funds
        reputationEarned:
          type: number
          x-sitrep-unit: rep
        totalReputation:
          type: number
          x-sitrep-unit: rep
        displayReputation:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            Whether reputation applies to this save (off in Science/Sandbox), gates the
            reputation row client-side.
        scienceBreakdown:
          type: array
          items:
            $ref: "#/components/schemas/RecoveryScienceEntry"
        partBreakdown:
          type: array
          items:
            $ref: "#/components/schemas/RecoveryPartEntry"
        resourceBreakdown:
          type: array
          items:
            $ref: "#/components/schemas/RecoveryResourceEntry"
        crewBreakdown:
          type: array
          items:
            $ref: "#/components/schemas/RecoveryCrewEntry"
    RecoveryResourceEntry:
      type: object
      description: |-
        One recovered-resource group: an entry of
        `RecoveryReport.resourceBreakdown`.
      required:
        - resourceName
        - amount
        - unitValue
        - totalValue
      properties:
        resourceName:
          type: string
          x-sitrep-unit: text
        amount:
          type: number
          x-sitrep-unit: units
        unitValue:
          type: number
          x-sitrep-unit: funds
        totalValue:
          type: number
          x-sitrep-unit: funds
    RecoveryScienceEntry:
      type: object
      description: |-
        One science subject recovered: an entry of
        `RecoveryReport.scienceBreakdown`.
      required:
        - subjectId
        - subjectTitle
        - dataGathered
        - scienceAmount
      properties:
        subjectId:
          type: string
          x-sitrep-unit: id
        subjectTitle:
          type: string
          x-sitrep-unit: text
        dataGathered:
          type: number
          x-sitrep-unit: Mit
        scienceAmount:
          type: number
          x-sitrep-unit: science
    ReliabilityBudget:
      type: object
      description: |-
        One consumed dimension of a part's rated life: the open-ended member of the
        per-part shape, and the reason this contract could shrink rather than grow.
        A provider declares a dimension the shared shape has never heard of without
        a core PR, which is what the extension bag cannot deliver for a SHARED
        renderer (a bag entry is readable only by a widget that already knows the
        provider id).

        A budget is BACKWARD-looking: how much of a rated allowance has been used.
        It is not a forecast; that is `ReliabilityPartEntry.survival`.
      properties:
        id:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Provider-chosen dimension id, stable across frames. Reserved ids with fixed
            meanings: "service" (scheduled maintenance clock), "burn.continuous",
            "burn.cumulative" (rated firing time, per RatingScope), "ignitions",
            "cycles". A provider with a dimension not listed here invents an id; the
            client renders it from Label and the numbers, never from the id.
        label:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Display-ready lower-case noun phrase, rendered verbatim in the operator's
            sentence: "continuous rated burn", "service". Max 40 chars; the producer
            clamps.
        kind:
          type:
            - string
            - "null"
          x-sitrep-unit: enum
          description: |-
            What crossing the limit means, and therefore which verb the client uses:
            "schedule" (a maintenance date falls due; nothing fails at the line),
            "risk-ramp" (failure probability begins climbing past the line, nothing is
            guaranteed), "hard-limit" (the part stops at the line), "advisory" (the
            provider models the count but not what the limit means). A provider that
            does not know writes "advisory"; NEVER null-by-ignorance, because the
            client's threshold table is keyed on this.
        consumed:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            Used/Limit as a fraction, 0..1+ (may exceed 1). Null when the provider has
            no denominator. This is the field the client thresholds on.
        usedSeconds:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: |-
            Seconds of the allowance used. RATED seconds, not wall-clock: TestFlight
            consumes engine life thrust-weighted (`currentRunTime += dt *
            thrustModifier.Evaluate(engine.thrustRatio)` in
            TestFlightReliability_EngineCycle.UpdateCycle), so remaining rated seconds
            are not seconds of burn at partial throttle. Every rendered sentence says
            "rated" for that reason, and no wall-clock conversion is attempted because
            the future throttle profile is unknown.
        limitSeconds:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: |-
            The rated allowance in the same seconds `ReliabilityBudget.usedSeconds`
            counts. Null when the provider has no denominator.
        usedCount:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: Countable events used (ignitions, cycles). Exclusive with the seconds pair.
        limitCount:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: The countable allowance. Null when the provider has no denominator.
    ReliabilityPartEntry:
      type: object
      description: |-
        Per-part reliability, in the terms a reliability model actually has: a
        condition, the provider's own word for it, an optional forward survival
        probability with its horizon, and any number of consumed budgets.
      properties:
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            UNIQUE within one reliability.parts payload. Producers MUST enforce this; it
            is not a KSP flightID and must not be treated as one.
        repairTrait:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Which crew trait may act on this part, as the provider states it: a single
            name, or several comma-separated, or empty meaning anyone.

            Carried so a console can offer only the crew who could actually do it,
            rather than listing everyone aboard and letting the operator spend a round
            trip discovering that the pilot cannot. This is the PROVIDER's own
            requirement read back, never our guess at one: guessing would put a second
            authority beside the one that actually decides.

            Already ELEVATED where the provider elevates it. Kerbalism asks more of a
            critical failure than an ordinary one, so this is the requirement for THIS
            part in THIS condition, not the part's baseline.
        repairLevel:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Minimum experience level the trait must hold, elevated with
            `ReliabilityPartEntry.repairTrait`. Null when the provider states none.
        title:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        condition:
          type:
            - string
            - "null"
          x-sitrep-unit: enum
          description: |-
            One of: "nominal", "service-due", "failed", "failed-critical", "unknown".
            The provider's WORST condition for this part.

            "failed-critical" means the provider grades this failure as its more severe
            / more costly class. It does NOT mean unrecoverable: Kerbalism's critical IS
            repairable (2 evaRepairKits, and ElevatedForCritical() raises the crew
            requirement by one level before Repair() clears it). Nothing in this
            contract asserts that a part cannot be recovered, because repairability is a
            function of the part AND the crew, kits and difficulty flags aboard, which
            no per-part field can answer.

            "unknown" means the provider could not read this part's condition. It is a
            first-class value and the client renders it; it must never be substituted
            with "nominal".

            There is deliberately NO "wear" value: wear is a threshold on a number, the
            numbers are in
            `ReliabilityPartEntry.budgets`/`ReliabilityPartEntry.survival`, and the
            thresholds live client-side in one table. Two authorities for one word is
            how "2 wearing" comes to disagree with the number of wearing rows beneath
            it.
        conditionDetail:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The provider's OWN word(s) for this condition, rendered verbatim as the
            row's detail clause: "busted", "needs service", "turbopump failure". Max 120
            chars; producer clamps. This is how a third mod's native vocabulary reaches
            the screen without a contract change.
        survival:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            P(this part survives the next `ReliabilityPartEntry.survivalHorizonSeconds`
            seconds of OPERATION), 0..1. Null when the provider models no forward
            probability. MUST be null whenever the horizon is null.
        survivalHorizonSeconds:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: |-
            The horizon the fraction is over, in seconds of operation. MANDATORY
            whenever `ReliabilityPartEntry.survival` is set: exp(-rate*t) is
            uninterpretable without t, and two parts' fractions are not comparable
            unless both horizons are on screen.
        budgets:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/ReliabilityBudget"
          description: |-
            Consumed dimensions. Null or empty when the provider models none. Order is
            producer-chosen and not significant.
        repairCost:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/RepairCostItem"
          description: |-
            What repairing THIS part in THIS condition consumes, as the provider states
            it. Null or empty means the provider models no consumable cost: that is not
            the same claim as a cost of zero, and a console must render it as "nothing
            is consumed" rather than as "needs 0".

            Carried because a consumable cost is the PROVIDER's arithmetic and nothing
            else can derive it. Kerbalism charges two EVA repair kits for its critical
            class and one for an ordinary failure, and nothing for a service; TestFlight
            has no consumable in its model at all. A client that derived the number from
            `ReliabilityPartEntry.condition` would be applying one backend's rule to
            every install, which is exactly what this field replaced: the
            fleet-reliability row asked a TestFlight player for a repair kit its mod
            never needs, and refused the command when none was aboard.

            Already ELEVATED where the provider elevates it, on the same rule as
            `ReliabilityPartEntry.repairTrait`: this is the cost for this part in this
            condition, not the part's baseline.
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag, per-part half. Same mechanism and
            same rule as `ReliabilitySummary.extensions`.
          additionalProperties: true
    ReliabilitySummary:
      type: object
      description: |-
        Vessel-level reliability summary. Two facts and a bag, and deliberately no
        judgements: who is modelling, and whether they are modelling at all.

        There are no roll-ups here. A malfunction/critical count over the same part
        list published from the same capture at the same UT is a second authority
        for a derivable quantity, and a second authority is how two adjacent numbers
        come to disagree. The client derives what it needs from `reliability.parts`.
      properties:
        source:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Which backend produced this: "kerbalism" | "testflight" | "none", or a
            third-party provider id.
        coverage:
          type:
            - string
            - "null"
          x-sitrep-unit: enum
          description: |-
            One of ReliabilityCoverage. Null only if a producer failed to set it; the
            client treats null and an unrecognised value identically.
        extensions:
          type:
            - object
            - "null"
          description: |-
            The provider-namespaced extension bag: how a reliability backend carries a
            field this shared shape does not declare, WITHOUT a PR against this file.
            See ProviderExtensionBagAttribute for the whole mechanism.
          additionalProperties: true
    RemoveManeuverNodeArgs:
      type: object
      required:
        - nodeId
      properties:
        nodeId:
          type: string
          x-sitrep-unit: id
    RepairCostItem:
      type: object
      description: |-
        One kind of thing a repair consumes, and how many of it. Deliberately shaped
        as `InventoryItem`'s name/quantity pair so a console can join the two
        directly rather than guessing which inventory line a cost refers to.
      required:
        - name
        - quantity
      properties:
        name:
          type: string
          x-sitrep-unit: id
          description: |-
            `AvailablePart.name`, the config id (e.g. `"evaRepairKit"`), matching
            `InventoryItem.name`. The provider names the item so no consumer has to know
            which one a backend uses.
        quantity:
          type: number
          x-sitrep-unit: count
          description: |-
            How many of this kind one repair consumes. An entry exists only where
            something IS consumed, so this is never a meaningful zero.
    RepairOutcome:
      type: object
      description: |-
        What a repair attempt did, or why it did nothing.

        Carries where the kit came from because that changes what the operator has
        left, and under delay they will not get to ask again cheaply.
      required:
        - repaired
        - kitsUsed
      properties:
        repaired:
          type: boolean
          x-sitrep-unit: flag
          description: Whether the part was actually repaired.
        refusal:
          type:
            - string
            - "null"
          x-sitrep-unit: enum
          description: |-
            Why not, when `RepairOutcome.repaired` is false: one of the RepairRefusal
            tokens. Null on success.

            The FINER half of the refusal. It travels beside the
            `CommandResult.errorCode` RepairRefusal.CodeFor derives from it, on the
            payload of a result whose `CommandResult.success` is false, because the enum
            deliberately collapses distinctions this vocabulary keeps: a part that does
            not resolve and a crew member who does not are both
            `CommandErrorCode.NotFound`, and only this says which.
        kitsUsed:
          type: number
          x-sitrep-unit: count
          description: |-
            Kits consumed. Kerbalism charges two for a critical failure and one
            otherwise.
        kitsFrom:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Where the kit came from: `carried` when the kerbal already held it,
            otherwise the part id of the store it was taken from, so the operator can
            see which locker just got lighter.
    RepairPartArgs:
      type: object
      description: "`vessel.repair`'s args: which part, and which crew member does it."
      required:
        - partId
        - crewName
      properties:
        partId:
          type: string
          x-sitrep-unit: id
          description: |-
            The failed part, joined by the same id `reliability.parts` and
            `vessel.parts` use.
        crewName:
          type: string
          x-sitrep-unit: text
          description: |-
            Who performs it. A NAME, because that is KSP's own stable identifier for a
            kerbal and what `vessel.crew` is keyed by.
    ResourceAmount:
      type: object
      description: |-
        One resource's current/max amounts: see `VesselResources`'s class doc
        comment for the three-way absence semantics this type participates in.
      required:
        - current
        - max
        - active
      properties:
        current:
          type: number
          x-sitrep-unit: units
        max:
          type: number
          x-sitrep-unit: units
        active:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            R7 Fix 2: explicit presence flag so a present-but-zero resource (`{current:
            0, max: > 0, active: true}`) is distinguishable from one that has stopped
            being reported, killing the R-3 "absence-as-signal" wart where a resource
            simply vanishing from the map created a 0-vs-unknown ambiguity. Producers
            set this true for every resource they actually report this tick; a consumer
            treating a missing/false entry as "not reported" then never confuses it with
            a genuine zero reading. This is presence ONLY, flow/rate is a separate
            future channel (see this class's doc comment), deliberately not added here.
    RevertAvailability:
      type: object
      description: |-
        The `ksp.revertAvailability` Topic payload: whether the two stock in-flight
        "revert" actions are currently available, so a widget (LaunchDirector) can
        gate its Revert-to-Launch / Revert-to-Editor controls exactly like KSP's own
        pause menu does. Produced by
        `Sitrep.Host.SystemViewProvider.BuildRevertAvailability` from the two static
        `FlightDriver` bools KSP reads when it draws those very buttons.

        The whole payload is `null` (no key emitted) outside the flight scene: the
        two flags are only meaningful in flight, and the backing `FlightDriver`
        statics carry stale values from the previous flight otherwise. When present,
        both bools are concrete (never null): a `false` means "this revert is
        genuinely not available right now," which is exactly what the gate needs.

        **Mapping (verified against KSP's `PauseMenu.drawStockRevertOptions` at
        build time):** the pause menu shows the "Revert to Launch" button (which
        calls `FlightDriver.RevertToLaunch()`, restoring the `PostInitState`) when
        `FlightDriver.CanRevertToPostInit` is set, and the "Revert to VAB/SPH"
        buttons (which call `FlightDriver.RevertToPrelaunch(...)`, returning to the
        editor) when `FlightDriver.CanRevertToPrelaunch` is set. So
        `RevertAvailability.canRevertToLaunch` maps to `CanRevertToPostInit` and
        `RevertAvailability.canRevertToEditor` maps to `CanRevertToPrelaunch`: the
        KSP field names read backwards to their button labels, so the mapping is
        deliberately the inverse of a naive name-match.

        Same `system`-uplink convention as `SystemBodies`: a scene-side fact carried
        on its own Topic with no per-payload `Meta` (it rides the envelope),
        classified `DelayRole.TrueNow`: a ground-side game-state fact, not
        comms-derived vessel telemetry.
      required:
        - canRevertToEditor
        - canRevertToLaunch
      properties:
        canRevertToEditor:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            Can the active flight be reverted back to the editor (VAB/SPH)? Mirrors
            `FlightDriver.CanRevertToPrelaunch`.
        canRevertToLaunch:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            Can the active flight be reverted to its launch (on-the-pad) state? Mirrors
            `FlightDriver.CanRevertToPostInit`.
    RevertToEditorArgs:
      type: object
      description: |-
        `ksp.revertToEditor`'s args, which editor the flight reverts back into.
        `RevertToEditorArgs.editor` is a small opaque string (`"vab"` or `"sph"`,
        case-insensitive) rather than the KSP `EditorFacility` enum, so the wire
        contract never leaks a native KSP type; the host bridges the string to the
        real facility (unrecognised value fails admission with
        `CommandErrorCode.Range` before the game is ever touched).

        `ksp.revertToLaunch`, `ksp.toTrackingStation` and `ksp.recover` take no args
        (they operate on the current flight / active vessel), so they have no arg
        type here.
      required:
        - editor
      properties:
        editor:
          type: string
          x-sitrep-unit: text
          description: |-
            `"vab"` or `"sph"` (case-insensitive). Any other value yields
            `CommandResult.errorCode` `CommandErrorCode.Range`.
    RoboticsAvailability:
      type: object
      description: |-
        The `robotics.available` channel payload: a single wrapper object (or `null`
        when there is no active vessel) whose one field states whether the active
        vessel carries ANY Breaking Ground robotic servo (rotor / hinge / piston).
        This is deliberately its OWN Topic, not a field folded into the bare-array
        `parts.robotics`: an empty `ServoEntry[]` can't disambiguate "vessel has no
        robotic parts" (`available: false`) from "no snapshot / no active vessel"
        (payload `null`): the very ambiguity a widget like `RoboticsConsole` /
        `RotorTachometer` needs resolved to decide whether to render a "no robotics
        on this craft" empty state versus stay dark. It is DISTINCT from the
        Breaking-Ground DLC-presence fact (that is the `deployed.available` /
        `Meta.Dlc` build): this reflects parts present on THIS vessel, so it rides
        the delay clock (Delayed), whereas DLC presence is a ground-side TrueNow
        fact.

        `RoboticsAvailability.available` is nullable to mirror
        `SnapshotDict.GetBool`'s null-on-absence rule: a snapshot recorded before
        this field existed reads as `null`; a live snapshot always carries a
        concrete `true`/`false`.

        **Typing-only mirror** of
        `Sitrep.Host.BreakingGroundViewProvider.BuildRoboticsAvailable`: see
        `PartsPower` for the "no wire change" rationale.
      properties:
        available:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
    RosterCommsControlSource:
      type: integer
      description: |-
        Roster-level control-link tier for `VesselRosterEntry.commsControlSource`.
        Deliberately its OWN enum, not a reuse of `CommsControlSource`, that type
        belongs to the active-vessel-only `comms.*` elected-backend family
        (ICommsBackend/`CommsElection`), which this roster read does not touch (see
        `VesselRosterEntry`'s own doc comment). The three tiers happen to mirror
        stock `Vessel.ControlLevel`'s none/partial/full shape, which is coincidence,
        not a shared contract.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: None
        - const: 1
          title: Partial
        - const: 2
          title: Full
    RotorReverseArgs:
      type: object
      description: |-
        Args for `robotics.rotor.reverse`: flips the rotor's spin direction. This is
        the one robotics command that is genuinely a toggle (the widget's intent is
        "spin the other way" relative to whatever the rotor is doing now), so it
        carries no state field, only the `RotorReverseArgs.partId` to act on.
      required:
        - partId
      properties:
        partId:
          type: string
          x-sitrep-unit: id
          description: |-
            The part's `flightID.ToString()`: the id the read side stamps on each
            `parts.robotics` entry.
    RotorSetValueArgs:
      type: object
      description: |-
        Args for the rotor scalar-limit commands
        (`robotics.rotor.setRpmLimit`/`setTorqueLimit`/`setBrake`), the ABSOLUTE
        value to apply, keyed by `RotorSetValueArgs.partId`. The bounded ones
        (torque 0–100, brake 0–200) are range-validated at the send gate; out of
        range yields `CommandResult.errorCode` `CommandErrorCode.Range`.
      required:
        - partId
        - value
      properties:
        partId:
          type: string
          x-sitrep-unit: id
          description: |-
            The part's `flightID.ToString()`: the id the read side stamps on each
            `parts.robotics` entry.
        value:
          type: number
          x-sitrep-unit: ratio
          description: |-
            The absolute value to apply (rpm limit, torque-limit percent 0–100, or brake
            percent 0–200).
    SasMode:
      type: integer
      description: |-
        Mirrors KSP's own `VesselAutopilot.AutopilotMode` enum (confirmed via
        decompile: `StabilityAssist, Prograde, Retrograde, Normal, Antinormal,
        RadialIn, RadialOut, Target, AntiTarget, Maneuver`: no `Navigation` member
        exists on this KSP version). `SasMode.Unknown` is the graceful fallback for
        a raw value this contract doesn't recognize yet, same convention as
        `VesselType`/`TransitionType`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: StabilityAssist
        - const: 1
          title: Prograde
        - const: 2
          title: Retrograde
        - const: 3
          title: Normal
        - const: 4
          title: Antinormal
        - const: 5
          title: RadialIn
        - const: 6
          title: RadialOut
        - const: 7
          title: Target
        - const: 8
          title: AntiTarget
        - const: 9
          title: Maneuver
        - const: 10
          title: Unknown
    SavedShipEntry:
      type: object
      description: |-
        One craft file in the `spaceCenter.savedShips` channel, a saved VAB or SPH
        design the player can launch, read from the save's craft folders via the
        stock `CraftProfileInfo` metadata loader. Produced by
        `Sitrep.Host.SpaceCenterViewProvider.BuildSavedShips`.

        The channel is a BARE ARRAY of these entries (tagged `isArray: true`), one
        per `.craft` file keyed by `SavedShipEntry.name`. The whole payload is
        `null` (not an empty array) when no sample has landed yet. A TS-shape-only
        typing/codegen marker (the provider hand-builds the dict, these POCOs never
        serialize). Classified `DelayRole.TrueNow`.
      properties:
        name:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: Craft name (`CraftProfileInfo.shipName`).
        partCount:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: Part count (`CraftProfileInfo.partCount`).
        totalMass:
          type:
            - number
            - "null"
          x-sitrep-unit: t
          description: Total mass in tonnes (`CraftProfileInfo.totalMass`).
        facility:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Which editor built it: the `EditorFacility` enum name, `"VAB"` or `"SPH"`
            (`CraftProfileInfo.shipFacility`). A display label; see
            `SavedShipEntry.facilityOrdinal`.
        facilityOrdinal:
          description: |-
            `SavedShipEntry.facility`'s KSP ORDINAL, typed to `KspEditorFacility`.

            This one is not a display concern. The client sends the facility straight
            back as the `ksp.launch` command's `facility` argument, and it used to
            accept the name only if it matched a hand-written `{"VAB", "SPH"}` set and
            otherwise substituted `"VAB"`. A substituted default that becomes a
            dispatched argument is not a fallback: it launches a spaceplane from the
            launchpad. The set also omitted `None`, which KSP declares.

            `null` when the capture carried no facility, which is a third answer and
            must not be read as either editor.
          anyOf:
            - $ref: "#/components/schemas/KspEditorFacility"
              x-sitrep-unit: enum
            - type: "null"
        requiresFunds:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
          description: |-
            Funds needed before this can launch, the full craft cost
            (`CraftProfileInfo.totalCost`).
        missingParts:
          type:
            - array
            - "null"
          items:
            type: string
            x-sitrep-unit: text
          description: |-
            Parts referenced by the craft that are not yet unlocked/purchased
            (`CraftProfileInfo.UnavailableShipParts`); an empty array when the craft is
            buildable as-is.
    ScetAlarm:
      type: object
      description: |-
        One armed SCET alarm as the simulation host holds it: the `alarm.scet`
        channel is a bare array of these.

        The roster exists so an operator can see what is still armed after a
        reconnect, and so a client can disarm an entry it no longer remembers. It is
        ground-side bookkeeping, a list of things somebody asked for rather than a
        reading of any craft, which is why the channel does not ride the reveal
        clock.
      required:
        - id
        - name
        - armedBy
        - subject
        - condition
        - state
      properties:
        id:
          type: string
          x-sitrep-unit: id
          description: |-
            The client's own id for the alarm, minted where the alarm was created and
            carried unchanged. Arming an id that is already armed REPLACES it, so a
            re-arm is idempotent and a reconnect cannot duplicate a row.
        name:
          type: string
          x-sitrep-unit: text
          description: |-
            What the operator called it. Echoed so a roster row a client no longer
            recognises can still be named on screen.
        armedBy:
          type: string
          x-sitrep-unit: id
          description: |-
            The command centre the arm command was sent from, as a
            `commandCentre.roster` id.

            Provenance only: a SCET alarm stops the warp for everybody, because warp is
            a property of the simulation rather than of any one vantage. This says who
            asked for it, never who it applies to.
        subject:
          type: string
          x-sitrep-unit: id
          description: |-
            What the condition is about: `"vessel:<guid>"` for a craft, or `"game"` for
            something the whole simulation shares. The same vocabulary `meta.source`
            uses.

            A time condition is always `"game"`: universal time is the game's, and a
            clock has no craft to belong to. The field is not therefore decoration, it
            is what stops a condition that DOES name a craft from silently re-aiming
            when the player switches vessels.
        condition:
          $ref: "#/components/schemas/ScetAlarmCondition"
        state:
          $ref: "#/components/schemas/ScetAlarmState"
          x-sitrep-unit: enum
        firedAtUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            The universal time the alarm fired at, or `null` while it is still armed. On
            the craft's clock, like `ScetAlarmCondition.ut`.
    ScetAlarmArmArgs:
      type: object
      description: |-
        `alarm.scet.arm`'s args: register an alarm with the simulation host, or
        replace one already registered under the same `ScetAlarmArmArgs.id`.

        Never delayed. Arming changes nothing aboard the craft, so there is no
        light-time fiction to honour, and the same reasoning `time.setWarpIndex` has
        always carried applies: this is a control on the simulation, not a signal to
        a spacecraft. Delayed it would also be unusable, because an alarm for an
        event less than one light-time away could never be armed in time, and a
        delayed command is dropped outright during a blackout, which is exactly when
        a SCET alarm earns its keep.
      required:
        - id
        - name
        - subject
        - condition
      properties:
        id:
          type: string
          x-sitrep-unit: id
        name:
          type: string
          x-sitrep-unit: text
        subject:
          type: string
          x-sitrep-unit: id
          description: See `ScetAlarm.subject`. Empty is read as `"game"`.
        condition:
          $ref: "#/components/schemas/ScetAlarmCondition"
    ScetAlarmCondition:
      type: object
      description: |-
        What a SCET alarm watches for, on the craft's own clock.

        A discriminated shape: `ScetAlarmCondition.kind` says which of the fields
        below carry meaning. For `ScetAlarmConditionKind.Time` that is
        `ScetAlarmCondition.ut` and `ScetAlarmCondition.leadSeconds`.
      required:
        - kind
        - ut
        - leadSeconds
      properties:
        kind:
          $ref: "#/components/schemas/ScetAlarmConditionKind"
          x-sitrep-unit: enum
        ut:
          type: number
          x-sitrep-unit: ut
          description: |-
            The instant the alarm is set for, as a universal time on the CRAFT's clock
            rather than on the clock the command centre is reading.

            The difference between the two is the one-way light time, and it moves
            continuously as the craft does. A client that subtracts it once, at the
            moment the operator clicks, is right only for that instant; this field is
            the instant itself, compared against the game's own clock every tick, so the
            answer stays right however the geometry changes between arming and firing.
        leadSeconds:
          type: number
          x-sitrep-unit: s
          description: |-
            How long before `ScetAlarmCondition.ut` warp is stopped, in seconds, so the
            operator has real time in hand before the instant they set the alarm for.
            Zero stops the warp at the instant itself.

            The stop and the notice are two separate moments: warp halts at `Ut -
            LeadSeconds`, and the alarm fires at `Ut`, which arrives in real time
            because the warp is already stopped.
    ScetAlarmConditionKind:
      type: integer
      description: |-
        Which kind of condition a SCET alarm watches for.

        Only `ScetAlarmConditionKind.Time` exists today. A SCET vantage is
        meaningful exactly where the craft's TRUE state and the state the ground has
        been told differ, which is why there is no member here for a contract
        parameter: career bookkeeping is known to the command centre without any
        link, so there is no gap for a vantage to straddle.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Time
          description: An instant on the craft's own clock, as a universal time.
    ScetAlarmDisarmArgs:
      type: object
      description: |-
        `alarm.scet.disarm`'s args: forget the alarm with this id. Silently succeeds
        for an id the host does not hold, so a client reconciling its list against
        the roster never has to ask first.

        Never delayed, for the reason its opposite is not: the inverse of an instant
        act must not be slower than the act.
      required:
        - id
      properties:
        id:
          type: string
          x-sitrep-unit: id
    ScetAlarmFired:
      type: object
      description: |-
        The notice that a SCET alarm has fired and the warp has been stopped.

        **It says THAT one fired and WHEN, and deliberately nothing about the
        craft.** The stop is universal, because warp is; the telemetry is not, and
        stays a light-time behind. So an operator can be told their alarm went off
        while every reading beside it still shows the craft as it was minutes ago,
        and this payload carries nothing that would close that gap early. Whatever
        the craft was doing at `ScetAlarmFired.firedAtUt` arrives when it arrives.

        The two fields are the honest minimum. The id is the operator's own handle,
        which tells them nothing they did not already write down. The instant is
        inseparable from the stop: without it a warp that halted at one universal
        time is indistinguishable from one that halted at another, and the operator
        cannot tell which of two armed alarms stopped them.
      required:
        - id
        - firedAtUt
      properties:
        id:
          type: string
          x-sitrep-unit: id
          description: Which alarm, as the `ScetAlarm.id` it was armed under.
        firedAtUt:
          type: number
          x-sitrep-unit: ut
          description: The universal time it fired at, on the craft's clock.
    ScetAlarmState:
      type: integer
      description: |-
        Where an armed SCET alarm has got to.

        A latch, not a level: `ScetAlarmState.Fired` is reached once and stays, so a
        condition that keeps holding cannot stop the warp again on the next tick.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Armed
          description: Watching. The condition has not been met.
        - const: 1
          title: Fired
          description: The condition was met, warp was stopped, and the notice went out.
    SendManeuverPlanArgs:
      type: object
      description: |-
        A whole flight plan, composed at a command centre and transmitted to be
        instantiated aboard.

        **Why a whole plan rather than per-burn edits.** Five burn edits are five
        messages, each with its own light-time, each able to arrive late, out of
        order, or not at all. A craft that received three of them would fly a plan
        nobody composed and nobody approved. One plan is one message, applied whole
        or not at all.

        **The burns are transmitted, never re-derived.** The receiving side installs
        these numbers rather than re-solving toward a goal. A plan re-solved on
        arrival would be solved against the craft's true state, which is ahead of
        everything the operator could see, so the craft would fly something nobody
        at the command centre ever looked at.
      properties:
        vesselId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        requestId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Stable per-intent id, so a plan that is retransmitted after a silence is
            recognised as the same plan rather than applied twice.
        composedAtViewUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            The view instant the plan was composed against: what the command centre
            could see when it decided.
        observedAtUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            The instant the state used for planning was actually TRUE, at or before
            `SendManeuverPlanArgs.composedAtViewUt`.

            Both travel because they answer different questions: one is when the
            operator decided, the other is how old their information already was.
            Together they make the divergence between what was planned against and what
            received the plan a measurement rather than a guess.
        burns:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/ComposedBurn"
          description: |-
            The burns, in order. An EMPTY list is a plan with no burns, which is a
            meaningful thing to send because it clears the plan; a NULL list is a
            malformed command and is refused. The two must not be confused.
        desiredFinalTimeUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: How far the plan is asked to run.
    SensorEntry:
      type: object
      description: |-
        One entry in the `science.sensors` channel payload, a single
        environmental-sensor module (`ModuleEnviroSensor`: thermometer, barometer,
        gravioli detector, accelerometer, and any modded sensor sharing the module)
        on the ACTIVE vessel. The channel payload is a BARE ARRAY (`SensorEntry[]`)
        or `null`.

        Deliberately a GENERAL sensor group: one entry per sensor module, with
        `SensorEntry.type` carrying the raw `SensorType` enum name
        (`TEMP`/`PRES`/`GRAV`/`ACC`/...) as a string: rather than four fixed
        `temp/pres/grav/acc` Values. Modded sensor types and multiple instances of
        the same type both fall out naturally; the consumer (ScienceBench)
        groups/labels by `SensorEntry.type`.

        Typing-only mirror of `Sitrep.Host.ScienceViewProvider.BuildSensorEntry`:
        see `ExperimentEntry` for the "no wire change, all fields nullable"
        rationale.
      properties:
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Flight-scoped `part.flightID` as a string (null when the sentinel 0), the
            join key that disambiguates symmetric same-named sensor parts.
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        type:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            The raw `SensorType` enum name (`TEMP`/`PRES`/`GRAV`/`ACC`/...) passed
            through as a string so modded types survive.
        readout:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The sensor's current human-readable readout string (KSP's `readoutInfo`,
            e.g. "293.1K" or "Off").
        active:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
    ServoEntry:
      type: object
      description: |-
        One entry in the `robotics.servos` channel payload, a single Breaking Ground
        robotic servo on the active vessel. The channel payload is a BARE ARRAY of
        these (`ServoEntry[]`) or `null` (never a wrapper object) so the Topic tag
        sits on this element type with `IsArray = true`.

        `ServoEntry.type` is the servo kind as a plain string on the wire, NOT an
        enum, mirroring what the provider emits today; the enum cleanup is a later
        phase. The kinds are `"rotor"`, `"hinge"`, `"rotationServo"` and `"piston"`,
        plus `"servo"` for a `BaseServo` subclass the capture does not recognise (a
        part pack's own, or one a later KSP adds), which carries only the readings
        every servo has.

        **This list is a description, not a rule.** The capture derives the kinds
        from `BaseServo` itself rather than from any written-down set, which is the
        whole point: the set used to be written down, rotation servos were left out
        of it, and every one on every craft was dropped before it reached the wire.
        A consumer should switch on the kinds it can draw and ignore the rest, never
        assume this sentence is exhaustive.

        **Typing-only mirror** of
        `Sitrep.Host.BreakingGroundViewProvider.BuildServoEntry`: see `PartsPower`
        for the "no wire change, all fields nullable" rationale.
      properties:
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        type:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        servoIsLocked:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        servoIsMotorized:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        servoMotorIsEngaged:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        servoMotorLimit:
          type:
            - number
            - "null"
          x-sitrep-unit: "%"
        motorState:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        currentAngle:
          type:
            - number
            - "null"
          x-sitrep-unit: °
        targetAngle:
          type:
            - number
            - "null"
          x-sitrep-unit: °
        traverseVelocity:
          type:
            - number
            - "null"
          x-sitrep-unit: n/a
        currentRPM:
          type:
            - number
            - "null"
          x-sitrep-unit: rpm
        rpmLimit:
          type:
            - number
            - "null"
          x-sitrep-unit: rpm
        normalizedOutput:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
        brakePercentage:
          type:
            - number
            - "null"
          x-sitrep-unit: "%"
        currentExtension:
          type:
            - number
            - "null"
          x-sitrep-unit: m
        targetExtension:
          type:
            - number
            - "null"
          x-sitrep-unit: m
        counterClockwise:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Rotor spin direction (rotor entries only: `null` for every other kind).
            Mirrors `ModuleRoboticServoRotor.rotateCounterClockwise`: `true` means the
            rotor spins counter-clockwise.
        maxTorque:
          type:
            - number
            - "null"
          x-sitrep-unit: kN
          description: |-
            Rotor torque ceiling in kN (rotor entries only, `null` for every other
            kind). Mirrors `ModuleRoboticServoRotor.maxTorque`: the scale
            `ServoMotorLimit` (a percentage) is a fraction of.

            The unit is kN, per KSP's own editor UI: `maxTorque` feeds
            `motorOutputInformation`, the part's editor-visible display, formatted with
            localization token `#autoLOC_8002342` ("<<1>>kN max: Extra mass <<2>>t"). A
            decompile of `ModuleRoboticServoRotor` shows the same value also feeds a
            Unity angular drive's `maximumForce`, which on an angular drive is
            technically a moment, but the wire states what KSP's own UI labels it, and
            that label is kN.
    ServoSetEnabledArgs:
      type: object
      description: |-
        Args shared by every robotics boolean actuation
        (`robotics.servo.setMotor`/`setLock` and
        `robotics.rotor.setMotor`/`setLock`): an ABSOLUTE state to apply, never a
        toggle, matching every other actuation command in this contract (see
        `SetEnabledArgs`'s doc comment). Keyed by `ServoSetEnabledArgs.partId` (the
        read side's `flightID.ToString()`).
      required:
        - partId
        - enabled
      properties:
        partId:
          type: string
          x-sitrep-unit: id
          description: |-
            The part's `flightID.ToString()`: the id the read side stamps on each
            `parts.robotics` entry.
        enabled:
          type: boolean
          x-sitrep-unit: flag
    ServoSetTargetArgs:
      type: object
      description: |-
        Args for the servo target commands (`robotics.servo.setTarget`), the
        ABSOLUTE angle (hinge) or extension (piston) to drive to, keyed by the
        part's `ServoSetTargetArgs.partId`. `ServoSetTargetArgs.partId` is the same
        `flightID.ToString()` the read side emits on each `parts.robotics` servo
        entry, so a widget round-trips the exact id it already displays. A rotor has
        no target (it spins continuously); a `setTarget` aimed at one comes back
        `CommandResult.errorCode` `CommandErrorCode.ModeUnavailable`.
      required:
        - partId
        - value
      properties:
        partId:
          type: string
          x-sitrep-unit: id
          description: |-
            The part's `flightID.ToString()`: the id the read side stamps on each
            `parts.robotics` entry.
        value:
          type: number
          x-sitrep-unit: ratio
          description: "Absolute target: hinge angle (degrees) or piston extension."
    SetActionGroupArgs:
      type: object
      description: |-
        `vessel.control.stage`'s result is `CommandResult<int>`, a real value comes
        back (the new current stage index in `Payload`), unlike the legacy `f.stage`
        void fire-and-forget. See `CommandResult`.
      required:
        - group
        - state
      properties:
        group:
          type: number
          x-sitrep-unit: id
          description: |-
            1..10. Any other value yields `CommandResult.errorCode`
            `CommandErrorCode.Range`.
        state:
          type: boolean
          x-sitrep-unit: flag
    SetControlAxesArgs:
      type: object
      description: |-
        `vessel.control.setAxes`'s args: a partial update of the held fly-by-wire
        override. Every field is nullable so the client can drive ONE axis at a time
        (set-pitch alone) without clobbering the others: only non-null fields
        overwrite their stored value. Rotation
        (`SetControlAxesArgs.pitch`/`SetControlAxesArgs.yaw`/`SetControlAxesArgs.roll`)
        and translation
        (`SetControlAxesArgs.x`/`SetControlAxesArgs.y`/`SetControlAxesArgs.z`) are
        −1..1; the analog value is preserved end-to-end (a mapped analog stick gives
        proportional RCS rather than the legacy fork's −1/0/1 quantisation). Trim
        (`SetControlAxesArgs.pitchTrim`/`SetControlAxesArgs.yawTrim`/`SetControlAxesArgs.rollTrim`)
        is applied from inside the callback each frame alongside the axes, so it
        stays durable while armed instead of being stomped by SAS. Out-of-range
        values are clamped to −1..1 at the admission gate (a hardware stick reading
        slightly past full is a routine quirk, not an error).
      properties:
        pitch:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
        yaw:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
        roll:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
        x:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
        y:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
        z:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
        pitchTrim:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
        yawTrim:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
        rollTrim:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
    SetControlFrameArgs:
      type: object
      description: |-
        Whatever knows what frame the game's navigation view is currently in.

        A capability rather than a method on core, because the answer belongs to
        whichever mod owns the view. Stock's answer is real and simple, a body and
        inertial axes; an n-body producer's is one of five kinds over sets of
        bodies. Core resolves the interface and never learns which is installed,
        which is the whole of what makes this side stock-shaped rather than
        producer-shaped.

        Frame is null when nothing could be read. That is not a gap to fill with a
        default: a substituted frame draws a trajectory that looks exactly like one
        drawn in the frame the player is actually in, and nothing downstream could
        tell them apart.
      required:
        - kind
      properties:
        kind:
          $ref: "#/components/schemas/ControlFrameKind"
          x-sitrep-unit: enum
        centreBody:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: The body to centre on. Required for the centred frames.
        primaryBody:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: The body a rotating frame turns about. Required for the rotating frames.
        secondaryBody:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: The body a rotating frame is anchored to. Required for the rotating frames.
        targetFrameSelected:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Ask for the target frame, which sits orthogonally to
            `SetControlFrameArgs.kind` rather than inside it.
    SetEnabledArgs:
      type: object
      description: |-
        Args shared by every plain boolean actuation command (`setSas`/
        `setRcs`/`setGear`/`setBrakes`/`setLights`): an ABSOLUTE state to apply,
        never a toggle. Under light-time delay a toggle arriving after unknown
        intervening state is a race by construction, the `toggleActionGroup`
        footgun. Every M1 actuation command is set-semantics only, so it does not
        exist here at all.
      required:
        - enabled
      properties:
        enabled:
          type: boolean
          x-sitrep-unit: flag
    SetFlyByWireArgs:
      type: object
      description: |-
        `vessel.control.setFlyByWire`'s args: arm/disarm the persistent fly-by-wire
        override. FBW is the one `vessel.control.*` command that is NOT a one-shot
        actuation: a raw control axis (pitch/yaw/roll/translation) is re-zeroed by
        KSP every physics frame, so the mod holds an override struct and re-applies
        it from a `Vessel.OnFlyByWire` callback while armed. This command flips that
        armed flag: `SetFlyByWireArgs.enabled` `true` attaches the callback (axes
        resume from their last-set values, or 0 on first arm), `false` detaches it
        and neutralizes the stored axes/trims so control is fully handed back to the
        player/SAS with no residual override.
      required:
        - enabled
      properties:
        enabled:
          type: boolean
          x-sitrep-unit: flag
    SetPausedArgs:
      type: object
      required:
        - paused
      properties:
        paused:
          type: boolean
          x-sitrep-unit: flag
    SetSasModeArgs:
      type: object
      required:
        - mode
      properties:
        mode:
          $ref: "#/components/schemas/SasMode"
          x-sitrep-unit: enum
    SetSimulationDelayPolicyArgs:
      type: object
      description: |-
        Arguments to `comms.setSimulationDelayPolicy`: apply signal delay during a
        simulation, or cut it.
      required:
        - applyDuringSimulation
      properties:
        applyDuringSimulation:
          type: boolean
          x-sitrep-unit: flag
    SetTargetArgs:
      type: object
      description: |-
        `vessel.target.set`'s args: a discriminated union expressed as
        `SetTargetArgs.kind` + the one field that kind actually uses (C# has no
        native union type; this mirrors `TargetKind`'s existing vessel/body/other
        split rather than inventing a parallel shape). T-1 fixed:
        `SetTargetArgs.vesselId` is the STABLE opaque vessel id (resolved
        server-side against `FlightGlobals.Vessels`), never a live array index a
        client would have to track itself. T-2 fixed: vessel id and body index are
        separate fields in separate namespaces, so they can never be confused for
        one another.
      required:
        - kind
      properties:
        kind:
          $ref: "#/components/schemas/TargetKind"
          x-sitrep-unit: enum
        vesselId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Required when `SetTargetArgs.kind` is `TargetKind.Vessel`. ALSO required
            when `SetTargetArgs.kind` is `TargetKind.Part`, the guid of the vessel that
            OWNS the target part (a part id is unique only within its vessel).
        partId:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Required when `SetTargetArgs.kind` is `TargetKind.Part`, the docking port's
            KSP `Part.flightID`, resolved server-side against the parts of the vessel
            named by `SetTargetArgs.vesselId`. Null for every other kind.
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Required when `SetTargetArgs.kind` is `TargetKind.Body`, the same
            `system.bodies` index `VesselOrbit.referenceBodyIndex` uses. ALSO required
            (T-POI-4) when `SetTargetArgs.kind` is `TargetKind.Position`, which body
            `SetTargetArgs.latitude`/ `SetTargetArgs.longitude` are measured against (a
            lat/lon pair has no meaning without one).
        latitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Required when `SetTargetArgs.kind` is `TargetKind.Position` (a map-picked
            surface fix, e.g. a `spaceCenter.pois` entry's own coordinate).
        longitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: Required when `SetTargetArgs.kind` is `TargetKind.Position`.
    SetThrottleArgs:
      type: object
      required:
        - value
      properties:
        value:
          type: number
          x-sitrep-unit: ratio
          description: |-
            0..1: validated (not silently clamped) at admission; out of range yields
            `CommandResult.errorCode` `CommandErrorCode.Range` (A-10's inconsistency
            fixed at the send gate).
    SetVantage:
      type: object
      description: |-
        Client-to-server: select the command centre this connection commands from
        and observes at (Plan 3 vantage selection). Governs both the downlink cursor
        read and the command-dispatch vantage. `"ksc"` (the default) is always
        selectable; any other id must name a currently-active command centre.
      required:
        - type
        - centreId
      properties:
        type:
          type: string
          const: set-vantage
          x-sitrep-unit: id
        centreId:
          type: string
          x-sitrep-unit: id
          description: The command centre Id to adopt as this connection's vantage.
    SetWarpIndexArgs:
      type: object
      description: |-
        `time.setWarpIndex`'s args: sim-meta, never delayed (light-time fiction
        doesn't apply to a ground-side simulation control).
      required:
        - index
      properties:
        index:
          type: number
          x-sitrep-unit: id
    Situation:
      type: integer
      description: |-
        Mirrors KSP's own `Vessel.Situations` enum by concept (member names here are
        this contract's own PascalCase spelling, `Sitrep.Host. VesselViewProvider`'s
        `ParseSituation` maps KSP's raw SCREAMING_SNAKE_CASE `.ToString()` onto
        these, never passing the raw string through directly). Kills V-13: the
        `v.situation`/ `v.situationString`/`v.landedAt` triplet collapses to this
        one typed field. `Situation.Unknown` is the graceful fallback for a raw
        value this contract doesn't yet recognize, rather than the mapper throwing
        on a future KSP version adding a situation.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Landed
        - const: 1
          title: Splashed
        - const: 2
          title: PreLaunch
        - const: 3
          title: Orbiting
        - const: 4
          title: Escaping
        - const: 5
          title: Flying
        - const: 6
          title: SubOrbital
        - const: 7
          title: Docked
        - const: 8
          title: Unknown
    SolarPanelEntry:
      type: object
      description: |-
        One solar panel in the `parts.power` payload's `solarPanels` array.
        Typing-only mirror of `Sitrep.Host.PartsViewProvider.BuildSolarPanelEntry`,
        every field nullable because each is read through `SnapshotDict.Get*`, which
        yields `null` (not a sentinel) on absence. See `PartsPower` for the "no wire
        change" rationale.
      properties:
        partName:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        partId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
        deployState:
          type:
            - string
            - "null"
          x-sitrep-unit: text
        flowRate:
          type:
            - number
            - "null"
          x-sitrep-unit: units/s
        chargeRate:
          type:
            - number
            - "null"
          x-sitrep-unit: units/s
        sunAOA:
          type:
            - number
            - "null"
          x-sitrep-unit: °
    SpaceCenterPartsAvailable:
      type: object
      description: |-
        The `spaceCenter.partsAvailable` channel payload: a wrapper carrying the
        count of parts the player can place right now (tech-unlocked AND purchased
        in career; the full `PartLoader` catalogue in sandbox). Produced by
        `Sitrep.Host.SpaceCenterViewProvider.BuildPartsAvailable`.

        A wrapper object (a bare scalar has no Topic shape); the SpaceCenterStatus
        widget reads `spaceCenter.partsAvailable.count`. The whole payload is `null`
        when no sample has landed yet. A TS-shape-only typing/codegen marker that
        never serializes. Classified `DelayRole.TrueNow`.
      properties:
        count:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: Count of buildable parts.
    SpaceCenterPoiEntry:
      type: object
      description: |-
        One point of interest in the `spaceCenter.pois` channel: the union of every
        launch site (`ksc`/`launchSite` kinds, the same
        `PSystemSetup.Instance.LaunchSites` walk `LaunchSiteEntry` already does,
        filtered to sites with a set spawn-point coordinate) and every surface
        contract waypoint currently Active or Offered (`contractTarget` kind, from
        `FinePrint.WaypointManager`). Produced by
        `Sitrep.Host.SpaceCenterViewProvider.BuildPois`.

        The channel is a BARE ARRAY of these entries (tagged `isArray: true`, like
        `LaunchSiteEntry`), one per POI keyed by `SpaceCenterPoiEntry.id`. The whole
        payload is `null` (not an empty array) when no sample has landed yet: the
        provider's "no data yet" vs. "zero POIs" distinction. A TS-shape-only
        typing/codegen marker (the provider hand-builds the dict, this POCO never
        serializes). Classified `DelayRole.TrueNow` (ground-side facts, same class
        as `LaunchSiteEntry`).
      properties:
        id:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            `"launchSite:<LaunchSite.name>"` for `ksc`/`launchSite` kinds,
            `"contract:<Waypoint.navigationId>"` for `contractTarget`.
        kind:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: "`\"ksc\"` | `\"launchSite\"` | `\"contractTarget\"`."
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index into `SystemBodies`; null when absent or unresolved (never a sentinel
            like -1).
        latitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
        longitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
        label:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: "Display label: the launch site's display name, or the contract's title."
        status:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: "`\"active\"` | `\"available\"` (null for `ksc`/`launchSite` kinds)."
        contractAgent:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: Contract-issuing agent name; null for `ksc`/`launchSite` kinds.
        contractFundsAdvance:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
        contractFundsCompletion:
          type:
            - number
            - "null"
          x-sitrep-unit: funds
        contractDateDeadline:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
    SpaceCenterScene:
      type: object
      description: |-
        The `spaceCenter.scene` channel payload: the single current KSP game scene,
        produced by `Sitrep.Host.SpaceCenterViewProvider.BuildScene`. This is the
        migration target for the legacy `kc.scene` key: `SpaceCenterScene.scene`
        carries exactly one of the six strings
        `{"Flight","SpaceCenter","Editor","TrackingStation","MainMenu","Other"}`
        (the provider folds KSP's `GameScenes` enum onto that fixed set; any scene
        outside the five named ones: `LOADING`, `PSYSTEM`, `MISSIONBUILDER`, ...:
        maps to `"Other"`).

        Mirrors the exact serialized shape the provider emits (a wrapper object `{
        "scene": string }`); a TS-shape-only typing/codegen marker that does NOT
        participate in serialization. The whole payload is `null` when no sample has
        landed yet. No per-payload `Meta` (it rides the envelope), classified
        `DelayRole.TrueNow`: a ground-side game-state fact, same class as
        `SystemBodies`.
      properties:
        scene:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The current scene, one of
            `"Flight"`/`"SpaceCenter"`/`"Editor"`/`"TrackingStation"`/`"MainMenu"`/`"Other"`.
        launchSite:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The launch site currently selected in the editor
            (`EditorLogic.launchSiteName`), the migration target for the legacy
            `kc.launchSite` key. Null outside the editor scene (EditorLogic isn't live),
            never a fabricated default.
    StageDeltaVEntry:
      type: object
      description: |-
        One ΔV-producing stage of the active vessel, straight from KSP's STOCK
        `VesselDeltaV` stage simulation: the same numbers the in-game ΔV app shows,
        with atmosphere, ISP, crossfeed and staging all handled by the game. Only
        the stages that actually produce ΔV appear, the same set the in-game app
        lists rather than the raw stage list, so stage numbers can be sparse.

        The `dv.stages` payload is a BARE ARRAY of these or `null`: never a wrapper
        object. The whole array is `null` when the stock simulation is not ready or
        there is no active vessel.

        **Every field is nullable, and `null` is never a sentinel.** A field is
        `null` whenever the raw value is absent OR non-finite, so a stage the
        simulation reports as `NaN` or `Infinity` reaches you as `null` rather than
        as a number you would have to test.

        Carries no `meta` of its own: provenance rides the envelope
        (`StreamData.Meta`), never the payload body.
      properties:
        stage:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: "`DeltaVStageInfo.stage`: the stage number this entry describes."
        dvVac:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: "`DeltaVStageInfo.deltaVinVac`: stage ΔV in vacuum (m/s)."
        dvAsl:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: "`DeltaVStageInfo.deltaVatASL`: stage ΔV at sea level (m/s)."
        dvActual:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: "`DeltaVStageInfo.deltaVActual`: stage ΔV at the current situation (m/s)."
        burnTime:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: "`DeltaVStageInfo.stageBurnTime`: full-throttle burn time for the stage (s)."
        twrVac:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: "`DeltaVStageInfo.TWRVac`: thrust-to-weight ratio in vacuum."
        twrAsl:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: "`DeltaVStageInfo.TWRASL`: thrust-to-weight ratio at sea level."
        twrActual:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: |-
            `DeltaVStageInfo.TWRActual`: thrust-to-weight ratio at the current
            situation.
        thrustVac:
          type:
            - number
            - "null"
          x-sitrep-unit: kN
          description: "`DeltaVStageInfo.thrustVac`: stage thrust in vacuum (kN)."
        thrustAsl:
          type:
            - number
            - "null"
          x-sitrep-unit: kN
          description: "`DeltaVStageInfo.thrustASL`: stage thrust at sea level (kN)."
        thrustActual:
          type:
            - number
            - "null"
          x-sitrep-unit: kN
          description: "`DeltaVStageInfo.thrustActual`: stage thrust at the current situation (kN)."
        startMass:
          type:
            - number
            - "null"
          x-sitrep-unit: t
          description: "`DeltaVStageInfo.startMass`: stage start mass (tonnes)."
        endMass:
          type:
            - number
            - "null"
          x-sitrep-unit: t
          description: "`DeltaVStageInfo.endMass`: stage end (burnout) mass (tonnes)."
        dryMass:
          type:
            - number
            - "null"
          x-sitrep-unit: t
          description: "`DeltaVStageInfo.dryMass`: stage dry mass (tonnes)."
        fuelMass:
          type:
            - number
            - "null"
          x-sitrep-unit: t
          description: "`DeltaVStageInfo.fuelMass`: stage fuel mass (tonnes)."
        resources:
          type:
            - object
            - "null"
          additionalProperties:
            $ref: "#/components/schemas/ResourceAmount"
          description: |-
            Per-resource current/max amounts for the parts active IN THIS STAGE, the old
            `r.resourceCurrent[X]`/`r.resourceCurrentMax[X]` pair (as opposed to
            `vessel.resources`'s vessel-WIDE totals). `DeltaVStageInfo` itself has no
            per-resource field (only aggregate dry/fuel mass), so this is built by
            walking every part's `DeltaVPartInfo.stageFuelMass` snapshot for this stage
            number and summing by resource name
            (`Gonogo.KSP.KspHost.BuildStageResources`). Never null: an empty map is a
            real "no tracked resources active in this stage" reading, distinct from the
            whole stage entry being absent.
    StageDeltaVSummary:
      type: object
      description: |-
        The `dv.summary` channel payload: the whole-vessel ΔV rollup KSP's stock
        `VesselDeltaV` exposes alongside the per-stage `StageDeltaVEntry` list: the
        ΔV-producing stage count plus the vacuum / sea-level / current totals and
        total burn time. A SINGLE WRAPPER OBJECT (or `null` when the stock sim isn't
        ready / there is no active vessel), so the Topic tag sits on this type
        directly with the default `IsArray = false`.

        **Typing-only mirror** of `StageDeltaVViewProvider.BuildSummary`, same
        convention as `StageDeltaVEntry`: hand-built by the provider, never
        serialized itself, no per-payload `Meta` (it rides the envelope).
      properties:
        stageCount:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: "`VesselDeltaV.OperatingStageInfo.Count`: the number of ΔV-producing stages."
        totalDvVac:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: "`VesselDeltaV.TotalDeltaVVac`: total vessel ΔV in vacuum (m/s)."
        totalDvAsl:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: "`VesselDeltaV.TotalDeltaVASL`: total vessel ΔV at sea level (m/s)."
        totalDvActual:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: |-
            `VesselDeltaV.TotalDeltaVActual`: total vessel ΔV at the current situation
            (m/s).
        totalBurnTime:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: |-
            `VesselDeltaV.TotalBurnTime`: total full-throttle burn time across all
            stages (s).
    Staleness:
      type: integer
      description: |-
        How current a delivered sample is, as the SERVER knows it (a client infers
        the rest from its own heartbeat tracking).

        `Staleness.Fresh` is a sample delivered on its own schedule.
        `Staleness.HeldStale` and `Staleness.LastBeforeBlackout` are catch-up grades
        for a late or reconnecting subscriber. `Staleness.Recorded` is different in
        kind from all three: the sample is EXACT as of its own `Meta.validAt`, it
        simply did not travel at the time it was taken. It was held aboard through a
        loss of signal and dumped on acquisition, so it arrives long after the
        instant it describes, and its `Meta.deliveredAt` is the real arrival, not
        `validAt + light-time`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Fresh
        - const: 1
          title: HeldStale
        - const: 2
          title: LastBeforeBlackout
        - const: 3
          title: Recorded
          description: |-
            Recovered from the subject's own recorder: taken while it was out of
            contact, replayed on reacquisition. Precisely dated and never a guess, but
            not a live reading, and never the state of the subject NOW.
    StreamData:
      type: object
      description: |-
        The envelope every telemetry frame arrives in.

        `topic` is the channel's `address` and `payload` is that channel's own
        shape; each channel's `stream-data` message pins both, so this is the shape to
        route on and that one is the shape to read. `meta` is stamped by the mod on
        every frame and describes the DELIVERY rather than the value, which is where
        `validAt`, `deliveredAt` and staleness live.
      required:
        - type
        - topic
        - payload
        - meta
      properties:
        type:
          type: string
          const: stream-data
          x-sitrep-unit: id
        topic:
          type: string
          x-sitrep-unit: id
        payload:
          description: The topic's own value. Each channel's `stream-data` message narrows this to that channel's payload schema.
        meta:
          $ref: "#/components/schemas/Meta"
      examples:
        - type: stream-data
          topic: vessel.flight
          payload:
            latitude: -0.0972
            longitude: -74.5577
            altitudeAsl: 72840.31
            altitudeTerrain: 72837.02
            verticalSpeed: -4.2
            surfaceSpeed: 2287.4
            orbitalSpeed: 2301.8
            gForce: 0.02
            dynamicPressureKPa: 0.0004
            mach: 7.63
            atmDensity: 0.0000012
            externalTemperature: 231.4
            atmosphericTemperature: 228.9
            meta:
              source: vessel:8f2c1d40-97ab-4a2e-9c1f-6d0b3e5a7c11
              quality: 1
          meta: *a5
    Subscribe:
      type: object
      required:
        - type
        - topic
      properties:
        type:
          type: string
          const: subscribe
          x-sitrep-unit: id
        topic:
          type: string
          x-sitrep-unit: id
    SwitchVesselArgs:
      type: object
      description: |-
        `ksp.switchVessel`'s args: the STABLE opaque vessel id
        (`vessel.id.ToString()`, the same id `SetTargetArgs.vesselId` uses),
        resolved server-side against `FlightGlobals.Vessels`. Never a live roster
        array index a client would have to track itself: the same index-vs-stable-id
        hazard the target commands already fixed (T-1). An empty id fails admission
        with `CommandErrorCode.NotFound` before the game is ever touched.
      required:
        - vesselId
      properties:
        vesselId:
          type: string
          x-sitrep-unit: id
    SystemBodies:
      type: object
      description: |-
        The `system.bodies` channel payload: the celestial-body tree, produced by
        `Sitrep.Host.SystemViewProvider.BuildSystemBodies`. This type MIRRORS that
        provider's existing hand-built serialized shape EXACTLY (a wrapper object `{
        "bodies": [ ... ] }`); it is a typing/codegen marker so a widget resolves a
        real payload type instead of `unknown`, and does NOT participate in
        serialization (the provider still emits the live value tree that
        `JsonWriter` walks; see SitrepTopicAttribute). The whole payload is `null`
        (not an empty-bodies object) when no sample has landed yet, the provider's
        "no data yet" vs. "zero bodies" distinction.

        Deliberately carries NO `Meta` field: unlike the `vessel.*` family, this
        `system`-domain snapshot has no per-payload provenance: its `Meta` rides the
        envelope (`StreamData.Meta`), never the payload body.
      required:
        - bodies
      properties:
        bodies:
          type: array
          items:
            $ref: "#/components/schemas/BodyEntry"
    SystemVessels:
      type: object
      description: |-
        The `system.vessels` channel payload: the full known-vessel roster (every
        vessel, not just the active one, for TargetPicker-style "what could I
        target" listings), produced by `SystemViewProvider.BuildSystemVessels`.
        Mirrors that provider's existing serialized shape EXACTLY (a wrapper object
        `{ "vessels": [ ... ] }`). The whole payload is `null` when nothing is
        loaded (main menu), distinct from an empty roster (`{ "vessels": [] }`) when
        the game genuinely reports zero vessels. Same `system`-domain convention as
        `SystemBodies`: no per-payload `Meta` (it rides the envelope).
      required:
        - vessels
      properties:
        vessels:
          type: array
          items:
            $ref: "#/components/schemas/VesselRosterEntry"
    TargetAvailable:
      type: object
      description: |-
        The `target.available` channel payload: the list of everything targetable
        from the active vessel. Wrapper object `{ "entries": [ ... ] }`, mirroring
        the provider's hand-built shape (like `system.vessels`). Emitted part-tree
        style: a full keyframe on subscribe (sticky-cached for late subscribers),
        then re-emitted on set-change (a target enters/leaves range, or the current
        target changes) plus a slow heartbeat re-key, per-entry
        `TargetListEntry.distance` rides that periodic re-key, deliberately NOT the
        change-gate.
      required:
        - entries
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/TargetListEntry"
    TargetKind:
      type: integer
      description: |-
        Coarse classification of what `vessel.target` points at. KspHost's raw
        `type` string is either a `VesselType`-shaped string (target is a vessel,
        `target.GetVessel() != null`), the literal `"CelestialBody"`, or an
        arbitrary CLR type name for anything else (a docking port, a waypoint, ...).
        Rather than reproduce that CLR-name passthrough on the wire (its own naming
        wart), this contract collapses it to the three cases a consumer actually
        needs to branch on; `TargetKind.Other` covers docking
        ports/waypoints/anything not yet classified more finely (a future, more
        specific target-kind split is a non-breaking additive change, same
        convention as every other unknown-style fallback in this contract).

        `TargetKind.Position` (additive, appended never inserted: enum member order
        is wire-significant per this contract's numeric-serialisation convention) is
        a client-chosen surface fix used ONLY as an input to `vessel.target.set`
        (see `SetTargetArgs.latitude`/ `SetTargetArgs.longitude`), a map-picked
        lat/lon that isn't backed by any live KSP target object, so it never appears
        as `vessel.target`'s own reported `VesselTarget.kind`.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Vessel
        - const: 1
          title: Body
        - const: 2
          title: Other
        - const: 3
          title: Position
        - const: 4
          title: Part
          description: |-
            A part of a vessel: in practice a docking port (`ModuleDockingNode`, which
            implements `ITargetable`). Identity is the owning vessel's
            `VesselTarget.vesselId` guid PLUS the part's `VesselTarget.partId` (KSP
            `Part.flightID`): a part id alone is not globally unique, only within its
            vessel. Appended (never inserted) per this contract's wire-significant
            enum-order convention.
    TargetListEntry:
      type: object
      description: |-
        One entry in the `target.available` list: anything the active vessel could
        set as its target right now. Produced generically off KSP's `ITargetable`
        contract (Vessel / CelestialBody / ModuleDockingNode all implement it), then
        classified by concrete type into a `TargetListEntry.kind` + its stable id,
        rather than three hardcoded per-kind lists, so a modded `ITargetable` shows
        up as `TargetKind.Other` with no code change. The stable id per kind
        (`TargetListEntry.vesselId` guid / `TargetListEntry.bodyIndex` /
        `TargetListEntry.partId` flightID) is the SAME id `SetTargetArgs` takes, so
        a widget hands an entry straight back into `vessel.target.set` with no
        lookup.
      required:
        - kind
        - name
        - isCurrent
      properties:
        kind:
          $ref: "#/components/schemas/TargetKind"
          x-sitrep-unit: enum
        name:
          type: string
          x-sitrep-unit: text
          description: Clean display name (KSP `GetDisplayName()`, falling back to `GetName()`).
        vesselId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Stable vessel guid: set for `TargetKind.Vessel`, and the OWNING vessel for a
            `TargetKind.Part`. Null otherwise.
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: "Index into `system.bodies`: set for `TargetKind.Body`. Null otherwise."
        partId:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            KSP `Part.flightID`: set for `TargetKind.Part` (scoped by
            `TargetListEntry.vesselId`). Null otherwise.
        vesselType:
          description: |-
            Vessel type: set for `TargetKind.Vessel` / `TargetKind.Part` (the owning
            vessel's type). Null otherwise.
          anyOf:
            - $ref: "#/components/schemas/VesselType"
              x-sitrep-unit: enum
            - type: "null"
        situation:
          description: "Flight situation: set for `TargetKind.Vessel`. Null otherwise."
          anyOf:
            - $ref: "#/components/schemas/Situation"
              x-sitrep-unit: enum
            - type: "null"
        distance:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Current metric distance (metres) from the active vessel. A coarse sort aid
            for the picker, NOT a HUD value, it rides the periodic re-key, not the
            change-gate (it moves every tick). Live distance for the CURRENT target
            comes off `vessel.target`. Null when a transform wasn't available this tick.
        isCurrent:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            True when this entry is the active vessel's current target
            (`FlightGlobals.fetch.VesselTarget`) right now.
    ThermalHottestPart:
      type: object
      description: |-
        Raw readings for whichever part is hottest by internal-temperature ratio;
        see `VesselThermal.hottestPart`.
      required:
        - internalTemp
        - maxTemp
        - skinTemp
        - skinMaxTemp
        - name
      properties:
        internalTemp:
          type: number
          x-sitrep-unit: K
          description: |-
            Kelvin (KSP's `Part.temperature`). All four readings on this record share
            the unit, which is what makes the ratios on `VesselThermal` dimensionless.
        maxTemp:
          type: number
          x-sitrep-unit: K
        skinTemp:
          type: number
          x-sitrep-unit: K
        skinMaxTemp:
          type: number
          x-sitrep-unit: K
        name:
          type: string
          x-sitrep-unit: text
          description: |-
            Display name of the hottest part (`Part.partInfo.title`, falling back to
            `Part.name`, same convention as `VesselPart.title`). Never null when
            `VesselThermal.hottestPart` itself is non-null.
    TimeCalendar:
      type: object
      description: |-
        The `time.calendar` channel payload: how long a day is, how long a year is,
        and what real-world instant UT 0 is (when the game has one), as the RUNNING
        GAME defines them rather than as anyone assumed.

        **Why this channel exists.** Every duration on the wire is SI seconds, so
        any consumer that wants to say "3 days" has to divide by something, and
        until this channel existed the only thing to divide by was a constant
        compiled into the client: 21,600, one Kerbin rotation. That is right for
        stock KSP on Kerbin time and wrong three ways otherwise.

        - **Stock, no mods.** `GameSettings.KERBIN_TIME` is a real setting a player
          can turn off, and KSP's own UI then reads in 24-hour days and 365-day
          years. A consumer holding 21,600 disagrees with the game on the same
          screen.
        - **A planet pack.** RSS and anything else built on Kopernicus replaces
          `KSPUtil.dateTimeFormatter` outright, so a day becomes 86,400s and a year
          365 days. A client dividing by 21,600 reports four times too many days, in
          a number that looks entirely plausible.
        - **Anything else.** The formatter is an interface with a public setter; a
          mod can put any calendar behind it. Reading the numbers off it is the only
          approach that does not need a list of which mods to know about.

        **Where the values come from.** Straight off `KSPUtil.dateTimeFormatter`,
        whose `Minute`, `Hour`, `Day` and `Year` are each a count of SECONDS
        (confirmed by decompiling `IDateTimeFormatter`). No arithmetic, no
        derivation, no per-mod special case: whatever the game is using to print its
        own clock is what this channel carries.

        **It can change mid-session**, which is why this is a channel and not a
        one-shot descriptor like `system.units`. The KERBIN_TIME setting is
        reachable from the in-game settings menu at any time.

        **Deliberately not derived here:** days-per-year. A consumer that wants it
        divides `TimeCalendar.yearSeconds` by `TimeCalendar.daySeconds`, which is
        exact and needs no second field to keep in step. Publishing both would
        create a pair that can disagree.
      required:
        - minuteSeconds
        - hourSeconds
        - daySeconds
        - yearSeconds
        - kerbinTime
        - meta
      properties:
        minuteSeconds:
          type: number
          x-sitrep-unit: s
          description: |-
            Seconds in one minute. 60 everywhere so far, carried because the formatter
            exposes it and assuming is what this channel exists to stop.
        hourSeconds:
          type: number
          x-sitrep-unit: s
          description: Seconds in one hour.
        daySeconds:
          type: number
          x-sitrep-unit: s
          description: |-
            Seconds in one day: 21,600 on stock Kerbin time, 86,400 under Earth time or
            a planet pack.
        yearSeconds:
          type: number
          x-sitrep-unit: s
          description: |-
            Seconds in one year: 9,201,600 on stock Kerbin time (426 days), 31,536,000
            under a 365-day Earth calendar.
        epoch:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The real-world instant UT 0 corresponds to, ISO-8601 in UTC
            (`1951-01-01T00:00:00Z`), or `null` when the running game has no such
            instant.

            **Why the four durations above are not enough.** They say how long a day is;
            they do not say which day it is. Every `Units.UniversalTime` field on this
            wire is an offset from an anchor the wire never named, so a programme
            deadline, a contract expiry and a launch window could only ever be rendered
            as `Y3 D122`. An RSS operator reads `14 Mar 1957`, and until this field
            existed there was nothing to render it from.

            **Where it comes from.** The date formatter itself, and nowhere else.
            `KSPUtil.dateTimeFormatter` is an interface whose implementations that model
            a real calendar (RSSTimeFormatter, Kronometer) hold their anchor in a
            private `DateTime` field; reflecting it out is the only way to read it, and
            it is what RP-1 does for the same reason (`RP0DTUtils.TryGetEpoch`). Nothing
            here knows which mod is installed.

            **Null is the normal answer, and it is not zero.** The stock formatter
            carries no epoch because stock KSP has no real calendar: its own UI prints
            Year 1, Day 1, and so should every consumer of this channel. That holds for
            a planet pack too whenever no DateTime-based formatter is installed
            alongside it. Rendering some default anchor for those games would invent a
            date the game itself never shows.
        kerbinTime:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            The stock `GameSettings.KERBIN_TIME` flag, for a consumer that wants to
            LABEL the calendar rather than just measure with it ("Kerbin time" against
            "Earth time"). Not the source of truth for any arithmetic: the seconds
            fields above are, and they already account for this flag and for anything a
            planet pack did on top of it.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    TrajectoryArc:
      type: object
      description: |-
        A sampled path a provider computed, as points in a NAMED frame.

        **Why this exists rather than a client sampling the elements.**
        `VesselOrbit`'s elements are osculating, so sampling them gives the conic
        the craft is tangent to at the sample instant. That is exactly the
        trajectory for an analytic provider and is NOT one for a provider that
        integrates: the curve it flies leaves that conic immediately, and drawing
        the conic under an integrated label is a confident wrong answer. So an
        integrating provider puts its real points here, and a client that has them
        draws them instead of solving anomalies.

        **Three dimensions and a frame, not two in the orbital plane.** An n-body
        path has no perifocal plane to be flat in, and in a rotating frame it has no
        central body either, so a pair of in-plane coordinates cannot express one.
        `TrajectoryArc.frame` is beside the points rather than assumed, because the
        same trajectory is a different SHAPE per frame and a curve quoted without
        its frame is a curve whose meaning is unknown.

        **The far end is where authority stops, never where the path ends.**
        `TrajectoryArc.toUt` is the last instant vouched for. A client draws a
        visible mark there: a prediction that stops short and a trajectory that ends
        look identical on a diagram and mean opposite things.
      required:
        - frame
        - points
        - fromUt
        - toUt
        - sourcePointCount
        - derivation
      properties:
        frame:
          description: Which frame `TrajectoryArc.points` are expressed in.
          allOf:
            - $ref: "#/components/schemas/TrajectoryFrameRef"
        points:
          type: array
          items:
            $ref: "#/components/schemas/TrajectoryPoint"
          description: |-
            The path, in time order, first point at `TrajectoryArc.fromUt` and last at
            `TrajectoryArc.toUt`. Never empty: a producer with no points publishes no
            arc and states a refusal instead, because "a trajectory with no points in
            it" and "there is no trajectory" read identically on a diagram.
        fromUt:
          type: number
          x-sitrep-unit: ut
          description: The instant of the first point.
        toUt:
          type: number
          x-sitrep-unit: ut
          description: The instant of the last point, and the far end the horizon mark goes on.
        sourcePointCount:
          type: number
          x-sitrep-unit: count
          description: |-
            How many points the propagation actually produced, before decimation. Equal
            to `Points.Count` when nothing was dropped.

            Carried so a reader can tell a DECIMATED curve from a short one. The two
            look the same as a polyline and are different facts: a decimated curve
            resolves less than the propagation knew, and no reader may treat one of its
            points as an event instant. Event instants are published as their own
            instants for that reason, never recovered from this polyline.
        derivation:
          description: Where the curve came from, so the mark can travel ON it.
          allOf:
            - $ref: "#/components/schemas/TrajectoryDerivation"
              x-sitrep-unit: enum
        forceModel:
          description: |-
            What the integration was against, when the producer integrated. Null for a
            closed-form curve, which has no force model to describe.
          anyOf:
            - $ref: "#/components/schemas/TrajectoryForceModel"
            - type: "null"
    TrajectoryDerivation:
      type: integer
      description: |-
        Who derived a curve, and how faithfully.

        The mark travels ON the curve rather than beside the widget, for the same
        reason a horizon does: a substituted answer that only says so in a panel
        elsewhere is a substituted answer nobody reads as one.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Unspecified
          description: No producer stated one.
        - const: 1
          title: Foreign
          description: The points are the n-body mod's own, read from it directly.
        - const: 2
          title: OwnNBody
          description: |-
            Our integration, against the n-body model we read from the installed mod's
            own configuration.
        - const: 3
          title: OwnNBodyDegraded
          description: |-
            Our integration, with the force model incompletely matched: some body's
            parameters could not be resolved, and `TrajectoryForceModel.missingTerm`
            says which.
        - const: 4
          title: OwnClosedForm
          description: A closed-form conic, ours, from the elements alone.
    TrajectoryForceModel:
      type: object
      description: |-
        What an integration was actually against, so a reader can tell how far to
        trust the curve without being told to trust it.
      required:
        - gravityModelFound
        - perturbingBodyCount
        - geopotentialDegree
        - stepSeconds
        - stepCount
        - vacuum
      properties:
        gravityModelFound:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            True when the force model's configuration was found and parsed. False means
            the curve is degraded and says which term is missing.
        perturbingBodyCount:
          type: number
          x-sitrep-unit: count
          description: How many perturbing bodies were summed, not counting the primary.
        geopotentialDegree:
          type: number
          x-sitrep-unit: count
          description: |-
            The highest geopotential degree used for any body. Zero means point masses
            throughout, which is a statement rather than an omission: oblateness is
            worth about 4e-8 of a frame's angular velocity at lunar distance and is
            deliberately not computed.
        bodyEphemeris:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            How the perturbing bodies' FUTURE positions were obtained, and the one
            approximation in the whole curve.

            `kepler-from-snapshot` means each body was Kepler-propagated forward from
            its present state rather than read from an integrated ephemeris. The n-body
            mod evaluates every body from its own integrated ephemeris fitted to a
            millimetre; no export it offers takes a future time that we may honestly
            call, so this is the substitute. It is acceptable because body positions
            enter only through the PERTURBING terms and planetary orbits are
            near-Keplerian over a week. It is NOT acceptable where a third body
            dominates: near a libration point, during a close flyby, or anywhere else
            `TrajectoryForceModel.thirdBodyDominance` is large, this approximation
            becomes the leading error and the curve diverges qualitatively rather than
            numerically. That is why the dominance is published on every arc and why the
            horizon closes when it crosses its bound.

            Stated on every payload rather than in documentation, because a caveat a
            reader has to go and find is a caveat nobody meets.
        thirdBodyDominance:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            The largest perturbing acceleration as a fraction of the primary's, over the
            arc. Makes the chaotic regime visible rather than inferred.
        missingTerm:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Which term is absent, when the model could not be fully matched. Null when
            nothing is missing; a degraded curve always names one.
        integrator:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: The integrator's name.
        stepSeconds:
          type: number
          x-sitrep-unit: s
          description: The step actually used. An interval, so seconds.
        stepCount:
          type: number
          x-sitrep-unit: count
          description: How many steps were taken.
        vacuum:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            True when neither drag nor thrust was modelled, which is always. A reentry
            countdown computed in a vacuum is a vacuum countdown, and a reader that does
            not know that will read it as a reentry one.
    TrajectoryFrameKind:
      type: integer
      description: |-
        The frames a trajectory may be published in.

        `TrajectoryFrameKind.Unspecified` is 0 so a producer that forgets gets the
        answer a client must refuse to draw, on the same terms as
        `PropagationHorizonKind.Unspecified`: the wrong direction to default in is
        the one where an unnamed frame silently reads as the frame the reader
        happened to expect.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Unspecified
          description: No producer stated one. The points cannot be drawn.
        - const: 1
          title: Perifocal
          description: |-
            The orbit's own plane, periapsis on +x, centred on the body the elements are
            about. What a body-centric orbit diagram already draws in, and what a conic
            sampled from osculating elements is expressed in.
        - const: 2
          title: BodyCentredInertial
          description: |-
            Centred on `TrajectoryFrameRef.centreBodyIndex`, axes fixed against the
            stars. The frame an integrated path is naturally computed in.
        - const: 3
          title: BodyCentredRotating
          description: |-
            Centred on a body and turning with its surface. A ground track is this frame
            by construction.
        - const: 4
          title: BodyCentredParentDirection
          description: |-
            Centred on a body, with one axis held on the bearing to its parent. The
            frame a transfer window is legible in, because the parent stays put.
        - const: 5
          title: RotatingPulsating
          description: |-
            Two bodies held at fixed coordinates, which costs the length unit: a
            coordinate here is a multiple of the pair's separation, not a distance. The
            Lagrange points are fixed locations in this frame and in no other, which is
            the whole reason to draw in it.

            Always accompanied by `TrajectoryFrameRef.lengthsPulsate` set, so a reader
            that does not know this member still knows not to quote a number from it as
            a distance.
    TrajectoryFrameRef:
      type: object
      description: |-
        Which frame a set of trajectory points is expressed in, named well enough
        that the curve can be read.

        Deliberately not the producing mod's own frame vocabulary. A frame is a
        property every provider's answer has, and putting one vendor's enum on the
        standard payload would make every other provider translate into it.
      required:
        - kind
        - lengthsPulsate
      properties:
        kind:
          $ref: "#/components/schemas/TrajectoryFrameKind"
          x-sitrep-unit: enum
        centreBodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index into `system.bodies` of the body the frame is centred on, or null for
            a frame with no centre. Three of the frames a player can plot in have none,
            which is also why apsides do not exist in them.
        lengthsPulsate:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            True when the frame's lengths are not lengths.

            A pulsating frame composes a dilatation onto a rotating one, so a fractional
            error in the scaling radius scales every coordinate. A readout the frame
            invalidates says so rather than showing a number.
    TrajectoryKind:
      type: integer
      description: |-
        What kind of thing an element set describes: a closed-form conic, or a
        snapshot of an integrated path.

        `TrajectoryKind.Unspecified` is 0 for the same reason
        `PropagationHorizonKind.Unspecified` is: a producer that forgets the field
        gets the answer that WITHHOLDS rather than the one that permits. Had
        `TrajectoryKind.Analytic` been zero, a provider that failed to populate it
        would have every client treating an integrated trajectory as an ellipse.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Unspecified
          description: No provider stated one. Treat the shape as unknown.
        - const: 1
          title: Analytic
          description: |-
            A closed-form conic. The orbit IS an ellipse, so a conic renderer is exactly
            right and stays right for as long as the horizon allows.
        - const: 2
          title: Integrated
          description: |-
            A numerically integrated path. The osculating conic on the wire is a
            SNAPSHOT of it, true at the sample instant and never the path itself, so a
            client that draws a closed ellipse from it is drawing something the craft
            will not fly.
    TrajectoryPoint:
      type: object
      description: "One sampled point: where, and when."
      required:
        - ut
        - x
        - y
        - z
      properties:
        ut:
          type: number
          x-sitrep-unit: ut
          description: |-
            The instant this point is at. An instant, so UT: the points are events on a
            path rather than offsets along one, and a reader interpolating between two
            of them needs to know which side of a burn it is on.
        x:
          type: number
          x-sitrep-unit: m
        y:
          type: number
          x-sitrep-unit: m
        z:
          type: number
          x-sitrep-unit: m
    TrajectoryRefusal:
      type: integer
      description: |-
        Why a producer that CAN integrate published no arc this sample.

        Separate from `PropagationHorizon`, which answers reach and shape for the
        ELEMENTS. These are refusals about the ARC, and each names a different
        remedy: a client that had to borrow the horizon's sentence for one of them
        would tell the operator to do the wrong thing.

        **Zero is the state of having sought nothing**, and it used to be "nothing
        was refused", which covered BOTH an arc that was computed and an arc nobody
        attempted. That conflation shipped and it read as reassurance: with the
        integrated path unable to execute at all, every live frame carried
        `Unspecified`, which said the feature had no complaint rather than that it
        had never run. So an unattempted arc now says exactly that, and
        `TrajectoryRefusal.NotRefused` is the separate thing a producer says when it
        did attempt one and got a curve.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: NotAttempted
          description: |-
            No integrated arc was sought. Every sample from a provider whose
            trajectories are closed-form is this, and so is a sample whose horizon named
            no instant to integrate up to.

            It does not mean an arc is fine, and it never accompanies one:
            `TrajectoryRefusal.NotRefused` does. Read it beside
            `PropagationHorizon.trajectoryKind`, which says WHY nothing was sought:
            `Analytic` is an install that propagates in conics, and the arc has nothing
            to add to that sentence.
        - const: 1
          title: BeyondBudget
          description: |-
            The integration hit its step budget before reaching the requested instant.
            The operator can shorten the window, or wait: it may resolve on its own.
        - const: 2
          title: NoForceModel
          description: |-
            The force model's configuration was not found or could not be parsed, so
            there is nothing to integrate against. There is no operator remedy: it is an
            install problem and it says so.

            The ordinary way to reach it is an n-body physics mod installed against a
            solar system it ships no gravity model for, which is a real and common
            install rather than a corner: the model config is guarded on the planet pack
            it belongs to, so the mod runs and the node is not there.
        - const: 3
          title: NotRefused
          description: |-
            An arc was attempted and nothing refused it, so `VesselOrbit.arc` carries
            one. Only ever paired with a present arc, so a reader can tell a computed
            curve from an absent one without looking at the arc field at all.
    TransitionType:
      type: integer
      description: |-
        Mirrors KSP's `Orbit.PatchTransitionType`: parsed from the raw
        `orbit.patchEndTransition.ToString()` value `KspHost` captures.
        `TransitionType.Unknown` is the graceful fallback.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Initial
        - const: 1
          title: Final
        - const: 2
          title: Encounter
        - const: 3
          title: Escape
        - const: 4
          title: Maneuver
        - const: 5
          title: Collision
        - const: 6
          title: Unknown
    UnlockTechArgs:
      type: object
      description: |-
        `career.tech.unlock`'s args: the tech node's `techID` (the same id the READ
        side emits for each tech node, `career.status`'s `tech.nodes[].id`).
        Unlocking deducts the node's science cost.
      required:
        - techId
      properties:
        techId:
          type: string
          x-sitrep-unit: id
    Unsubscribe:
      type: object
      required:
        - type
        - topic
      properties:
        type:
          type: string
          const: unsubscribe
          x-sitrep-unit: id
        topic:
          type: string
          x-sitrep-unit: id
    UpdateManeuverNodeArgs:
      type: object
      description: |-
        Result of `vessel.maneuver.add` is `CommandResult<string>`, O-6 fixed: the
        created node's opaque id is actually returned in `Payload`. See
        `CommandResult`.
      required:
        - nodeId
        - ut
        - prograde
        - normal
        - radialOut
      properties:
        nodeId:
          type: string
          x-sitrep-unit: id
        ut:
          type: number
          x-sitrep-unit: ut
        prograde:
          type: number
          x-sitrep-unit: m/s
        normal:
          type: number
          x-sitrep-unit: m/s
        radialOut:
          type: number
          x-sitrep-unit: m/s
    UpgradeFacilityArgs:
      type: object
      description: |-
        `career.facility.upgrade`'s args: the facility's id as the READ side keys
        it: the `SpaceCenterFacility` enum name (e.g. `"VehicleAssemblyBuilding"`,
        `"LaunchPad"`), the same id `career.status`'s `facilities` map uses.
        Upgrading raises the facility one tier and deducts its upgrade cost from
        funds.
      required:
        - facilityId
      properties:
        facilityId:
          type: string
          x-sitrep-unit: id
    VantagePlanReply:
      type: object
      description: |-
        The answer, or why there is not one.

        `VantagePlanReply.seededAtUt` is not decoration. An arc detached from the
        instant its seed was true is a path with no claim about when, and a
        divergence measured against it later would be measured against nothing in
        particular.
      required:
        - solved
      properties:
        solved:
          type: boolean
          x-sitrep-unit: flag
        arc:
          anyOf:
            - $ref: "#/components/schemas/TrajectoryArc"
            - type: "null"
        seededAtUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: When the state this was computed from was actually TRUE.
        vantage:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            Which command centre's view produced it, echoed so a client that switched
            vantage mid-flight can tell whose answer it is holding.
        refusal:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: Why there is no trajectory. Null when there is one.
    VantagePlanRequest:
      type: object
      description: |-
        Args for `vessel.trajectory.forVantage`: where does this craft go, given
        what my command centre has been told.

        There is deliberately no vantage field. The answer depends on who is asking,
        and a client that could name its own vantage could name somebody else's and
        be shown what they can see. It is resolved where the command enters instead.
      required:
        - toUt
        - maxPoints
      properties:
        topic:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: The channel carrying the craft's orbit.
        toUt:
          type: number
          x-sitrep-unit: ut
          description: |-
            How far ahead to propagate. Allowed to be past what this vantage can
            currently see, because a prediction reaching beyond the news is the whole
            point of asking.
        maxPoints:
          type: number
          x-sitrep-unit: count
          description: Points to publish on the arc. Zero takes the provider's default.
    VesselAttitude:
      type: object
      description: |-
        The `vessel.attitude` channel payload: pitch/heading/roll in TWO named
        frames, both anchored to the same reference-transform ORIENTATION but
        measuring the surface up/north vectors from a different POSITION (see
        `Gonogo.KSP.KspHost.BuildAttitude`'s doc comment for the shared
        construction). Kills V-9: the legacy `n.heading`/
        `n.heading2`/`n.rawheading`/`n.rawheading2` quartet (root vs CoM, raw vs
        adjusted, no guidance which to use) is NOT reproduced by numeric suffix: per
        this class's original decision, a second frame is a new NAMED field with a
        frame tag: `VesselAttitude.pitch`/`VesselAttitude.heading`/
        `VesselAttitude.roll` are the CoM-referenced frame (up/north measured from
        `Vessel.CoM`: MechJeb's construction), and
        `VesselAttitude.pitchRootFrame`/`VesselAttitude.headingRootFrame`/
        `VesselAttitude.rollRootFrame` are the genuinely distinct
        ROOT-PART-referenced frame (up/north measured from `Vessel.rootPart`'s
        position instead, the two diverge whenever the root part sits away from the
        vessel's centre of mass). Not derivable from orbital elements (attitude
        depends on vessel orientation, not trajectory), hence streamed raw.
      required:
        - pitch
        - heading
        - roll
        - pitchRootFrame
        - headingRootFrame
        - rollRootFrame
        - meta
      properties:
        pitch:
          type: number
          x-sitrep-unit: °
          description: CoM-referenced frame. Degrees, -90..90 (nose down/up).
        heading:
          type: number
          x-sitrep-unit: °
          description: CoM-referenced frame. Degrees, 0..360.
        roll:
          type: number
          x-sitrep-unit: °
          description: CoM-referenced frame. Degrees, -180..180.
        pitchRootFrame:
          type: number
          x-sitrep-unit: °
          description: Root-part-referenced frame (see class doc). Degrees, -90..90.
        headingRootFrame:
          type: number
          x-sitrep-unit: °
          description: Root-part-referenced frame (see class doc). Degrees, 0..360.
        rollRootFrame:
          type: number
          x-sitrep-unit: °
          description: Root-part-referenced frame (see class doc). Degrees, -180..180.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselComms:
      type: object
      description: |-
        The `vessel.comms` channel payload: the raw CommNet VESSEL snapshot. Kills
        M-3 (one typed `VesselComms.controlState` enum replaces the magic-int
        `comm.controlState` + parallel `comm.controlStateName` string key) and M-4
        (no `0`/`0d` no-data sentinel, absence is the WHOLE channel being null when
        `vessel.connection` is null, R1(b), never a fake zero reading
        indistinguishable from "no telemetry at all").

        **Scope fence**: this is what the vessel itself reports. The delay authority
        and link modelling live in a future `comms.*` CAPABILITY channel
        (RemoteTech-default): the legacy `comm.signalDelay` does NOT get a field
        here; that successor is `comms.delay`, a different provider entirely.
      required:
        - connected
        - signalStrength
        - controlState
        - meta
      properties:
        connected:
          type: boolean
          x-sitrep-unit: flag
        signalStrength:
          type: number
          x-sitrep-unit: ratio
        controlState:
          $ref: "#/components/schemas/ControlState"
          x-sitrep-unit: enum
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselControl:
      type: object
      description: |-
        The `vessel.control` channel payload: the READ half of what the legacy
        vocabulary split across `f.` (toggle/action) and `v.` (value-read) prefixes
        for the same concept (N-1's read half; the WRITE half is a future
        typed-command task). Every field is individually nullable, R1(a): a null
        field is a normal, meaningful "this input isn't available this tick" (e.g.
        no `ctrlState`/no action-group data), never a sentinel default: while the
        record ITSELF is present whenever a vessel is (KspHost's `BuildControl`
        always returns a group, never a null one).

        **V-3 documented, not silently "fixed":** `VesselControl.throttle` is 0..1
        NOMINALLY, but KSP's own `FlightInputHandler.state.mainThrottle` isn't
        clamped upstream: a kOS/mod-driven throttle can genuinely read > 1 (the
        "200% throttle" phantom). Silently clamping it here would be a NEW wart
        (lying about upstream game truth); the range is documented, reader beware.
      required:
        - meta
      properties:
        sas:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            SAS master switch. Its control channel pairs it with `setSas` so a client
            can read the confirmed state and dispatch a change through ONE handle.
        sasMode:
          anyOf:
            - $ref: "#/components/schemas/SasMode"
              x-sitrep-unit: enum
            - type: "null"
        rcs:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        gear:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        brakes:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        lights:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        abort:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
        precisionControl:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Precision-control (fine-control / caps-lock) mode. Mirrors KSP's
            `FlightInputHandler.fetch.precisionMode`. Null when there's no active flight
            scene (`FlightInputHandler.fetch` is null), never a sentinel default
            (R1(a)).
        throttle:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            0..1 nominal range: NOT guaranteed clamped upstream (V-3), see the class doc
            comment.
        pitch:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Commanded pitch axis input, -1..1 (FlightInputHandler ctrlState.pitch).
        yaw:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Commanded yaw axis input, -1..1 (FlightInputHandler ctrlState.yaw).
        roll:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Commanded roll axis input, -1..1 (FlightInputHandler ctrlState.roll).
        translationX:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Commanded translation X (RCS right/left) input, -1..1 (ctrlState.X).
        translationY:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Commanded translation Y (RCS up/down) input, -1..1 (ctrlState.Y).
        translationZ:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: Commanded translation Z (RCS fwd/back) input, -1..1 (ctrlState.Z).
        actionGroups:
          type:
            - array
            - "null"
          items:
            $ref: "#/components/schemas/ActionGroupState"
          description: |-
            Every CUSTOM action group the elected action-groups backend knows, each
            NAMED and carrying its own index (see `ActionGroupState`). Stock KSP yields
            ten entries (`AG1..AG10`); an AGX backend may yield up to 250 with the
            player's own names. Null when action-group data wasn't available this tick:
            never a partial list. Order is by `ActionGroupState.index` ascending, but
            read `ActionGroupState.index` rather than relying on array position:
            position does not carry identity here.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselCrew:
      type: object
      description: |-
        The `vessel.crew` channel payload. Started count-only for M1 (G-13: grows to
        a full roster later WITHOUT a topic rename, per the "misc junk drawer split"
        ruling). The roster (`VesselCrew.crew`) and `VesselCrew.capacity` are that
        additive growth, new fields on the same record, same topic. Splitting this
        out of KspHost's `misc` group into its own coherent, independently-growable
        channel is itself part of the wart-fix.
      required:
        - count
        - capacity
        - crew
        - meta
      properties:
        count:
          type: number
          x-sitrep-unit: count
        capacity:
          type: number
          x-sitrep-unit: count
        crew:
          type: array
          items:
            $ref: "#/components/schemas/CrewMember"
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselFlight:
      type: object
      description: |-
        The `vessel.flight` channel payload: MEASUREMENTS, not evaluations:
        quantities the game measures that aren't derivable from orbital elements
        (terrain height, aero state) or that serve as off-rails ground truth
        (speeds). Kills V-10 (no (0,0) lat/long sentinel, the channel is simply
        absent when there's no vessel, never a fake origin point) and V-12 (one
        canonical field per quantity: the srfSpeed/speed/surfaceSpeed triplet and
        kPa/Pa variants collapse to `VesselFlight.surfaceSpeed` and
        `VesselFlight.dynamicPressureKPa`). `missionTime` deliberately does NOT
        appear here: see `VesselIdentity.launchUt`'s doc comment.
      required:
        - latitude
        - longitude
        - altitudeAsl
        - altitudeTerrain
        - verticalSpeed
        - surfaceSpeed
        - orbitalSpeed
        - gForce
        - dynamicPressureKPa
        - mach
        - atmDensity
        - externalTemperature
        - atmosphericTemperature
        - meta
      properties:
        latitude:
          type: number
          x-sitrep-unit: °
          description: |-
            Degrees. PRESENT means valid, no (0,0) no-data sentinel (V-10); absence is
            the whole channel being unavailable.
        longitude:
          type: number
          x-sitrep-unit: °
        altitudeAsl:
          type: number
          x-sitrep-unit: m
          description: Altitude above sea level, metres (KSP's `Vessel.altitude`).
        altitudeTerrain:
          type: number
          x-sitrep-unit: m
          description: |-
            Height above terrain (AGL, radar altitude), metres, NOT derivable from
            orbital elements, hence streamed raw.
        verticalSpeed:
          type: number
          x-sitrep-unit: m/s
          description: |-
            Metres per second (KSP's `Vessel.verticalSpeed`), signed: negative is
            descending.
        surfaceSpeed:
          type: number
          x-sitrep-unit: m/s
          description: Speed relative to the surface, metres per second (KSP's `Vessel.srfSpeed`).
        orbitalSpeed:
          type: number
          x-sitrep-unit: m/s
          description: |-
            Speed relative to the parent body's inertial frame, metres per second (KSP's
            `Vessel.obt_speed`).
        gForce:
          type: number
          x-sitrep-unit: g
          description: Multiples of standard gravity (KSP's `Vessel.geeForce`).
        dynamicPressureKPa:
          type: number
          x-sitrep-unit: kPa
        mach:
          type: number
          x-sitrep-unit: "1"
          description: |-
            Mach number: dimensionless by definition, so it carries the explicit "1"
            unit token rather than being left unannotated.
        atmDensity:
          type: number
          x-sitrep-unit: kg/m³
          description: |-
            Atmospheric density at the vessel's position, kg/m³ (KSP's
            `Vessel.atmDensity`).
        externalTemperature:
          type: number
          x-sitrep-unit: K
          description: |-
            Skin/ambient external temperature the vessel is exposed to, Kelvin
            (Vessel.externalTemperature).
        atmosphericTemperature:
          type: number
          x-sitrep-unit: K
          description: |-
            Ambient atmospheric temperature at the vessel's position, Kelvin
            (Vessel.atmosphericTemperature).
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselIdentity:
      type: object
      description: |-
        The `vessel.identity` channel payload: kills V-13 (one typed
        `VesselIdentity.situation` enum replaces the v.situation/v.situationString/
        v.landedAt triplet) and moves `missionTime` off the wire entirely:
        `VesselIdentity.launchUt` is static after liftoff (sampleUt - missionTime),
        so MET (mission elapsed time) is a consumer-side derivation (viewUt -
        launchUt) rather than a tick-rate field that would force this whole record
        to re-emit every tick.
      required:
        - vesselId
        - name
        - vesselType
        - situation
        - meta
      properties:
        vesselId:
          type: string
          x-sitrep-unit: id
          description: |-
            The stable subject id (KSP's `Vessel.id` GUID, as a string), the currency of
            target/vessel-scoped commands (T-1 groundwork) and of `Meta.Source`'s
            "vessel:<guid>" provenance stamp.
        name:
          type: string
          x-sitrep-unit: text
        vesselType:
          $ref: "#/components/schemas/VesselType"
          x-sitrep-unit: enum
        situation:
          $ref: "#/components/schemas/Situation"
          x-sitrep-unit: enum
        parentBodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index into the `system.bodies` collection; null when the vessel has no orbit
            driver yet (e.g. a just-spawned EVA before it attaches).
        launchUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            sampleUt - missionTime; null before the vessel's launch clock has started.
            See the class doc comment.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselInventory:
      type: object
      description: |-
        The stock cargo a vessel's PARTS are carrying: the supply aboard.

        **Why the crew's own inventories are not here.** A kerbal carries the same
        KSP module, `ModuleInventoryPart`, so one topic was the obvious shape and it
        is the wrong one. These answer different questions. This channel answers
        "what is aboard, and where", which is SUPPLY. `vessel.crew` answers "who is
        here, are they qualified, and what are they holding", which is the ACTOR
        list, and that is where a kerbal's two slots belong: beside the trait and
        experience level that decide whether they may do the job at all.

        **Why location is carried rather than a per-vessel total.** A kerbal has two
        slots, forty volume and a 65kg limit, one slot of which defaults to a
        parachute; a cargo container has far more. A craft can be carrying plenty of
        something while the kerbal who needs it has none, and a single total reports
        the reassuring half of that.

        This is STOCK, and deliberately not any Uplink's. Stock KSP's own repair
        mechanic consumes stock cargo, so a stock-career player has inventories
        worth showing whether or not a modelling mod is installed.
      required:
        - stores
        - meta
      properties:
        stores:
          type: array
          items:
            $ref: "#/components/schemas/InventoryStore"
          description: |-
            Every part-hosted inventory on the active vessel this tick, in vessel
            part-list order. Always present (possibly empty); a vessel-less tick yields
            a `null` payload, not an empty list.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselLanding:
      type: object
      description: |-
        The `vessel.landing` channel payload: terrain-informed landing data that
        needs KSP's PQS heightmap, which no client-side derivation can source (no
        client-side height grid is anywhere near fine-grained enough for a
        lander-scale slope), plus an atmosphere-aware descent estimate that needs
        per-part drag the client does not have.

        Distinct from the vacuum ballistic scalars a client solves for itself, which
        need no terrain and stay client-side.

        Whole-channel absence means "not descending toward a solid surface",
        relevance-gated at the source on situation + a descent test +
        `CelestialBody.hasSolidSurface` / a non-null `pqsController`, so this never
        carries a stale reading from orbit or a fabricated 0.0 from a body with no
        PQS. This is the third instance of the CaptureCrash house pattern (one
        source-gated channel published to every screen), with a continuous numeric
        gate rather than a categorical event.

        Every field is nullable: a field is null when its input is unavailable this
        tick (e.g. no PQS, no touchdown solution, not in atmosphere). Never ship a
        0.0 that was not verified.
      required:
        - meta
      properties:
        outcome:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Which class of landing readout is valid this tick, so the client renders
            state rather than inferring it from a pile of nulls. One of:
            `"vacuum-solved"`, `"atmospheric-aware"`, `"no-solution"`,
            `"terrain-assessed"`. Null before the first classification.
        sampleSource:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Which sampling source produced the terrain fields this tick: `"predicted"`
            (sampled at the mod's predicted downrange touchdown point: the site you are
            heading for) or `"sub-vessel"` (the graceful fallback: sampled directly
            under the vessel when no touchdown solution is available). The client
            surfaces this so the operator knows whether they are seeing downrange or
            under-ship terrain. Null when no terrain was sampled.
        terrainElevationUnderVessel:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Metres: terrain elevation above the body mean radius directly beneath the
            vessel. Currently null (see the Tier-1 note); the sub-vessel-fallback
            `VesselLanding.predictedTerrainElevation` carries the under-ship elevation.
        slopeAngleUnderVessel:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Degrees, 0 = flat: under-vessel terrain slope. Currently null (see the
            Tier-1 note); under-vessel slope comes from the Tier-2 plane-fit via the
            sub-vessel sampling fallback.
        predictedLatitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Degrees: predicted touchdown latitude. Terrain-independent: the client
            patch-walk supplies the point; this channel samples terrain there. Null when
            no touchdown is predicted within the horizon.
        predictedLongitude:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Degrees: predicted touchdown longitude. Always defined together with
            `VesselLanding.predictedLatitude`.
        predictedTerrainElevation:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Metres: terrain elevation at the predicted touchdown point.
            `CelestialBody.TerrainAltitude(lat, lon, allowNegative: true)` so ocean
            floor reads honestly rather than clamping to a fabricated 0.
        predictedSlopeAngle:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Degrees: terrain slope at the predicted touchdown point, from a plane fit
            over sampled heights (not an abs-average, which cannot tell a bowl from an
            incline). The tipover-risk readout, available while still descending.
        predictedSlopeHeading:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Degrees, 0 = north, clockwise: the downhill direction at the predicted point
            (which way the lander falls if it tips). Null below the noise floor.
        predictedRoughness:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Metres: RESIDUAL standard deviation of sampled terrain height at the
            predicted point, AFTER removing the fitted slope plane (so tilt is not
            double-counted as roughness). Sampled over
            `VesselLanding.roughnessFootprintMeters` so it lives on the client's
            calibrated sigma grade. The boulder-risk proxy.
        roughnessFootprintMeters:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Metres: the footprint radius the roughness sigma was sampled over. Ships so
            the client can label honestly and grade it on the shared sigma scale.
        slopeSampleRadiusMeters:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: "Metres: the (tighter) radius the slope plane-fit samples span."
        predictedBiome:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            KSP's biome name at the PREDICTED touchdown point (not the current position,
            that is `vessel.surface.biome`). Via `ScienceUtil.GetExperimentBiome` (which
            takes degrees). Null when the body has no biome map.
        terrainPatch:
          type:
            - array
            - "null"
          items:
            type: number
            x-sitrep-unit: m
          description: |-
            Flattened row-major NxN grid of terrain elevations (metres) around the
            predicted point, for the reticle's shaded relief. Null until the relief
            patch ships / when over the PQS budget (the reticle falls back to a flat
            roughness tint). Length is `VesselLanding.terrainPatchSize` squared.
        terrainPatchSize:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: The N of the NxN `VesselLanding.terrainPatch` grid. Null when no patch.
        terrainPatchExtentMeters:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: "Metres: the full width the `VesselLanding.terrainPatch` grid spans."
        terminalVelocity:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: |-
            m/s: terminal velocity at the CURRENT altitude/config, from the measured
            aggregate drag force against local gravity. Null outside an atmosphere. An
            ESTIMATE assuming current config holds (attitude, no pending chute).
        projectedTouchdownSpeed:
          type:
            - number
            - "null"
          x-sitrep-unit: m/s
          description: |-
            m/s: projected touchdown speed under a terminal descent to the ground
            (terminal velocity scaled to ground density). The atmosphere-aware
            replacement for the (wrong) vacuum impact speed. Null outside an atmosphere.
        atmosphericTimeToImpact:
          type:
            - number
            - "null"
          x-sitrep-unit: s
          description: |-
            Seconds: atmosphere-aware time to impact, integrating the terminal-velocity
            profile down the density column. Null outside an atmosphere.
        descentRegime:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The instantaneous descent regime: `"at-terminal"` / `"decelerating"` /
            `"accelerating"`. Null outside an atmosphere.
        dragToWeightRatio:
          type:
            - number
            - "null"
          x-sitrep-unit: "1"
          description: |-
            The aggregate aerodynamic drag force divided by the vessel's weight (local
            gravity): the numeric form of `VesselLanding.descentRegime`. >1 decelerating
            (drag beats gravity), 1 at terminal, <1 still accelerating. A dimensionless
            0..N ratio like TWR, not a 0..1 fraction. Null outside an atmosphere.
        parachuteState:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            Parachute state affecting the estimate: `"none"` / `"armed"` (a future step
            change the instant model cannot see, flag the estimate) / `"deployed"` (drag
            already in the measurement, self-corrected). Null outside an atmosphere.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselManeuver:
      type: object
      description: |-
        The `vessel.maneuver` channel payload. `VesselManeuver.nodes` is ALWAYS an
        array: kills R2's empty-vs-null inconsistency (KspHost's
        `BuildManeuverNodes` returns `null` for "no nodes queued," the common case;
        this mapper normalizes that to `[]`, never a null collection). *Derived,
        SDK-side, NOT streamed here:* the post-burn orbit preview (elements + node →
        new elements, consumer-side math).

        **`VesselManeuver.nodes` is ordered by execution**, earliest
        `ManeuverNode.ut` first, and that ordering IS the plan: burn N is flown
        after burn N-1 and acts on what burn N-1 left behind. No separate ordinal or
        predecessor field is carried, because array position already says it and a
        second expression of the same fact is a second thing that can be wrong. The
        per-burn patch chain expresses the same linkage a third time, in a form only
        a patched-conic planner can produce; see `ManeuverNode.patches`.
      required:
        - nodes
        - meta
      properties:
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/ManeuverNode"
        planner:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            The elected maneuver-plan provider's id, or null when THERE IS NO PLANNER AT
            ALL.

            That is not the same fact as an empty plan, and stock reaches it on its own:
            an un-upgraded Tracking Station leaves `Vessel.patchedConicSolver` null, so
            an early-career craft cannot hold a plan rather than merely not holding one.
            Without this field both arrive as `Nodes: []` and an operator is told their
            plan is empty when the truth is that they cannot make one.

            Nothing outside the election may branch on the VALUE: a provider says what
            it is so a readout can name it and a diagnostic can record it, never so a
            consumer can special-case one. Present-versus-null is the only part anything
            should test.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselOrbit:
      type: object
      description: |-
        The `vessel.orbit` channel payload: elements are the CAUSE; every kinematic
        quantity (position/velocity/apsides/anomalies/period) is a consumer-side
        derivation at view-UT via the propagation capability, never streamed here
        (the "elements-not-position" ruling). Kills O-1 (there is no
        `eccentricAnomaly` field at all, the copy-paste-bug class can't exist on a
        wire that never carries one), O-8 (spelled-out, unit-annotated fields, UT
        always `double`), O-9 (`VesselOrbit.encounter` is a typed nullable record,
        never the -1/0/1 + "" + NaN sentinel spray of o.encounterExists/Time/Body),
        O-10 (no duplicate apsis keys). Units: `VesselOrbit.sma` in metres;
        `VesselOrbit.inc`/`VesselOrbit.lan`/ `VesselOrbit.argPe` in DEGREES
        (KSP-native); `VesselOrbit.meanAnomalyAtEpoch` in RADIANS (also KSP-native):
        this degrees/radians split is an inherited KSP inconsistency deliberately
        KEPT, not "fixed": converting would desync from every KSP reference and the
        recorder's own raw values).
      required:
        - referenceBodyIndex
        - sma
        - ecc
        - inc
        - meanAnomalyAtEpoch
        - epoch
        - mu
        - patches
        - horizon
        - arcRefusal
        - meta
      properties:
        referenceBodyIndex:
          type: number
          x-sitrep-unit: id
        sma:
          type: number
          x-sitrep-unit: m
          description: Semi-major axis, metres (see the class doc comment's units block).
        ecc:
          type: number
          x-sitrep-unit: "1"
          description: |-
            Eccentricity: dimensionless by definition, hence the explicit "1" token
            rather than no annotation.
        inc:
          type: number
          x-sitrep-unit: °
        lan:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Null = undefined ascending node (KSP's own LAN is NaN for a near-equatorial
            orbit, inc ~ 0 -- a routine case, not an error). Never NaN, never 0 as a
            stand-in (R1/F-1).
        argPe:
          type:
            - number
            - "null"
          x-sitrep-unit: °
          description: |-
            Null = undefined periapsis (KSP's own argumentOfPeriapsis is NaN for a
            near-circular orbit, ecc ~ 0 -- a routine case, not an error). Never NaN,
            never 0 as a stand-in (R1/F-1).
        meanAnomalyAtEpoch:
          type: number
          x-sitrep-unit: rad
          description: |-
            RADIANS, not degrees. The KSP-native degrees/radians split this record
            deliberately keeps (see the class doc comment) is exactly the kind of trap a
            machine-readable unit exists to defuse.
        epoch:
          type: number
          x-sitrep-unit: ut
          description: |-
            Epoch UT, in seconds -- the same UT-seconds convention as every other
            UT-typed field on this record (matches KSP's own `Orbit.epoch` units).
        mu:
          type: number
          x-sitrep-unit: m³/s²
          description: |-
            Parent body's standard gravitational parameter (GM): self-sufficient
            propagation, no separate body lookup required.
        encounter:
          description: |-
            Null = no upcoming SOI transition on the current trajectory (the common
            case); NEVER a sentinel (kills O-9).
          anyOf:
            - $ref: "#/components/schemas/OrbitEncounter"
            - type: "null"
        patches:
          type: array
          items:
            $ref: "#/components/schemas/OrbitPatch"
          description: |-
            The vessel's future-orbit patch chain: element 0 is THIS patch (the current
            orbit, same elements as the fields above, restated in `OrbitPatch`'s shape
            for a uniform client-side walk), followed by any subsequent SOI-transition
            patches KSP's own patched-conic solver has already resolved. ALWAYS an array
            (R2), empty (not null) when there is no upcoming SOI transition, the
            overwhelmingly common case for a stable orbit. See
            `Gonogo.KSP.KspHost.BuildOrbitPatchChain` for the walk.
        horizon:
          description: |-
            How far ahead these elements may be propagated before they stop being
            trustworthy. NOT nullable: a producer states its horizon or its samples read
            as unpropagatable, because "nobody said" must never be the permissive
            answer.

            A client cannot compute this. Deriving it needs the perturbation environment
            (which bodies are near, how massive, how far), so it has to arrive on the
            sample from the only thing that knows. It rides HERE rather than on a
            sibling Topic because a horizon and the elements it bounds share one
            lifetime and one `validAt`: split across frames a client could hold one
            sample's elements beside another's horizon and draw a conic authorised by
            the wrong sample, silently. `OrbitPatch.startUt`/`OrbitPatch.endUt` already
            set the precedent for a validity window living with its elements.
          allOf:
            - $ref: "#/components/schemas/PropagationHorizon"
        arc:
          description: |-
            The path the craft actually flies, when the provider integrated one.

            Null under an analytic provider, and that is not a gap: its elements ARE the
            curve, so a client draws a conic from them and an arc beside it would be a
            second, redundant copy of the same answer. Null also under an integrating
            provider that has nothing to publish this sample, in which case
            `VesselOrbit.arcRefusal` says why.

            It rides HERE, on the elements, for the reason `VesselOrbit.horizon` does:
            the arc, the elements and the horizon that bounds both share one `validAt`,
            and split across frames a client could hold one sample's arc beside
            another's elements.
          anyOf:
            - $ref: "#/components/schemas/TrajectoryArc"
            - type: "null"
        arcRefusal:
          description: |-
            What became of the arc: why `VesselOrbit.arc` is absent when a provider
            tried to build one and stopped, `TrajectoryRefusal.NotAttempted` when none
            was sought at all, and `TrajectoryRefusal.NotRefused` beside one that was
            drawn.

            Those last two used to be one value, and a client could not tell an install
            where the integrated path never runs from one where it runs cleanly.
          allOf:
            - $ref: "#/components/schemas/TrajectoryRefusal"
              x-sitrep-unit: enum
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselOrbitTruth:
      type: object
      description: |-
        The `vessel.orbit.truth` channel payload: KSP's own maintained ground-truth
        state vector, parent-body-relative. DEV-GATED, not a product channel: exists
        so the propagator-diff harness / a debug widget can verify element->position
        math against KSP's own state, never as a widget-facing altitude/velocity
        source (that would rebuild the elements-not-position discipline's failure
        mode / V-12). `VesselOrbitTruth.frameRotating` gates whether
        `VesselOrbitTruth.position`/`VesselOrbitTruth.velocity` are directly
        comparable to a fixed-frame Kepler propagator's output (false) or sit in a
        frame co-rotating with the body's spin instead (true); see
        `Gonogo.KSP.KspHost.BuildOrbit`'s doc comment for the full derivation. There
        is no engine-level "hide from the data picker" flag yet (that's a future
        SDK/picker concern): this channel is dev-only BY CONVENTION today, enforced
        by never binding it from a widget, not by engine-level gating.
      required:
        - position
        - velocity
        - frameRotating
        - meta
      properties:
        position:
          type: object
          description: A three-component vector. Its components share one unit, declared on the field that holds the vector rather than on the shape.
          required:
            - x
            - y
            - z
          properties:
            x: &a6
              type: number
              x-sitrep-unit: m
            y: *a6
            z: *a6
        velocity:
          type: object
          description: A three-component vector. Its components share one unit, declared on the field that holds the vector rather than on the shape.
          required:
            - x
            - y
            - z
          properties:
            x: &a7
              type: number
              x-sitrep-unit: m/s
            y: *a7
            z: *a7
        frameRotating:
          type: boolean
          x-sitrep-unit: flag
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselPart:
      type: object
      description: |-
        One part in the `VesselParts.parts` tree. Provenance-scoped like
        `VesselStructure` (whole payload absent when there is no vessel), so the
        always-present required fields (`VesselPart.id`/
        `VesselPart.name`/`VesselPart.position`/`VesselPart.dryMass`/
        `VesselPart.inverseStage`/`VesselPart.maxTemp`) are non-nullable, while the
        genuinely-optional ones (`VesselPart.parentId` null for the root,
        `VesselPart.up`, `VesselPart.skinMaxTemp`/`VesselPart.currentTemp`/
        `VesselPart.skinTemp` unset before physics runs,
        `VesselPart.fuelLineTargetId`) are nullable.

        **Join key.** `VesselPart.id` is `Part.flightID` stringified, the SAME
        string form `parts.power`/`parts.robotics`'s `partId` uses, so a consumer
        (RoboticsConsole, PowerSystems) can id-join a part across those channels.
        `VesselPart.parentId` and `VesselPart.fuelLineTargetId` are the same string
        form for the same reason. flightID's stability across a docking/undocking
        round-trip is a KSP-side caveat carried forward from the design's open
        questions.
      required:
        - id
        - name
        - title
        - position
        - bounds
        - dryMass
        - inverseStage
        - maxTemp
        - category
        - modules
        - isRobotics
        - isPowerRelated
        - resources
        - moduleStates
        - actionBindings
      properties:
        id:
          type: string
          x-sitrep-unit: id
          description: |-
            `Part.flightID` stringified: the tree/cross-channel join key. Empty string
            only for the uninitialized-0 sentinel (no live flight id yet).
        parentId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: "`Part.parent?.flightID` stringified; `null` for the root part."
        name:
          type: string
          x-sitrep-unit: text
          description: |-
            `Part.partInfo.name` (the `AvailablePart.name` config id, e.g.
            `"solarPanels1"`).
        title:
          type: string
          x-sitrep-unit: text
          description: |-
            `Part.partInfo.title` (the display title, e.g. `"OX-STAT Photovoltaic
            Panels"`).
        position:
          type: object
          description: |-
            `Part.orgPos`: the part's original vessel-local position (metres, vessel
            frame).
          required:
            - x
            - y
            - z
          properties:
            x: &a8
              type: number
              x-sitrep-unit: m
            y: *a8
            z: *a8
        up:
          type:
            - object
            - "null"
          description: |-
            The part's local up axis (`Part.orgRot * Vector3.up`), for orienting
            flow/thrust glyphs. `null` on a snapshot recorded before this field existed.
          required:
            - x
            - y
            - z
          properties:
            x: &a9
              type: number
              x-sitrep-unit: "1"
            y: *a9
            z: *a9
        bounds:
          $ref: "#/components/schemas/PartBounds"
        dryMass:
          type: number
          x-sitrep-unit: t
          description: "`Part.mass`: dry mass (tonnes)."
        inverseStage:
          type: number
          x-sitrep-unit: id
          description: |-
            `Part.inverseStage` (KSP's own inverted staging numbering, carried forward
            unchanged; see `VesselStructure.currentStage`).
        maxTemp:
          type: number
          x-sitrep-unit: K
          description: "`Part.maxTemp`: internal max temperature (K)."
        skinMaxTemp:
          type:
            - number
            - "null"
          x-sitrep-unit: K
          description: |-
            `Part.skinMaxTemp` (K); `null` for the `-1` "no skin-thermal model"
            sentinel.
        currentTemp:
          type:
            - number
            - "null"
          x-sitrep-unit: K
          description: |-
            `Part.temperature`: current internal temperature (K); `null` for the `-1`
            "not yet simulated" sentinel.
        skinTemp:
          type:
            - number
            - "null"
          x-sitrep-unit: K
          description: "`Part.skinTemperature`: current skin temperature (K)."
        category:
          type: string
          x-sitrep-unit: text
          description: |-
            `Part.partInfo.category` (`PartCategories` enum name, e.g. `"Engine"`). A
            display label; see `VesselPart.categoryOrdinal`.
        categoryOrdinal:
          description: |-
            `VesselPart.category`'s KSP ORDINAL, typed to `KspPartCategory`.

            ShipMap picks a part's diagram glyph from this. It used to switch on the
            NAME, so a member KSP renamed dropped every part of that category through to
            the name/title heuristic underneath: engines drawn as whatever "engine"
            happened to match in a part's title, and nothing to say it had happened.

            `null` when the part had no `partInfo` to read, the same case that already
            leaves `VesselPart.category` empty. Note `PartCategories.none` is `-1` and
            is a real value, NOT an absence.
          anyOf:
            - $ref: "#/components/schemas/KspPartCategory"
              x-sitrep-unit: enum
            - type: "null"
        modules:
          type: array
          items:
            type: string
            x-sitrep-unit: text
          description: |-
            Each `PartModule`'s CLR class name (e.g. `"ModuleEngines"`,
            `"CModuleFuelLine"`), which is what a client classifies a part by.
        isRobotics:
          type: boolean
          x-sitrep-unit: flag
          description: "`Part.isRobotic()`: a Breaking Ground robotic servo part."
        isPowerRelated:
          type: boolean
          x-sitrep-unit: flag
          description: |-
            True when the part carries a solar panel, alternator, EC-producing
            converter, or an ElectricCharge resource capacity.
        fuelLineTargetId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            For a fuel-line part, the stringified `flightID` of the part it feeds;
            `null` otherwise.
        resources:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/PartResourceFlow"
          description: |-
            Every resource this part carries (join key: resource name, e.g.
            `"ElectricCharge"`), storage plus live production/consumption flow: the
            per-part live-data slice a client used to have to fetch off the legacy
            `r.resourceFor[flightId]` key. Empty dict when the part carries no
            resources.
        moduleStates:
          type: array
          items:
            $ref: "#/components/schemas/PartModuleState"
          description: |-
            Per-module behavioural state (solar deployed, engine firing, parachute
            armed, etc.): one entry per module on the part that maps to
            `PartModuleState`'s vocabulary, in `Part.Modules` order. The per-part
            live-data slice the SDK used to fetch off the legacy `v.partState[flightId]`
            key. Empty list when the part carries no module of a mapped type.
        actionBindings:
          type: array
          items:
            $ref: "#/components/schemas/ActionBinding"
          description: |-
            Action-group bindings on this part: one entry per bound part action
            (`ActionBinding.action` = `BaseAction.guiName`, and the named groups its
            `BaseAction.actionGroup` Flags bitmask decodes to). Per-ACTION, not
            per-part. Empty when no action on the part is bound to any group. Retires
            the legacy `f.ag.bindings` shim: the client derives the human-readable
            action-group caption from this field.
    VesselParts:
      type: object
      description: |-
        The `vessel.parts` channel payload: the active vessel's full part-tree
        topology (P1b slice 2), the foundation ShipMap / PowerSystems topology /
        ThermalStatus all build on. A SINGLE WRAPPER OBJECT (or `null` when there is
        no active vessel / no topology group this tick), so the Topic tag sits on
        this type directly with the default `IsArray = false`: same posture as
        `VesselStructure` and the sibling structured `vessel.*` channels, NOT the
        bare-array `parts.robotics`.

        **Thermal folds in here.** Per-part temperatures ride each `VesselPart`
        (`VesselPart.currentTemp`/ `VesselPart.maxTemp`/`VesselPart.skinTemp`/
        `VesselPart.skinMaxTemp`), so the hottest-part / engine / heat-shield
        rollups are SDK-DERIVABLE on top of this channel, there is no separate
        `therm.hottestPart*` Topic (v-topology-redesign.md). The existing
        `vessel.thermal` rollup channel is NOT removed by this build; that is a
        later cleanup.

        **Typing-only mirror.** This reproduces, field-for-field, the exact
        serialized shape `Sitrep.Host.VesselPartsViewProvider.ToWire` already emits
        (same names, same camelCase wire keys via `RtConfig.CamelCaseForProperties`,
        same units). It is NOT serialized itself: the wire is written by
        `JsonWriter` walking the provider's dictionary: so adding it changes no
        bytes.
      required:
        - parts
        - meta
      properties:
        parts:
          type: array
          items:
            $ref: "#/components/schemas/VesselPart"
          description: |-
            Every part on the active vessel this tick, in vessel part-list order. Always
            present (possibly empty); a vessel-less tick yields a `null` payload, not an
            empty list.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselPhysicsMode:
      type: object
      description: |-
        The `vessel.physics.mode` Topic payload: the active vessel's physics regime
        (`PhysicsMode`). Its own Topic Value per the 2026-07-09 decision that
        reverses the earlier "fold physics mode into stream meta" call:
        `PayloadMeta.quality` was a bad stand-in for a discrete enum).
        DelayRole-Delayed like every other vessel-derived channel: it describes the
        vessel itself, so ground learns about it at UT+delay, not as a ground-side
        fact.
      required:
        - mode
        - meta
      properties:
        mode:
          $ref: "#/components/schemas/PhysicsMode"
          x-sitrep-unit: enum
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselPropulsion:
      type: object
      description: |-
        The `vessel.propulsion` channel payload: the TWR/burn-time derivation inputs
        (G-4). `VesselPropulsion.totalMass`/`VesselPropulsion.dryMass` in tonnes,
        `VesselPropulsion.currentThrust`/`VesselPropulsion.availableThrust` in kN
        (dimensionally consistent for TWR: kN/(t·m/s²): see kN/(t·m/s²)).
        `VesselPropulsion.availableThrust` already excludes shut-down/flamed-out
        engines at capture (only `EngineIgnited && !flameout` engines contribute):
        it is "what this vessel can produce RIGHT NOW," not its rated maximum.
        *Derived, SDK-side, NOT streamed here:* TWR (`currentThrust / (totalMass ·
        g)`), max-TWR, and a crude vessel-level burn-time estimate (retiring
        `dv.currentTWR`/`dv.*` until a stage sim exists, G-14).
      required:
        - totalMass
        - dryMass
        - currentThrust
        - availableThrust
        - meta
      properties:
        totalMass:
          type: number
          x-sitrep-unit: t
        dryMass:
          type: number
          x-sitrep-unit: t
        currentThrust:
          type: number
          x-sitrep-unit: kN
        availableThrust:
          type: number
          x-sitrep-unit: kN
        thrustStartedUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            UT the craft's CURRENT continuous period of thrust began, or null when it is
            not under thrust as of the last measurable reading.

            **An observation instant.** It says when something was SEEN to be true,
            which is a different kind of UT from a plan's `ManeuverNode.Ut` or an
            orbit's `epoch`, and the `ut` token does not separate them. Subtracting this
            from a planned instant is type-legal and meaningless; the only duration it
            belongs in is one measured against the reader's own view clock.

            Latched rather than emitted as an edge, and that is the whole design. Every
            vessel channel is `Delivery.LossyLatest` over a UT-gated snapshot, so a
            "thrust just started" event is a one-shot the transport is entitled to drop,
            and a consumer that missed it cannot tell that from nothing having happened.
            A latched instant is on every subsequent frame until it changes.

            Held, not cleared, while thrust is unmeasurable (an on-rails or packed craft
            has no parts to read). Otherwise switching away from a burning craft would
            read as its engines quitting.
        lastThrustEndUt:
          type:
            - number
            - "null"
          x-sitrep-unit: ut
          description: |-
            UT the craft's most recent period of thrust ENDED, or null when no period of
            thrust has been observed to end since this craft became the subject. Same
            observation-instant reading as `VesselPropulsion.thrustStartedUt`.

            **Present here with a null `VesselPropulsion.thrustStartedUt` is the fact
            nothing else on the wire can state:** the engines ran, and they have
            stopped. `VesselPropulsion.currentThrust` at zero cannot say it (a craft
            that never lit reads the same), and `vessel.control.throttle` certainly
            cannot: that is where the pilot left the lever, and it sits at full through
            a flameout, a dry tank and an unlit stage.

            It does NOT say why the engines stopped, and no reading can. A burn paused
            to be re-planned and a burn abandoned produce the same instant, because the
            difference between them is whether the operator comes back, which has not
            happened yet. A consumer may report that thrust ceased with delta-v owed; it
            may not report a shortfall.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselResources:
      type: object
      description: |-
        The `vessel.resources` channel payload: a keyframed map, keyed by resource
        name. Kills R-1 (`SumResources`'s `-1` sentinel for an absent/empty
        resource: never reproduced here), R-3 (row-vanishing ambiguity), R-4
        (`{}`-for-dead-id).

        **Three-way typed absence (R1):**

        - **Key ABSENT** from `VesselResources.resources`: structural: this vessel
          does not carry the resource at all (KspHost omits any resource with
          `maxAmount <= 0`). Changes only on staging/docking.
        - **Key present, `{current: 0, max: > 0}`**, carried but currently empty (a
          real, meaningful reading, not an error).
        - **Whole channel absent/stale**: no vessel at all (R1(b), same convention
          as every other `vessel.*` channel).

        Because every emission is the FULL map (a structured, keyframed channel,
        never a delta), a key disappearing between two emissions is itself a real
        structural statement (the vessel stopped carrying that resource, e.g. a tank
        was staged away), never an ambiguous "did it change or did the stream just
        drop it" (R-3's ambiguity).

        **Deliberately deferred**: flow/rates (R-2/R-5), those belong to a future
        parts/power channel family with per-module provenance; bolting a
        vessel-total `flow` on now would reproduce R-6 (a "truth" number that isn't
        the game's truth).
      required:
        - resources
        - meta
      properties:
        resources:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/ResourceAmount"
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselRosterEntry:
      type: object
      description: |-
        One vessel in the `SystemVessels` roster. Mirrors the exact per-vessel dict
        the provider emits. A roster entry with no resolvable stable id is dropped
        by the provider, never emitted with a fabricated one, so
        `VesselRosterEntry.vesselId` is always present.
      required:
        - vesselId
        - name
        - vesselType
        - situation
      properties:
        vesselId:
          type: string
          x-sitrep-unit: id
          description: |-
            Stable subject id (KSP vessel GUID). Always present, entries without one are
            dropped.
        name:
          type: string
          x-sitrep-unit: text
          description: Display name; defaults to the empty string, never null.
        vesselType:
          description: |-
            Vessel type. On the wire this is the enum ORDINAL (the provider emits
            `(int)` of the parsed type); typed here to the shared `VesselType` enum,
            whose numeric members match those ordinals.
          allOf:
            - $ref: "#/components/schemas/VesselType"
              x-sitrep-unit: enum
        situation:
          description: |-
            Flight situation. On the wire this is the enum ORDINAL; typed here to the
            shared `Situation` enum.
          allOf:
            - $ref: "#/components/schemas/Situation"
              x-sitrep-unit: enum
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            Index into `SystemBodies` of this vessel's main body; null when absent or
            unresolved.
        crewCount:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Kerbals aboard right now. Read off the LOADED vessel's crew when loaded, off
            `ProtoVessel` otherwise (`KspHost.BuildVesselRosterEntry`'s doc comment): so
            an unloaded background vessel still reports a real count. Null only if the
            read itself failed (the producer omits the raw key rather than fabricate a
            zero); never used to distinguish "probe" from "unknown", that is
            `VesselRosterEntry.crewCount` == 0 vs. null.
        crewCapacity:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: |-
            Seat capacity, same loaded/proto read as `VesselRosterEntry.crewCount`. Null
            only if the read failed.
        commsConnected:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            Whether stock CommNet reports a live control link home for this vessel right
            now: a raw `Vessel.connection.IsConnected` read against EVERY roster vessel
            (loaded or not), NOT the active-vessel-only elected-backend `comms.*`
            family. Null when CommNet has no connection object to read for this vessel
            this tick, an honest "unknown", not a fabricated "no link". Two distinct
            causes collapse to the same null: a transient scene-transition race (rare),
            and a PERMANENT, by-design absence for `Debris`/`SpaceObject`
            (asteroids/comets)/`Unknown` vessel types: verified against
            `CommNet.CommNetVessel.OnStart`, which never assigns `vessel.connection` for
            those three types. A debris or asteroid roster entry is expected to carry
            null here on every sample, not occasionally.
        commsControlSource:
          description: |-
            The same read's control-level tier, for the roster's connected/partial/ none
            link-quality tag. Null under the same "nothing to read" condition as
            `VesselRosterEntry.commsConnected`: including the permanent
            Debris/SpaceObject/Unknown-vessel-type case documented there.
          anyOf:
            - $ref: "#/components/schemas/RosterCommsControlSource"
              x-sitrep-unit: enum
            - type: "null"
        orbit:
          description: |-
            This vessel's own orbital elements, the same shape (and the same
            `SystemViewProvider.BuildOrbit` routine) that fills `BodyEntry.orbit`. This
            is what positions a roster vessel (and a SystemView graph node keyed to it
            via `VesselRosterEntry.vesselId`): no separate node-position field exists, a
            client derives position by joining a node's id to this orbit. Null when the
            vessel has no orbitDriver yet (a scene-transition race), never a sentinel.
          anyOf:
            - $ref: "#/components/schemas/OrbitEntry"
            - type: "null"
    VesselStructure:
      type: object
      description: |-
        The `vessel.structure` channel payload: the other half of KspHost's `misc`
        junk-drawer split (see `VesselCrew`'s doc comment).
        `VesselStructure.currentStage` uses KSP's own (P-4-flagged "inverted vs.
        visible staging") numbering UNCHANGED: documented here, not silently
        renumbered, so this contract doesn't invent a second numbering scheme to
        reconcile. `VesselStructure.stageCount` is already `maxInverseStage + 1`
        (KspHost's own normalization). A future part-tree/topology channel
        (`vessel.parts`) is a SIBLING of this record, not a growth of it (R-8's
        "bulk topology is its own ASSET-class design" lesson).
      required:
        - currentStage
        - meta
      properties:
        currentStage:
          type: number
          x-sitrep-unit: id
          description: |-
            KSP's own `Vessel.currentStage` numbering (capsule/high stages have LOW
            numbers); see the class doc comment.
        stageCount:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: Null when the vessel has no parts this tick.
        partCount:
          type:
            - number
            - "null"
          x-sitrep-unit: count
          description: Null when the vessel has no parts this tick.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselSurface:
      type: object
      description: |-
        The `vessel.surface` channel payload: the landing capture-add (M3 R3): data
        a LandingStatus widget needs that `vessel.flight` doesn't already carry.
        `vessel.flight.AltitudeTerrain` (KSP's `radarAltitude`, measured from the
        vessel's centre of mass) already ships: `VesselSurface.heightFromTerrain`
        here is a DIFFERENT, additional reading (KSP's own `heightFromTerrain`,
        which accounts for the vessel's physical extent: effectively "how far is my
        LOWEST point from the ground," the number a landing-gear/suicide-burn widget
        actually cares about, not the CoM-to-ground distance).

        Whole-channel absence means "not near any surface right now", guarded on
        `Situation.Orbiting`/ `Situation.Escaping` on the capture side
        (`Gonogo.KSP.KspHost.BuildSurface`), never a stale/garbage AGL reading from
        deep space (KSP keeps whatever it last computed for `heightFromTerrain` even
        when there's no meaningful "terrain" underneath at all).
      required:
        - meta
      properties:
        biome:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            KSP's biome name at the vessel's current lat/long (e.g. "Highlands",
            "Shores"). Null when the body has no biome map (e.g. gas giants) or the
            lookup failed this tick.
        landedAt:
          type:
            - string
            - "null"
          x-sitrep-unit: text
          description: |-
            The named launch/landing site the vessel is currently at (e.g.
            "KSC_LaunchPad", "Runway"), null when landed/splashed somewhere with no
            named site, or when not landed/splashed at all.
        heightFromTerrain:
          type:
            - number
            - "null"
          x-sitrep-unit: m
          description: |-
            Metres: KSP's own `heightFromTerrain`, accounting for the vessel's physical
            extent (see the class doc comment for how this differs from
            `vessel.flight.altitudeTerrain`). Null if unavailable this tick.
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselTarget:
      type: object
      description: |-
        The `vessel.target` channel payload: the active vessel's CURRENT target only
        (no roster; `system.vessels`/`tar.availableVessels`'s replacement is a
        deferred M1.5 add). Kills V-8:
        `VesselTarget.relativePosition`/`VesselTarget.relativeVelocity` both use the
        ONE canonical `Vec3` shape, replacing the legacy vocabulary's two
        incompatible vector encodings (bare `[x,y,z]` array vs. `{x,y,z}` object)
        that coexisted across different key families.

        `VesselTarget.orbit` reuses `VesselOrbit` itself (not a separate "target
        orbit" shape), and that is load-bearing: it lets the SDK propagate a target
        with the EXACT SAME code path as the self vessel, so both are evaluated at
        the same view-UT by the same propagation logic (the single-view-time
        invariant). Its nested `VesselTarget.meta` is stamped with the SAME subject
        (the active vessel producing this sample), not a separate target-vessel
        identity, `VesselTarget.vesselId`/`VesselTarget.bodyIndex` below (M3 R3) now
        DO carry the target's own identity, closing the gap this doc comment used to
        flag as deferred.

        Whole-channel absence (the outer `VesselTarget?` being null) means nothing
        is targeted, the common case, R1(b), never a sentinel
        zero-distance/zero-vector record.
      required:
        - name
        - kind
        - meta
      properties:
        name:
          type: string
          x-sitrep-unit: text
        kind:
          $ref: "#/components/schemas/TargetKind"
          x-sitrep-unit: enum
        vesselId:
          type:
            - string
            - "null"
          x-sitrep-unit: id
          description: |-
            The target's own stable id: the M3 R3 fix for the "no target id to
            round-trip into `vessel.target.set`" gap this class's doc comment originally
            flagged as deferred. Populated ONLY when `VesselTarget.kind` is
            `TargetKind.Vessel`, KSP's `Vessel.id` guid, the same opaque id
            `system.vessels`' roster and `SetTargetArgs.VesselId` both use, so a widget
            can read this straight off `vessel.target` and hand it back into a re-target
            command with no extra lookup. Null for a body/other target; see
            `VesselTarget.bodyIndex` for the body case.
        bodyIndex:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            The target's `system.bodies` index: populated ONLY when `VesselTarget.kind`
            is `TargetKind.Body`, mirroring `VesselTarget.vesselId`'s vessel case and
            `SetTargetArgs.bodyIndex`'s own field. Null for a vessel/other target, or if
            the body name couldn't be resolved against `system.bodies` this tick.
        partId:
          type:
            - number
            - "null"
          x-sitrep-unit: id
          description: |-
            The target part's KSP `Part.flightID`: populated ONLY when
            `VesselTarget.kind` is `TargetKind.Part` (a docking port), scoped by
            `VesselTarget.vesselId` (which carries the owning vessel's guid in the Part
            case). Null for every other kind. A widget reads this pair straight off
            `vessel.target` and hands it back into `SetTargetArgs.partId` to re-target
            the same port.
        relativePosition:
          type:
            - object
            - "null"
          description: |-
            Metres, self-relative. Null only when the transform data needed to compute
            it wasn't available this tick.
          required:
            - x
            - y
            - z
          properties:
            x: &a10
              type: number
              x-sitrep-unit: m
            y: *a10
            z: *a10
        relativeVelocity:
          type:
            - object
            - "null"
          description: |-
            m/s, self-relative. R7 Fix 3: nullable for consistency with
            `VesselTarget.relativePosition`, null (never a sentinel `(0,0,0)`, the V-10
            ambiguity) when the transform data needed to compute it wasn't available
            this tick.
          required:
            - x
            - y
            - z
          properties:
            x: &a11
              type: number
              x-sitrep-unit: m/s
            y: *a11
            z: *a11
        orbit:
          description: |-
            Null when the target has no orbit (e.g. it's landed, or its orbit couldn't
            be resolved this tick).
          anyOf:
            - $ref: "#/components/schemas/VesselOrbit"
            - type: "null"
        closestApproach:
          description: |-
            Next closest approach (mod-side, elected solver). Null when there is no
            encounter to report; see `VesselTarget.closestApproach`.
          anyOf:
            - $ref: "#/components/schemas/ClosestApproach"
            - type: "null"
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselThermal:
      type: object
      description: |-
        The `vessel.thermal` channel payload: kills P-5 (the int-where-
        object-expected "partless-paused" sentinel, and the divide-by-zero/NaN risk
        of a part with `maxTemp <= 0`): both ratios are typed `double?`, null
        meaning "no part had a valid `maxTemp`/ `skinMaxTemp` this tick": a
        distinct, typed state, never an indistinguishable-from-real-data `0.0` ("no
        valid part" vs. "coldest possible part").

        Whole-channel absence (the outer `VesselThermal?` being null) means the
        vessel currently has no parts at all (KspHost's `BuildThermal` returns no
        group in that case), a DIFFERENT, coarser absence than an individual null
        ratio.
      required:
        - meta
      properties:
        maxSkinTempRatio:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: Null = no part this tick had a valid (> 0) `skinMaxTemp`, typed, never 0.0.
        maxInternalTempRatio:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: Null = no part this tick had a valid (> 0) `maxTemp`, typed, never 0.0.
        hottestPart:
          description: |-
            Null = no part qualified as "hottest" (same no-valid-part condition as
            `VesselThermal.maxInternalTempRatio`).
          anyOf:
            - $ref: "#/components/schemas/ThermalHottestPart"
            - type: "null"
        heatShieldTemp:
          type:
            - number
            - "null"
          x-sitrep-unit: K
          description: |-
            Hottest heat-shield part's internal temperature (K, raw: the part carrying a
            `ModuleAblator`, `Part.temperature`). Null when the vessel carries no
            ablative heat shield this tick. Was °C until the units audit: the wire is
            SI, and a Celsius display is the client's choice to make.
        heatShieldFlux:
          type:
            - number
            - "null"
          x-sitrep-unit: kW
          description: |-
            The same heat shield's ablative heat flux (`ModuleAblator.flux`, kW). Null
            when the vessel carries no ablative heat shield this tick.
        hottestEngineTemp:
          type:
            - number
            - "null"
          x-sitrep-unit: K
          description: |-
            Internal temperature (K, raw: same unit as
            `ThermalHottestPart.internalTemp`) of whichever part carrying a
            `ModuleEngines`/`ModuleEnginesFX` module has the highest
            internal-temperature ratio. Null when the vessel carries no engine parts
            this tick.
        hottestEngineMaxTemp:
          type:
            - number
            - "null"
          x-sitrep-unit: K
          description: |-
            That same engine part's max internal temperature (K, raw). Null under the
            same no-engine-parts condition as `VesselThermal.hottestEngineTemp`.
        hottestEngineTempRatio:
          type:
            - number
            - "null"
          x-sitrep-unit: ratio
          description: |-
            That same engine part's internal-temperature ratio (`temperature /
            maxTemp`). Null under the same no-engine-parts condition as
            `VesselThermal.hottestEngineTemp`.
        anyEnginesOverheating:
          type:
            - boolean
            - "null"
          x-sitrep-unit: flag
          description: |-
            True when ANY engine part's internal-temperature ratio is at or above 0.9,
            the same ">90% max" threshold ThermalStatus's own inline alert copy already
            states. False (not null) whenever the vessel has engine parts and none
            crosses it; null only alongside a null
            `VesselThermal.hottestEngineTempRatio` (no engine parts at all this tick).
        meta:
          $ref: "#/components/schemas/PayloadMeta"
    VesselType:
      type: integer
      description: |-
        Mirrors KSP's own `VesselType` enum. KSP's `.ToString()` already yields
        PascalCase matching these members, so the mapper uses a case-insensitive
        `Enum.TryParse` rather than a hand-written switch (see
        `VesselViewProvider.ParseVesselType`). `VesselType.Unknown` both mirrors
        KSP's own `Unknown` member and is the fallback for a value this contract
        doesn't recognize yet.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: Ship
        - const: 1
          title: Station
        - const: 2
          title: Lander
        - const: 3
          title: Probe
        - const: 4
          title: Rover
        - const: 5
          title: Base
        - const: 6
          title: Relay
        - const: 7
          title: EVA
        - const: 8
          title: Flag
        - const: 9
          title: Debris
        - const: 10
          title: SpaceObject
        - const: 11
          title: DeployedScienceController
        - const: 12
          title: DeployedSciencePart
        - const: 13
          title: DroppedPart
        - const: 14
          title: Unknown
    WarpMode:
      type: integer
      description: |-
        Mirrors KSP's own `TimeWarp.Modes` enum (confirmed via decompile: only
        `HIGH`/`LOW` exist on this KSP version, no third mode). `WarpMode.Unknown`
        is the graceful fallback for a future/unrecognized raw value.
      x-sitrep-enum-kind: ordinal
      oneOf:
        - const: 0
          title: High
        - const: 1
          title: Low
        - const: 2
          title: Unknown
    WarpState:
      type: object
      description: |-
        The `time.warp` channel payload: kills N-3 (the legacy `p.paused` conflates
        game-pause, no-power, off, antenna-not-found, and scene state into one
        undocumented int, with a doc/impl mismatch: the docs say `0..4` but
        `partPaused()` can return an undocumented `5`). This record is instead
        orthogonal typed fields, no single int can arrive with a meaning outside its
        own documented range.

        **Current UT is deliberately NOT a field here** (or anywhere in this
        contract): `meta.validAt` stamps every sample and the SDK's view-clock is
        the consumer-facing "what time is it" surface, polling `t.universalTime`
        over the wire (a tick-rate channel by definition) is not reproduced.

        **Decoupled from vessel presence (M1 Task 3 fold-in fix):** this record's
        `WarpState.meta` is stamped `Source = "game"`, NOT `"vessel:<guid>"`:
        warp/pause is genuinely GLOBAL game state (`Gonogo.KSP.KspHost.BuildTime`
        reads it unconditionally, with or without an active vessel), so it emits at
        the Space Center / tracking station too, not just in flight. An earlier
        draft gated this channel on active-vessel presence as a scoping
        simplification (reusing the vessel provenance/epoching mechanism uniformly);
        that gate silenced the channel exactly where warp control matters most
        (out-of-flight scenes), so it was removed: see
        `Sitrep.Host.VesselViewProvider.BuildWarp`'s doc comment for the emission
        rule now in force (present whenever `Values["time"]` itself is present,
        nothing else).
      required:
        - warpRate
        - warpRateIndex
        - warpMode
        - paused
        - meta
      properties:
        warpRate:
          type: number
          x-sitrep-unit: "1"
        warpRateIndex:
          type: number
          x-sitrep-unit: id
        warpMode:
          $ref: "#/components/schemas/WarpMode"
          x-sitrep-unit: enum
        paused:
          type: boolean
          x-sitrep-unit: flag
        meta:
          $ref: "#/components/schemas/PayloadMeta"
